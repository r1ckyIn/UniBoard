---
phase: 01-foundation-data-acquisition
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  # Task 1: Scaffolding
  - pyproject.toml
  - .env.example
  - .gitignore
  - docker-compose.yml
  - src/__init__.py
  - src/py.typed
  - tests/__init__.py
  - tests/integration/__init__.py
  # Task 2: Configuration
  - src/config.py
  # Task 3: Logging
  - src/logging.py
  # Task 4: Response wrapper + error handling
  - src/schemas/__init__.py
  - src/schemas/common.py
  - src/web/__init__.py
  - src/web/main.py
  # Task 5: ORM models
  - src/database.py
  - src/models/__init__.py
  - src/models/base.py
  - src/models/user.py
  - src/models/course.py
  - src/models/grade.py
  - src/models/discussion.py
  - src/models/deadline.py
  - src/models/unit_outline.py
  - src/models/module.py
  - src/models/lesson.py
  - src/models/push_record.py
  # Task 6: Alembic
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/001_initial_schema.py
  # Task 7: Encryption
  - src/security/__init__.py
  - src/security/encryption.py
  # Task 8: Tests
  - tests/conftest.py
  - tests/integration/test_models.py
  - tests/integration/test_migrations.py
  - tests/integration/test_encryption.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-07
  - INFRA-09

must_haves:
  truths:
    - "Docker Compose starts PostgreSQL 16 and backend can connect"
    - "All 11 ORM models support CRUD operations against real PostgreSQL"
    - "Alembic migration creates all tables, indexes, and constraints in a single upgrade"
    - "AES-256-GCM encryption round-trip preserves plaintext; canary check passes on startup"
    - "mypy --strict, ruff check, and pytest all pass"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project definition with all backend dependencies"
      contains: "fastapi"
    - path: "src/database.py"
      provides: "Async engine, sessionmaker, get_session dependency"
      contains: "async_sessionmaker"
    - path: "src/models/base.py"
      provides: "DeclarativeBase with TimestampMixin and UUIDMixin"
      contains: "DeclarativeBase"
    - path: "src/models/user.py"
      provides: "User ORM model with encrypted token fields"
      contains: "canvas_api_token_encrypted"
    - path: "src/security/encryption.py"
      provides: "AES-256-GCM TokenEncryption class"
      contains: "AESGCM"
    - path: "alembic/versions/001_initial_schema.py"
      provides: "Initial migration with all 11 tables"
      contains: "create_table"
    - path: "tests/conftest.py"
      provides: "Shared async test fixtures"
      contains: "test_engine"
  key_links:
    - from: "src/database.py"
      to: "src/config.py"
      via: "get_settings().database_url"
      pattern: "get_settings"
    - from: "alembic/env.py"
      to: "src/models/base.py"
      via: "Base.metadata import"
      pattern: "target_metadata = Base.metadata"
    - from: "src/web/main.py"
      to: "src/schemas/common.py"
      via: "ErrorResponse import for exception handlers"
      pattern: "from src.schemas.common import"
    - from: "tests/conftest.py"
      to: "src/models/base.py"
      via: "Base.metadata.create_all for test DB setup"
      pattern: "Base.metadata"
---

<objective>
Establish the complete backend foundation for UniBoard: project scaffolding, configuration, structured logging, unified API response envelope, all 11 SQLAlchemy 2.0 async ORM models with indexes, Alembic async migrations, AES-256-GCM token encryption, and integration tests proving everything works against real PostgreSQL.

Purpose: Every subsequent plan in Phase 1 (auth, adapters) and Phase 2 (services, API) depends on these models, database setup, configuration patterns, and error handling conventions. This plan creates the ground truth for the entire project.

Output: A working Python project that passes `mypy src/ && pytest && ruff check .` with all 11 ORM models persisting to PostgreSQL via Alembic migration, encryption utility verified, and unified error handling in place.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-acquisition/01-CONTEXT.md
@.planning/phases/01-foundation-data-acquisition/01-RESEARCH.md
@.planning/phases/01-foundation-data-acquisition/01-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@docs/UniBoard_TRD_v2.md (sections: SS4 data model, SS7 security, SS12.1 API conventions, SS14 error handling, SS15 database management + indexes, SS18 local dev)

<interfaces>
<!-- This plan creates the foundational interfaces. No existing code to reference. -->
<!-- Downstream plans (01-02 auth, 01-03 adapters) will import from these modules. -->

Contracts this plan establishes (for downstream consumption):

From src/config.py:
  - Settings class with: database_url, canvas_api_token, ed_api_token, canvas_base_url, ed_base_url, encryption_key, secret_key, debug, log_level, access_token_expire_minutes, refresh_token_expire_days
  - get_settings() -> Settings

From src/database.py:
  - async_engine: AsyncEngine
  - async_session_factory: async_sessionmaker[AsyncSession]
  - get_session() -> AsyncGenerator[AsyncSession, None]  (FastAPI Depends)

From src/models/base.py:
  - Base(AsyncAttrs, DeclarativeBase)
  - TimestampMixin (created_at, updated_at)
  - UUIDMixin (id: UUID primary key)

From src/schemas/common.py:
  - SuccessResponse[T], ErrorResponse, MetaInfo, PaginationMeta
  - UniboardError, TokenInvalidError, UpstreamAPIError, RateLimitedError, UpstreamUnavailableError

From src/security/encryption.py:
  - TokenEncryption (encrypt, decrypt, canary_check)
  - get_encryption() -> TokenEncryption

From src/web/main.py:
  - create_app() -> FastAPI
  - app = create_app()  (module-level for uvicorn)

From src/logging.py:
  - configure_logging(json_output: bool = True) -> None
</interfaces>
</context>

<tasks>

<!-- ============================================================ -->
<!-- WAVE 1: Foundation scaffolding (no dependencies, all parallel) -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 1: Project scaffolding + pyproject.toml + Docker Compose</name>
  <files>pyproject.toml, .env.example, .gitignore, docker-compose.yml, src/__init__.py, src/py.typed, tests/__init__.py, tests/integration/__init__.py</files>
  <read_first>
    @.planning/research/STACK.md (pyproject.toml template at line 102-129)
    @.planning/phases/01-foundation-data-acquisition/01-RESEARCH.md (Pattern 9 Docker Compose at line 988-1011)
    @docs/UniBoard_TRD_v2.md SS18.2 (Docker Compose), SS18.3 (.env.example)
  </read_first>
  <action>
Create project directory structure and configuration files:

1. **pyproject.toml** -- Copy exact dependency list from STACK.md lines 105-128. Add metadata:
   - name = "uniboard"
   - version = "0.1.0"
   - requires-python = ">=3.12"
   - Add `[tool.pytest.ini_options]`: asyncio_mode = "auto", testpaths = ["tests"], timeout = 120
   - Add `[tool.mypy]`: strict = true, plugins = ["pydantic.mypy"]
   - Add `[tool.ruff]`: target-version = "py312", line-length = 100
   - Add `[tool.ruff.lint]`: select = ["E", "F", "I", "UP", "B", "SIM"]

2. **docker-compose.yml** -- From RESEARCH.md Pattern 9 (line 990-1011):
   - postgres:16-alpine image
   - Port 5432:5432
   - Environment: POSTGRES_DB=uniboard_dev, POSTGRES_USER=uniboard, POSTGRES_PASSWORD=devpassword
   - Volume: pgdata for persistence
   - Healthcheck: `pg_isready -U uniboard -d uniboard_dev` every 5s, 5 retries

3. **.env.example** -- From TRD SS18.3:
   ```
   DATABASE_URL=postgresql+asyncpg://uniboard:devpassword@localhost:5432/uniboard_dev
   TEST_DATABASE_URL=postgresql+asyncpg://uniboard:devpassword@localhost:5432/uniboard_test
   CANVAS_API_TOKEN=your_canvas_token_here
   ED_API_TOKEN=your_ed_token_here
   CANVAS_BASE_URL=https://canvas.sydney.edu.au/api/v1
   ED_BASE_URL=https://edstem.org/api
   ENCRYPTION_KEY=generate-a-64-char-hex-string-for-32-bytes
   SECRET_KEY=dev-secret-change-in-production
   DEBUG=true
   LOG_LEVEL=INFO
   ```

4. **.gitignore** -- Standard Python + project-specific:
   - CLAUDE.md, .env, .env.*, __pycache__/, *.pyc, .mypy_cache/, .pytest_cache/, .ruff_cache/
   - dist/, *.egg-info/, .venv/, venv/, node_modules/
   - *_CN.md, *.cn.md, .DS_Store, .idea/, .vscode/

5. **src/__init__.py** -- Empty file
6. **src/py.typed** -- Empty marker file for PEP 561
7. **tests/__init__.py** -- Empty file
8. **tests/integration/__init__.py** -- Empty file

After creating files, run `uv sync && uv sync --group dev` to install dependencies.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && uv sync --group dev && python -c "import fastapi; import sqlalchemy; import structlog; print('deps OK')"</automated>
  </verify>
  <done>pyproject.toml has all dependencies from STACK.md, docker-compose.yml has PostgreSQL 16-alpine with healthcheck, .env.example has all required variables, .gitignore excludes CLAUDE.md and .env, `uv sync --group dev` succeeds</done>
</task>

<task type="auto">
  <name>Task 2: pydantic-settings configuration</name>
  <files>src/config.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-acquisition/01-RESEARCH.md (Pattern 9: pydantic-settings at line 758-803)
    @docs/UniBoard_TRD_v2.md SS18.3 (env variables)
    @.planning/research/PITFALLS.md (Pitfall 14: Pydantic v2 patterns)
  </read_first>
  <action>
Create `src/config.py` following RESEARCH.md Pattern 9 exactly:

1. **Settings class** using pydantic-settings `BaseSettings`:
   - `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")`
   - Fields (all with type annotations for mypy --strict):
     - database_url: str (default: "postgresql+asyncpg://uniboard:devpassword@localhost:5432/uniboard_dev")
     - canvas_api_token: str (default: "")
     - ed_api_token: str (default: "")
     - canvas_base_url: str (default: "https://canvas.sydney.edu.au/api/v1")
     - ed_base_url: str (default: "https://edstem.org/api")
     - encryption_key: str (default: "")
     - secret_key: str (default: "dev-secret-change-in-production")
     - access_token_expire_minutes: int (default: 30)
     - refresh_token_expire_days: int (default: 7)
     - debug: bool (default: False)
     - log_level: str (default: "INFO")

2. **get_settings()** function with module-level caching (singleton pattern from RESEARCH.md):
   ```python
   _settings: Settings | None = None
   def get_settings() -> Settings:
       global _settings
       if _settings is None:
           _settings = Settings()
       return _settings
   ```

CRITICAL: Use Pydantic v2 `model_config = SettingsConfigDict(...)` pattern, NOT legacy `class Config:` (Pitfall 14).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && python -c "from src.config import get_settings; s = get_settings(); print(f'DB: {s.database_url[:20]}...'); print('OK')" && mypy src/config.py --strict && ruff check src/config.py</automated>
  </verify>
  <done>Settings class loads from .env file with all fields typed, get_settings() returns cached singleton, mypy --strict passes</done>
</task>

<task type="auto">
  <name>Task 3: structlog JSON logging with sensitive field redaction</name>
  <files>src/logging.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-acquisition/01-RESEARCH.md (Pattern 10: structlog at line 805-847)
    @.planning/phases/01-foundation-data-acquisition/01-CONTEXT.md (Logging decision: structlog JSON, auto-redaction)
  </read_first>
  <action>
Create `src/logging.py` following RESEARCH.md Pattern 10 exactly:

1. **SENSITIVE_KEYS** set: `{"token", "password", "secret", "api_key", "authorization"}`

2. **redact_sensitive_fields()** processor:
   - Iterate over event_dict keys
   - If any key contains a sensitive substring (case-insensitive), replace value with "[REDACTED]"
   - Return modified event_dict

3. **configure_logging(json_output: bool = True)** function:
   - Processor chain in order:
     1. `structlog.contextvars.merge_contextvars`
     2. `structlog.processors.add_log_level`
     3. `structlog.processors.TimeStamper(fmt="iso")`
     4. `redact_sensitive_fields`
     5. `structlog.processors.StackInfoRenderer()`
     6. `structlog.processors.format_exc_info`
     7. If json_output: `structlog.processors.JSONRenderer()`, else: `structlog.dev.ConsoleRenderer()`
   - Configure structlog with: `wrapper_class=structlog.make_filtering_bound_logger(0)`, `context_class=dict`, `logger_factory=structlog.PrintLoggerFactory()`, `cache_logger_on_first_use=True`

All types must be fully annotated for mypy --strict.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && python -c "
from src.logging import configure_logging
import structlog
configure_logging(json_output=False)
log = structlog.get_logger()
log.info('test_event', canvas_api_token='secret123', user='test')
print('OK')
" && mypy src/logging.py --strict && ruff check src/logging.py</automated>
  </verify>
  <done>structlog configured with JSON output, sensitive fields containing token/password/secret/api_key/authorization are replaced with [REDACTED], mypy --strict passes</done>
</task>

<task type="auto">
  <name>Task 4: Unified API response wrapper + exception hierarchy + FastAPI app factory</name>
  <files>src/schemas/__init__.py, src/schemas/common.py, src/web/__init__.py, src/web/main.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-acquisition/01-RESEARCH.md (Pattern 11: response wrapper at line 849-900, FastAPI app factory at line 1052-1112)
    @docs/UniBoard_TRD_v2.md SS12.1 (API conventions), SS14.1-14.2 (error codes, exception hierarchy)
    @.planning/research/PITFALLS.md (Pitfall 14: Pydantic v2 patterns)
  </read_first>
  <action>
Create the API response layer and FastAPI application:

1. **src/schemas/__init__.py** -- Empty file

2. **src/schemas/common.py** -- From RESEARCH.md Pattern 11:
   - `MetaInfo(BaseModel)`: request_id (str), timestamp (datetime)
   - `SuccessResponse(BaseModel, Generic[T])`: data (T), meta (MetaInfo)
   - `PaginationMeta(BaseModel)`: cursor (str | None), limit (int), has_more (bool)
   - `ErrorDetail(BaseModel)`: code (str), message (str), details (Any = None)
   - `ErrorResponse(BaseModel)`: error (ErrorDetail), meta (MetaInfo)
   - Exception hierarchy (TRD SS14.1-14.2):
     - `UniboardError(Exception)`: code, message, status_code (default 500)
     - `TokenInvalidError(UniboardError)`: platform -> 422
     - `UpstreamAPIError(UniboardError)`: platform, detail -> 502
     - `RateLimitedError(UniboardError)`: detail -> 429
     - `UpstreamUnavailableError(UniboardError)`: detail -> 502
     - `ValidationError(UniboardError)`: detail -> 400
     - `NotFoundError(UniboardError)`: resource -> 404

   Use `model_config = ConfigDict(from_attributes=True)` where needed (Pydantic v2 pattern).

3. **src/web/__init__.py** -- Empty file

4. **src/web/main.py** -- From RESEARCH.md FastAPI app factory pattern:
   - `create_app()` function:
     - Call `configure_logging()` from src.logging
     - Create FastAPI instance with title="UniBoard API", version="0.1.0"
     - Add request_id middleware: generates UUID, stores in request.state.request_id, adds X-Request-ID response header
     - Register exception_handler for UniboardError: returns JSONResponse with ErrorResponse envelope
     - Register catch-all exception_handler for Exception: logs error with exc_info, returns 500 with "An unexpected error occurred" (never leak stack traces)
     - Add health route inline: `GET /api/v1/health` returning `{"status": "ok"}`
     - Do NOT register auth/user routes yet (those come in plan 01-02)
   - Module-level: `app = create_app()`

CRITICAL: Import UniboardError and its subclasses from src.schemas.common (not src.web). The schemas module is the canonical location for the exception hierarchy because adapters and services (which don't depend on web) also raise these exceptions.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && python -c "
from src.web.main import app
from fastapi.testclient import TestClient
client = TestClient(app)
r = client.get('/api/v1/health')
assert r.status_code == 200, f'Expected 200, got {r.status_code}'
print(f'Health: {r.json()}')
print('OK')
" && mypy src/schemas/ src/web/ --strict && ruff check src/schemas/ src/web/</automated>
  </verify>
  <done>FastAPI app serves GET /api/v1/health returning 200, request_id middleware adds X-Request-ID header, UniboardError exception handler returns structured ErrorResponse, mypy --strict passes for both src/schemas/ and src/web/</done>
</task>

<!-- ============================================================ -->
<!-- WAVE 2: Data layer (depends on Wave 1 config + schemas)      -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 5: SQLAlchemy 2.0 async ORM models with all indexes</name>
  <files>src/database.py, src/models/__init__.py, src/models/base.py, src/models/user.py, src/models/course.py, src/models/grade.py, src/models/discussion.py, src/models/deadline.py, src/models/unit_outline.py, src/models/module.py, src/models/lesson.py, src/models/push_record.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-acquisition/01-RESEARCH.md (Pattern 1: ORM models at line 212-294, Pattern 2: async engine at line 296-331)
    @docs/UniBoard_TRD_v2.md SS4 (all table schemas, field types, relationships), SS15.5 (all 11 indexes)
    @.planning/research/PITFALLS.md (Pitfall 11: async session lifecycle)
  </read_first>
  <action>
Create the complete data layer following RESEARCH.md Patterns 1-2 and TRD SS4 field definitions.

1. **src/database.py** -- From RESEARCH.md Pattern 2:
   - Import get_settings from src.config
   - Create `async_engine` with pool settings: pool_size=5, max_overflow=10, pool_recycle=300, pool_pre_ping=True, echo=settings.debug
   - Create `async_session_factory` with expire_on_commit=False (CRITICAL: Pitfall 11)
   - `get_session()` async generator as FastAPI dependency: yields session, commits on success, rollback on exception
   - IMPORTANT: Use lazy initialization pattern -- engine created on first call to get_session, not at module import time. This avoids import-time side effects during testing and allows test fixtures to override the database URL.

2. **src/models/base.py** -- DeclarativeBase + mixins:
   - `Base(AsyncAttrs, DeclarativeBase)` -- base class for all models
   - `TimestampMixin`: created_at (Mapped[datetime], server_default=func.now()), updated_at (Mapped[datetime], server_default=func.now(), onupdate=func.now())
   - UUIDMixin: id (Mapped[uuid.UUID], UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
   - Use `datetime` from `datetime` module, `func` from `sqlalchemy`

3. **src/models/__init__.py** -- Import all models so Base.metadata is populated:
   ```python
   from src.models.base import Base
   from src.models.user import User
   from src.models.course import Course
   # ... all 11 models
   ```

4. **Individual model files** -- All use Mapped[] annotations (NEVER legacy Column()):

   **src/models/user.py** -- User (TRD SS4.2):
   - Inherits UUIDMixin, TimestampMixin, Base
   - __tablename__ = "users"
   - email: Mapped[str] = mapped_column(String(255), unique=True, index=True)
   - hashed_password: Mapped[str] = mapped_column(String(255))
   - university_id: Mapped[str | None] = mapped_column(String(50), nullable=True)
   - canvas_api_token_encrypted: Mapped[str | None] = mapped_column(Text, nullable=True)
   - ed_api_token_encrypted: Mapped[str | None] = mapped_column(Text, nullable=True)
   - gpa_target: Mapped[float | None] = mapped_column(Float, nullable=True)
   - gpa_scale: Mapped[str] = mapped_column(String(10), default="wam")
   - last_sync_at: Mapped[datetime | None] = mapped_column(nullable=True)
   - Relationships: courses (list["Course"]), push_records (list["PushRecord"]) -- cascade="all, delete-orphan"
   - NOTE: No cognito_sub field -- MVP uses simple JWT, not Cognito (per PROJECT.md constraints)

   **src/models/course.py** -- Course (TRD SS4.2):
   - __tablename__ = "courses"
   - __table_args__ with Index("ix_courses_user_semester", "user_id", "semester") and Index("ix_courses_canvas_id", "user_id", "canvas_course_id")
   - user_id: FK to users.id
   - canvas_course_id, ed_course_id: String(50), nullable
   - name: String(255), code: String(20), semester: String(20)
   - credit_points: int, default=6
   - grading_weights: Mapped[dict | None] = mapped_column(JSON, nullable=True)
   - unit_outline_url: Mapped[str | None] = mapped_column(String(500), nullable=True)
   - Relationships: user (back_populates), grades, modules, lessons, discussion_threads, unified_deadlines, unit_outlines

   **src/models/grade.py** -- Grade (TRD SS4.2):
   - __tablename__ = "grades"
   - __table_args__ with Index("ix_grades_course", "course_id", "graded_at")
   - course_id: FK to courses.id
   - assessment_name: String(255)
   - score: Mapped[float | None] (nullable -- null means not yet graded)
   - max_score: Mapped[float]
   - weight: Mapped[float] (0-1)
   - group_name: Mapped[str] = mapped_column(String(255), default="")
   - submitted_at: Mapped[datetime | None]
   - graded_at: Mapped[datetime | None]

   **src/models/discussion.py** -- DiscussionThread (TRD SS4.2):
   - __tablename__ = "discussion_threads"
   - __table_args__ with 4 indexes from TRD SS15.5:
     - Index("ix_threads_course_created", "course_id", "created_at")
     - Index("ix_threads_ed_id", "course_id", "ed_thread_id", unique=True)
     - Index("ix_threads_gpa_score", "course_id", "gpa_relevance_score")
     - Index("ix_threads_search", "search_vector", postgresql_using="gin")
   - course_id: FK to courses.id
   - ed_thread_id: String(50)
   - title, author, category, content (Text)
   - is_endorsed: bool, default=False
   - is_staff_post: bool, default=False
   - gpa_relevance_score: Mapped[float] = mapped_column(Float, default=0.0)
   - search_vector: Mapped[Any | None] -- Use `from sqlalchemy.dialects.postgresql import TSVECTOR`. This column is GENERATED ALWAYS AS in PostgreSQL, so mark as `server_default=text("")` or handle in migration SQL. In the ORM model, declare it but do NOT set it from Python -- it's auto-populated by PostgreSQL.
   - synced_at: Mapped[datetime | None]

   **src/models/deadline.py** -- UnifiedDeadline (TRD SS4.2):
   - __tablename__ = "unified_deadlines"
   - __table_args__ with:
     - Index("ix_deadlines_course_due", "course_id", "due_date")
     - Index("ix_deadlines_dedup", "dedup_key", unique=True)
   - course_id: FK to courses.id
   - title: String(255)
   - due_date: Mapped[datetime]
   - source: Mapped[str] = mapped_column(String(30)) -- values: "canvas_assignment", "ed_lesson", "ed_discussion", "unit_outline"
   - source_id: Mapped[str] = mapped_column(String(100))
   - weight: Mapped[float | None]
   - description: Mapped[str | None] = mapped_column(Text, nullable=True)
   - dedup_key: Mapped[str] = mapped_column(String(64)) -- SHA-256 hex
   - is_confirmed: Mapped[bool] = mapped_column(default=True)

   **src/models/unit_outline.py** -- UnitOutline (TRD SS4.2):
   - __tablename__ = "unit_outlines"
   - course_id: FK to courses.id
   - outline_url: String(500)
   - assessments: Mapped[list | None] = mapped_column(JSON, nullable=True) -- [{name, weight, description, due_date, type}]
   - learning_outcomes: Mapped[list | None] = mapped_column(JSON, nullable=True)
   - raw_html: Mapped[str | None] = mapped_column(Text, nullable=True)
   - fetched_at: Mapped[datetime | None]
   - semester: Mapped[str] = mapped_column(String(20))

   **src/models/module.py** -- Module + ModuleItem (TRD SS4.2):
   - Module: __tablename__ = "modules"
     - course_id: FK to courses.id
     - canvas_module_id: String(50)
     - name: String(255)
     - position: int
     - Relationship: items (list["ModuleItem"])
   - ModuleItem: __tablename__ = "module_items"
     - module_id: FK to modules.id
     - title: String(255)
     - type: String(50) -- "File", "Page", "Discussion", "Assignment", "Quiz", "ExternalUrl", "ExternalTool"
     - content_id: Mapped[str | None] = mapped_column(String(50), nullable=True)
     - url: Mapped[str | None] = mapped_column(String(500), nullable=True)

   **src/models/lesson.py** -- Lesson + Slide (TRD SS4.2 + SS9.4):
   - Lesson: __tablename__ = "lessons"
     - course_id: FK to courses.id
     - ed_lesson_id: String(50)
     - title: String(255)
     - number: Mapped[int | None] -- field name correction from TRD SS9.4 (NOT lesson_number)
     - kind: Mapped[str] = mapped_column(String(50), default="")
     - state: Mapped[str] = mapped_column(String(50), default="")
     - slide_count: Mapped[int] = mapped_column(default=0)
     - due_at: Mapped[datetime | None]
     - Relationship: slides (list["Slide"])
   - Slide: __tablename__ = "slides"
     - lesson_id: FK to lessons.id
     - content: Mapped[str | None] = mapped_column(Text, nullable=True) -- XML format, field name "content" NOT "passage" (TRD SS9.4)
     - type: Mapped[str] = mapped_column(String(50), default="")
     - index: Mapped[int] = mapped_column(default=0)

   **src/models/push_record.py** -- PushRecord (TRD SS4.2):
   - __tablename__ = "push_records"
   - __table_args__ with Index("ix_push_records_user_hash", "user_id", "content_hash", unique=True)
   - user_id: FK to users.id
   - content_hash: String(64) -- SHA-256
   - source_type: Mapped[str] = mapped_column(String(30)) -- "thread", "announcement", "grade", "deadline"
   - source_id: Mapped[str] = mapped_column(String(100))
   - pushed_at: Mapped[datetime]
   - channel: Mapped[str] = mapped_column(String(20)) -- "email", "web", "digest"

CRITICAL RULES:
- ALL models use Mapped[] + mapped_column() (SQLAlchemy 2.0). NEVER use legacy Column().
- ALL relationships use back_populates with cascade="all, delete-orphan" where parent owns children.
- NEVER use lazy loading. When querying with relationships, the caller must use selectinload() or joinedload().
- Type annotations must satisfy mypy --strict (use `Mapped[str | None]` not `Optional[str]`).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && mypy src/models/ src/database.py --strict && ruff check src/models/ src/database.py && python -c "
from src.models import Base, User, Course, Grade
from src.models.discussion import DiscussionThread
from src.models.deadline import UnifiedDeadline
from src.models.unit_outline import UnitOutline
from src.models.module import Module, ModuleItem
from src.models.lesson import Lesson, Slide
from src.models.push_record import PushRecord
tables = Base.metadata.tables
print(f'Tables: {len(tables)} -- {list(tables.keys())}')
assert len(tables) == 11, f'Expected 11 tables, got {len(tables)}'
print('OK')
"</automated>
  </verify>
  <done>All 11 ORM models defined with Mapped[] annotations, all 11 indexes from TRD SS15.5 declared in __table_args__, Base.metadata contains all 11 tables, mypy --strict passes for all model files</done>
</task>

<task type="auto">
  <name>Task 6: Alembic async migration (001_initial_schema)</name>
  <files>alembic.ini, alembic/env.py, alembic/script.py.mako, alembic/versions/001_initial_schema.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-acquisition/01-RESEARCH.md (Alembic async env.py at line 1013-1050)
    @docs/UniBoard_TRD_v2.md SS15.2 (migration strategy), SS15.5 (indexes, tsvector SQL)
  </read_first>
  <action>
Set up Alembic with async support and create initial migration.

1. **Initialize Alembic**: Run `alembic init -t async alembic` to generate async template.

2. **alembic.ini** -- Update:
   - Set `sqlalchemy.url` to empty string (will be overridden in env.py from Settings)
   - Set `file_template` to `%%(rev)s_%%(slug)s` for readable names

3. **alembic/env.py** -- Customize from RESEARCH.md pattern:
   - Import Base from src.models (which triggers all model imports)
   - Import get_settings from src.config
   - Set target_metadata = Base.metadata
   - Override sqlalchemy.url from get_settings().database_url
   - Async run_async_migrations() using create_async_engine

4. **Create migration**: Run `alembic revision --autogenerate -m "initial_schema"` to auto-detect all 11 tables from the ORM models.

5. **Post-process the generated migration file** (alembic/versions/001_initial_schema.py):
   - Verify all 11 tables are present: users, courses, grades, discussion_threads, unified_deadlines, unit_outlines, modules, module_items, lessons, slides, push_records
   - Verify all indexes from TRD SS15.5 are included
   - Add the tsvector GENERATED column and GIN index for discussion_threads.search_vector:
     ```python
     # In upgrade():
     # After the discussion_threads table is created, add the generated tsvector column
     op.execute("""
         ALTER TABLE discussion_threads
         ADD COLUMN IF NOT EXISTS search_vector tsvector
         GENERATED ALWAYS AS (
             setweight(to_tsvector('english', coalesce(title, '')), 'A') ||
             setweight(to_tsvector('english', coalesce(content, '')), 'B')
         ) STORED
     """)
     ```
   - Ensure the downgrade() properly drops all tables in reverse dependency order

6. **Test migration**: Run `docker compose up -d && alembic upgrade head` to verify the migration applies cleanly.

DISCRETION DECISION: Single initial migration (001_initial_schema) containing all Phase 1 tables. Rationale: no existing data to migrate, all tables are new, simpler to reason about. Future phases add incremental migrations.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && docker compose up -d --wait && sleep 2 && alembic upgrade head && python -c "
import asyncio
from sqlalchemy import text
from sqlalchemy.ext.asyncio import create_async_engine
async def check():
    engine = create_async_engine('postgresql+asyncpg://uniboard:devpassword@localhost:5432/uniboard_dev')
    async with engine.connect() as conn:
        result = await conn.execute(text(\"SELECT tablename FROM pg_tables WHERE schemaname='public'\"))
        tables = [row[0] for row in result.fetchall()]
        print(f'Tables in DB: {sorted(tables)}')
        assert len(tables) >= 11, f'Expected >=11 tables, got {len(tables)}: {tables}'
        # Check indexes
        result = await conn.execute(text(\"SELECT indexname FROM pg_indexes WHERE schemaname='public'\"))
        indexes = [row[0] for row in result.fetchall()]
        print(f'Indexes: {len(indexes)}')
    await engine.dispose()
asyncio.run(check())
print('OK')
"</automated>
  </verify>
  <done>Alembic async env.py reads DATABASE_URL from Settings, `alembic upgrade head` creates all 11 tables with all indexes including GIN for tsvector, `alembic downgrade base` drops everything cleanly</done>
</task>

<task type="auto">
  <name>Task 7: AES-256-GCM token encryption utility</name>
  <files>src/security/__init__.py, src/security/encryption.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-acquisition/01-RESEARCH.md (Pattern 4: encryption at line 386-439)
    @.planning/research/PITFALLS.md (Pitfall 4: encryption key management)
    @docs/UniBoard_TRD_v2.md SS7.1 (AES-256-GCM spec)
  </read_first>
  <action>
Create encryption utility following RESEARCH.md Pattern 4 exactly:

1. **src/security/__init__.py** -- Empty file

2. **src/security/encryption.py**:

   **TokenEncryption class**:
   - `__init__(self, key: bytes)`: Validate key is exactly 32 bytes. Create AESGCM instance.
   - `encrypt(self, plaintext: str) -> str`: Generate 12-byte random nonce (os.urandom(12)). Encrypt with AESGCM. Return base64(nonce + ciphertext). CRITICAL: fresh nonce per operation -- nonce reuse compromises all encrypted data.
   - `decrypt(self, encrypted: str) -> str`: base64 decode, split nonce (first 12 bytes) from ciphertext, decrypt with AESGCM, return plaintext string.
   - `canary_check(self) -> bool`: Encrypt then decrypt "uniboard-canary-check". Return True if round-trip succeeds, False on any exception.

   **get_encryption() factory function**:
   - Read ENCRYPTION_KEY from environment (via os.environ, not Settings -- encryption module should be independent of config to avoid circular imports)
   - Support two formats:
     - 64-char hex string -> `bytes.fromhex()` to get 32 bytes
     - Raw string >= 32 chars -> `encode()[:32]` (for dev convenience)
   - Raise RuntimeError if key is missing or too short
   - Return TokenEncryption instance

All types fully annotated for mypy --strict.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef python -c "
from src.security.encryption import get_encryption
enc = get_encryption()
# Test round-trip
original = 'my-secret-canvas-token-with-special-chars/+=&'
encrypted = enc.encrypt(original)
decrypted = enc.decrypt(encrypted)
assert decrypted == original, f'Round-trip failed: {original} != {decrypted}'
print(f'Encrypted length: {len(encrypted)}')
# Test canary
assert enc.canary_check(), 'Canary check failed'
# Test different nonces
encrypted2 = enc.encrypt(original)
assert encrypted != encrypted2, 'Same plaintext produced same ciphertext (nonce reuse!)'
print('All encryption tests passed')
" && mypy src/security/ --strict && ruff check src/security/</automated>
  </verify>
  <done>TokenEncryption encrypts/decrypts round-trip correctly, canary_check returns True, each encryption produces different ciphertext (fresh nonce), get_encryption() reads key from ENCRYPTION_KEY env var, mypy --strict passes</done>
</task>

<!-- ============================================================ -->
<!-- WAVE 3: Integration tests (depends on Wave 1+2)               -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 8: Integration tests for models + migrations + encryption</name>
  <files>tests/conftest.py, tests/integration/test_models.py, tests/integration/test_migrations.py, tests/integration/test_encryption.py</files>
  <read_first>
    @.planning/phases/01-foundation-data-acquisition/01-RESEARCH.md (Test fixtures at line 1114-1187)
    @.planning/phases/01-foundation-data-acquisition/01-VALIDATION.md (Test architecture, fixture strategy, coverage targets)
    @.planning/phases/01-foundation-data-acquisition/01-CONTEXT.md (Testing decision: pure integration, real PostgreSQL, no mocks)
  </read_first>
  <action>
Create test infrastructure and integration tests hitting real PostgreSQL.

1. **tests/conftest.py** -- From RESEARCH.md and VALIDATION.md fixture patterns:
   - `@pytest.fixture(scope="session") def event_loop()` -- Create event loop for entire session
   - `@pytest_asyncio.fixture(scope="session") async def test_engine()` -- Create async engine using TEST_DATABASE_URL (fallback to uniboard_test DB). Run Base.metadata.create_all. Yield engine. Drop all tables and dispose on teardown.
   - `@pytest_asyncio.fixture async def session(test_engine)` -- Per-test session with rollback for isolation. Uses async_sessionmaker with expire_on_commit=False.
   - `@pytest_asyncio.fixture async def client()` -- httpx AsyncClient with ASGITransport wrapping create_app()
   - `@pytest.fixture def encryption()` -- TokenEncryption with a test key (hardcoded 32-byte key for testing, NOT production key)

   IMPORTANT: The test database must be a separate database (uniboard_test) from the dev database (uniboard_dev). The conftest should create this database if it doesn't exist, or at minimum document that the user needs to create it. Simplest: use the same PostgreSQL instance from docker-compose but a different database name. Add to docker-compose environment: `POSTGRES_DB: uniboard_dev` and in conftest, connect to uniboard_dev first to CREATE DATABASE uniboard_test.

   Actually, simplest approach: use the same uniboard_dev database for tests but with per-test transaction rollback. This avoids needing to create a separate test database. The rollback ensures test isolation.

2. **tests/integration/test_models.py** -- CRUD tests for all 11 models:
   - `test_create_user(session)` -- Create User with email, hashed_password. Verify can be read back.
   - `test_create_course_with_user(session)` -- Create User, then Course linked to user. Verify relationship.
   - `test_create_grade(session)` -- Create User -> Course -> Grade. Verify score, weight fields.
   - `test_create_discussion_thread(session)` -- Create User -> Course -> DiscussionThread with is_endorsed, gpa_relevance_score.
   - `test_create_unified_deadline(session)` -- Create User -> Course -> UnifiedDeadline with dedup_key, source enum.
   - `test_create_unit_outline(session)` -- Create User -> Course -> UnitOutline with JSON assessments and raw_html.
   - `test_create_module_with_items(session)` -- Create User -> Course -> Module -> ModuleItem. Verify relationship.
   - `test_create_lesson_with_slides(session)` -- Create User -> Course -> Lesson -> Slide. Verify content field.
   - `test_create_push_record(session)` -- Create User -> PushRecord with content_hash.
   - `test_user_cascade_delete(session)` -- Create User with Course + Grade. Delete User. Verify Course and Grade are deleted.
   - `test_unique_email_constraint(session)` -- Create two users with same email. Verify IntegrityError.
   - `test_unique_dedup_key_constraint(session)` -- Create two deadlines with same dedup_key. Verify IntegrityError.

   Each test: create objects, add to session, flush, query back, assert fields match. Use `await session.execute(select(Model).where(...))` pattern.

3. **tests/integration/test_migrations.py**:
   - `test_alembic_upgrade_head()` -- Run alembic upgrade head programmatically (via alembic.command). Verify all 11 tables exist by querying pg_tables.
   - `test_alembic_downgrade_base()` -- Run alembic downgrade base. Verify all tables are dropped.
   - `test_alembic_upgrade_then_downgrade()` -- Full cycle: upgrade head -> verify tables -> downgrade base -> verify no tables.

4. **tests/integration/test_encryption.py**:
   - `test_encrypt_decrypt_round_trip(encryption)` -- Encrypt a token string, decrypt it, verify equality.
   - `test_encrypt_produces_unique_ciphertext(encryption)` -- Encrypt same plaintext twice, verify different ciphertext (fresh nonce).
   - `test_canary_check_passes(encryption)` -- Verify canary_check() returns True.
   - `test_decrypt_with_wrong_key()` -- Create two TokenEncryption instances with different keys. Encrypt with one, decrypt with other. Verify raises InvalidTag.
   - `test_decrypt_corrupted_data(encryption)` -- Pass garbage base64 to decrypt(). Verify raises exception.
   - `test_key_validation()` -- Attempt to create TokenEncryption with key shorter than 32 bytes. Verify raises ValueError.

All tests use real PostgreSQL via Docker Compose. No mocks (per CONTEXT.md locked decision).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && docker compose up -d --wait && sleep 2 && mypy src/ --strict && ruff check . && pytest tests/ -x -v --timeout=120</automated>
  </verify>
  <done>All integration tests pass against real PostgreSQL: 11 models CRUD verified, Alembic upgrade/downgrade cycle works, encryption round-trip + canary + bad key + corrupted data all pass. Full `mypy src/ && pytest && ruff check .` succeeds.</done>
</task>

</tasks>

<verification>
After all 8 tasks complete, run the full validation chain:

```bash
# 1. Ensure PostgreSQL is running
docker compose up -d --wait

# 2. Run Alembic migration
alembic upgrade head

# 3. Full verification (must all pass)
mypy src/ --strict && pytest tests/ -v --timeout=120 && ruff check .

# 4. Verify table count in database
python -c "
import asyncio
from sqlalchemy import text
from sqlalchemy.ext.asyncio import create_async_engine
async def verify():
    engine = create_async_engine('postgresql+asyncpg://uniboard:devpassword@localhost:5432/uniboard_dev')
    async with engine.connect() as conn:
        result = await conn.execute(text(\"SELECT count(*) FROM pg_tables WHERE schemaname='public'\"))
        count = result.scalar()
        print(f'Tables: {count}')
        assert count >= 11, f'Expected >=11 tables, got {count}'
    await engine.dispose()
asyncio.run(verify())
"

# 5. Verify health endpoint
python -c "
from fastapi.testclient import TestClient
from src.web.main import app
client = TestClient(app)
r = client.get('/api/v1/health')
assert r.status_code == 200
print(f'Health endpoint: {r.json()}')
"
```
</verification>

<success_criteria>
1. `docker compose up -d` starts PostgreSQL 16 and backend can connect via asyncpg
2. `alembic upgrade head` creates all 11 tables (users, courses, grades, discussion_threads, unified_deadlines, unit_outlines, modules, module_items, lessons, slides, push_records) with all 11 indexes from TRD SS15.5
3. All 11 ORM models support create/read operations against real PostgreSQL (verified by test_models.py)
4. AES-256-GCM encryption round-trip preserves plaintext, canary check passes, wrong key raises error
5. FastAPI app serves GET /api/v1/health returning 200 with X-Request-ID header
6. UniboardError exception hierarchy produces structured ErrorResponse JSON
7. structlog redacts fields containing token/password/secret
8. `mypy src/ --strict` passes with zero errors
9. `ruff check .` passes with zero warnings
10. `pytest tests/ -v` all green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-acquisition/01-01-SUMMARY.md`
</output>
