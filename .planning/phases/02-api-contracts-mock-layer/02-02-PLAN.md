---
phase: 02-api-contracts-mock-layer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/lib/fixtures/helpers.ts
  - frontend/lib/fixtures/users.ts
  - frontend/lib/fixtures/courses.ts
  - frontend/lib/fixtures/grades.ts
  - frontend/lib/fixtures/deadlines.ts
  - frontend/lib/fixtures/materials.ts
  - frontend/lib/fixtures/discussions.ts
  - frontend/lib/fixtures/gpa.ts
  - frontend/lib/fixtures/digest.ts
  - frontend/lib/fixtures/alerts.ts
  - frontend/lib/fixtures/notifications.ts
  - frontend/lib/fixtures/sync.ts
  - frontend/lib/fixtures/search.ts
  - frontend/app/api/v1/health/route.ts
  - frontend/app/api/v1/auth/register/route.ts
  - frontend/app/api/v1/auth/login/route.ts
  - frontend/app/api/v1/auth/refresh/route.ts
  - frontend/app/api/v1/auth/logout/route.ts
  - frontend/app/api/v1/users/me/route.ts
  - frontend/app/api/v1/users/me/tokens/[platform]/route.ts
  - frontend/app/api/v1/users/me/tokens/[platform]/verify/route.ts
  - frontend/app/api/v1/users/me/export/route.ts
  - frontend/app/api/v1/courses/route.ts
  - frontend/app/api/v1/courses/[id]/route.ts
  - frontend/app/api/v1/courses/[id]/grades/route.ts
  - frontend/app/api/v1/courses/[id]/materials/route.ts
  - frontend/app/api/v1/courses/[id]/discussions/route.ts
  - frontend/app/api/v1/courses/[id]/deadlines/route.ts
  - frontend/app/api/v1/courses/[id]/outline/route.ts
  - frontend/app/api/v1/gpa/route.ts
  - frontend/app/api/v1/gpa/predict/route.ts
  - frontend/app/api/v1/gpa/path/route.ts
  - frontend/app/api/v1/deadlines/route.ts
  - frontend/app/api/v1/deadlines/upcoming/route.ts
  - frontend/app/api/v1/digest/latest/route.ts
  - frontend/app/api/v1/digest/history/route.ts
  - frontend/app/api/v1/alerts/route.ts
  - frontend/app/api/v1/notifications/route.ts
  - frontend/app/api/v1/sync/trigger/route.ts
  - frontend/app/api/v1/sync/status/route.ts
  - frontend/app/api/v1/search/route.ts
autonomous: true
requirements: [INFRA-11]

must_haves:
  truths:
    - "Route Handler mocks return realistic fixture data for all endpoints matching TRD section 12 response format"
    - "All mock responses use the standard envelope: {data: ..., meta: {request_id, timestamp}} for success, {error: {code, message}} for errors"
    - "Mock endpoints validate Bearer token presence and return 401 AUTH_REQUIRED when missing (except /auth/* and /health)"
    - "Mock data uses real USYD course codes and names for realistic demo data"
    - "Simulated latency (100-800ms) and error rate (~5%) test frontend resilience"
  artifacts:
    - path: "frontend/lib/fixtures/helpers.ts"
      provides: "Shared mock helpers: mockResponse, mockError, mockDelay, shouldSimulateError, requireAuth"
      exports: ["mockResponse", "mockError", "mockDelay", "shouldSimulateError", "requireAuth"]
    - path: "frontend/lib/fixtures/courses.ts"
      provides: "Fixture data for 5 USYD courses with realistic grades and assessment weights"
      exports: ["courses", "courseDetails"]
    - path: "frontend/app/api/v1/courses/route.ts"
      provides: "GET /api/v1/courses mock endpoint"
      contains: "export async function GET"
    - path: "frontend/app/api/v1/gpa/route.ts"
      provides: "GET /api/v1/gpa mock endpoint"
      contains: "export async function GET"
    - path: "frontend/app/api/v1/auth/login/route.ts"
      provides: "POST /api/v1/auth/login mock endpoint returning fake JWT"
      contains: "export async function POST"
  key_links:
    - from: "frontend/app/api/v1/courses/route.ts"
      to: "frontend/lib/fixtures/courses.ts"
      via: "import courses fixture data"
      pattern: "from.*fixtures/courses"
    - from: "frontend/app/api/v1/courses/route.ts"
      to: "frontend/lib/fixtures/helpers.ts"
      via: "import mockResponse, mockDelay, requireAuth helpers"
      pattern: "from.*fixtures/helpers"
---

<objective>
Create realistic fixture data for all API domains and implement Next.js Route Handler mocks for every endpoint defined in the OpenAPI spec. Every mock returns data matching the spec's response envelope format and validates auth tokens.

Purpose: Provide a working mock API layer so all page phases (3-12) can develop against realistic data without waiting for the backend.

Output: ~13 fixture data files and ~22 Route Handler files covering all endpoints.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-api-contracts-mock-layer/02-CONTEXT.md
@.planning/phases/02-api-contracts-mock-layer/02-RESEARCH.md
@docs/UniBoard_TRD_v2.md (sections 12.1 through 12.10 — response formats and example data)

@.planning/phases/02-api-contracts-mock-layer/02-01-SUMMARY.md (Plan 01 output — OpenAPI spec, generated types, ky client, auth store)

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From frontend/openapi/openapi.yaml (generated types via):
```typescript
// frontend/lib/api/types.gen.d.ts
export interface paths { ... }    // All endpoint paths with request/response types
export interface components { ... } // All schema types (Course, Grade, Deadline, etc.)
```

From frontend/lib/auth/store.ts:
```typescript
export const useAuthStore: UseBoundStore<StoreApi<AuthState>>;
// AuthState.accessToken: string | null
// AuthState.clearAuth: () => void
```

From frontend/lib/api/client.ts:
```typescript
export const api: KyInstance; // prefixUrl: "/api/v1", auth injection via beforeRequest hook
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fixture data files and mock helpers</name>
  <files>
    frontend/lib/fixtures/helpers.ts,
    frontend/lib/fixtures/users.ts,
    frontend/lib/fixtures/courses.ts,
    frontend/lib/fixtures/grades.ts,
    frontend/lib/fixtures/deadlines.ts,
    frontend/lib/fixtures/materials.ts,
    frontend/lib/fixtures/discussions.ts,
    frontend/lib/fixtures/gpa.ts,
    frontend/lib/fixtures/digest.ts,
    frontend/lib/fixtures/alerts.ts,
    frontend/lib/fixtures/notifications.ts,
    frontend/lib/fixtures/sync.ts,
    frontend/lib/fixtures/search.ts
  </files>
  <read_first>
    - frontend/openapi/openapi.yaml (all component schemas — fixture data must match these types exactly)
    - frontend/lib/api/types.gen.d.ts (verify generated type names to use for type annotations)
    - docs/UniBoard_TRD_v2.md (lines 1497-2183, section 12 — example response data for each endpoint)
    - .planning/phases/02-api-contracts-mock-layer/02-CONTEXT.md (mock data requirements: real USYD courses, edge cases, risk scenarios, simulated errors)
  </read_first>
  <action>
1. Create `frontend/lib/fixtures/helpers.ts` — shared mock utilities:
   - `mockResponse<T>(data: T, status = 200)`: Returns `NextResponse.json({ data, meta: { request_id: crypto.randomUUID(), timestamp: new Date().toISOString() } }, { status })`
   - `mockError(code: string, message: string, status: number)`: Returns `NextResponse.json({ error: { code, message } }, { status })`
   - `mockDelay(min = 100, max = 800)`: Returns a Promise that resolves after random ms between min and max
   - `shouldSimulateError(rate = 0.05)`: Returns `Math.random() < rate`
   - `requireAuth(request: Request)`: Checks for `Authorization: Bearer ...` header. Returns `mockError("AUTH_REQUIRED", "Authentication required", 401)` if missing, or `null` if auth is OK.
   - `mockPaginatedResponse<T>(data: T[], cursor: string | null, limit: number)`: Slices array based on cursor/limit, returns `{ data: sliced, meta: ResponseMeta, pagination: { next_cursor, has_more } }`.

2. Create `frontend/lib/fixtures/users.ts`:
   - Export `mockUser` object: `{ id: "usr_001", email: "student@sydney.edu.au", display_name: "Alex Chen", gpa_target: 85.0, gpa_scale: "wam", tokens: { canvas: { status: "active", last_verified_at: "2026-03-01T10:00:00Z" }, ed: { status: "active", last_verified_at: "2026-03-01T10:00:00Z" } }, created_at: "2026-02-01T00:00:00Z" }`
   - Export `mockTokens`: `{ access: "mock-jwt-access-token-xxx", refresh: "mock-jwt-refresh-token-xxx" }`

3. Create `frontend/lib/fixtures/courses.ts`:
   Use REAL USYD course codes. Export `courses` array with 5 courses representing diverse scenarios:
   - COMP2017 "Systems Programming" — semester "2026-S1", credit_points 6, current_mark 82.5, grade_letter "D", completed_weight 0.40 (normal path, some graded)
   - COMP3221 "Distributed Systems" — current_mark 71.0, grade_letter "CR", completed_weight 0.25 (below target, risk scenario)
   - STAT2011 "Probability and Estimation Theory" — current_mark 62.0, grade_letter "P", completed_weight 0.30 (at-risk, declining grades)
   - INFO2222 "Computing 2 Usability" — current_mark 91.0, grade_letter "HD", completed_weight 0.50 (high performer)
   - MATH1005 "Statistics" — current_mark null, grade_letter null, completed_weight 0.0 (no grades yet, empty state)

   Export `courseDetails` map (course id -> CourseDetail with assessment_weights). Each course should have 3-5 assessments with mix of graded/upcoming/submitted statuses.

4. Create `frontend/lib/fixtures/grades.ts`:
   Export `gradesByCourse` map (course id -> Grade[]). Include graded assessments for each course matching the assessment_weights. Include graded_at and submitted_at timestamps.

5. Create `frontend/lib/fixtures/deadlines.ts`:
   Export `deadlines` array with 8-10 deadlines across all courses. Include:
   - 2 past-due (status "overdue")
   - 3 upcoming within 7 days (status "upcoming", days_remaining 1-7)
   - 3 upcoming within 30 days
   - 1-2 from ed_lesson source, 1 from ed_discussion source (is_confirmed: false), rest from canvas_assignment
   Include course_code and course_name on each. Use realistic assignment names (Assignment 2, Lab 5, Midterm Exam, etc.)

6. Create `frontend/lib/fixtures/materials.ts`:
   Export `materialsByCourse` map. Each course has 3-5 materials: mix of Canvas modules (with items array) and Ed lessons (with slide_count). Use realistic titles like "Week 1 - Introduction to C", "Week 2 - Memory Management".

7. Create `frontend/lib/fixtures/discussions.ts`:
   Export `discussionsByCourse` map. Each course has 5-8 threads. Include:
   - Staff posts (is_staff_post: true) about exam scope, assignment clarifications
   - Endorsed posts (is_endorsed: true)
   - High relevance_score posts (0.8-1.0) and low ones (0.2-0.4)
   - Categories: Announcements, General, Assignments, Exams

8. Create `frontend/lib/fixtures/gpa.ts`:
   - Export `gpaReport` object matching GpaReport schema: scale "wam", current_wam 78.5, target_wam 85.0, gap -6.5, with GpaCourseSummary for all 5 courses (matching course fixtures).
   - Export `gpaPredictionDefault` sample prediction result for testing.
   - Export `gpaPathDefault` sample path result for testing.

9. Create `frontend/lib/fixtures/digest.ts`:
   Export `digestLatest` (full daily digest with 3 courses, 2 urgent deadlines) and `digestHistory` (array of 5 past digests).

10. Create `frontend/lib/fixtures/alerts.ts`:
    Export `alerts` array with 3-4 alerts: gpa_risk for STAT2011 (current 62, target 85), deadline_risk for an imminent deadline, token_expired for testing. Mix of warning/critical severity and read/unread.

11. Create `frontend/lib/fixtures/notifications.ts`:
    Export `notifications` array with 5-6 items: deadline_reminder (72h, 24h, 3h variants), grade_published, token_expired, sync_complete. Mix of read/unread.

12. Create `frontend/lib/fixtures/sync.ts`:
    Export `syncStatus` object with last_sync completed, platforms healthy.

13. Create `frontend/lib/fixtures/search.ts`:
    Export `searchResults` array with 5 items: mix of material and discussion types across courses.

**All fixture files must:** Import generated types from `@/lib/api/types.gen.d.ts` for type safety using `import type { components } from "@/lib/api/types.gen"` and annotate exported data. If the generated types don't export conveniently, use inline `satisfies` or type assertions to ensure data matches the OpenAPI schema.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm typecheck && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/lib/fixtures/helpers.ts exports mockResponse, mockError, mockDelay, shouldSimulateError, requireAuth
    - frontend/lib/fixtures/helpers.ts contains `NextResponse.json` usage
    - frontend/lib/fixtures/helpers.ts contains `crypto.randomUUID()` in mockResponse
    - frontend/lib/fixtures/users.ts exports mockUser with email "student@sydney.edu.au"
    - frontend/lib/fixtures/courses.ts exports courses array with 5 entries
    - frontend/lib/fixtures/courses.ts contains "COMP2017", "COMP3221", "STAT2011", "INFO2222", "MATH1005"
    - frontend/lib/fixtures/deadlines.ts exports deadlines array with 8+ entries
    - frontend/lib/fixtures/gpa.ts exports gpaReport with current_wam 78.5
    - frontend/lib/fixtures/alerts.ts exports alerts array with gpa_risk and deadline_risk types
    - frontend/lib/fixtures/notifications.ts exports notifications array with deadline_reminder type
    - All fixture files import types from "@/lib/api/types.gen" or use inline type annotations
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>13 fixture data files created with realistic USYD course data covering normal, empty, and risk scenarios. Mock helpers provide consistent response envelope formatting. All files type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Create all Route Handler mock endpoints</name>
  <files>
    frontend/app/api/v1/health/route.ts,
    frontend/app/api/v1/auth/register/route.ts,
    frontend/app/api/v1/auth/login/route.ts,
    frontend/app/api/v1/auth/refresh/route.ts,
    frontend/app/api/v1/auth/logout/route.ts,
    frontend/app/api/v1/users/me/route.ts,
    frontend/app/api/v1/users/me/tokens/[platform]/route.ts,
    frontend/app/api/v1/users/me/tokens/[platform]/verify/route.ts,
    frontend/app/api/v1/users/me/export/route.ts,
    frontend/app/api/v1/courses/route.ts,
    frontend/app/api/v1/courses/[id]/route.ts,
    frontend/app/api/v1/courses/[id]/grades/route.ts,
    frontend/app/api/v1/courses/[id]/materials/route.ts,
    frontend/app/api/v1/courses/[id]/discussions/route.ts,
    frontend/app/api/v1/courses/[id]/deadlines/route.ts,
    frontend/app/api/v1/courses/[id]/outline/route.ts,
    frontend/app/api/v1/gpa/route.ts,
    frontend/app/api/v1/gpa/predict/route.ts,
    frontend/app/api/v1/gpa/path/route.ts,
    frontend/app/api/v1/deadlines/route.ts,
    frontend/app/api/v1/deadlines/upcoming/route.ts,
    frontend/app/api/v1/digest/latest/route.ts,
    frontend/app/api/v1/digest/history/route.ts,
    frontend/app/api/v1/alerts/route.ts,
    frontend/app/api/v1/notifications/route.ts,
    frontend/app/api/v1/sync/trigger/route.ts,
    frontend/app/api/v1/sync/status/route.ts,
    frontend/app/api/v1/search/route.ts
  </files>
  <read_first>
    - frontend/lib/fixtures/helpers.ts (mock helper functions — mockResponse, mockError, mockDelay, shouldSimulateError, requireAuth)
    - frontend/lib/fixtures/courses.ts (course fixture data)
    - frontend/lib/fixtures/users.ts (user fixture data)
    - frontend/openapi/openapi.yaml (verify response schema shape for each endpoint)
    - docs/UniBoard_TRD_v2.md (lines 1497-2183, section 12 — endpoint behaviors, query params, status codes)
    - .planning/phases/02-api-contracts-mock-layer/02-RESEARCH.md (Pattern 4: Route Handler mock structure, Pitfall 4: response must match OpenAPI envelope, Pitfall 7: mock auth validation)
  </read_first>
  <action>
**Every Route Handler follows this pattern:**
```typescript
import { mockDelay, mockResponse, mockError, requireAuth, shouldSimulateError } from "@/lib/fixtures/helpers";
import { fixtureData } from "@/lib/fixtures/domain";

export async function GET(request: Request) {
  await mockDelay();
  if (shouldSimulateError()) {
    return mockError("INTERNAL_ERROR", "Mock server error", 500);
  }
  const authError = requireAuth(request);
  if (authError) return authError;
  // ... return mockResponse(data)
}
```

**Auth endpoints (NO auth required):**

1. `POST /auth/register` — Parse body {email, password, display_name}. Validate email contains "@". Return `mockResponse({ user_id: "usr_001", pending_verification: true })` with status 201. On validation fail, return `mockError("VALIDATION_ERROR", "Invalid email format", 400)`.

2. `POST /auth/login` — Parse body {email, password}. Accept any email/password combo for mock. Return `mockResponse({ access_token: "mock-jwt-...", refresh_token: "mock-jwt-refresh-...", expires_in: 900, user: mockUser })`.

3. `POST /auth/refresh` — Parse body {refresh_token}. Return new mock tokens.

4. `POST /auth/logout` — Return `NextResponse.json(null, { status: 204 })`.

5. `POST /auth/forgot-password` — Return `mockResponse({ message: "Password reset code sent" })`.

6. `POST /auth/confirm-password` — Return `mockResponse({ message: "Password reset successful" })`.

**User endpoints (auth required):**

7. `GET /users/me` — Return `mockResponse(mockUser)`.
8. `PATCH /users/me` — Parse body, merge with mockUser, return updated user.
9. `DELETE /users/me` — Return 204.
10. `GET /users/me/export` — Return `mockResponse({ download_url: "https://s3.example.com/export/...", expires_at: new Date(Date.now() + 3600000).toISOString() })`.
11. `PUT /users/me/tokens/{platform}` — Extract platform from URL segment. Return `mockResponse({ status: "active", courses_found: 5 })`.
12. `DELETE /users/me/tokens/{platform}` — Return 204.
13. `POST /users/me/tokens/{platform}/verify` — Return `mockResponse({ status: "active", valid: true })`.

**Course endpoints (auth required):**

14. `GET /courses` — Read `semester` query param. Filter courses fixture by semester if provided. Return `mockResponse(filtered)`.
15. `GET /courses/{id}` — Look up courseDetails by id param. Return `mockResponse(detail)` or `mockError("NOT_FOUND", "Course not found", 404)`.
16. `GET /courses/{id}/grades` — Look up gradesByCourse. Return `mockResponse(grades)`.
17. `GET /courses/{id}/materials` — Look up materialsByCourse. Filter by `source` query param if present. Return `mockResponse(materials)`.
18. `GET /courses/{id}/discussions` — Look up discussionsByCourse. Support `filter` query param (high_value/endorsed/staff/all). Apply cursor/limit pagination via mockPaginatedResponse.
19. `GET /courses/{id}/deadlines` — Filter deadlines fixture by course id. Return `mockResponse(filtered)`.
20. `GET /courses/{id}/outline` — Return outline fixture matching the course.

**GPA endpoints (auth required):**

21. `GET /gpa` — Return `mockResponse(gpaReport)`.
22. `POST /gpa/predict` — Parse body {what_if_scores, scale}. Return `mockResponse(gpaPredictionDefault)`.
23. `POST /gpa/path` — Parse body {target_wam}. Return `mockResponse(gpaPathDefault)`.

**Deadline endpoints (auth required):**

24. `GET /deadlines` — Support `from`, `to`, `course_id` query params for filtering. Return `mockResponse(filtered)`.
25. `GET /deadlines/upcoming` — Filter deadlines where days_remaining <= 7. Return `mockResponse(filtered)`.

**Intelligence endpoints (auth required):**

26. `GET /digest/latest` — Return `mockResponse(digestLatest)`.
27. `GET /digest/history` — Support cursor/limit. Return paginated `mockPaginatedResponse(digestHistory, ...)`.
28. `GET /alerts` — Return `mockResponse(alerts)`.
29. `GET /notifications` — Support `unread_only` query param. Filter if true. Support cursor/limit pagination.

**Sync endpoints (auth required):**

30. `POST /sync/trigger` — Parse body {scope}. Return `mockResponse({ sync_id: "sync_" + crypto.randomUUID().slice(0,6), status: "in_progress", started_at: new Date().toISOString() })` with status 202.
31. `GET /sync/status` — Return `mockResponse(syncStatus)`.

**Search endpoint (auth required):**

32. `GET /search` — Require `q` query param (return 400 if missing). Filter searchResults by query string (case-insensitive includes on title/snippet). Support `scope`, `course_id`, `limit` params. Return `mockResponse(filtered)`.

**Health endpoint (NO auth required):**

33. `GET /health` — No auth check. No simulated error. Return `mockResponse({ status: "healthy", version: "1.0.0", checks: { database: "ok", canvas_api: "ok", ed_api: "ok", cognito: "ok" }, timestamp: new Date().toISOString() })`.

**For Route Handlers with dynamic segments** like `[id]` and `[platform]`, use the Next.js App Router convention:
```typescript
export async function GET(
  request: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params;
  // ...
}
```
Note: In Next.js 15, `params` is a Promise and must be awaited.

**File organization:** Each Route Handler file is placed at the path matching the API route. Files are at `frontend/app/api/v1/...` (NOT inside `[locale]`). The existing middleware matcher `["/((?!api|_next|.*\\..*).*)"]` already excludes `/api/` from i18n processing.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm build && pnpm typecheck && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/api/v1/health/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/auth/login/route.ts exists and contains `export async function POST`
    - frontend/app/api/v1/auth/register/route.ts exists and contains `export async function POST`
    - frontend/app/api/v1/courses/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/courses/[id]/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/courses/[id]/grades/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/courses/[id]/materials/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/courses/[id]/discussions/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/courses/[id]/deadlines/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/courses/[id]/outline/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/gpa/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/gpa/predict/route.ts exists and contains `export async function POST`
    - frontend/app/api/v1/gpa/path/route.ts exists and contains `export async function POST`
    - frontend/app/api/v1/deadlines/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/deadlines/upcoming/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/digest/latest/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/digest/history/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/alerts/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/notifications/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/sync/trigger/route.ts exists and contains `export async function POST`
    - frontend/app/api/v1/sync/status/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/search/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/users/me/route.ts exists and contains both `export async function GET` and `export async function PATCH`
    - All route handlers import from `@/lib/fixtures/helpers`
    - `pnpm build` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>All 22 Route Handler files created covering 33 endpoint operations. Each mock returns fixture data in the correct envelope format, validates auth tokens, and simulates latency/errors. Build and typecheck pass.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm build` exits 0 (all Route Handlers compile)
2. `cd frontend && pnpm typecheck` exits 0 (fixture types match OpenAPI schema)
3. `cd frontend && pnpm test -- --run` exits 0 (existing tests still pass)
4. Start dev server (`pnpm dev`), then:
   - `curl http://localhost:3001/api/v1/health` returns `{"data":{"status":"healthy",...},"meta":{...}}`
   - `curl http://localhost:3001/api/v1/courses` returns `{"error":{"code":"AUTH_REQUIRED",...}}` (no auth header)
   - `curl -H "Authorization: Bearer test" http://localhost:3001/api/v1/courses` returns `{"data":[...],"meta":{...}}`
</verification>

<success_criteria>
- 13 fixture data files with realistic USYD course data covering normal, empty, and risk scenarios
- 22 Route Handler files covering all 33 endpoint operations from the OpenAPI spec
- All mock responses use {data, meta} envelope (success) or {error: {code, message}} (errors)
- Auth-required endpoints return 401 without Bearer token
- Simulated latency and error rate included for frontend resilience testing
- Build and typecheck pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-contracts-mock-layer/02-02-SUMMARY.md`
</output>
