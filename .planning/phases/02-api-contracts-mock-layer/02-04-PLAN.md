---
phase: 02-api-contracts-mock-layer
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - frontend/app/api/v1/digest/latest/route.ts
  - frontend/app/api/v1/digest/history/route.ts
  - frontend/app/api/v1/alerts/route.ts
  - frontend/app/api/v1/notifications/route.ts
  - frontend/app/api/v1/sync/trigger/route.ts
  - frontend/app/api/v1/sync/status/route.ts
  - frontend/app/api/v1/search/route.ts
  - frontend/app/api/v1/health/route.ts
  - frontend/__tests__/api/mock-routes.test.ts
autonomous: true
requirements: [INFRA-11]

must_haves:
  truths:
    - "Intelligence route handlers return digest, alerts, and notification fixture data"
    - "Sync route handlers return status and accept trigger requests"
    - "Search route handler filters results by query string"
    - "Health endpoint returns 200 without auth requirement"
    - "Wave 0 mock-routes test validates response envelope format across representative endpoints"
  artifacts:
    - path: "frontend/app/api/v1/health/route.ts"
      provides: "GET /api/v1/health mock endpoint (no auth)"
      contains: "export async function GET"
    - path: "frontend/app/api/v1/digest/latest/route.ts"
      provides: "GET /api/v1/digest/latest mock endpoint"
      contains: "export async function GET"
    - path: "frontend/app/api/v1/search/route.ts"
      provides: "GET /api/v1/search mock endpoint with query filtering"
      contains: "export async function GET"
    - path: "frontend/__tests__/api/mock-routes.test.ts"
      provides: "Wave 0 test for route handler envelope format"
      contains: "describe"
  key_links:
    - from: "frontend/app/api/v1/digest/latest/route.ts"
      to: "frontend/lib/fixtures/digest.ts"
      via: "import digestLatest fixture data"
      pattern: "from.*fixtures/digest"
    - from: "frontend/app/api/v1/search/route.ts"
      to: "frontend/lib/fixtures/search.ts"
      via: "import searchResults fixture data"
      pattern: "from.*fixtures/search"
---

<objective>
Implement Route Handler mocks for intelligence (digest, alerts, notifications), sync, search, and health endpoints — the remaining API domains. Also create the Wave 0 mock-routes test that validates the response envelope format.

Purpose: Complete the mock API layer so all page phases can develop with full endpoint coverage. The mock-routes test ensures envelope consistency across all route handlers.

Output: 8 Route Handler files + 1 test file.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-api-contracts-mock-layer/02-CONTEXT.md
@.planning/phases/02-api-contracts-mock-layer/02-RESEARCH.md
@docs/UniBoard_TRD_v2.md (sections 12.7, 12.8, 12.9, 12.10 — intelligence, sync, search, health endpoint specs)

@.planning/phases/02-api-contracts-mock-layer/02-01-SUMMARY.md (Plan 01 — OpenAPI spec, generated types)
@.planning/phases/02-api-contracts-mock-layer/02-02-SUMMARY.md (Plan 02 — fixtures, helpers, auth/user routes)

<interfaces>
<!-- From Plan 02 (must exist before this plan runs) -->

From frontend/lib/fixtures/helpers.ts:
```typescript
export function mockResponse<T>(data: T, status?: number): NextResponse;
export function mockError(code: string, message: string, status: number): NextResponse;
export function mockDelay(min?: number, max?: number): Promise<void>;
export function shouldSimulateError(rate?: number): boolean;
export function requireAuth(request: Request): NextResponse | null;
export function mockPaginatedResponse<T>(data: T[], cursor: string | null, limit: number): NextResponse;
```

From frontend/lib/fixtures/digest.ts:
```typescript
export const digestLatest: DigestLatest;
export const digestHistory: DigestSummary[];
```

From frontend/lib/fixtures/alerts.ts:
```typescript
export const alerts: Alert[];
```

From frontend/lib/fixtures/notifications.ts:
```typescript
export const notifications: Notification[];
```

From frontend/lib/fixtures/sync.ts:
```typescript
export const syncStatus: SyncStatusResponse;
```

From frontend/lib/fixtures/search.ts:
```typescript
export const searchResults: SearchResult[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create intelligence, sync, search, and health Route Handler mocks</name>
  <files>
    frontend/app/api/v1/digest/latest/route.ts,
    frontend/app/api/v1/digest/history/route.ts,
    frontend/app/api/v1/alerts/route.ts,
    frontend/app/api/v1/notifications/route.ts,
    frontend/app/api/v1/sync/trigger/route.ts,
    frontend/app/api/v1/sync/status/route.ts,
    frontend/app/api/v1/search/route.ts,
    frontend/app/api/v1/health/route.ts
  </files>
  <read_first>
    - frontend/lib/fixtures/helpers.ts (mock helper functions)
    - frontend/lib/fixtures/digest.ts (digest fixture data)
    - frontend/lib/fixtures/alerts.ts (alerts fixture data)
    - frontend/lib/fixtures/notifications.ts (notification fixture data)
    - frontend/lib/fixtures/sync.ts (sync status fixture data)
    - frontend/lib/fixtures/search.ts (search results fixture data)
    - frontend/openapi/openapi.yaml (verify response schema shapes)
    - docs/UniBoard_TRD_v2.md (sections 12.7-12.10 — endpoint behaviors)
  </read_first>
  <action>
**Intelligence endpoints (auth required):**

1. `GET /digest/latest` — Return `mockResponse(digestLatest)`.

2. `GET /digest/history` — Support `cursor` and `limit` query params. Return paginated response using `mockPaginatedResponse(digestHistory, cursor, limit || 10)`.

3. `GET /alerts` — Return `mockResponse(alerts)`.

4. `GET /notifications` — Support `unread_only` query param (boolean). If `unread_only=true`, filter notifications where `is_read === false`. Support `cursor` and `limit` for pagination. Return paginated response.

**Sync endpoints (auth required):**

5. `POST /sync/trigger` — Parse body {scope}. Return `mockResponse({ sync_id: "sync_" + crypto.randomUUID().slice(0,6), status: "in_progress", started_at: new Date().toISOString() })` with status 202.

6. `GET /sync/status` — Return `mockResponse(syncStatus)`.

**Search endpoint (auth required):**

7. `GET /search` — Require `q` query param (return `mockError("VALIDATION_ERROR", "Query parameter 'q' is required", 400)` if missing). Filter searchResults by query string (case-insensitive includes on title and snippet). Support `scope` (filter by type), `course_id` (filter by course_code), `limit` (cap results) params. Return `mockResponse(filtered)`.

**Health endpoint (NO auth required, NO simulated errors):**

8. `GET /health` — No auth check. No simulated error. No delay. Return `mockResponse({ status: "healthy", version: "1.0.0", checks: { database: "ok", canvas_api: "ok", ed_api: "ok", cognito: "ok" }, timestamp: new Date().toISOString() })`.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm build && pnpm typecheck && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/api/v1/health/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/health/route.ts does NOT contain `requireAuth`
    - frontend/app/api/v1/digest/latest/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/digest/history/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/alerts/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/notifications/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/sync/trigger/route.ts exists and contains `export async function POST`
    - frontend/app/api/v1/sync/status/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/search/route.ts exists and contains `export async function GET`
    - frontend/app/api/v1/search/route.ts validates `q` param presence
    - All route handlers (except health) import from `@/lib/fixtures/helpers`
    - `pnpm build` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>8 Route Handler files for intelligence, sync, search, and health domains created. Health endpoint is public. Search validates required query param. All responses use correct envelope format. Build and typecheck pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave 0 mock-routes test</name>
  <files>frontend/__tests__/api/mock-routes.test.ts</files>
  <read_first>
    - frontend/lib/fixtures/helpers.ts (the mockResponse and mockError functions being tested)
    - frontend/app/api/v1/health/route.ts (simplest route to test against)
    - frontend/app/api/v1/courses/route.ts (representative auth-required route)
    - frontend/vitest.config.ts (test configuration)
  </read_first>
  <behavior>
    - Test 1: mockResponse wraps data in {data, meta: {request_id, timestamp}} envelope
    - Test 2: mockError wraps error in {error: {code, message}} format
    - Test 3: requireAuth returns null when Authorization Bearer header is present
    - Test 4: requireAuth returns 401 error when Authorization header is missing
    - Test 5: health endpoint returns {data: {status: "healthy", ...}} without auth
    - Test 6: mockPaginatedResponse includes pagination.next_cursor and pagination.has_more
  </behavior>
  <action>
Create `frontend/__tests__/api/mock-routes.test.ts`:

1. Test the `mockResponse` helper directly:
   - Call `mockResponse({ foo: "bar" })`
   - Parse the response JSON
   - Assert structure: `{ data: { foo: "bar" }, meta: { request_id: expect.any(String), timestamp: expect.any(String) } }`
   - Assert request_id is UUID format

2. Test the `mockError` helper:
   - Call `mockError("AUTH_REQUIRED", "Authentication required", 401)`
   - Parse the response JSON
   - Assert structure: `{ error: { code: "AUTH_REQUIRED", message: "Authentication required" } }`
   - Assert response status is 401

3. Test the `requireAuth` helper:
   - Create mock Request with `Authorization: Bearer test-token` header -> returns null
   - Create mock Request without Authorization header -> returns 401 response

4. Test `mockPaginatedResponse`:
   - Pass array of 5 items with limit 2 -> returns first 2 items + has_more: true + next_cursor
   - Pass array of 2 items with limit 5 -> returns all items + has_more: false + next_cursor: null

5. Integration test for health endpoint:
   - Import GET from health route
   - Call GET with a mock Request
   - Assert response contains `data.status === "healthy"` and `data.checks`

Note: Use `new Request("http://localhost/api/v1/health")` to create test requests. For route handlers that need auth, use `new Request("http://localhost/api/v1/courses", { headers: { Authorization: "Bearer test" } })`.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm test -- --run __tests__/api/mock-routes.test.ts && echo "PASS"</automated>
  </verify>
  <done>Wave 0 mock-routes test created. Validates response envelope format, error format, auth helper, pagination helper, and health endpoint integration. All tests pass.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm build` exits 0 (all Route Handlers compile)
2. `cd frontend && pnpm typecheck` exits 0
3. `cd frontend && pnpm test -- --run` exits 0 (all tests including new mock-routes test pass)
4. Start dev server (`pnpm dev`), then:
   - `curl http://localhost:3001/api/v1/health` returns `{"data":{"status":"healthy",...},"meta":{...}}`
   - `curl http://localhost:3001/api/v1/search?q=test` returns 401 (no auth)
   - `curl -H "Authorization: Bearer test" "http://localhost:3001/api/v1/search?q=memory"` returns filtered results
</verification>

<success_criteria>
- 8 Route Handler files for intelligence, sync, search, and health domains
- Health endpoint returns 200 without auth
- Search endpoint validates required `q` parameter
- Notification endpoint supports unread_only filtering
- Digest history supports cursor pagination
- Wave 0 mock-routes test validates envelope format
- Build, typecheck, and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-contracts-mock-layer/02-04-SUMMARY.md`
</output>
