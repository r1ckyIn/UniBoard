---
phase: 04-intelligence-skills
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .claude/skills/data-collection/SKILL.md
  - .claude/skills/data-collection/rules/canvas-api.md
  - .claude/skills/data-collection/rules/ed-api.md
  - .claude/skills/data-collection/rules/unit-outline.md
  - .claude/skills/data-processing/SKILL.md
  - .claude/skills/data-processing/rules/dedup.md
  - .claude/skills/data-processing/rules/gpa-math.md
  - .claude/skills/data-processing/rules/parsing.md
  - .claude/skills/ai-analysis/SKILL.md
  - .claude/skills/ai-analysis/rules/thread-eval.md
  - .claude/skills/ai-analysis/rules/qa-pipeline.md
  - .claude/skills/ai-analysis/rules/digest-scoring.md
  - .claude/skills/ai-analysis/rules/quality-gate.md
  - .claude/skills/user-actions/SKILL.md
  - .claude/skills/user-actions/rules/api-patterns.md
  - .claude/skills/user-actions/rules/error-handling.md
  - .claude/skills/user-actions/rules/notification-patterns.md
  - .claude/skills/courses/SKILL.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
  - PLAT-03

must_haves:
  truths:
    - "After Phase 4 development, skill files document the optimal data acquisition paths discovered"
    - "Subsequent Claude Code sessions can load skills instead of re-exploring the codebase"
    - "Skills capture per-course differentiation patterns (different Ed course structures)"
    - "At least 20 skill rules covering data collection, processing, AI analysis, and user actions"
    - "PLAT-03 (MCP server) is explicitly documented as deferred to v1.1+"
  artifacts:
    - path: ".claude/skills/data-collection/SKILL.md"
      provides: "Index of data collection skills (Canvas, Ed, Unit Outline)"
      contains: "data-collection"
    - path: ".claude/skills/data-processing/SKILL.md"
      provides: "Index of data processing skills (dedup, GPA math, parsing)"
      contains: "data-processing"
    - path: ".claude/skills/ai-analysis/SKILL.md"
      provides: "Index of AI analysis skills (thread eval, Q&A, digest, quality gate)"
      contains: "ai-analysis"
    - path: ".claude/skills/user-actions/SKILL.md"
      provides: "Index of user action skills (API patterns, error handling, notifications)"
      contains: "user-actions"
    - path: ".claude/skills/courses/SKILL.md"
      provides: "Per-course skill differentiation patterns"
      contains: "course-specific"
  key_links:
    - from: ".claude/skills/data-collection/SKILL.md"
      to: ".claude/skills/data-collection/rules/"
      via: "SKILL.md references rules/*.md files"
      pattern: "rules/"
    - from: ".claude/skills/ai-analysis/SKILL.md"
      to: ".claude/skills/ai-analysis/rules/"
      via: "SKILL.md references rules/*.md files"
      pattern: "rules/"
---

<objective>
Create the Claude Code skill system: auto-generated developer tooling skill files in `.claude/skills/` that document optimal data acquisition paths, error handling approaches, and per-course patterns discovered during UniBoard development. This is NOT runtime code -- these are markdown files that teach future Claude Code sessions how to work with the UniBoard codebase efficiently. Also explicitly document PLAT-03 (MCP server) as deferred to v1.1+.

Purpose: Encode the knowledge gained during Phases 1-4 into reusable skill templates so future development sessions start with expert-level understanding instead of re-exploring the codebase.
Output: ~20+ skill rule files across 5 categories (data-collection, data-processing, ai-analysis, user-actions, courses).
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-intelligence-skills/04-CONTEXT.md
@.planning/phases/04-intelligence-skills/04-RESEARCH.md
@.planning/phases/04-intelligence-skills/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data-collection and data-processing skill categories</name>
  <files>
    .claude/skills/data-collection/SKILL.md,
    .claude/skills/data-collection/rules/canvas-api.md,
    .claude/skills/data-collection/rules/ed-api.md,
    .claude/skills/data-collection/rules/unit-outline.md,
    .claude/skills/data-processing/SKILL.md,
    .claude/skills/data-processing/rules/dedup.md,
    .claude/skills/data-processing/rules/gpa-math.md,
    .claude/skills/data-processing/rules/parsing.md
  </files>
  <read_first>
    src/adapters/canvas.py, src/adapters/ed_discussion.py, src/adapters/ed_lessons.py,
    src/parsers/unit_outline.py,
    src/services/deadline.py, src/services/gpa.py, src/services/materials.py,
    src/models/discussion.py, src/models/grade.py,
    CLAUDE.md
  </read_first>
  <action>
    Create the `.claude/skills/` directory structure. Each category gets a SKILL.md index (~100-130 lines) and rules/ subdirectory with specific skill files.

    **1. `.claude/skills/data-collection/SKILL.md` (index, ~120 lines):**
    ```markdown
    # Data Collection Skills

    Skills for fetching data from external platforms (Canvas LMS, Ed Discussion, Ed Lessons, USYD Unit Outline).

    ## Quick Reference

    | Skill | File | When to Use |
    |-------|------|-------------|
    | Canvas API | rules/canvas-api.md | Fetching courses, grades, assignments, modules |
    | Ed API | rules/ed-api.md | Fetching threads, posts, lessons |
    | Unit Outline | rules/unit-outline.md | Parsing USYD assessment weights |

    ## Key Patterns

    - All adapters use `_request()` pattern: circuit breaker -> rate limiter -> HTTP -> header update
    - Pydantic response models with ConfigDict(extra='ignore', strict=False) for undocumented fields
    - Per-item error handling: ValidationError caught per-item, logged, skipped (never crash batch)
    - Ed adapters return empty on network failure (graceful degradation)

    ## Files

    - `src/adapters/canvas.py` — CanvasAdapter
    - `src/adapters/ed_discussion.py` — EdDiscussionAdapter
    - `src/adapters/ed_lessons.py` — EdLessonsAdapter
    - `src/parsers/unit_outline.py` — UnitOutlineParser

    ## Common Pitfalls

    - Canvas rate limiting uses X-Rate-Limit-Remaining header (sliding window)
    - Ed API is undocumented; field names verified in TRD SS9.4
    - Canvas Modules API requires `include[]=items` to avoid N+1
    - zsh export may escape special chars in tokens; use inline in curl
    ```

    **2. `.claude/skills/data-collection/rules/canvas-api.md`:**
    Document Canvas adapter patterns: rate limiting (CanvasRateLimiter reading X-Rate-Limit-Remaining), Link header pagination, circuit breaker (per-platform instance), explicit params instead of **kwargs (mypy --strict), token validation via httpx GET to /users/self. Include concrete function signatures from `src/adapters/canvas.py`. Document that `get_modules(course_id)` needs `include[]=items` param. List all Canvas endpoints used: `/courses`, `/courses/{id}/assignments?include[]=submission`, `/courses/{id}/modules?include[]=items`, `/users/self`.

    **3. `.claude/skills/data-collection/rules/ed-api.md`:**
    Document Ed Discussion and Lessons adapter patterns. Ed API has NO public documentation -- reference hschafer/edstem OSS. Verified field names: `content` not `passage`, `number` not `lesson_number` (TRD SS9.4). Document parse_ed_document() shared XML parser. Document that Ed adapters return empty lists on failure (graceful degradation). List all Ed endpoints: `/courses/{id}/threads`, `/threads/{id}`, `/courses/{id}/lessons`, `/lessons/{id}`.

    **4. `.claude/skills/data-collection/rules/unit-outline.md`:**
    Document UnitOutlineParser patterns: BeautifulSoup4 + lxml, weight-sum validation (95%-105%), CSS class selector with positional cell fallback, extraction of assessment name + weight + due_date from USYD HTML table format. Document that this is HTML scraping (not API).

    **5. `.claude/skills/data-processing/SKILL.md` (index, ~100 lines):**
    ```markdown
    # Data Processing Skills

    Skills for deduplication, GPA calculation, and data parsing.

    ## Quick Reference

    | Skill | File | When to Use |
    |-------|------|-------------|
    | Dedup | rules/dedup.md | Deadline dedup, notification dedup |
    | GPA Math | rules/gpa-math.md | WAM/GPA calculation, What-if |
    | Parsing | rules/parsing.md | Ed XML, HTML, JSON response parsing |

    ## Key Patterns

    - All GPA math uses Decimal with ROUND_HALF_UP (never float)
    - SHA-256 content_hash for deterministic dedup
    - rapidfuzz token_set_ratio(95) for fuzzy deadline matching
    - PushRecord model for notification dedup (unique constraint on user_id+content_hash)
    ```

    **6. `.claude/skills/data-processing/rules/dedup.md`:**
    Document SHA-256 dedup for deadlines (source+title hash) and notifications (user+type+title hash). Document rapidfuzz token_set_ratio with threshold 95 (not fuzz.ratio at 80). Document PushRecord unique constraint pattern. Include code snippets from `src/services/deadline.py` and `src/services/notification.py`.

    **7. `.claude/skills/data-processing/rules/gpa-math.md`:**
    Document Decimal(str(float)) conversion pattern. ROUND_HALF_UP for all WAM/GPA. Grade band thresholds (HD>=85, D>=75, CR>=65, P>=50, F<50). Credit-weighted WAM formula. JSONB for WhatIfScenario scores. Hypothesis property test strategies for mathematical invariants.

    **8. `.claude/skills/data-processing/rules/parsing.md`:**
    Document parse_ed_document() for Ed XML content. Document BeautifulSoup patterns for Unit Outline. Document Pydantic response models with extra='ignore' for undocumented API fields. Document per-item ValidationError handling pattern.
  </action>
  <verify>
    <automated>ls -la /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/data-collection/SKILL.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/data-collection/rules/canvas-api.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/data-collection/rules/ed-api.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/data-collection/rules/unit-outline.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/data-processing/SKILL.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/data-processing/rules/dedup.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/data-processing/rules/gpa-math.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/data-processing/rules/parsing.md && echo "All skill files exist"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/data-collection/SKILL.md exists and contains `Canvas API` and `Ed API` and `Unit Outline`
    - .claude/skills/data-collection/rules/canvas-api.md exists and contains `X-Rate-Limit-Remaining` and `include[]=items`
    - .claude/skills/data-collection/rules/ed-api.md exists and contains `content not passage` or `hschafer/edstem`
    - .claude/skills/data-collection/rules/unit-outline.md exists and contains `BeautifulSoup` and `weight-sum`
    - .claude/skills/data-processing/SKILL.md exists and contains `Dedup` and `GPA Math` and `Parsing`
    - .claude/skills/data-processing/rules/dedup.md exists and contains `sha256` or `SHA-256` and `token_set_ratio`
    - .claude/skills/data-processing/rules/gpa-math.md exists and contains `Decimal` and `ROUND_HALF_UP`
    - .claude/skills/data-processing/rules/parsing.md exists and contains `parse_ed_document` or `extra='ignore'`
    - Each SKILL.md file is between 80 and 150 lines
  </acceptance_criteria>
  <done>
    Data collection and data processing skill categories created with 3+3 rule files documenting Canvas adapter patterns, Ed API quirks, Unit Outline parsing, dedup algorithms, GPA math precision, and parsing patterns. All skills reference concrete source files and include code patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ai-analysis, user-actions, and courses skill categories, document PLAT-03 deferral</name>
  <files>
    .claude/skills/ai-analysis/SKILL.md,
    .claude/skills/ai-analysis/rules/thread-eval.md,
    .claude/skills/ai-analysis/rules/qa-pipeline.md,
    .claude/skills/ai-analysis/rules/digest-scoring.md,
    .claude/skills/ai-analysis/rules/quality-gate.md,
    .claude/skills/user-actions/SKILL.md,
    .claude/skills/user-actions/rules/api-patterns.md,
    .claude/skills/user-actions/rules/error-handling.md,
    .claude/skills/user-actions/rules/notification-patterns.md,
    .claude/skills/courses/SKILL.md
  </files>
  <read_first>
    src/services/ai_engine.py (created in 04-02),
    src/services/qa.py (created in 04-02),
    src/services/intelligence.py,
    src/services/notification.py (created in 04-01),
    src/services/digest.py (created in 04-01),
    src/prompts/thread_eval.py (created in 04-02),
    src/prompts/qa.py (created in 04-02),
    src/web/routes/__init__.py,
    src/schemas/common.py,
    .claude/skills/data-collection/SKILL.md (created in Task 1)
  </read_first>
  <action>
    **1. `.claude/skills/ai-analysis/SKILL.md` (index, ~120 lines):**
    ```markdown
    # AI Analysis Skills

    Skills for LLM integration, thread evaluation, Q&A pipeline, digest scoring, and quality gates.

    ## Quick Reference

    | Skill | File | When to Use |
    |-------|------|-------------|
    | Thread Evaluation | rules/thread-eval.md | Scoring Ed Discussion posts for GPA relevance |
    | Q&A Pipeline | rules/qa-pipeline.md | Course material Q&A with citations |
    | Digest Scoring | rules/digest-scoring.md | AI urgency scoring for daily digests |
    | Quality Gate | rules/quality-gate.md | F1 monitoring and rule-engine fallback |

    ## Key Patterns

    - AIEngine class wraps AsyncAnthropic, NOT called inline during HTTP requests (except Q&A)
    - Thread evaluation and digest scoring run in background jobs (APScheduler)
    - Sonnet for routine tasks (scoring, evaluation), Opus for deep tasks (Q&A, review, risk analysis)
    - All AI calls check ai_daily_limit_per_user (100/day) via User.ai_calls_today
    - Structured output via messages.create() with JSON parsing (NOT messages.parse() which may not be GA)
    - Prompts stored in src/prompts/ module, not hardcoded in services

    ## Files

    - `src/services/ai_engine.py` — AIEngine (Anthropic wrapper)
    - `src/services/qa.py` — QAService (hybrid Q&A)
    - `src/services/digest.py` — DigestService (AI-enhanced digest)
    - `src/prompts/` — All prompt constants
    ```

    **2. `.claude/skills/ai-analysis/rules/thread-eval.md`:**
    Document AIEngine.evaluate_thread pattern. Input: title, content, category, is_endorsed, is_staff_post. Output: ThreadEvaluation (gpa_relevance 0-1, category, summary, urgency, key_facts). Model: claude-sonnet-4-20250514 (routine scoring). Prompt: THREAD_EVAL_SYSTEM_PROMPT from src/prompts/thread_eval.py. Only evaluate NEW threads (synced_at > last_evaluation_at or gpa_relevance_score == 0.0). Store result in DiscussionThread.gpa_relevance_score column.

    **3. `.claude/skills/ai-analysis/rules/qa-pipeline.md`:**
    Document hybrid Q&A architecture:
    - Step 1: Count tokens via tiktoken cl100k_base
    - Step 2: If < 100K tokens, use direct context injection
    - Step 3: If >= 100K tokens, use RAG (Voyage AI voyage-3 embeddings + pgvector cosine_distance)
    - Citation format: `[Canvas: {source_name}]` or `[Ed: {lesson_title}]`
    - Model: claude-opus-4-6 (per locked decision -- Q&A needs deep understanding)
    - RAG: 512-token chunks, 50-token overlap, top-15 retrieval
    - pgvector: VECTOR(1024), HNSW index, cosine_distance operator

    **4. `.claude/skills/ai-analysis/rules/digest-scoring.md`:**
    Document AI urgency scoring for digest items. Input: list of digest items (grades, deadlines, posts). Output: urgency_score 1-5 per item + summary. Model: claude-sonnet-4-20250514 (routine). Scale: 5=immediate-action, 4=act-today, 3=this-week, 2=informational, 1=low-priority. AI summary: 2-3 sentences focusing on actionable items and GPA impact. Batch processing: all items in one API call for efficiency.

    **5. `.claude/skills/ai-analysis/rules/quality-gate.md`:**
    Document TRD SS6.4 quality gate:
    - Track Precision/Recall/F1 for thread evaluation (endorsed+staff as ground truth)
    - F1 < 75% triggers automatic fallback to rule engine (is_endorsed + is_staff_answered)
    - Log quality metrics in structlog for monitoring
    - Rule-engine fallback: threads with is_endorsed=True or is_staff_post=True
    - AI failure (network, parse error): fall back silently, log error, return rule-based result

    **6. `.claude/skills/user-actions/SKILL.md` (index, ~100 lines):**
    ```markdown
    # User Actions Skills

    Skills for API endpoint patterns, error handling, and notification delivery.

    ## Quick Reference

    | Skill | File | When to Use |
    |-------|------|-------------|
    | API Patterns | rules/api-patterns.md | Creating new REST endpoints |
    | Error Handling | rules/error-handling.md | UniboardError hierarchy, response format |
    | Notification Patterns | rules/notification-patterns.md | Creating and delivering notifications |

    ## Key Patterns

    - All routes use SuccessResponse[T] envelope with MetaInfo
    - Services injected via Depends(get_xxx_service) factory
    - get_current_user dependency for auth
    - UniboardError hierarchy for structured error responses
    ```

    **7. `.claude/skills/user-actions/rules/api-patterns.md`:**
    Document route patterns from the codebase:
    - Router: `APIRouter()`, included via api_router.include_router(prefix=, tags=)
    - Dependencies: `Depends(get_session)`, `Depends(get_current_user)`
    - Response envelope: `SuccessResponse[T](data=..., meta=get_request_meta(request))`
    - Service factory pattern: `async def get_xxx_service(session = Depends(get_session)) -> XxxService`
    - B008 ruff suppression for Depends() in function defaults
    - Per-file ignores in pyproject.toml for src/web/** and src/security/auth.py

    **8. `.claude/skills/user-actions/rules/error-handling.md`:**
    Document UniboardError hierarchy: TokenInvalidError, UpstreamAPIError, etc. Document error response format: `ErrorResponse(error=ErrorDetail(code=, message=), meta=)`. Document that errors never expose internal state or stack traces. Document circuit breaker pattern for external APIs.

    **9. `.claude/skills/user-actions/rules/notification-patterns.md`:**
    Document notification creation pattern: SHA-256 content_hash dedup via PushRecord, dual-channel (in_app + email), severity levels (critical/warning/info), async email sending via asyncio.create_task, APScheduler jobs for periodic checks. Document tiered reminder system: 72h=info, 24h=warning, 3h=critical.

    **10. `.claude/skills/courses/SKILL.md` (per-course differentiation, ~80 lines):**
    Document per-course skill differentiation:
    - Different courses have different Ed Discussion structures (some use categories, some don't)
    - Canvas module organization varies (some use weekly modules, some use topic-based)
    - Assessment weight sources differ: Unit Outline HTML vs Canvas assignment_groups (with fallback)
    - Course linking uses (course_code, semester) composite key with regex extraction
    - AI evaluation quality varies by course (STEM courses have more structured posts)
    - Include template for documenting course-specific patterns:
    ```
    ## Course: {COURSE_CODE}
    - Ed structure: {categories used, typical thread types}
    - Canvas structure: {module organization pattern}
    - Weight source: {unit_outline | assignment_groups}
    - AI evaluation notes: {any course-specific quirks}
    ```
    Note: PLAT-03 (MCP server for Claude Desktop) is DEFERRED to v1.1+. The skill system here serves development tooling, not runtime MCP operations.
  </action>
  <verify>
    <automated>ls -la /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/ai-analysis/SKILL.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/ai-analysis/rules/thread-eval.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/ai-analysis/rules/qa-pipeline.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/ai-analysis/rules/digest-scoring.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/ai-analysis/rules/quality-gate.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/user-actions/SKILL.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/user-actions/rules/api-patterns.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/user-actions/rules/error-handling.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/user-actions/rules/notification-patterns.md /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills/courses/SKILL.md && echo "All skill files exist" && find /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills -name "*.md" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/ai-analysis/SKILL.md exists and contains `Thread Evaluation` and `Q&A Pipeline` and `Quality Gate`
    - .claude/skills/ai-analysis/rules/thread-eval.md exists and contains `gpa_relevance` and `claude-sonnet`
    - .claude/skills/ai-analysis/rules/qa-pipeline.md exists and contains `direct context` and `RAG` and `cosine_distance` and `voyage-3`
    - .claude/skills/ai-analysis/rules/digest-scoring.md exists and contains `urgency_score` and `1-5`
    - .claude/skills/ai-analysis/rules/quality-gate.md exists and contains `F1` and `75%` and `fallback`
    - .claude/skills/user-actions/SKILL.md exists and contains `API Patterns` and `Error Handling` and `Notification`
    - .claude/skills/user-actions/rules/api-patterns.md exists and contains `SuccessResponse` and `Depends`
    - .claude/skills/user-actions/rules/error-handling.md exists and contains `UniboardError`
    - .claude/skills/user-actions/rules/notification-patterns.md exists and contains `PushRecord` and `SHA-256`
    - .claude/skills/courses/SKILL.md exists and contains `course-specific` and `PLAT-03` and `DEFERRED`
    - Total skill .md files count >= 18 (5 SKILL.md + 13+ rules)
  </acceptance_criteria>
  <done>
    AI analysis, user actions, and courses skill categories created with 4+3+1 rule files. Quality gate documents F1<75% fallback. Course skills document per-course differentiation patterns. PLAT-03 explicitly marked as deferred to v1.1+. Total skill system has 18+ markdown files across 5 categories covering data collection, processing, AI analysis, user actions, and course-specific patterns.
  </done>
</task>

</tasks>

<verification>
Verify all skill files exist and have substantive content:
```bash
find /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills -name "*.md" | wc -l && find /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.claude/skills -name "*.md" -exec wc -l {} + | tail -1
```
Expected: 18+ files, 1500+ total lines.
</verification>

<success_criteria>
1. 5 SKILL.md index files exist (data-collection, data-processing, ai-analysis, user-actions, courses)
2. Each SKILL.md is 80-150 lines with Quick Reference table and Key Patterns
3. 13+ rules/*.md files with concrete code patterns and source file references
4. data-collection skills document Canvas rate limiting, Ed API quirks, Unit Outline parsing
5. data-processing skills document SHA-256 dedup, Decimal GPA math, parsing patterns
6. ai-analysis skills document thread evaluation, hybrid Q&A, digest scoring, quality gate (F1<75%)
7. user-actions skills document API patterns (SuccessResponse envelope), error hierarchy, notification dedup
8. courses/SKILL.md documents per-course differentiation and includes course template
9. PLAT-03 (MCP server) is explicitly documented as DEFERRED to v1.1+
10. Total 18+ skill files that a future Claude Code session can load to understand UniBoard patterns
</success_criteria>

<output>
After completion, create `.planning/phases/04-intelligence-skills/04-03-SUMMARY.md`
</output>
