---
phase: 04-intelligence-skills
plan: 05
type: execute
wave: 1
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/components/layout/Header.tsx
  - frontend/components/layout/AppShell.tsx
  - frontend/components/layout/Sidebar.tsx
  - frontend/components/notifications/NotificationDropdown.tsx
  - frontend/components/ai/UnitReview.tsx
  - frontend/components/ai/AIHighValuePosts.tsx
  - frontend/components/ai/AIErrorBoundary.tsx
  - frontend/app/[locale]/(dashboard)/courses/[id]/page.tsx
  - frontend/__tests__/layout/Header.test.tsx
autonomous: true
gap_closure: true
requirements: [INTEL-02, INTEL-03, FILE-03]

must_haves:
  truths:
    - "Notification bell is in the header bar, not the sidebar, and accessible regardless of sidebar collapse state"
    - "UnitReview shows structured placeholder sections (key concepts, common mistakes, scope, tips) even when API is unavailable"
    - "AIHighValuePosts renders a visible card with fallback content even when API fails or returns empty"
  artifacts:
    - path: "frontend/components/layout/Header.tsx"
      provides: "Header bar with search placeholder + notification bell + user menu"
      min_lines: 40
    - path: "frontend/components/layout/AppShell.tsx"
      provides: "Updated layout with header row above content area"
      contains: "Header"
    - path: "frontend/components/layout/Sidebar.tsx"
      provides: "NotificationBell and NotificationDropdown removed"
    - path: "frontend/components/ai/AIErrorBoundary.tsx"
      provides: "Error boundary wrapper for AI components"
      min_lines: 20
    - path: "frontend/components/ai/UnitReview.tsx"
      provides: "Placeholder skeleton sections when API unavailable"
    - path: "frontend/components/ai/AIHighValuePosts.tsx"
      provides: "Error boundary wrapped, visible fallback guaranteed"
  key_links:
    - from: "frontend/components/layout/AppShell.tsx"
      to: "frontend/components/layout/Header.tsx"
      via: "import and render above main content"
      pattern: "import Header"
    - from: "frontend/components/layout/Header.tsx"
      to: "frontend/components/notifications/NotificationBell.tsx"
      via: "import and render in header-right"
      pattern: "NotificationBell"
    - from: "frontend/app/[locale]/(dashboard)/courses/[id]/page.tsx"
      to: "frontend/components/ai/AIErrorBoundary.tsx"
      via: "wraps UnitReview and AIHighValuePosts"
      pattern: "AIErrorBoundary"
---

<objective>
Fix 3 major UAT gaps: (1) relocate notification bell from sidebar to header per TRD §13.4, (2) show UnitReview placeholder skeleton when API unavailable instead of error state, (3) ensure AIHighValuePosts renders visibly with error boundary protection.

Purpose: User reported all three issues during UAT — notification bell inaccessible when sidebar collapsed, UnitReview shows nothing useful without API key, AIHighValuePosts invisible. These are visual/UX issues that block UAT sign-off.
Output: New Header component, updated AppShell layout, resilient AI component rendering.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-intelligence-skills/04-01-SUMMARY.md
@.planning/phases/04-intelligence-skills/04-02-SUMMARY.md
@.planning/phases/04-intelligence-skills/04-UAT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/components/layout/AppShell.tsx (CURRENT — will be modified):
```tsx
interface AppShellProps {
  children: ReactNode;
  rightPanel?: ReactNode;
}
// Current layout: flex with Sidebar (fixed left) + main (center) + RightPanel (fixed right)
// No header row exists
```

From frontend/components/layout/Sidebar.tsx (lines 142-165 — will be REMOVED):
```tsx
// Lines 17-18: NotificationBell and NotificationDropdown imports
// Lines 142-165: Bell icon + dropdown rendered at bottom of sidebar
```

From frontend/components/notifications/NotificationBell.tsx:
```tsx
export default function NotificationBell()
// Renders bell icon button with unread count badge
// Uses useNotificationStore() for toggleDropdown
// Uses useUnreadCount() for API polling
```

From frontend/components/notifications/NotificationDropdown.tsx:
```tsx
export default function NotificationDropdown()
// Absolute positioned dropdown
// Currently positioned: bottom: "100%" (opens upward from sidebar bell)
// Needs repositioning: top: "100%" (opens downward from header bell)
```

From frontend/lib/api/types.ts:
```tsx
interface UnitReviewResponse {
  key_concepts: string[];
  common_mistakes: string[];
  exam_scope: string;
  study_tips: string[];
  generated_at: string;
}
```

Design system tokens (from CSS vars):
- --color-dark, --color-orange, --color-cream
- --font-serif (Source Serif 4), --font-sans (Inter)
- --sidebar-w (68px), --sidebar-w-expanded (224px), --right-panel-w (300px)
- --ease, --ease-fast (transitions)
- --color-text-1, --color-text-2, --color-text-3
- --color-divider, --color-card-bg, --color-card-border
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component and relocate notification bell from sidebar</name>
  <files>
    frontend/components/layout/Header.tsx,
    frontend/components/layout/AppShell.tsx,
    frontend/components/layout/Sidebar.tsx,
    frontend/components/notifications/NotificationDropdown.tsx,
    frontend/__tests__/layout/Header.test.tsx
  </files>
  <action>
    **MAJOR 1 — Notification bell in wrong location:**

    TRD §13.4 specifies: `shared/` contains `header（搜索+通知+用户菜单）`. Currently no Header component exists — bell is in sidebar bottom, inaccessible when sidebar is collapsed.

    1. **Create `frontend/components/layout/Header.tsx`:**
       - "use client" component
       - Full-width header bar that sits above the main content area
       - Height: 56px, background: transparent or very subtle (var(--color-cream) at 60% opacity)
       - Bottom border: 1px solid var(--color-divider)
       - Left margin: var(--sidebar-w) to avoid overlapping sidebar
       - Right margin: var(--right-panel-w) to avoid overlapping right panel
       - Contains three sections (flex justify-between):
         a. **Left**: Page title placeholder (can be empty or show breadcrumb later)
         b. **Center/Right**: Search placeholder (text input styled with border, rounded, 240px width, placeholder "Search courses, deadlines...") — functionality deferred, just visual placeholder
         c. **Right**: NotificationBell + NotificationDropdown (from existing components) + User avatar circle (initials, using var(--color-orange) background)
       - Import NotificationBell from "@/components/notifications/NotificationBell"
       - Import NotificationDropdown from "@/components/notifications/NotificationDropdown"
       - Wrap bell+dropdown in a `relative` div so dropdown positions correctly
       - Style consistent with Anthropic-inspired design system: warm colors, serif for headings, Inter for body

    2. **Update `frontend/components/layout/AppShell.tsx`:**
       - Import Header
       - Add Header above the `<main>` element
       - Update main element: add `paddingTop` or `marginTop` to account for header height (56px)
       - Header should be `position: fixed`, `top: 0`, `z-index: 90` (below sidebar z-100)
       - Main content gets `marginTop: 56px` (header height) in addition to existing padding

    3. **Update `frontend/components/layout/Sidebar.tsx`:**
       - Remove the NotificationBell import (line 17)
       - Remove the NotificationDropdown import (line 18)
       - Remove the entire notification bell section (lines 142-165: the `<div className="relative">` block)
       - Keep the Settings link at the bottom as-is

    4. **Update `frontend/components/notifications/NotificationDropdown.tsx`:**
       - Change positioning from `bottom: "100%"` (upward from sidebar) to `top: "100%"` (downward from header)
       - Change `left: 0` to `right: 0` (dropdown aligns to right edge of bell, not left)
       - Change `marginBottom: 8` to `marginTop: 8`
       - Keep everything else the same

    5. **Create test `frontend/__tests__/layout/Header.test.tsx`:**
       - Test that Header renders NotificationBell
       - Test that search placeholder input exists
       - Mock useNotificationStore and useUnreadCount
       - Use vitest + @testing-library/react

    **Design notes:**
    - The header must be always visible regardless of sidebar collapse state
    - Search input is a visual placeholder only (no functionality yet)
    - Use the same warm color palette and design tokens as existing components
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm build && pnpm test -- --run __tests__/layout/Header.test.tsx && pnpm lint --max-warnings 0 && pnpm typecheck</automated>
  </verify>
  <done>
    - Header.tsx exists with notification bell, search placeholder, and user avatar
    - AppShell renders Header above main content with proper spacing
    - Sidebar no longer contains NotificationBell or NotificationDropdown
    - NotificationDropdown opens downward from header (not upward from sidebar)
    - Notification bell is always accessible regardless of sidebar state
    - Header test passes
    - Build, lint, typecheck all clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Add AI component error boundaries and UnitReview/AIHighValuePosts fallbacks</name>
  <files>
    frontend/components/ai/AIErrorBoundary.tsx,
    frontend/components/ai/UnitReview.tsx,
    frontend/components/ai/AIHighValuePosts.tsx,
    frontend/app/[locale]/(dashboard)/courses/[id]/page.tsx
  </files>
  <action>
    **MAJOR 2 — UnitReview shows error state instead of structure:**
    **MAJOR 3 — AIHighValuePosts not visible:**

    Both AI components exist and work correctly when the API is available, but show unhelpful error/empty states when ANTHROPIC_API_KEY is not set. Users reported these as "missing" during UAT.

    1. **Create `frontend/components/ai/AIErrorBoundary.tsx`:**
       - React class component error boundary (cannot use hooks in error boundaries)
       - Catches render errors in children
       - Renders a RoughCard with a friendly message: "This feature requires AI services. Please check your API configuration."
       - Accepts `fallback` prop (ReactNode) for custom fallback UI
       - Accepts `featureName` prop (string) for the error message
       - Style consistent with design system (warm colors, serif heading)

    2. **Update `frontend/components/ai/UnitReview.tsx`:**
       - Modify the `isError || !review` branch (lines 39-50) to show **placeholder skeleton sections** instead of a generic error message
       - Show all 4 section headings with their icons (BookOpen, AlertTriangle, Target, Lightbulb) and placeholder text:
         - "Key Concepts" with: "Connect AI services to generate concept analysis"
         - "Common Mistakes" with: "AI analysis will highlight frequent errors"
         - "Exam Scope" with: "Assessment coverage analysis will appear here"
         - "Study Tips" with: "Personalized study recommendations will be generated"
       - Use muted text color (var(--color-text-3)) for placeholder text
       - This way the component always shows its structure, giving users a preview of what they'll get

    3. **Update `frontend/components/ai/AIHighValuePosts.tsx`:**
       - The empty/error fallback at lines 185-194 currently shows "No AI-scored posts available for this course" — this is fine for empty data
       - Add a minimum visible height and more descriptive message for the error case specifically: "AI post analysis is being configured. Check back after discussions are synced."
       - Ensure the RoughCard is always rendered (never returns null) so the component section is visible on the page

    4. **Update `frontend/app/[locale]/(dashboard)/courses/[id]/page.tsx`:**
       - Import AIErrorBoundary
       - Wrap `<AIHighValuePosts>`, `<CourseQA>`, and `<UnitReview>` each in `<AIErrorBoundary featureName="...">` to catch any uncaught render errors
       - This prevents one AI component crash from taking down the entire course detail page

    **Important constraints:**
    - All code comments in English only
    - Use existing design system tokens (--color-text-3, --font-serif, etc.)
    - Do NOT change the happy path rendering (when API works, everything stays the same)
    - The error boundary is a safety net — the primary fix is the placeholder sections in UnitReview
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm build && pnpm lint --max-warnings 0 && pnpm typecheck</automated>
  </verify>
  <done>
    - AIErrorBoundary.tsx exists and catches render errors with friendly fallback
    - UnitReview error/no-data state shows all 4 section headings with placeholder descriptions (not blank)
    - AIHighValuePosts always renders a visible RoughCard (never invisible/null)
    - Course detail page wraps all AI components in AIErrorBoundary
    - Build, lint, typecheck all clean
    - Happy path rendering unchanged (when API available, full content shows)
  </done>
</task>

</tasks>

<verification>
```bash
# Full frontend verification
cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend
pnpm build && pnpm test -- --run && pnpm lint --max-warnings 0 && pnpm typecheck
```
</verification>

<success_criteria>
1. Notification bell renders in the header bar, always visible regardless of sidebar state
2. Sidebar no longer contains notification bell
3. UnitReview without API key shows 4 structured sections with placeholder text (not a generic error)
4. AIHighValuePosts always renders a visible card on the course detail page
5. AI components are wrapped in error boundaries preventing cascade failures
6. All existing tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-intelligence-skills/04-05-SUMMARY.md`
</output>
