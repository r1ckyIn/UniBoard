---
phase: 04-setup-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/messages/en.json
  - frontend/messages/zh.json
  - frontend/lib/validation/token.ts
  - frontend/components/setup/SetupGuard.tsx
  - frontend/app/[locale]/(auth)/layout.tsx
  - frontend/app/[locale]/(auth)/auth/page.tsx
  - frontend/app/[locale]/(auth)/setup/page.tsx
  - frontend/__tests__/setup/SetupGuard.test.tsx
  - frontend/__tests__/setup/token-validation.test.ts
autonomous: true
requirements:
  - UI-10
  - PLAT-01

must_haves:
  truths:
    - "Unauthenticated users visiting /setup are redirected to /auth"
    - "Already-configured users (tokenConfigured=true) visiting /setup are redirected to /dashboard"
    - "Authenticated + !tokenConfigured users can see the setup page content"
    - "Auth page still works correctly after layout restructure (AuthGuard moved to page level)"
    - "Canvas token format validation accepts numeric strings ~70 chars and rejects non-numeric"
    - "Ed token format validation accepts alphanumeric strings 10-50 chars and rejects invalid"
    - "i18n setup namespace exists in both en.json and zh.json with identical key structure"
  artifacts:
    - path: "frontend/components/setup/SetupGuard.tsx"
      provides: "Setup route guard: requires auth + !tokenConfigured"
      exports: ["SetupGuard"]
    - path: "frontend/lib/validation/token.ts"
      provides: "Token format validation functions"
      exports: ["validateCanvasToken", "validateEdToken"]
    - path: "frontend/app/[locale]/(auth)/setup/page.tsx"
      provides: "Setup page route entry"
    - path: "frontend/messages/en.json"
      provides: "English i18n with setup namespace"
      contains: "setup.welcome.title"
    - path: "frontend/messages/zh.json"
      provides: "Chinese i18n with setup namespace"
      contains: "setup.welcome.title"
  key_links:
    - from: "frontend/components/setup/SetupGuard.tsx"
      to: "frontend/lib/auth/store.ts"
      via: "useAuthStore for isAuthenticated + tokenConfigured checks"
      pattern: "useAuthStore.*isAuthenticated.*tokenConfigured"
    - from: "frontend/app/[locale]/(auth)/auth/page.tsx"
      to: "frontend/components/auth/AuthGuard.tsx"
      via: "AuthGuard wrapping moved from layout to page"
      pattern: "AuthGuard"
    - from: "frontend/app/[locale]/(auth)/setup/page.tsx"
      to: "frontend/components/setup/SetupGuard.tsx"
      via: "SetupGuard wrapping at page level"
      pattern: "SetupGuard"
---

<objective>
Build the foundation for the Setup page: i18n namespace with all EN/ZH translations, SetupGuard component (inverse of AuthGuard), token format validation functions, restructure (auth) layout to move guards to page level, and create the /setup route entry.

Purpose: Unblock all subsequent Setup page component plans by providing i18n keys, route structure, guard logic, and validation utilities.
Output: SetupGuard, token validation, i18n setup namespace, restructured auth layout, setup route entry, tests for guard and validation.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-setup-page/04-CONTEXT.md
@.planning/phases/04-setup-page/04-RESEARCH.md
@.planning/phases/04-setup-page/04-UI-SPEC.md
@.planning/phases/04-setup-page/04-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/lib/auth/store.ts:
```typescript
interface AuthState {
  accessToken: string | null;
  refreshToken: string | null;
  user: AuthUser | null;
  isAuthenticated: boolean;
  tokenConfigured: boolean;
  setAuth: (tokens: { access: string; refresh: string }, user: AuthUser) => void;
  setTokenConfigured: (configured: boolean) => void;
  clearAuth: () => void;
}
export const useAuthStore = create<AuthState>()(persist(...));
// persist key: "uniboard-auth"
// persist methods: useAuthStore.persist.onFinishHydration(), useAuthStore.persist.hasHydrated()
```

From frontend/components/auth/AuthGuard.tsx:
```typescript
export function AuthGuard({ children }: { children: React.ReactNode })
// Hydration pattern: onFinishHydration + hasHydrated
// Redirects authenticated users: tokenConfigured ? /dashboard : /setup
```

From frontend/app/[locale]/(auth)/layout.tsx:
```typescript
// Currently wraps children in <AuthGuard>
// Must be restructured: remove AuthGuard from layout, each page provides its own guard
```

From frontend/app/[locale]/(auth)/auth/page.tsx:
```typescript
import { Suspense } from "react";
import AuthPage from "@/components/auth/AuthPage";
export default function AuthPageRoute() {
  return <Suspense><AuthPage /></Suspense>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: i18n setup namespace, token validation, and SetupGuard with tests</name>
  <files>
    frontend/messages/en.json,
    frontend/messages/zh.json,
    frontend/lib/validation/token.ts,
    frontend/components/setup/SetupGuard.tsx,
    frontend/__tests__/setup/SetupGuard.test.tsx,
    frontend/__tests__/setup/token-validation.test.ts
  </files>
  <read_first>
    frontend/messages/en.json,
    frontend/messages/zh.json,
    frontend/lib/auth/store.ts,
    frontend/components/auth/AuthGuard.tsx,
    frontend/__tests__/auth/AuthGuard.test.tsx,
    .planning/phases/04-setup-page/04-UI-SPEC.md,
    .planning/phases/04-setup-page/04-CONTEXT.md
  </read_first>
  <behavior>
    Token validation tests (token-validation.test.ts):
    - validateCanvasToken("1234567890123456789012345678901234567890123456789012345678901234567890") => true (70 digits)
    - validateCanvasToken("123456789012345678901234567890123456789012345678901") => true (51 digits, min bound)
    - validateCanvasToken("abc123") => false (contains letters)
    - validateCanvasToken("12345") => false (too short)
    - validateCanvasToken("  1234567890123456789012345678901234567890123456789012345678901234567890  ") => true (trims whitespace)
    - validateEdToken("abcDEF123_-test") => true (alphanumeric + _ + -)
    - validateEdToken("a2b3c4d5e6") => true (10 chars, min bound)
    - validateEdToken("abc") => false (too short)
    - validateEdToken("valid token with spaces") => false (contains spaces)
    - validateEdToken("  validtoken123  ") => true (trims whitespace)

    SetupGuard tests (SetupGuard.test.tsx):
    - When not authenticated: redirects to /auth, does not render children
    - When authenticated + tokenConfigured=true: redirects to /dashboard, does not render children
    - When authenticated + tokenConfigured=false: renders children, no redirect
    - When not yet hydrated: renders nothing (no flash)
  </behavior>
  <action>
    1. Create `frontend/__tests__/setup/token-validation.test.ts`:
       - Import validateCanvasToken and validateEdToken from @/lib/validation/token
       - Write tests per behavior spec above
       - Run tests -- should FAIL (RED)

    2. Create `frontend/lib/validation/token.ts`:
       - `export function validateCanvasToken(value: string): boolean` -- trims, tests `/^\d{50,100}$/`
       - `export function validateEdToken(value: string): boolean` -- trims, tests `/^[a-zA-Z0-9_-]{10,50}$/`
       - Run tests -- should PASS (GREEN)

    3. Create `frontend/__tests__/setup/SetupGuard.test.tsx`:
       - Mirror the AuthGuard.test.tsx mock pattern exactly: mock next/navigation with mockReplace, mock @/lib/auth/store with mockAuthState and hydration control
       - Test cases per behavior spec above
       - Run tests -- should FAIL (RED)

    4. Create `frontend/components/setup/SetupGuard.tsx`:
       - "use client"
       - Import useEffect, useState from react; useRouter from next/navigation; useAuthStore from @/lib/auth/store
       - Same zustand hydration pattern as AuthGuard (onFinishHydration + hasHydrated)
       - Inverse redirect logic:
         - if (!hydrated) return null
         - if (!isAuthenticated) router.replace("/auth"); return null
         - if (tokenConfigured) router.replace("/dashboard"); return null
         - else render children
       - Run tests -- should PASS (GREEN)

    5. Add "setup" namespace to `frontend/messages/en.json` (append after "auth" key):
       Complete key structure from 04-UI-SPEC.md Copywriting Contract -- English Copy section:
       ```json
       "setup": {
         "welcome": {
           "title": "Welcome to UniBoard",
           "description": "Let's connect your Canvas LMS and Ed Discussion accounts so we can start tracking your grades, deadlines, and course materials in one place.",
           "subtitle": "This takes about 2 minutes. You'll need access to Canvas and Ed.",
           "features": {
             "readonly": "Read-only access",
             "encrypted": "AES-256 encrypted",
             "deletable": "Delete anytime"
           },
           "cta": "Get Started"
         },
         "tutorial": {
           "title": "How to get your API tokens",
           "description": "Follow these steps to generate tokens from Canvas and Ed. Don't worry, it's straightforward!",
           "canvas": {
             "name": "Canvas LMS",
             "steps": {
               "1": "Go to canvas.sydney.edu.au and log in",
               "2": "Click your profile picture (bottom-left) → Settings",
               "3": "Scroll down to Approved Integrations → click + New Access Token",
               "4": "Enter purpose: UniBoard, leave expiry blank → click Generate Token",
               "5": "Copy the token immediately — it won't be shown again!"
             }
           },
           "ed": {
             "name": "Ed Discussion",
             "steps": {
               "1": "Go to edstem.org/eu/login and log in",
               "2": "Select region → Australia (critical — default may be wrong)",
               "3": "Log in with your student credentials",
               "4": "Navigate to API Tokens settings",
               "5": "Click Generate Token, name it UniBoard, and copy it"
             }
           },
           "back": "Back",
           "cta": "I have my tokens"
         },
         "tokens": {
           "title": "Connect your accounts",
           "description": "Paste the API tokens you generated. We'll validate them instantly.",
           "canvas": {
             "label": "Canvas API Token",
             "placeholder": "Paste your Canvas token here"
           },
           "ed": {
             "label": "Ed Discussion API Token",
             "placeholder": "Paste your Ed token here"
           },
           "security": {
             "title": "Your tokens are safe.",
             "description": "They are encrypted with AES-256-GCM and stored securely. UniBoard only uses read-only access — we never post, submit, or modify anything on your behalf."
           },
           "back": "Back",
           "cta": "Validate & Connect",
           "validating": "Validating...",
           "errors": {
             "canvas": "Canvas token should be a numeric string (~70 characters). Check that you copied the full token.",
             "ed": "Ed token should be a shorter alphanumeric string. Check that you copied the full token."
           }
         },
         "success": {
           "title": "You're all set!",
           "description": "Canvas and Ed are connected. We're syncing your courses, grades, and deadlines right now.",
           "syncing": "Syncing your data...",
           "synced": "{count} courses synced successfully",
           "cta": "Go to Dashboard",
           "toast": "Welcome! Your accounts are connected."
         }
       }
       ```

    6. Add "setup" namespace to `frontend/messages/zh.json` (append after "auth" key):
       Complete key structure from 04-UI-SPEC.md Copywriting Contract -- Chinese Copy section:
       ```json
       "setup": {
         "welcome": {
           "title": "欢迎来到 UniBoard",
           "description": "让我们连接你的 Canvas LMS 和 Ed Discussion 账户，一站式追踪你的成绩、截止日期和课程资料。",
           "subtitle": "大约需要 2 分钟。你需要先登录 Canvas 和 Ed。",
           "features": {
             "readonly": "只读访问",
             "encrypted": "AES-256 加密",
             "deletable": "随时删除"
           },
           "cta": "开始设置"
         },
         "tutorial": {
           "title": "如何获取 API 令牌",
           "description": "按照以下步骤从 Canvas 和 Ed 生成令牌。别担心，非常简单！",
           "canvas": {
             "name": "Canvas LMS",
             "steps": {
               "1": "访问 canvas.sydney.edu.au 并登录",
               "2": "点击个人头像（左下角）→ 设置",
               "3": "向下滚动到已批准的集成 → 点击 + New Access Token",
               "4": "用途填写：UniBoard，到期时间留空 → 点击生成令牌",
               "5": "立即复制令牌 — 之后将无法再次查看！"
             }
           },
           "ed": {
             "name": "Ed Discussion",
             "steps": {
               "1": "访问 edstem.org/eu/login 并登录",
               "2": "选择地区 → Australia（关键步骤 — 默认地区可能不正确）",
               "3": "使用学生账号登录",
               "4": "进入 API Tokens 设置页面",
               "5": "点击生成令牌，命名为 UniBoard，并复制"
             }
           },
           "back": "返回",
           "cta": "我已获取令牌"
         },
         "tokens": {
           "title": "连接你的账户",
           "description": "粘贴你生成的 API 令牌。我们会立即验证。",
           "canvas": {
             "label": "Canvas API 令牌",
             "placeholder": "在此粘贴你的 Canvas 令牌"
           },
           "ed": {
             "label": "Ed Discussion API 令牌",
             "placeholder": "在此粘贴你的 Ed 令牌"
           },
           "security": {
             "title": "你的令牌安全无忧。",
             "description": "我们使用 AES-256-GCM 加密存储。UniBoard 仅使用只读访问 — 绝不会代替你发布、提交或修改任何内容。"
           },
           "back": "返回",
           "cta": "验证并连接",
           "validating": "验证中...",
           "errors": {
             "canvas": "Canvas 令牌应为约 70 位的数字字符串。请检查是否完整复制了令牌。",
             "ed": "Ed 令牌应为较短的字母数字字符串。请检查是否完整复制了令牌。"
           }
         },
         "success": {
           "title": "设置完成！",
           "description": "Canvas 和 Ed 已连接。我们正在同步你的课程、成绩和截止日期。",
           "syncing": "正在同步数据...",
           "synced": "已成功同步 {count} 门课程",
           "cta": "前往仪表盘",
           "toast": "欢迎！你的账户已连接。"
         }
       }
       ```

    7. Run existing i18n parity test to confirm: `cd frontend && pnpm test -- --run --testPathPattern i18n/message-keys`
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm test -- --run --testPathPattern "setup|i18n/message-keys"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/lib/validation/token.ts contains `export function validateCanvasToken(`
    - frontend/lib/validation/token.ts contains `export function validateEdToken(`
    - frontend/lib/validation/token.ts contains `/^\d{50,100}$/`
    - frontend/lib/validation/token.ts contains `/^[a-zA-Z0-9_-]{10,50}$/`
    - frontend/components/setup/SetupGuard.tsx contains `export function SetupGuard(`
    - frontend/components/setup/SetupGuard.tsx contains `useAuthStore.persist.onFinishHydration`
    - frontend/components/setup/SetupGuard.tsx contains `router.replace("/auth")`
    - frontend/components/setup/SetupGuard.tsx contains `router.replace("/dashboard")`
    - frontend/messages/en.json contains `"setup"` key with nested `"welcome"`, `"tutorial"`, `"tokens"`, `"success"`
    - frontend/messages/zh.json contains `"setup"` key with nested `"welcome"`, `"tutorial"`, `"tokens"`, `"success"`
    - frontend/messages/en.json key `setup.welcome.title` equals `"Welcome to UniBoard"`
    - frontend/messages/zh.json key `setup.welcome.title` equals `"欢迎来到 UniBoard"`
    - frontend/__tests__/setup/token-validation.test.ts exits 0
    - frontend/__tests__/setup/SetupGuard.test.tsx exits 0
    - i18n/message-keys test still passes (key parity maintained)
  </acceptance_criteria>
  <done>Token validation functions pass all format tests; SetupGuard correctly redirects unauthenticated to /auth, configured to /dashboard, and renders children for authenticated+!configured; i18n setup namespace has all keys in both en.json and zh.json with parity.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure (auth) layout and create /setup route entry</name>
  <files>
    frontend/app/[locale]/(auth)/layout.tsx,
    frontend/app/[locale]/(auth)/auth/page.tsx,
    frontend/app/[locale]/(auth)/setup/page.tsx
  </files>
  <read_first>
    frontend/app/[locale]/(auth)/layout.tsx,
    frontend/app/[locale]/(auth)/auth/page.tsx,
    frontend/components/auth/AuthGuard.tsx,
    frontend/components/setup/SetupGuard.tsx,
    .planning/phases/04-setup-page/04-RESEARCH.md
  </read_first>
  <action>
    1. Modify `frontend/app/[locale]/(auth)/layout.tsx`:
       - Remove the AuthGuard import and wrapper from layout
       - Keep: AuthDoodles (withClientOnly), LanguageSwitcher, Toaster with same styles
       - Layout now only provides visual scaffolding, NOT auth logic
       - Result:
         ```tsx
         "use client";
         import { Toaster } from "sonner";
         import { withClientOnly } from "@/components/design-system/ClientOnly";
         import LanguageSwitcher from "@/components/auth/LanguageSwitcher";

         const AuthDoodles = withClientOnly(
           () => import("@/components/auth/AuthDoodles"),
         );

         export default function AuthLayout({ children }: { children: React.ReactNode }) {
           return (
             <div className="min-h-screen relative bg-cream">
               <AuthDoodles />
               <LanguageSwitcher />
               <Toaster
                 position="top-center"
                 toastOptions={{
                   style: {
                     fontFamily: "var(--font-inter)",
                     background: "var(--color-card-bg)",
                     border: "1px solid var(--color-divider)",
                     color: "var(--color-text-1)",
                   },
                 }}
               />
               {children}
             </div>
           );
         }
         ```

    2. Modify `frontend/app/[locale]/(auth)/auth/page.tsx`:
       - Add AuthGuard wrapper at page level (moved from layout)
       - Result:
         ```tsx
         import { Suspense } from "react";
         import { AuthGuard } from "@/components/auth/AuthGuard";
         import AuthPage from "@/components/auth/AuthPage";

         export default function AuthPageRoute() {
           return (
             <AuthGuard>
               <Suspense>
                 <AuthPage />
               </Suspense>
             </AuthGuard>
           );
         }
         ```

    3. Create `frontend/app/[locale]/(auth)/setup/page.tsx`:
       - Wrap with SetupGuard at page level
       - Use Suspense boundary (Next.js 15 pattern)
       - Temporarily render a placeholder div with data-testid="setup-page" and text "Setup Page" (actual SetupPage component comes in Plan 03)
       - Result:
         ```tsx
         import { Suspense } from "react";
         import { SetupGuard } from "@/components/setup/SetupGuard";

         export default function SetupPageRoute() {
           return (
             <SetupGuard>
               <Suspense>
                 <div data-testid="setup-page" className="flex items-center justify-center min-h-screen">
                   <p>Setup Page</p>
                 </div>
               </Suspense>
             </SetupGuard>
           );
         }
         ```
         NOTE: The placeholder will be replaced with the real SetupPage component in Plan 03, Task 2.

    4. Verify auth page still works: run existing auth tests to confirm no regression from layout restructure.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm test -- --run --testPathPattern "auth|setup|i18n"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/[locale]/(auth)/layout.tsx does NOT contain `AuthGuard` import or usage
    - frontend/app/[locale]/(auth)/layout.tsx contains `AuthDoodles`, `LanguageSwitcher`, `Toaster`
    - frontend/app/[locale]/(auth)/auth/page.tsx contains `import { AuthGuard }` from `@/components/auth/AuthGuard`
    - frontend/app/[locale]/(auth)/auth/page.tsx wraps content in `<AuthGuard>`
    - frontend/app/[locale]/(auth)/setup/page.tsx contains `import { SetupGuard }` from `@/components/setup/SetupGuard`
    - frontend/app/[locale]/(auth)/setup/page.tsx wraps content in `<SetupGuard>`
    - frontend/app/[locale]/(auth)/setup/page.tsx contains `<Suspense>`
    - All existing auth tests pass (no regression from layout change)
    - All setup tests pass
    - TypeScript builds without errors: `cd frontend && npx tsc --noEmit`
  </acceptance_criteria>
  <done>Auth layout provides only visual scaffolding (doodles + switcher + toaster); AuthGuard moved to auth/page.tsx; SetupGuard wraps setup/page.tsx; all existing auth tests pass unchanged; setup route is accessible at /[locale]/setup.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm test -- --run --testPathPattern "setup|auth|i18n"` -- all tests green
2. `cd frontend && npx tsc --noEmit` -- no type errors
3. Token validation rejects invalid formats and accepts valid ones
4. SetupGuard redirects correctly in all 3 scenarios
5. Auth page functionality preserved after layout restructure
</verification>

<success_criteria>
- SetupGuard component exists and handles 3 redirect scenarios (unauthenticated, configured, valid)
- Token validation functions exist with correct regex patterns
- i18n setup namespace complete in both en.json and zh.json (key parity verified)
- Auth layout restructured: guards at page level, layout provides only visuals
- Setup route entry exists at /[locale]/(auth)/setup/page.tsx
- All tests pass: setup guard, token validation, auth regression, i18n parity
</success_criteria>

<output>
After completion, create `.planning/phases/04-setup-page/04-01-SUMMARY.md`
</output>
