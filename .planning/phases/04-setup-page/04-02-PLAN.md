---
phase: 04-setup-page
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/components/setup/StepIndicator.tsx
  - frontend/components/setup/WelcomeStep.tsx
  - frontend/components/setup/TutorialStep.tsx
  - frontend/components/setup/GuideCard.tsx
  - frontend/__tests__/setup/StepIndicator.test.tsx
  - frontend/__tests__/setup/GuideCard.test.tsx
autonomous: true
requirements:
  - UI-10
  - PLAT-01

must_haves:
  truths:
    - "Step indicator shows 3 circles with correct states: active (orange), completed (green check), upcoming (border only)"
    - "Step indicator connecting lines change from card-border to green as steps complete"
    - "Welcome step displays logo, title, description, subtitle, 3 feature badges, and Get Started button"
    - "Tutorial step shows Canvas and Ed guide cards, both default expanded"
    - "Guide cards can be independently collapsed/expanded (not accordion)"
    - "Guide card chevron rotates when collapsed"
    - "Tutorial steps include correct Ed flow with Australia region selection"
    - "All text comes from i18n setup namespace (no hardcoded strings)"
  artifacts:
    - path: "frontend/components/setup/StepIndicator.tsx"
      provides: "3-circle step progress indicator"
      exports: ["default"]
    - path: "frontend/components/setup/WelcomeStep.tsx"
      provides: "Step 1: welcome with features and CTA"
      exports: ["default"]
    - path: "frontend/components/setup/TutorialStep.tsx"
      provides: "Step 2: Canvas + Ed token tutorial"
      exports: ["default"]
    - path: "frontend/components/setup/GuideCard.tsx"
      provides: "Collapsible platform tutorial card"
      exports: ["default"]
  key_links:
    - from: "frontend/components/setup/WelcomeStep.tsx"
      to: "frontend/messages/en.json"
      via: "useTranslations('setup.welcome')"
      pattern: "useTranslations.*setup"
    - from: "frontend/components/setup/TutorialStep.tsx"
      to: "frontend/components/setup/GuideCard.tsx"
      via: "Renders two GuideCard instances (canvas + ed)"
      pattern: "GuideCard.*platform"
    - from: "frontend/components/setup/GuideCard.tsx"
      to: "frontend/messages/en.json"
      via: "useTranslations('setup.tutorial') for step text"
      pattern: "useTranslations.*setup\\.tutorial"
---

<objective>
Build the display-only step components: StepIndicator (3-circle progress bar), WelcomeStep (step 1 with logo, features, CTA), TutorialStep (step 2 orchestrator), and GuideCard (collapsible platform tutorial). These are the visual building blocks that Step 1 and Step 2 of the setup flow need.

Purpose: Create all read-only/display components so Plan 03 can focus on interactive token entry, validation, and the SetupPage orchestrator.
Output: StepIndicator, WelcomeStep, TutorialStep, GuideCard components with tests for StepIndicator states and GuideCard collapse behavior.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-setup-page/04-CONTEXT.md
@.planning/phases/04-setup-page/04-RESEARCH.md
@.planning/phases/04-setup-page/04-UI-SPEC.md
@.planning/phases/04-setup-page/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/components/design-system/RoughCard.tsx:
```typescript
interface RoughCardProps {
  children: React.ReactNode;
  className?: string;
  padding?: string;
  disableHover?: boolean;
}
export default function RoughCard({ children, className, padding, disableHover }: RoughCardProps)
```

From frontend/components/shared/AnimatedEntry.tsx:
```typescript
interface AnimatedEntryProps {
  children: ReactNode;
  delay?: number;    // 1-10 mapping to stagger delays
  className?: string;
}
export default function AnimatedEntry({ children, delay, className }: AnimatedEntryProps)
```

From frontend/lib/utils/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

i18n keys available (created in Plan 01):
```
setup.welcome.title, setup.welcome.description, setup.welcome.subtitle
setup.welcome.features.readonly, setup.welcome.features.encrypted, setup.welcome.features.deletable
setup.welcome.cta
setup.tutorial.title, setup.tutorial.description
setup.tutorial.canvas.name, setup.tutorial.canvas.steps.{1-5}
setup.tutorial.ed.name, setup.tutorial.ed.steps.{1-5}
setup.tutorial.back, setup.tutorial.cta
```

StepIndicator expected props (from UI-SPEC Component Inventory):
```typescript
interface StepIndicatorProps {
  currentStep: 1 | 2 | 3 | "success";
}
```

WelcomeStep expected props:
```typescript
interface WelcomeStepProps {
  onNext: () => void;
}
```

TutorialStep expected props:
```typescript
interface TutorialStepProps {
  onNext: () => void;
  onBack: () => void;
}
```

GuideCard expected props:
```typescript
interface GuideCardProps {
  platform: "canvas" | "ed";
  defaultExpanded?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StepIndicator and WelcomeStep components</name>
  <files>
    frontend/components/setup/StepIndicator.tsx,
    frontend/components/setup/WelcomeStep.tsx,
    frontend/__tests__/setup/StepIndicator.test.tsx
  </files>
  <read_first>
    frontend/components/shared/AnimatedEntry.tsx,
    frontend/lib/utils/cn.ts,
    .planning/phases/04-setup-page/04-UI-SPEC.md,
    frontend/messages/en.json
  </read_first>
  <behavior>
    StepIndicator tests (StepIndicator.test.tsx):
    - currentStep=1: first circle has bg orange class and shows "1", second and third circles have border class and show "2"/"3", both lines have card-border bg
    - currentStep=2: first circle has bg green + check icon (no number), second circle has bg orange + "2", third has border + "3", first line green, second line card-border
    - currentStep=3: first two circles green + check icons, third circle orange + "3", both lines green
    - currentStep="success": all 3 circles green + check icons, both lines green
    - Step circles have aria-label attributes (accessibility)
    - Container has role="group" with aria-label="Setup progress"
  </behavior>
  <action>
    1. Create `frontend/__tests__/setup/StepIndicator.test.tsx`:
       - Import render, screen from @testing-library/react
       - Mock next-intl (not needed for StepIndicator since it uses no i18n)
       - Test all 4 states: currentStep 1, 2, 3, "success"
       - For each state, verify: circle CSS classes (bg-[#d97757] for active, bg-[#788c5d] for completed, border-2 for upcoming), check icon presence vs number text, line colors
       - Use aria-label to locate each step circle: `aria-label="Step 1"`, `aria-label="Step 2"`, `aria-label="Step 3"`
       - Run tests -- should FAIL (RED)

    2. Create `frontend/components/setup/StepIndicator.tsx`:
       - "use client"
       - Import Fragment from react, Check from lucide-react, cn from @/lib/utils/cn
       - Props: `{ currentStep: 1 | 2 | 3 | "success" }`
       - Compute stepNum: currentStep === "success" ? 4 : currentStep
       - Circle classes:
         - Completed (step < stepNum || success): `bg-[#788c5d] text-white` -- show Check icon (size 16)
         - Active (step === stepNum && !success): `bg-[#d97757] text-white` -- show step number
         - Upcoming: `border-2 border-card-border text-text-3` -- show step number
       - Line classes:
         - Completed (afterStep < stepNum || success): `bg-[#788c5d]`
         - Upcoming: `bg-card-border`
       - Circle size: `w-8 h-8 rounded-full flex items-center justify-center text-sm font-semibold`
       - Line: `w-12 h-0.5 mx-2`
       - Transitions: `transition-all duration-300 ease` on circles, `transition-[background] duration-300 ease` on lines
       - Container: `flex items-center justify-center mb-8` with `role="group" aria-label="Setup progress"`
       - Each circle: `aria-label={Step ${step}}`
       - Run tests -- should PASS (GREEN)

    3. Create `frontend/components/setup/WelcomeStep.tsx`:
       - "use client"
       - Import useTranslations from next-intl, ShieldCheck/Lock/Trash2 from lucide-react, cn from @/lib/utils/cn
       - Props: `{ onNext: () => void }`
       - Use `const t = useTranslations("setup.welcome")`
       - Layout (all centered text-center):
         - Logo block: 60px square div, bg-[#d97757], rounded-2xl, white "U" text (font-serif text-[30px] font-semibold)
         - Title: text-[28px] font-serif font-semibold leading-[1.2] text-text-1, mt-5
         - Description: text-base text-text-2 leading-[1.6] max-w-[420px] mx-auto mt-3
         - Italic subtitle: text-base italic text-text-3 mt-2
         - Feature badges row: flex items-center justify-center gap-4 mt-6 (desktop: row, mobile <680px: flex-col)
           - Badge 1: ShieldCheck icon (18px) in 40px square with bg-[rgba(217,119,87,.11)] rounded-lg, label "readonly" 14px font-semibold text-text-2
           - Badge 2: Lock icon (18px) in 40px square with bg-[rgba(106,155,204,.11)] rounded-lg, label "encrypted"
           - Badge 3: Trash2 icon (18px) in 40px square with bg-[rgba(120,140,93,.11)] rounded-lg, label "deletable"
         - Each badge: flex flex-col items-center gap-2 (mobile: flex-row gap-3)
         - CTA button: mt-8, bg-[#d97757] hover:bg-[#c5674a] text-white font-semibold py-3 px-10 rounded-lg transition-all duration-150 ease hover:-translate-y-px
       - No separate test file -- WelcomeStep is display-only with i18n. Covered by integration in SetupPage test (Plan 03).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm test -- --run --testPathPattern "setup/StepIndicator"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/setup/StepIndicator.tsx contains `role="group"` and `aria-label="Setup progress"`
    - frontend/components/setup/StepIndicator.tsx contains `bg-[#d97757]` (active), `bg-[#788c5d]` (completed), `border-card-border` (upcoming)
    - frontend/components/setup/StepIndicator.tsx contains `Check` import from lucide-react
    - frontend/components/setup/WelcomeStep.tsx contains `useTranslations("setup.welcome")`
    - frontend/components/setup/WelcomeStep.tsx contains `ShieldCheck` and `Lock` and `Trash2` imports
    - frontend/components/setup/WelcomeStep.tsx contains `onNext` prop usage (button onClick)
    - frontend/components/setup/WelcomeStep.tsx contains logo "U" text element
    - frontend/__tests__/setup/StepIndicator.test.tsx exits 0
  </acceptance_criteria>
  <done>StepIndicator renders correct visual states for all 4 step values (1/2/3/success) with proper accessibility attributes. WelcomeStep displays logo, title, description, subtitle, 3 feature badges with correct icons/colors, and Get Started CTA button using i18n keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GuideCard and TutorialStep components</name>
  <files>
    frontend/components/setup/GuideCard.tsx,
    frontend/components/setup/TutorialStep.tsx,
    frontend/__tests__/setup/GuideCard.test.tsx
  </files>
  <read_first>
    frontend/components/setup/StepIndicator.tsx,
    frontend/messages/en.json,
    .planning/phases/04-setup-page/04-UI-SPEC.md,
    .planning/phases/04-setup-page/04-CONTEXT.md
  </read_first>
  <behavior>
    GuideCard tests (GuideCard.test.tsx):
    - platform="canvas" with defaultExpanded=true: renders "Canvas LMS" header, shows all 5 step texts, chevron points down
    - platform="ed" with defaultExpanded=true: renders "Ed Discussion" header, shows all 5 step texts including "Australia" region step
    - Clicking the header toggles collapsed state: step content becomes hidden (height 0 / display none), chevron rotates
    - Both cards can be expanded independently (render two GuideCards, collapse one, other stays expanded)
    - Header uses aria-expanded attribute reflecting current state
  </behavior>
  <action>
    1. Create `frontend/__tests__/setup/GuideCard.test.tsx`:
       - Mock next-intl: `vi.mock("next-intl", () => ({ useTranslations: () => (key: string) => key }))`
       - Test all behaviors above
       - For collapse detection: check that content area has `aria-hidden="true"` when collapsed or use data-testid on content wrapper and check style/class
       - Run tests -- should FAIL (RED)

    2. Create `frontend/components/setup/GuideCard.tsx`:
       - "use client"
       - Import useState, useRef from react; useTranslations from next-intl; ChevronDown, ExternalLink, Settings, Key, Copy, MessageCircle, LayoutDashboard from lucide-react; cn from @/lib/utils/cn
       - Props: `{ platform: "canvas" | "ed", defaultExpanded?: boolean }`
       - State: `const [expanded, setExpanded] = useState(defaultExpanded ?? true)`
       - Use `const t = useTranslations("setup.tutorial")`
       - Platform config:
         - canvas: icon = LayoutDashboard (16px), iconBg = "rgba(217,60,50,.08)", iconColor = "#d93c32", name key = "canvas.name"
         - ed: icon = MessageCircle (16px), iconBg = "rgba(106,155,204,.11)", iconColor = "#6a9bcc", name key = "ed.name"
       - Step icons array (per step): [ExternalLink, Settings, Key, Key, Copy] for canvas; [ExternalLink, ExternalLink, Key, Settings, Copy] for ed
       - Layout:
         - Outer wrapper: `border border-card-border rounded-2xl p-5 px-6` (1px solid card-border, 16px radius, 20px 24px padding)
         - Header row (clickable): flex items-center justify-between cursor-pointer, aria-expanded={expanded}
           - Left side: flex items-center gap-3
             - Platform icon: 32px square, rounded-lg, platform bg + color
             - Platform name: font-serif text-[21px] font-semibold text-text-1
           - Right side: ChevronDown (18px), text-text-3, transform: expanded ? "rotate(0deg)" : "rotate(-90deg)", transition 0.28s cubic-bezier(.4,0,.2,1)
         - Content area: overflow hidden, transition max-height 0.35s cubic-bezier(.4,0,.2,1) + opacity 0.25s ease
           - When expanded: max-height auto (use scrollHeight via ref or large enough value like 500px), opacity 1
           - When collapsed: max-height 0, opacity 0, aria-hidden="true"
           - Steps list: mt-4, flex flex-col gap-3 (12px gap between steps)
           - Each step: flex items-start gap-3
             - Step number circle: 24px diameter (w-6 h-6), bg-[#d97757] text-white text-sm font-semibold rounded-full flex items-center justify-center flex-shrink-0
             - Step text: text-base text-text-2 leading-[1.5]
               - Parse text for links: if step text contains URLs (canvas.sydney.edu.au, edstem.org), wrap in `<a>` with className "text-[#d97757] font-semibold hover:underline" target="_blank" rel="noopener noreferrer"
               - Parse text for code snippets: wrap `UniBoard` in `<code>` with className "text-sm font-mono bg-[rgba(217,119,87,.08)] text-[#d97757] px-1.5 py-0.5 rounded"
       - Run tests -- should PASS (GREEN)

    3. Create `frontend/components/setup/TutorialStep.tsx`:
       - "use client"
       - Import useTranslations from next-intl
       - Import GuideCard
       - Props: `{ onNext: () => void, onBack: () => void }`
       - Use `const t = useTranslations("setup.tutorial")`
       - Layout:
         - Title: text-[21px] font-serif font-semibold text-text-1 text-center
         - Description: text-base text-text-2 leading-[1.6] text-center mt-2 mb-6
         - GuideCard for canvas: `<GuideCard platform="canvas" defaultExpanded={true} />`
         - GuideCard gap: mt-4 (16px) between canvas and ed cards
         - GuideCard for ed: `<GuideCard platform="ed" defaultExpanded={true} />`
         - Button row: flex items-center justify-between mt-8
           - Back button: bg-transparent border-[1.5px] border-card-border text-text-2 font-semibold py-3 px-6 rounded-lg hover:bg-card-bg-hover hover:text-text-1 transition-all duration-150 ease
           - "I have my tokens" button: bg-[#d97757] hover:bg-[#c5674a] text-white font-semibold py-3 px-7 rounded-lg transition-all duration-150 ease hover:-translate-y-px
       - No separate test file for TutorialStep -- it delegates to GuideCard. Integration tested in SetupPage (Plan 03).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm test -- --run --testPathPattern "setup/GuideCard"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/setup/GuideCard.tsx contains `aria-expanded` attribute on header
    - frontend/components/setup/GuideCard.tsx contains `platform: "canvas" | "ed"` in props type
    - frontend/components/setup/GuideCard.tsx contains `ChevronDown` import from lucide-react
    - frontend/components/setup/GuideCard.tsx contains `rotate(-90deg)` or `rotate-[-90deg]` for collapsed chevron
    - frontend/components/setup/GuideCard.tsx contains `useTranslations("setup.tutorial")`
    - frontend/components/setup/TutorialStep.tsx contains two `<GuideCard` usages (canvas and ed)
    - frontend/components/setup/TutorialStep.tsx contains `defaultExpanded={true}` on both GuideCards
    - frontend/components/setup/TutorialStep.tsx contains `onBack` and `onNext` in button onClick handlers
    - frontend/__tests__/setup/GuideCard.test.tsx exits 0
    - `cd frontend && pnpm test -- --run --testPathPattern setup` all pass
  </acceptance_criteria>
  <done>GuideCard renders platform tutorial with correct icons, 5 numbered steps, collapsible behavior with chevron rotation and aria-expanded; both cards can operate independently (not accordion). TutorialStep displays title, description, two GuideCards (canvas + ed) both default expanded, with Back and "I have my tokens" buttons using i18n.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm test -- --run --testPathPattern setup` -- all setup tests green
2. `cd frontend && npx tsc --noEmit` -- no type errors
3. StepIndicator shows correct visual states for 4 step values
4. GuideCard collapses/expands independently with animation classes
5. WelcomeStep and TutorialStep use i18n keys exclusively
</verification>

<success_criteria>
- StepIndicator renders 4 states correctly with accessibility attributes
- WelcomeStep displays complete Step 1 UI (logo, features, CTA) from i18n
- TutorialStep renders 2 GuideCards for Canvas and Ed, both default expanded
- GuideCards independently collapsible with chevron rotation animation
- Ed tutorial includes Australia region selection step
- All tests pass (StepIndicator, GuideCard)
</success_criteria>

<output>
After completion, create `.planning/phases/04-setup-page/04-02-SUMMARY.md`
</output>
