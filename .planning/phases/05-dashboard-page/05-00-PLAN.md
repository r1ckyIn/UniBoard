---
phase: 05-dashboard-page
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/__tests__/dashboard/HeroSection.test.tsx
  - frontend/__tests__/dashboard/StatsRow.test.tsx
  - frontend/__tests__/dashboard/CourseGradesTable.test.tsx
  - frontend/__tests__/dashboard/DeadlineTimeline.test.tsx
  - frontend/__tests__/dashboard/AssessmentDonut.test.tsx
  - frontend/__tests__/dashboard/MiniCalendar.test.tsx
  - frontend/__tests__/dashboard/RecentActivity.test.tsx
  - frontend/__tests__/dashboard/ExternalLinkDialog.test.tsx
  - frontend/__tests__/dashboard/SkeletonCard.test.tsx
  - frontend/__tests__/dashboard/encouragement.test.ts
  - frontend/__tests__/dashboard/course-colors.test.ts
  - frontend/__tests__/layout/NotificationPanel.test.tsx
  - frontend/__tests__/layout/AvatarMenu.test.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "All 13 test stub files exist and each contains at least one describe block with a pending or placeholder test"
    - "Running pnpm vitest run --testPathPattern dashboard passes with no failures"
    - "Running pnpm vitest run --testPathPattern 'NotificationPanel|AvatarMenu' passes with no failures"
  artifacts:
    - path: "frontend/__tests__/dashboard/HeroSection.test.tsx"
      provides: "Test stub for HeroSection component"
      contains: "describe"
    - path: "frontend/__tests__/dashboard/StatsRow.test.tsx"
      provides: "Test stub for StatsRow component"
      contains: "describe"
    - path: "frontend/__tests__/dashboard/CourseGradesTable.test.tsx"
      provides: "Test stub for CourseGradesTable component"
      contains: "describe"
    - path: "frontend/__tests__/dashboard/DeadlineTimeline.test.tsx"
      provides: "Test stub for DeadlineTimeline component"
      contains: "describe"
    - path: "frontend/__tests__/dashboard/AssessmentDonut.test.tsx"
      provides: "Test stub for AssessmentDonut component"
      contains: "describe"
    - path: "frontend/__tests__/dashboard/MiniCalendar.test.tsx"
      provides: "Test stub for MiniCalendar component"
      contains: "describe"
    - path: "frontend/__tests__/dashboard/RecentActivity.test.tsx"
      provides: "Test stub for RecentActivity component"
      contains: "describe"
    - path: "frontend/__tests__/dashboard/ExternalLinkDialog.test.tsx"
      provides: "Test stub for ExternalLinkDialog component"
      contains: "describe"
    - path: "frontend/__tests__/dashboard/SkeletonCard.test.tsx"
      provides: "Test stub for SkeletonCard component"
      contains: "describe"
    - path: "frontend/__tests__/dashboard/encouragement.test.ts"
      provides: "Test stub for encouragement provider utility"
      contains: "describe"
    - path: "frontend/__tests__/dashboard/course-colors.test.ts"
      provides: "Test stub for course-colors utility"
      contains: "describe"
    - path: "frontend/__tests__/layout/NotificationPanel.test.tsx"
      provides: "Test stub for NotificationPanel component"
      contains: "describe"
    - path: "frontend/__tests__/layout/AvatarMenu.test.tsx"
      provides: "Test stub for AvatarMenu component"
      contains: "describe"
  key_links: []
---

<objective>
Create test stub files for all 13 dashboard components and utilities identified in RESEARCH.md Validation Architecture. Each stub has a describe block with placeholder tests that will be filled in by subsequent plans as components are built.

Purpose: Establishes the Nyquist-compliant test scaffold so every subsequent plan's `<verify>` can run component-specific tests. Without these stubs, there is no automated feedback loop.
Output: 13 test files in `__tests__/dashboard/` and `__tests__/layout/`.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-page/05-CONTEXT.md
@.planning/phases/05-dashboard-page/05-RESEARCH.md
@.planning/phases/05-dashboard-page/05-VALIDATION.md

<interfaces>
<!-- Existing test patterns from prior phases -->
From frontend/__tests__/layout/:
- Tests use @testing-library/react render(), screen, within()
- Tests use vi.mock() for next-intl, next/navigation, auth store
- Tests follow pattern: describe("ComponentName") > it("behavior description")

From frontend/__tests__/design-system/:
- Rough.js component tests mock roughjs module
- Tests verify rendered DOM elements, not canvas/SVG internals
- ResizeObserver polyfill available in test setup
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 11 dashboard test stubs (components + utilities)</name>
  <files>
    frontend/__tests__/dashboard/HeroSection.test.tsx,
    frontend/__tests__/dashboard/StatsRow.test.tsx,
    frontend/__tests__/dashboard/CourseGradesTable.test.tsx,
    frontend/__tests__/dashboard/DeadlineTimeline.test.tsx,
    frontend/__tests__/dashboard/AssessmentDonut.test.tsx,
    frontend/__tests__/dashboard/MiniCalendar.test.tsx,
    frontend/__tests__/dashboard/RecentActivity.test.tsx,
    frontend/__tests__/dashboard/ExternalLinkDialog.test.tsx,
    frontend/__tests__/dashboard/SkeletonCard.test.tsx,
    frontend/__tests__/dashboard/encouragement.test.ts,
    frontend/__tests__/dashboard/course-colors.test.ts
  </files>
  <read_first>
    frontend/__tests__/layout/Header.test.tsx,
    frontend/__tests__/design-system/RoughCard.test.tsx,
    frontend/vitest.config.ts
  </read_first>
  <action>
Create the `frontend/__tests__/dashboard/` directory and 11 test stub files. Each stub should follow the existing test patterns from the codebase.

**Pattern for component test stubs (.test.tsx):**
```typescript
import { describe, it, expect } from "vitest";

describe("ComponentName", () => {
  it.todo("renders without crashing");
  // Additional placeholder tests listed below per component
});
```

**Pattern for utility test stubs (.test.ts):**
```typescript
import { describe, it, expect } from "vitest";

describe("utilityName", () => {
  it.todo("basic functionality");
});
```

**Specific placeholder tests per file:**

1. **HeroSection.test.tsx**: `renders without crashing`, `displays greeting with user name`, `shows encouragement text`, `renders scroll prompt`

2. **StatsRow.test.tsx**: `renders without crashing`, `displays WAM value`, `displays GPA target`, `displays alert count`

3. **CourseGradesTable.test.tsx**: `renders without crashing`, `renders table headers`, `renders course rows`, `shows predict link on hover`

4. **DeadlineTimeline.test.tsx**: `renders without crashing`, `renders deadline items`, `applies urgency-based colors`, `calls onDeadlineClick when item clicked`

5. **AssessmentDonut.test.tsx**: `renders without crashing`, `renders empty state when no weights`, `renders SVG element for donut`

6. **MiniCalendar.test.tsx**: `renders without crashing`, `renders day headers`, `navigates to next month`, `highlights today`

7. **RecentActivity.test.tsx**: `renders without crashing`, `renders activity items`, `opens external link dialog on click`

8. **ExternalLinkDialog.test.tsx**: `renders without crashing`, `calls onConfirm when confirm button clicked`, `calls onCancel when cancel button clicked`

9. **SkeletonCard.test.tsx**: `renders without crashing`, `renders with role status`, `renders aria-label Loading`

10. **encouragement.test.ts**: `returns encouragement for multiple completed items`, `returns encouragement for single completed item`, `returns default encouragement for no activity`

11. **course-colors.test.ts**: `returns correct color for known course code`, `returns default color for unknown course code`, `COURSE_COLORS has 4 entries`

All `.test.tsx` stubs should import `{ describe, it, expect }` from `"vitest"`. Do NOT import the actual components yet (they don't exist) -- use `it.todo()` which skips without needing imports.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm vitest run --reporter=verbose -- __tests__/dashboard/ 2>&1 | tail -20</automated>
  </verify>
  <done>11 test stub files exist in __tests__/dashboard/ with it.todo() placeholders, all pass vitest (todos are skipped, not failures)</done>
</task>

<task type="auto">
  <name>Task 2: Create 2 layout test stubs (NotificationPanel + AvatarMenu)</name>
  <files>
    frontend/__tests__/layout/NotificationPanel.test.tsx,
    frontend/__tests__/layout/AvatarMenu.test.tsx
  </files>
  <read_first>
    frontend/__tests__/layout/Header.test.tsx
  </read_first>
  <action>
Create 2 test stubs in `frontend/__tests__/layout/`:

1. **NotificationPanel.test.tsx**:
```typescript
import { describe, it, expect } from "vitest";

describe("NotificationPanel", () => {
  it.todo("renders without crashing");
  it.todo("renders notification items");
  it.todo("applies unread styling to unread notifications");
  it.todo("calls onViewAll when footer clicked");
  it.todo("formats timestamps with relative time");
});
```

2. **AvatarMenu.test.tsx**:
```typescript
import { describe, it, expect } from "vitest";

describe("AvatarMenu", () => {
  it.todo("renders without crashing");
  it.todo("displays user name and email");
  it.todo("renders menu items (Profile, Settings, API Tokens, Log out)");
  it.todo("calls onLogout when logout clicked");
  it.todo("calls onNavigate with correct path");
});
```
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm vitest run --reporter=verbose -- __tests__/layout/NotificationPanel __tests__/layout/AvatarMenu 2>&1 | tail -15</automated>
  </verify>
  <done>2 layout test stubs exist with it.todo() placeholders, vitest passes</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm vitest run --reporter=verbose` passes (full suite including new stubs)
- 13 test files exist: 11 in __tests__/dashboard/, 2 in __tests__/layout/
- Each file has at least one describe block
- No test failures (it.todo() tests are skipped, not failed)
</verification>

<success_criteria>
- All 13 test stub files created matching RESEARCH.md Wave 0 Gaps checklist
- Full vitest suite passes with no regressions
- Each stub contains component-specific placeholder test descriptions
- Test directory structure matches existing patterns (__tests__/dashboard/, __tests__/layout/)
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-page/05-00-SUMMARY.md`
</output>
