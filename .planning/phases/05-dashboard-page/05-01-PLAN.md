---
phase: 05-dashboard-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/messages/en.json
  - frontend/messages/zh.json
  - frontend/lib/dashboard/encouragement.ts
  - frontend/lib/dashboard/course-colors.ts
  - frontend/components/dashboard/SkeletonCard.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Dashboard i18n namespace contains all keys from UI-SPEC i18n Key Structure section"
    - "EncouragementProvider interface accepts ActivitySummary and returns EncouragementText with message + highlightPhrase"
    - "Course color mapping returns unique brand colors for each of the 4 active courses"
    - "SkeletonCard renders warm-toned shimmer matching paper-texture aesthetic"
  artifacts:
    - path: "frontend/messages/en.json"
      provides: "Dashboard namespace with all EN copy"
      contains: "dashboard.hero.greeting"
    - path: "frontend/messages/zh.json"
      provides: "Dashboard namespace with all ZH copy"
      contains: "dashboard.hero.greeting"
    - path: "frontend/lib/dashboard/encouragement.ts"
      provides: "EncouragementProvider interface + default mock implementation"
      exports: ["EncouragementProvider", "EncouragementText", "ActivitySummary", "defaultEncouragementProvider"]
    - path: "frontend/lib/dashboard/course-colors.ts"
      provides: "Per-course color mapping utility"
      exports: ["getCourseColor", "COURSE_COLORS"]
    - path: "frontend/components/dashboard/SkeletonCard.tsx"
      provides: "Skeleton loading card with warm shimmer variants"
      exports: ["default"]
  key_links:
    - from: "frontend/lib/dashboard/encouragement.ts"
      to: "UI-SPEC EncouragementText Interface"
      via: "type export"
      pattern: "EncouragementProvider"
    - from: "frontend/lib/dashboard/course-colors.ts"
      to: "UI-SPEC Per-Course Color Assignment"
      via: "color mapping"
      pattern: "getCourseColor"
---

<objective>
Create the Dashboard foundation layer: full i18n messages (EN + ZH), encouragement text provider interface, per-course color mapping utility, and skeleton loading card component.

Purpose: All subsequent dashboard plans depend on these shared utilities and i18n strings. Building them first prevents duplication and ensures consistent patterns.
Output: i18n messages, 2 utility modules, 1 component.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-page/05-CONTEXT.md
@.planning/phases/05-dashboard-page/05-UI-SPEC.md
@.planning/phases/05-dashboard-page/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n dashboard namespace + utility modules (encouragement + course-colors)</name>
  <files>
    frontend/messages/en.json,
    frontend/messages/zh.json,
    frontend/lib/dashboard/encouragement.ts,
    frontend/lib/dashboard/course-colors.ts
  </files>
  <read_first>
    frontend/messages/en.json,
    frontend/messages/zh.json,
    .planning/phases/05-dashboard-page/05-UI-SPEC.md
  </read_first>
  <action>
1. **Add `dashboard` namespace to `en.json`** with all keys from UI-SPEC i18n Key Structure section. The existing `en.json` has top-level namespaces (nav, header, rightPanel, dashboard, common, auth, setup). The current `dashboard` key only has `{"welcome": "...", "subtitle": "..."}` — REPLACE it entirely with the full dashboard namespace:

```json
"dashboard": {
  "hero": {
    "greeting": {
      "morning": "Good morning, {firstName}",
      "afternoon": "Good afternoon, {firstName}",
      "evening": "Good evening, {firstName}"
    },
    "date": "It's {weekday}, {weekNumber} of Semester {semester}.",
    "scrollPrompt": "your dashboard"
  },
  "stats": {
    "wam": { "label": "Current WAM", "sub": "Weighted Average Mark" },
    "target": { "label": "GPA Target", "sub": "{gradeBand}", "badge": "+{gap} to go" },
    "alerts": { "label": "Alerts", "sub": "{course} below target \u00b7 {count} deadline soon" }
  },
  "grades": {
    "title": "Course Grades Overview",
    "badge": "{year} S{semester}",
    "th": { "course": "Course", "assessed": "Assessed", "earned": "Earned", "target": "Target" },
    "predictLink": "predict \u2192"
  },
  "deadlines": {
    "title": "Upcoming Deadlines",
    "badge": "7 days",
    "days": "{count} days",
    "seeDetails": "see details"
  },
  "donut": {
    "title": "Assessment Weights",
    "empty": {
      "heading": "No upcoming deadlines",
      "body": "Assessment weights will appear when you have upcoming deadlines."
    }
  },
  "profile": {
    "statLabels": { "courses": "Courses", "credits": "Credit Pts" }
  },
  "calendar": { "monthFormat": "{month} {year}" },
  "activity": {
    "title": "Recent Activity",
    "grade": "Grade posted \u2014 {course} {assignment}: {score}",
    "discussion": "Staff reply on {course} {topic}",
    "deadline": "Reminder \u2014 {course} {assignment} in {days} days",
    "endorsed": "Endorsed answer on {course} \"{topic}\""
  },
  "externalLink": {
    "title": "Open external link?",
    "body": "You are about to leave UniBoard and open an external page.",
    "confirm": "Open link",
    "cancel": "Stay on UniBoard"
  },
  "notifications": { "title": "Notifications", "footer": "View all notifications" },
  "avatarMenu": {
    "profile": "Profile",
    "settings": "Settings",
    "apiTokens": "API Tokens",
    "logout": "Log out"
  },
  "error": { "message": "Something went wrong loading this section.", "retry": "Try again" },
  "loading": { "aria": "Loading..." }
}
```

2. **Add `dashboard` namespace to `zh.json`** with corresponding Chinese translations from UI-SPEC Copywriting Contract:
- Hero: "早上好/下午好/晚上好，{firstName}", date: "今天是{weekday}，第{semester}学期 第{weekNumber}周。", scrollPrompt: "你的仪表盘"
- Stats: WAM label "当前 WAM", sub "加权平均分", Target label "GPA 目标", sub "{gradeBand}", badge "还差 {gap} 分", Alerts label "提醒", sub "{course}低于目标 · {count} 个截止日期将至"
- Grades: title "课程成绩概览", badge "{year} S{semester}", th: "课程/已评估/已得分/目标", predictLink "预测 →"
- Deadlines: title "即将到来的截止日期", badge "7 天", days "{count} 天", seeDetails "详情"
- Donut: title "评估权重", empty heading "暂无即将到来的截止日期", empty body "当你有即将到来的截止日期时，评估权重将在此显示。"
- Profile: statLabels "课程数/学分"
- Calendar: monthFormat "{year}年{month}月"
- Activity: title "最近动态", grade "成绩已发布 — {course} {assignment}: {score}", discussion "教职人员回复了 {course} {topic}", deadline "提醒 — {course} {assignment}还有{days}天", endorsed "{course}「{topic}」有新的官方回答"
- ExternalLink: title "打开外部链接？", body "你即将离开 UniBoard 并打开一个外部页面。", confirm "打开链接", cancel "留在 UniBoard"
- Notifications: title "通知", footer "查看所有通知"
- AvatarMenu: "个人资料/设置/API 令牌/退出登录"
- Error: message "加载此部分时出错。", retry "重试"
- Loading: aria "加载中..."

3. **Create `frontend/lib/dashboard/encouragement.ts`**:
```typescript
export interface ActivitySummary {
  completedDeadlinesLast7Days: number;
  recentCompletedItems: string[];
}

export interface EncouragementText {
  message: string;
  highlightPhrase: string;
}

export type EncouragementProvider = (activity: ActivitySummary) => EncouragementText;

export const defaultEncouragementProvider: EncouragementProvider = (activity) => {
  // 5 hardcoded messages with different triggers
  if (activity.recentCompletedItems.length >= 2) {
    const items = activity.recentCompletedItems.slice(0, 2);
    return {
      message: `The ${items[0]} and the ${items[1]} are done and behind you now. You've been working so hard — it's okay to take it slow today.`,
      highlightPhrase: "it's okay to take it slow today",
    };
  }
  if (activity.recentCompletedItems.length === 1) {
    return {
      message: `${activity.recentCompletedItems[0]} is behind you now. Take a deep breath — you're making steady progress.`,
      highlightPhrase: "you're making steady progress",
    };
  }
  if (activity.completedDeadlinesLast7Days > 3) {
    return {
      message: `You've wrapped up ${activity.completedDeadlinesLast7Days} tasks this week. That's impressive — you've earned a moment to breathe.`,
      highlightPhrase: "you've earned a moment to breathe",
    };
  }
  if (activity.completedDeadlinesLast7Days > 0) {
    return {
      message: `You're ${activity.completedDeadlinesLast7Days} tasks into the week already. Keep this momentum going — you've got this.`,
      highlightPhrase: "you've got this",
    };
  }
  return {
    message: "A new week stretches ahead. Take it one task at a time — every small step counts.",
    highlightPhrase: "every small step counts",
  };
};
```

4. **Create `frontend/lib/dashboard/course-colors.ts`**:
```typescript
export const COURSE_COLORS: Record<string, { base: string; soft: string }> = {
  COMP2017: { base: "#d97757", soft: "rgba(217,119,87,.11)" },
  COMP3221: { base: "#6a9bcc", soft: "rgba(106,155,204,.11)" },
  STAT2011: { base: "#b08968", soft: "rgba(176,137,104,.11)" },
  INFO2222: { base: "#788c5d", soft: "rgba(120,140,93,.11)" },
};

const DEFAULT_COLOR = { base: "#9b9b94", soft: "rgba(155,155,148,.11)" };

export function getCourseColor(courseCode: string): { base: string; soft: string } {
  return COURSE_COLORS[courseCode] ?? DEFAULT_COLOR;
}
```
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && node -e "const en=require('./messages/en.json'); console.assert(en.dashboard.hero.greeting.morning, 'missing EN hero greeting'); console.assert(en.dashboard.stats.wam.label, 'missing EN stats'); console.assert(en.dashboard.externalLink.title, 'missing EN externalLink'); console.log('EN OK');" && node -e "const zh=require('./messages/zh.json'); console.assert(zh.dashboard.hero.greeting.morning, 'missing ZH hero greeting'); console.log('ZH OK');" && npx tsx -e "import { defaultEncouragementProvider } from './lib/dashboard/encouragement'; const r = defaultEncouragementProvider({ completedDeadlinesLast7Days: 2, recentCompletedItems: ['Lab 4', 'Quiz 3'] }); console.assert(r.message.includes('Lab 4'), 'missing item in message'); console.assert(r.highlightPhrase.length > 0, 'empty highlight'); console.log('Encouragement OK');" && npx tsx -e "import { getCourseColor, COURSE_COLORS } from './lib/dashboard/course-colors'; console.assert(getCourseColor('COMP2017').base === '#d97757', 'wrong COMP2017 color'); console.assert(Object.keys(COURSE_COLORS).length === 4, 'expected 4 courses'); console.log('Colors OK');"</automated>
  </verify>
  <acceptance_criteria>
    - en.json contains key path `dashboard.hero.greeting.morning` with value containing `{firstName}`
    - en.json contains key path `dashboard.externalLink.cancel` with value `Stay on UniBoard`
    - zh.json contains key path `dashboard.hero.greeting.morning` with value containing `{firstName}`
    - zh.json contains key path `dashboard.externalLink.cancel` with value `留在 UniBoard`
    - encouragement.ts exports `EncouragementProvider` type, `ActivitySummary` interface, `EncouragementText` interface, `defaultEncouragementProvider` function
    - defaultEncouragementProvider returns object with `message` string and `highlightPhrase` string
    - course-colors.ts exports `COURSE_COLORS` with keys COMP2017, COMP3221, STAT2011, INFO2222
    - course-colors.ts exports `getCourseColor` function returning `{ base: string; soft: string }`
    - getCourseColor("COMP2017") returns `{ base: "#d97757", soft: "rgba(217,119,87,.11)" }`
  </acceptance_criteria>
  <done>All dashboard i18n keys present in both EN/ZH, encouragement provider interface and mock implementation exported, course-color mapping returns correct brand palette colors for all 4 courses</done>
</task>

<task type="auto">
  <name>Task 2: SkeletonCard component with warm-toned shimmer variants</name>
  <files>
    frontend/components/dashboard/SkeletonCard.tsx
  </files>
  <read_first>
    frontend/components/design-system/RoughCard.tsx,
    frontend/components/shared/AnimatedEntry.tsx,
    .planning/phases/05-dashboard-page/05-UI-SPEC.md
  </read_first>
  <action>
Create `frontend/components/dashboard/SkeletonCard.tsx` — a loading skeleton component with 7 variants matching the dashboard sections.

Per UI-SPEC: Skeleton bg uses `linear-gradient` between card-bg (#f6f5f0) and card-bg-hover (#efede6) with shimmer animation (1.5s ease-in-out infinite, background-position shift left to right). Aria label "Loading...".

```typescript
"use client";

import { cn } from "@/lib/utils/cn";

type SkeletonVariant = "stat" | "table" | "timeline" | "donut" | "profile" | "calendar" | "activity";

interface SkeletonCardProps {
  variant: SkeletonVariant;
  className?: string;
}

// Shimmer bar component (reusable within variants)
function ShimmerBar({ className }: { className?: string }) {
  return (
    <div
      className={cn(
        "rounded-[6px] animate-skeleton-shimmer",
        "bg-[length:200%_100%]",
        "bg-gradient-to-r from-[#f6f5f0] via-[#efede6] to-[#f6f5f0]",
        className
      )}
    />
  );
}

export default function SkeletonCard({ variant, className }: SkeletonCardProps) {
  return (
    <div
      role="status"
      aria-label="Loading..."
      className={cn(
        "bg-card-bg rounded-card border border-card-border",
        className
      )}
    >
      {/* Render variant-specific skeleton shapes */}
      {variant === "stat" && <StatSkeleton />}
      {variant === "table" && <TableSkeleton />}
      {variant === "timeline" && <TimelineSkeleton />}
      {variant === "donut" && <DonutSkeleton />}
      {variant === "profile" && <ProfileSkeleton />}
      {variant === "calendar" && <CalendarSkeleton />}
      {variant === "activity" && <ActivitySkeleton />}
    </div>
  );
}
```

Each variant renders ShimmerBar elements approximating the section layout:
- **StatSkeleton**: padding 20px 24px. ShimmerBar h-3 w-20 (label), ShimmerBar h-8 w-16 mt-2 (value), ShimmerBar h-3 w-32 mt-2 (sub text).
- **TableSkeleton**: padding 20px 24px. ShimmerBar h-4 w-40 mb-4 (title). Then 4 rows each: flex, gap-3, ShimmerBar h-3 w-16, ShimmerBar h-3 w-24, ShimmerBar h-2.5 w-20 (progress), ShimmerBar h-3 w-12, ShimmerBar h-3 w-14.
- **TimelineSkeleton**: padding 20px 24px. ShimmerBar h-4 w-36 mb-4. Then 3 items: flex gap-3, ShimmerBar w-2.5 h-2.5 rounded-full, div flex-col gap-1.5 with ShimmerBar h-3 w-28 + ShimmerBar h-2.5 w-20.
- **DonutSkeleton**: padding 20px 24px, flex center. ShimmerBar h-4 w-36 mb-4. ShimmerBar w-[160px] h-[160px] rounded-full mx-auto (circle shape).
- **ProfileSkeleton**: padding 24px 20px, text-center. ShimmerBar w-[52px] h-[52px] rounded-[14px] mx-auto (avatar). ShimmerBar h-4 w-24 mx-auto mt-3. ShimmerBar h-3 w-32 mx-auto mt-1. Grid 2-col gap-2 mt-4: 2x ShimmerBar h-12.
- **CalendarSkeleton**: padding 16px 20px. ShimmerBar h-4 w-28 mx-auto mb-3. Grid 7-col gap-px: 7 header ShimmerBar h-3 + 35 day ShimmerBar h-6.
- **ActivitySkeleton**: padding 20px. ShimmerBar h-4 w-28 mb-4. Then 3 items: flex gap-[10px], ShimmerBar w-8 h-8 rounded-[8px], div flex-col gap-1 with ShimmerBar h-3 w-36 + ShimmerBar h-2.5 w-16.

Add CSS keyframe for `skeleton-shimmer` animation in the component via Tailwind arbitrary values: `animate-skeleton-shimmer` should be defined. Use the existing Tailwind config `@keyframes` approach — add to `frontend/tailwind.config.ts` under `extend.keyframes` and `extend.animation`:
```
keyframes: { "skeleton-shimmer": { "0%": { backgroundPosition: "200% 0" }, "100%": { backgroundPosition: "-200% 0" } } }
animation: { "skeleton-shimmer": "skeleton-shimmer 1.5s ease-in-out infinite" }
```

If Tailwind config uses CSS-based @theme config (check globals.css), add the keyframe there instead.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx tsx -e "import SkeletonCard from './components/dashboard/SkeletonCard'; console.log('SkeletonCard imported OK');" && pnpm exec tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - SkeletonCard.tsx exists at frontend/components/dashboard/SkeletonCard.tsx
    - Component accepts `variant` prop with type `"stat" | "table" | "timeline" | "donut" | "profile" | "calendar" | "activity"`
    - Component renders `role="status"` and `aria-label="Loading..."` on wrapper div
    - ShimmerBar uses gradient from #f6f5f0 via #efede6 to #f6f5f0
    - skeleton-shimmer animation defined (1.5s ease-in-out infinite)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>SkeletonCard renders 7 skeleton variants with warm-toned shimmer animation matching paper-texture aesthetic, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` passes
- `cd frontend && pnpm vitest run --reporter=verbose` passes (no regressions)
- en.json and zh.json parse as valid JSON with dashboard namespace
- encouragement.ts and course-colors.ts export expected types
</verification>

<success_criteria>
- All dashboard i18n keys present in both locales (50+ keys per locale)
- EncouragementProvider interface exported with ActivitySummary input and EncouragementText output
- defaultEncouragementProvider returns different messages for 5 activity scenarios
- getCourseColor returns correct colors for COMP2017 (orange), COMP3221 (blue), STAT2011 (amber), INFO2222 (green)
- SkeletonCard renders 7 variants with consistent warm shimmer
- No TypeScript errors, no test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-page/05-01-SUMMARY.md`
</output>
