---
phase: 05-dashboard-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/layout/Header.tsx
  - frontend/components/layout/NotificationPanel.tsx
  - frontend/components/layout/AvatarMenu.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Notification bell click opens a dropdown panel with data-driven notification items from useNotifications hook"
    - "Avatar click opens a dropdown menu with user info from auth store and navigation actions"
    - "Clicking outside either dropdown closes it"
    - "Both dropdowns appear smoothly below their trigger buttons and visually connect to them"
  artifacts:
    - path: "frontend/components/layout/NotificationPanel.tsx"
      provides: "Data-driven notification dropdown panel"
      exports: ["default"]
    - path: "frontend/components/layout/AvatarMenu.tsx"
      provides: "Avatar dropdown menu with navigation and logout"
      exports: ["default"]
    - path: "frontend/components/layout/Header.tsx"
      provides: "Header with integrated NotificationPanel and AvatarMenu"
      contains: "NotificationPanel"
  key_links:
    - from: "frontend/components/layout/NotificationPanel.tsx"
      to: "frontend/hooks/use-notifications.ts"
      via: "props (notifications data passed from Header)"
      pattern: "notifications.*Notification"
    - from: "frontend/components/layout/AvatarMenu.tsx"
      to: "frontend/lib/auth/store.ts"
      via: "props (user data passed from Header)"
      pattern: "user.*name.*email"
    - from: "frontend/components/layout/Header.tsx"
      to: "NotificationPanel, AvatarMenu"
      via: "import and render"
      pattern: "import.*NotificationPanel"
---

<objective>
Extract Header dropdown functionality into dedicated NotificationPanel and AvatarMenu components, replacing the current hardcoded placeholder content with data-driven components that accept props.

Purpose: The Header currently has hardcoded notification items and avatar menu items. Phase 5 requires these to be data-driven (from hooks). Extracting into components keeps Header clean and makes dropdowns testable/reusable.
Output: 2 new layout components + refactored Header.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-page/05-CONTEXT.md
@.planning/phases/05-dashboard-page/05-UI-SPEC.md

<interfaces>
<!-- Existing Header component structure (from Phase 01) -->
From frontend/components/layout/Header.tsx:
- "use client" component with notifOpen/avatarOpen state and click-outside refs
- Hardcoded notification items with static JSX
- Hardcoded avatar menu with static "Ricky Qin" and "rickyqin919@gmail.com"
- Uses useTranslations("header") and useTranslations("nav")
- Click-outside logic via document.addEventListener in useEffect

From frontend/lib/auth/store.ts:
- AuthUser: { id: string; email: string; displayName: string }
- useAuthStore with user, isAuthenticated, clearAuth

From frontend/hooks/use-notifications.ts:
- useNotifications(unreadOnly?: boolean) returns UseQueryResult
- Notification type from components["schemas"]["Notification"]: { id, type, severity, title, body, is_read, action_url, created_at }

From frontend/hooks/use-user.ts:
- useCurrentUser() returns UseQueryResult with User: { id, email, display_name, gpa_target, gpa_scale, tokens, created_at }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationPanel component</name>
  <files>
    frontend/components/layout/NotificationPanel.tsx
  </files>
  <read_first>
    frontend/components/layout/Header.tsx,
    frontend/hooks/use-notifications.ts,
    frontend/lib/fixtures/notifications.ts,
    .planning/phases/05-dashboard-page/05-UI-SPEC.md
  </read_first>
  <action>
Create `frontend/components/layout/NotificationPanel.tsx` — a dropdown panel component that receives notification data as props.

Per UI-SPEC Header Dropdowns section:
- Container: position absolute, top calc(100% + 12px), right 0, bg #fff, 12px radius, 1.5px card-border, shadow-dropdown, z-[200], w-[320px], animate-drop-in
- Arrow notch: 12px rotated square, -7px top, 14px from right, same bg/border (already styled in current Header — replicate exactly)
- Title: Source Serif 4, Heading role (0.88rem), 600 weight, px-4, pt-4 pb-3 (snapped from 14px 16px 10px to 16px 16px 12px)
- Notification items: flex, gap-3 (12px snapped from 10px), py-3 px-4 (12px 16px snapped from 10px), cursor pointer
- Notification icon: 28px, 8px radius (snapped from 7px), centered icon
- Notification text: Body role (0.78rem), text-2, line-height 1.4, strong=text-1 600
- Notification time: Small role (0.66rem), text-3
- Unread items: bg rgba(217,119,87,.05), hover rgba(217,119,87,.09)
- Read items: bg transparent, hover bg card-bg-hover
- Max height scrollable: max-h-[260px] overflow-y-auto
- Footer: 12px 16px padding (snapped from 10px), center, Small role (0.76rem), 600, orange, border-top 1px divider, cursor pointer, hover bg card-bg-hover

Props interface:
```typescript
interface NotificationPanelProps {
  notifications: Array<{
    id: string;
    type: string;
    severity: string;
    title: string;
    body: string;
    is_read: boolean;
    created_at: string;
  }>;
  onViewAll: () => void;
  onItemClick?: (notification: { id: string; action_url?: string }) => void;
}
```

Icon mapping for notification types:
- `deadline_reminder`: Clock icon (lucide `Clock`), orange-soft bg, orange icon color
- `grade_published`: CheckCircle icon (lucide `CheckCircle`), green-soft bg, green icon color
- `token_expired`: AlertTriangle icon (lucide `AlertTriangle`), amber-soft bg (rgba(176,137,104,.11)), amber text
- `sync_complete`: RefreshCw icon (lucide `RefreshCw`), blue-soft bg, blue icon color
- default: Bell icon, card-bg-hover bg, text-2

Format `created_at` to relative time using date-fns `formatDistanceToNow` with `{ addSuffix: true }`.

Use `useTranslations("dashboard")` for `dashboard.notifications.title` and `dashboard.notifications.footer` strings.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx tsx -e "import NotificationPanel from './components/layout/NotificationPanel'; console.log('NotificationPanel imported OK');" && pnpm exec tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - NotificationPanel.tsx exists at frontend/components/layout/NotificationPanel.tsx
    - Component accepts `notifications` array prop, `onViewAll` callback, optional `onItemClick` callback
    - Component renders arrow notch div with classes `-top-[7px] right-[14px] w-3 h-3 rotate-45`
    - Unread notifications have bg `rgba(217,119,87,.05)`
    - Footer text uses `useTranslations("dashboard")` with key `notifications.footer`
    - Contains `formatDistanceToNow` import from date-fns
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>NotificationPanel renders data-driven notification items with proper icon mapping, unread styling, relative timestamps, and i18n strings</done>
</task>

<task type="auto">
  <name>Task 2: AvatarMenu component + Header refactor to use both dropdowns</name>
  <files>
    frontend/components/layout/AvatarMenu.tsx,
    frontend/components/layout/Header.tsx
  </files>
  <read_first>
    frontend/components/layout/Header.tsx,
    frontend/lib/auth/store.ts,
    .planning/phases/05-dashboard-page/05-UI-SPEC.md
  </read_first>
  <action>
**Part A: Create `frontend/components/layout/AvatarMenu.tsx`**

Per UI-SPEC Header Dropdowns section:
- Container: position absolute, top calc(100% + 12px), right 0, bg #fff, 12px radius, 1.5px card-border, shadow-dropdown, z-[200], w-[240px], animate-drop-in
- Arrow notch: same 12px rotated square pattern
- Header: flex, gap-3 (12px), items-center, p-4
- Panel avatar: 40px, 10px radius, gradient bg (from-orange to-[#e8956e]), white text, Source Serif 4, 600 weight, Heading role (17px mapped to Heading)
- Panel name: Heading role, 600 weight
- Panel email: Small role (0.72rem), text-3
- Divider: 1px height, bg divider, my-1
- Menu items: flex, gap-3 (12px snapped from 10px), py-3 px-4 (12px snapped from 9px, 16px), Body role (0.82rem), text-2, icons 16px (snapped from 15px)
- Menu item hover: bg card-bg-hover, text-1
- Logout: color #cc4455 (#c45), hover bg rgba(204,68,85,.05)

Props interface:
```typescript
interface AvatarMenuProps {
  user: {
    name: string;
    email: string;
    initials: string;
  };
  onNavigate: (path: string) => void;
  onLogout: () => void;
}
```

Menu items with icons (all from lucide-react):
1. User icon -> "Profile" (dashboard.avatarMenu.profile) -> onNavigate("/settings")
2. Settings icon -> "Settings" (dashboard.avatarMenu.settings) -> onNavigate("/settings")
3. Key icon -> "API Tokens" (dashboard.avatarMenu.apiTokens) -> onNavigate("/setup")
4. LogOut icon -> "Log out" (dashboard.avatarMenu.logout) -> onLogout()

Use `useTranslations("dashboard")` for menu item labels.

**Part B: Refactor `frontend/components/layout/Header.tsx`**

Replace the hardcoded notification and avatar dropdown JSX with the new components:

1. Import `NotificationPanel` and `AvatarMenu`
2. Import `useNotifications` from hooks, `useAuthStore` from auth store
3. In the component body:
   - Call `useNotifications()` to get notification data
   - Get user from `useAuthStore((s) => s.user)`
   - Compute `initials` from user.displayName (first letter of first name + first letter of last name, or first 2 chars)
   - Compute `hasUnread` from notifications data (any `is_read === false`)
4. Replace the hardcoded notification dropdown block with:
   ```tsx
   {notifOpen && (
     <NotificationPanel
       notifications={notifications.data?.data ?? []}
       onViewAll={() => { setNotifOpen(false); /* navigate to notifications */ }}
       onItemClick={(n) => { setNotifOpen(false); }}
     />
   )}
   ```
5. Replace the hardcoded avatar dropdown block with:
   ```tsx
   {avatarOpen && (
     <AvatarMenu
       user={{
         name: user?.displayName ?? "User",
         email: user?.email ?? "",
         initials,
       }}
       onNavigate={(path) => { setAvatarOpen(false); router.push(`/${locale}${path}`); }}
       onLogout={() => { setAvatarOpen(false); clearAuth(); router.push(`/${locale}/auth`); }}
     />
   )}
   ```
6. Replace hardcoded avatar button text ("RQ") with dynamic `initials`
7. Keep the existing click-outside logic, refs, search bar, and notification bell unread dot unchanged.
8. The unread dot on the bell should be conditionally rendered based on `hasUnread`.
9. Import `useRouter` from next/navigation and `useLocale` from next-intl for navigation.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx tsx -e "import AvatarMenu from './components/layout/AvatarMenu'; console.log('AvatarMenu imported OK');" && pnpm exec tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AvatarMenu.tsx exists at frontend/components/layout/AvatarMenu.tsx
    - AvatarMenu accepts `user` object prop with `name`, `email`, `initials` fields
    - AvatarMenu renders 4 menu items: Profile, Settings, API Tokens, Log out
    - Logout item has color `#cc4455` or `#c45`
    - Header.tsx imports NotificationPanel and AvatarMenu
    - Header.tsx imports useNotifications from hooks
    - Header.tsx imports useAuthStore from lib/auth/store
    - Header.tsx no longer contains hardcoded "Ricky Qin" or "rickyqin919@gmail.com"
    - Header.tsx unread dot visibility is conditional on notification data
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Header uses data-driven NotificationPanel and AvatarMenu components, avatar initials derived from auth store user, unread dot conditional on notification data</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` passes
- `cd frontend && pnpm vitest run --reporter=verbose` passes (no regressions)
- Header no longer contains hardcoded user data
- NotificationPanel and AvatarMenu accept typed props
</verification>

<success_criteria>
- Notification panel renders items from `useNotifications()` hook data
- Avatar menu renders user info from auth store
- Click-outside-to-close works for both dropdowns
- Both dropdowns use animate-drop-in CSS animation
- Arrow notch renders at top-right of each dropdown
- Logout action calls clearAuth and navigates to auth page
- No hardcoded user data remains in Header.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-page/05-02-SUMMARY.md`
</output>
