---
phase: 05-dashboard-page
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/components/dashboard/ProfileCard.tsx
  - frontend/components/dashboard/MiniCalendar.tsx
  - frontend/components/dashboard/RecentActivity.tsx
  - frontend/components/dashboard/ExternalLinkDialog.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Profile card shows user name, faculty/program, semester, course count, and credit points"
    - "Mini calendar is navigable with left/right month arrows and shows deadline dots with color depth"
    - "Recent activity shows latest items with color-coded icons, clicking opens external link confirmation dialog"
    - "External link dialog uses HTML dialog element with accessible focus trap"
  artifacts:
    - path: "frontend/components/dashboard/ProfileCard.tsx"
      provides: "Right panel profile card with avatar, name, faculty, stats"
      exports: ["default"]
    - path: "frontend/components/dashboard/MiniCalendar.tsx"
      provides: "Navigable calendar with deadline dot markers and weight-based color depth"
      exports: ["default"]
    - path: "frontend/components/dashboard/RecentActivity.tsx"
      provides: "Activity list with color-coded icons and click-to-external"
      exports: ["default"]
    - path: "frontend/components/dashboard/ExternalLinkDialog.tsx"
      provides: "HTML dialog for external link confirmation"
      exports: ["default"]
  key_links:
    - from: "frontend/components/dashboard/MiniCalendar.tsx"
      to: "date-fns"
      via: "getDaysInMonth, startOfMonth, getDay"
      pattern: "date-fns"
    - from: "frontend/components/dashboard/RecentActivity.tsx"
      to: "frontend/components/dashboard/ExternalLinkDialog.tsx"
      via: "import and render"
      pattern: "ExternalLinkDialog"
---

<objective>
Build the Right Panel dashboard components: ProfileCard, MiniCalendar (with navigable months and deadline color-depth dots), RecentActivity (with external link dialog), and ExternalLinkDialog.

Purpose: These 4 components populate the right panel sticky sidebar. They are independent of the main content area and can be built in parallel with Plan 03.
Output: 4 components for the right panel.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-page/05-CONTEXT.md
@.planning/phases/05-dashboard-page/05-UI-SPEC.md

<interfaces>
From frontend/components/layout/RightPanel.tsx (will be replaced/refactored in Plan 05):
- Current RightPanel has hardcoded profile card, calendar, and activity items
- Calendar logic: getDaysInMonth, getFirstDayOfWeek, MONTH_NAMES, DAY_HEADERS
- RoughCard wrapped with withClientOnly(), AnimatedEntry for stagger

From frontend/components/design-system/RoughCard.tsx:
```typescript
interface RoughCardProps { children: ReactNode; className?: string; padding?: string; disableHover?: boolean }
```

From frontend/lib/fixtures/deadlines.ts:
```typescript
type Deadline = { id, title, due_date, source, weight, status, days_remaining, course_code, course_name, is_confirmed }
```

From frontend/lib/fixtures/notifications.ts:
```typescript
type Notification = { id, type, severity, title, body, is_read, action_url?, created_at }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfileCard + ExternalLinkDialog</name>
  <files>
    frontend/components/dashboard/ProfileCard.tsx,
    frontend/components/dashboard/ExternalLinkDialog.tsx
  </files>
  <read_first>
    frontend/components/layout/RightPanel.tsx,
    frontend/components/design-system/RoughCard.tsx,
    .planning/phases/05-dashboard-page/05-UI-SPEC.md
  </read_first>
  <action>
**Part A: Create `frontend/components/dashboard/ProfileCard.tsx`**

"use client" component.

Props:
```typescript
interface ProfileCardProps {
  name: string;
  email: string;
  faculty: string;      // e.g., "Bachelor of Science"
  year: number;
  semester: number;
  courseCount: number;
  creditPoints: number;
}
```

Per UI-SPEC Profile Card section:
- Wrapper: RoughCard with className="text-center" padding="py-6 px-5" (24px 20px)
- Avatar: 52px (snapped from 54px), 14px radius, gradient linear-gradient(135deg, #d97757, #e8956e), Source Serif 4, 600, 22px (Display role), white text, shadow 0 2px 10px rgba(217,119,87,.25), centered (mx-auto), mb-3
- Compute initials from name: first letter of first name + first letter of last name (or first 2 chars if no space)
- Name: Source Serif 4, Heading role (1.02rem), 600 weight, mb-[2px]
- Detail line 1: "{faculty} . Year {year}" (e.g., "Bachelor of Science . Year 3"), Body role (0.76rem), text-3
- Detail line 2: "{year} Semester {semester}" (e.g., "2026 Semester 1"), Body role (0.76rem), text-3, mt-[2px]
- Stats grid: 2 columns, gap-2 (8px), mt-4 (16px)
- Each stat: border 1px card-border, radius-sm, py-2 (8px, snapped from 10px 6px)
- Stat value: Source Serif 4, Display role (1.12rem), 600 weight
- Stat label: Small role (0.66rem), text-3, uppercase, letter-spacing 0.04em
- Labels: t("profile.statLabels.courses") and t("profile.statLabels.credits")

Use `useTranslations("dashboard")` for stat labels.

**Part B: Create `frontend/components/dashboard/ExternalLinkDialog.tsx`**

"use client" component using HTML `<dialog>` element for native accessibility (focus trap, Escape close, aria-modal).

Props:
```typescript
interface ExternalLinkDialogProps {
  open: boolean;
  url: string;
  onConfirm: () => void;
  onCancel: () => void;
}
```

Per UI-SPEC and CONTEXT.md:
- Use native `<dialog>` element with `ref.showModal()` / `ref.close()` based on `open` prop
- Backdrop: dialog::backdrop bg rgba(0,0,0,0.3)
- Dialog: max-w-[360px], bg white, rounded-[12px], p-6, shadow-lg, border none
- Title: Source Serif 4, Heading role, 600 weight, text-text-1, mb-2
  - t("externalLink.title") = "Open external link?"
- Body text: Body role (0.88rem), text-2, mb-1
  - t("externalLink.body") = "You are about to leave UniBoard..."
- URL preview: truncated URL in Small role, text-3, bg card-bg, py-1 px-3, rounded-[6px], mb-4, overflow hidden, text-ellipsis, whitespace-nowrap, max-w-full
- Buttons row: flex, justify-end, gap-3
  - Cancel: Ghost button style — py-2 px-4, border 1px card-border, rounded-[8px], text-text-2, bg card-bg, hover bg card-bg-hover, Small role (14px), 600 weight
    - t("externalLink.cancel") = "Stay on UniBoard"
  - Confirm: Primary button style — py-2 px-4, bg #d97757, text white, rounded-[8px], hover bg #c5674a, Small role (14px), 600 weight
    - t("externalLink.confirm") = "Open link"
- onConfirm: calls `window.open(url, "_blank", "noopener,noreferrer")` then closes dialog
- Handle `<dialog>` `cancel` event (Escape key) to call `onCancel`
- Use `useEffect` to sync `open` prop with dialog showModal/close

Use `useTranslations("dashboard")`.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx tsx -e "import ProfileCard from './components/dashboard/ProfileCard'; console.log('ProfileCard OK');" && npx tsx -e "import ExternalLinkDialog from './components/dashboard/ExternalLinkDialog'; console.log('ExternalLinkDialog OK');" && pnpm exec tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ProfileCard.tsx accepts name, email, faculty, year, semester, courseCount, creditPoints props
    - ProfileCard renders avatar div with gradient from-orange to-[#e8956e] and computed initials
    - ProfileCard renders 2-column stats grid with t("profile.statLabels.courses") and t("profile.statLabels.credits")
    - ExternalLinkDialog.tsx uses `<dialog>` HTML element
    - ExternalLinkDialog renders URL preview in truncated format
    - ExternalLinkDialog has confirm button with bg #d97757 and cancel button
    - ExternalLinkDialog calls window.open on confirm
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>ProfileCard shows avatar with initials, faculty, semester, course count + credit points. ExternalLinkDialog uses native dialog with accessible focus trap and i18n strings.</done>
</task>

<task type="auto">
  <name>Task 2: MiniCalendar with month navigation and deadline color-depth dots + RecentActivity</name>
  <files>
    frontend/components/dashboard/MiniCalendar.tsx,
    frontend/components/dashboard/RecentActivity.tsx
  </files>
  <read_first>
    frontend/components/layout/RightPanel.tsx,
    frontend/lib/fixtures/deadlines.ts,
    frontend/lib/fixtures/notifications.ts,
    .planning/phases/05-dashboard-page/05-UI-SPEC.md
  </read_first>
  <action>
**Part A: Create `frontend/components/dashboard/MiniCalendar.tsx`**

"use client" component. Extract and enhance calendar logic from existing RightPanel.tsx (which has getDaysInMonth, getFirstDayOfWeek, MONTH_NAMES, DAY_HEADERS).

Props:
```typescript
interface DeadlineDay {
  date: string;         // ISO date string (YYYY-MM-DD)
  totalWeight: number;  // Cumulative weight of deadlines on this day (0-1)
}

interface MiniCalendarProps {
  deadlineDays: DeadlineDay[];
  onDateClick: (date: Date) => void;
}
```

Per UI-SPEC Mini Calendar section:
- Container: RoughCard padding="py-4 px-5" (16px 20px snapped from 16px 18px)
- Month navigation bar: flex justify-between, items-center, mb-3 (12px snapped from 10px)
- Month text: Source Serif 4, Heading role (0.88rem), 600 weight
  - EN format: "March 2026" (t("calendar.monthFormat", { month, year }))
- Left/right arrows: ChevronLeft/ChevronRight from lucide, no bg/border, text-3, p-1, 6px radius, hover bg card-bg-hover, hover text-1, cursor pointer
- Month navigation: useState for `[viewYear, viewMonth]`, initialized to current date. Left decrements month (handle year wrap). Right increments month.
- Grid: 7 columns, gap-px, text-center
- Day headers: Small role (0.62rem), 600 weight, text-3, uppercase, py-1. Use localized day abbreviations if possible, else ["Mon","Tue","Wed","Thu","Fri","Sat","Sun"]
- Day cells: Small role (0.74rem), py-1 (4px snapped from 5px), 6px radius, text-2
- Today: orange bg (#d97757), white text, 600 weight
- Has-deadline: orange-soft bg with **opacity scaling by totalWeight**:
  - Low weight (totalWeight <= 0.1): opacity 0.08 -> `rgba(217,119,87,0.08)`
  - Medium weight (0.1 < totalWeight <= 0.3): opacity 0.15 -> `rgba(217,119,87,0.15)`
  - High weight (totalWeight > 0.3): opacity 0.22 -> `rgba(217,119,87,0.22)`
  - Text: orange, 600 weight
- Outside-month: text-3, opacity 0.35
- Click: cursor pointer on deadline dates, call `onDateClick(new Date(year, month, day))`
- Use date-fns `getDaysInMonth`, `startOfMonth`, `getDay`, `isToday`, `isSameMonth`, `format` for calendar computations.
- Build calendar grid: previous month padding, current month days, next month padding (fill to complete 7-col row).

**Part B: Create `frontend/components/dashboard/RecentActivity.tsx`**

"use client" component.

Props:
```typescript
interface ActivityItem {
  id: string;
  type: "grade" | "discussion" | "deadline" | "endorsed";
  text: string;         // Pre-formatted display text
  strongText: string;   // Bold portion of text
  time: string;         // Relative time string (e.g., "3h ago")
  externalUrl?: string; // URL for external link
}

interface RecentActivityProps {
  activities: ActivityItem[];
}
```

Per UI-SPEC Recent Activity section:
- Wrapper: RoughCard padding="p-5" (20px snapped from 18px)
- Title: flex items-center gap-2, Heading role (0.95rem), 600 weight, with Activity icon (lucide) 16px in orange, mb-4 (14px -> 16px snapped)
  - t("activity.title") = "Recent Activity"
- Item list: flex column, gap-4 (16px snapped from 14px)
- Each item: flex gap-[10px] items-start, cursor pointer
- Icon box: 32px (snapped from 30px), 8px radius, centered icon (14px)
  - grade: green-soft bg (#788c5d with 0.11 alpha), green icon (CheckCircle)
  - discussion: blue-soft bg, blue icon (MessageCircle)
  - deadline: orange-soft bg, orange icon (AlertCircle)
  - endorsed: green-soft bg, green icon (Star)
- Text: Body role (0.76rem), text-2, line-height 1.4
- Strong text: text-1, 600 weight (wrapped in `<strong>`)
- Time: Small role (0.66rem), text-3, mt-0 (minimal)
- Item click: opens ExternalLinkDialog — manage dialog state with useState:
  ```tsx
  const [dialogUrl, setDialogUrl] = useState<string | null>(null);
  ```
  - On item click: `setDialogUrl(item.externalUrl ?? null)` if externalUrl exists
  - Render ExternalLinkDialog with open={!!dialogUrl}, url={dialogUrl ?? ""}, onConfirm/onCancel close dialog

Import ExternalLinkDialog from `@/components/dashboard/ExternalLinkDialog`.
Use `useTranslations("dashboard")` for title.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx tsx -e "import MiniCalendar from './components/dashboard/MiniCalendar'; console.log('MiniCalendar OK');" && npx tsx -e "import RecentActivity from './components/dashboard/RecentActivity'; console.log('RecentActivity OK');" && pnpm exec tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - MiniCalendar.tsx accepts deadlineDays array and onDateClick callback
    - MiniCalendar renders 7-column grid with day headers
    - MiniCalendar has useState for viewYear/viewMonth with arrow button handlers
    - MiniCalendar applies different rgba(217,119,87,X) opacities based on totalWeight thresholds (0.08, 0.15, 0.22)
    - MiniCalendar highlights today with orange bg + white text
    - RecentActivity.tsx accepts activities array prop
    - RecentActivity renders icon boxes with 4 color mappings (grade/discussion/deadline/endorsed)
    - RecentActivity imports and renders ExternalLinkDialog component
    - RecentActivity has useState for dialogUrl to track which external link dialog is open
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>MiniCalendar navigates months with arrows, shows deadline dots with weight-based color depth, highlights today. RecentActivity shows color-coded items with external link confirmation dialog.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` passes
- `cd frontend && pnpm vitest run --reporter=verbose` passes
- MiniCalendar renders correct number of days for current month
- RecentActivity opens ExternalLinkDialog on item click
</verification>

<success_criteria>
- Profile card shows avatar with gradient + initials, faculty name (not major), course count, credit points
- Calendar navigates forward/backward months with ChevronLeft/ChevronRight
- Calendar deadline dots have 3-tier color depth based on cumulative assessment weight
- Calendar today cell is solid orange with white text
- Activity items have correct icon color mapping (grade=green, discussion=blue, deadline=orange, endorsed=green)
- Clicking activity item opens ExternalLinkDialog with URL preview
- ExternalLinkDialog confirm opens URL in new tab, cancel closes dialog
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-page/05-04-SUMMARY.md`
</output>
