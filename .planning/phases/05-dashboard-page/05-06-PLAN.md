---
phase: 05-dashboard-page
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/dashboard/encouragement.ts
  - frontend/lib/utils/grade-band.ts
  - frontend/messages/en.json
  - frontend/messages/zh.json
  - frontend/components/dashboard/HeroSection.tsx
  - frontend/components/dashboard/StatsRow.tsx
  - frontend/components/dashboard/DashboardPage.tsx
  - frontend/components/dashboard/SkeletonCard.tsx
  - frontend/components/layout/RightPanel.tsx
autonomous: true
requirements: [UI-01]
gap_closure: true

must_haves:
  truths:
    - "Grade band for WAM 85 displays 'HD' (not 'D')"
    - "Encouragement text switches to Chinese when locale is zh"
    - "Skeleton loading states show warm-toned shimmer with visible animated gradient on card backgrounds"
    - "Right sidebar is position: sticky and does not scroll away"
    - "Scrollbar on right panel is hidden by default, only visible while scrolling"
  artifacts:
    - path: "frontend/lib/utils/grade-band.ts"
      provides: "getGradeBand utility with USYD scale HD/D/CR/P/F"
      exports: ["getGradeBand"]
    - path: "frontend/lib/dashboard/encouragement.ts"
      provides: "i18n-aware encouragement provider using next-intl translations"
    - path: "frontend/components/dashboard/SkeletonCard.tsx"
      provides: "Warm-toned skeleton with visible shimmer on card-bg background"
  key_links:
    - from: "frontend/components/dashboard/StatsRow.tsx"
      to: "frontend/lib/utils/grade-band.ts"
      via: "getGradeBand(wam) call"
      pattern: "getGradeBand"
    - from: "frontend/components/dashboard/HeroSection.tsx"
      to: "next-intl translations"
      via: "useTranslations for encouragement keys"
      pattern: "t\\(\"hero\\.encourage"
---

<objective>
Close 4 UAT gaps: fix grade band calculation (Gap 1), add encouragement i18n (Gap 3), improve skeleton loading design (Gap 4 partial), and fix right sidebar sticky/scrollbar behavior (Gap 4 partial).

Purpose: Fix major functional and visual issues found during user acceptance testing.
Output: Corrected grade display, bilingual encouragement text, warm-toned skeletons, sticky right sidebar.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-page/05-CONTEXT.md
@.planning/phases/05-dashboard-page/05-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix grade band calculation + encouragement i18n</name>
  <files>
    frontend/lib/utils/grade-band.ts
    frontend/lib/dashboard/encouragement.ts
    frontend/messages/en.json
    frontend/messages/zh.json
    frontend/components/dashboard/HeroSection.tsx
    frontend/components/dashboard/StatsRow.tsx
    frontend/components/dashboard/DashboardPage.tsx
  </files>
  <read_first>
    frontend/lib/fixtures/gpa.ts
    frontend/components/dashboard/StatsRow.tsx
    frontend/components/dashboard/DashboardPage.tsx
    frontend/components/dashboard/HeroSection.tsx
    frontend/lib/dashboard/encouragement.ts
    frontend/messages/en.json
    frontend/messages/zh.json
  </read_first>
  <action>
**Grade band fix:**

1. Create `frontend/lib/utils/grade-band.ts` with a `getGradeBand(mark: number): string` function using USYD grading scale:
   - HD: 85-100
   - D: 75-84
   - CR: 65-74
   - P: 50-64
   - F: 0-49
   - Returns "—" for null/undefined/NaN

2. In `StatsRow.tsx`: Remove the hardcoded `D` badge (line ~77). Import `getGradeBand` from the new utility. Replace the hardcoded badge text with `getGradeBand(wam)`. The WAM card badge should display the computed grade band, not a static "D".

3. In `DashboardPage.tsx` line ~160: Change `gradeBand: courseGrades[0]?.grade_letter ?? "—"` to instead use `getGradeBand(report?.target_wam ?? 0)` so the GPA Target card subtitle shows the correct grade band for the target WAM (85 = "HD"), not the first course's grade letter.

**Encouragement i18n fix:**

4. In `frontend/messages/en.json`, add encouragement keys under `dashboard.hero.encourage`:
   ```json
   "encourage": {
     "multiComplete": "The {item1} and the {item2} are done and behind you now. You've been working so hard — it's okay to take it slow today.",
     "multiCompleteHighlight": "it's okay to take it slow today",
     "singleComplete": "{item} is behind you now. Take a deep breath — you're making steady progress.",
     "singleCompleteHighlight": "you're making steady progress",
     "highProductivity": "You've wrapped up {count} tasks this week. That's impressive — you've earned a moment to breathe.",
     "highProductivityHighlight": "you've earned a moment to breathe",
     "someProgress": "You're {count} tasks into the week already. Keep this momentum going — you've got this.",
     "someProgressHighlight": "you've got this",
     "freshStart": "A new week stretches ahead. Take it one task at a time — every small step counts.",
     "freshStartHighlight": "every small step counts"
   }
   ```

5. In `frontend/messages/zh.json`, add matching Chinese translations:
   ```json
   "encourage": {
     "multiComplete": "{item1} 和 {item2} 都已完成了。你一直很努力——今天可以放慢脚步休息一下。",
     "multiCompleteHighlight": "今天可以放慢脚步休息一下",
     "singleComplete": "{item} 已经完成了。深呼吸——你正在稳步前进。",
     "singleCompleteHighlight": "你正在稳步前进",
     "highProductivity": "这周你已经完成了 {count} 项任务。太厉害了——你值得喘口气。",
     "highProductivityHighlight": "你值得喘口气",
     "someProgress": "这周已经完成 {count} 项任务了。保持这个势头——你一定可以的。",
     "someProgressHighlight": "你一定可以的",
     "freshStart": "新的一周在前方展开。一步一步来——每一小步都很重要。",
     "freshStartHighlight": "每一小步都很重要"
   }
   ```

6. Refactor `frontend/lib/dashboard/encouragement.ts`:
   - Change the `EncouragementProvider` type to accept a translation function `(key: string, params?: Record<string, string>) => string` as the second parameter.
   - The `defaultEncouragementProvider` should use this translation function instead of hardcoded English strings.
   - The function signature becomes: `(activity: ActivitySummary, t: (key: string, params?: Record<string, string>) => string) => EncouragementText`
   - Each scenario calls `t("hero.encourage.multiComplete", { item1, item2 })` etc. for message, and `t("hero.encourage.multiCompleteHighlight")` for the highlight phrase.

7. In `HeroSection.tsx`:
   - Pass the `t` function from `useTranslations("dashboard")` to `defaultEncouragementProvider` as the second argument: `defaultEncouragementProvider(mockActivity, t)`
   - This ensures encouragement text renders in the current locale.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/lib/utils/grade-band.ts contains `export function getGradeBand` with HD at >= 85, D at >= 75, CR at >= 65, P at >= 50, F below 50
    - StatsRow.tsx imports getGradeBand and calls getGradeBand(wam) for the WAM badge
    - DashboardPage.tsx uses getGradeBand for the target grade band display
    - en.json contains "dashboard.hero.encourage.multiComplete" key
    - zh.json contains "dashboard.hero.encourage.multiComplete" key with Chinese text
    - encouragement.ts accepts a translation function parameter
    - HeroSection.tsx passes t to defaultEncouragementProvider
  </acceptance_criteria>
  <done>Grade band 85 shows "HD" not "D". Encouragement text displays in Chinese when locale is zh.</done>
</task>

<task type="auto">
  <name>Task 2: Fix skeleton warm-toned shimmer + right sidebar sticky/scrollbar</name>
  <files>
    frontend/components/dashboard/SkeletonCard.tsx
    frontend/components/layout/RightPanel.tsx
  </files>
  <read_first>
    frontend/components/dashboard/SkeletonCard.tsx
    frontend/components/layout/RightPanel.tsx
    frontend/app/globals.css
    prototype/dashboard.html
  </read_first>
  <action>
**Skeleton card improvements:**

1. In `SkeletonCard.tsx`, update the `ShimmerBar` component to be more visible against the card background:
   - Change the gradient from `from-[#f6f5f0] via-[#efede6] to-[#f6f5f0]` to a warmer, more visible shimmer: `from-[#f0ede6] via-[#e8e3d9] to-[#f0ede6]`. This creates more contrast against the `#f6f5f0` card background.
   - Add a subtle warm-toned base color behind the shimmer: add `bg-[#ede9e1]` as a fallback so the shimmer bars are visible even before the animation starts.

2. In the outer `SkeletonCard` wrapper div, add a subtle inner shadow/border to give the skeleton card more visual presence:
   - Add `shadow-[0_1px_3px_rgba(20,20,19,0.04),0_4px_14px_rgba(20,20,19,0.025)]` to match the standard card shadow.
   - This makes skeleton cards look like real card placeholders, not just flat gray rectangles.

**Right sidebar fixes:**

3. In `RightPanel.tsx`, verify the sticky positioning is correct. The current CSS uses:
   ```
   sticky top-[calc(var(--spacing-header-h)+28px)]
   self-start max-h-[calc(100vh-var(--spacing-header-h)-56px)]
   ```
   This should already be sticky. If the user reports it scrolls away, it might be because the parent flex container needs `align-items: flex-start`. Add the `items-start` class to ensure sticky works correctly within the flex parent.

4. For the scrollbar hiding: The current implementation uses CSS classes `[&::-webkit-scrollbar-thumb]:bg-transparent` and shows on `.scrolling` class. This is correct for WebKit but needs Firefox support. Add:
   - `scrollbar-width: none` for default state (Firefox)
   - On scroll, toggle a class that sets `scrollbar-width: thin`
   - Use `-ms-overflow-style: none` for IE/Edge legacy
   - Add these as Tailwind arbitrary properties: `[scrollbar-width:none]` and `[&.scrolling]:[scrollbar-width:thin]`

5. Also add `[&::-webkit-scrollbar]:w-[5px]` to match the prototype's 5px scrollbar width.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - SkeletonCard.tsx ShimmerBar contains gradient colors `#f0ede6` and `#e8e3d9` (more visible shimmer)
    - SkeletonCard.tsx outer div has shadow class matching card shadow
    - RightPanel.tsx contains `scrollbar-width` CSS for Firefox scrollbar hiding
    - RightPanel.tsx has `[&::-webkit-scrollbar]:w-[5px]`
    - TypeScript compilation succeeds with no errors
  </acceptance_criteria>
  <done>Skeleton shimmer is visibly warm-toned against card background. Right sidebar stays fixed on scroll. Scrollbar is hidden by default across all browsers.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` passes
2. Navigate to dashboard - WAM 78.5 shows "D" badge, Target 85.0 subtitle says "HD"
3. Switch locale to zh - encouragement text renders in Chinese
4. Throttle network - skeleton cards show warm-toned animated shimmer
5. Scroll down - right sidebar stays sticky, scrollbar hidden
</verification>

<success_criteria>
- Grade band calculation uses correct USYD scale (HD >= 85)
- Encouragement text is fully bilingual via next-intl
- Skeleton cards have visible warm shimmer animation
- Right sidebar is sticky with hidden scrollbar
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-page/05-06-SUMMARY.md`
</output>
