---
phase: 05-dashboard-page
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/dashboard/AssessmentDonut.tsx
autonomous: true
requirements: [UI-01]
gap_closure: true

must_haves:
  truths:
    - "Assessment Donut renders as smooth SVG donut with gradient blue-tone fills, not Rough.js cross-hatch"
    - "Donut has inner radius hole (annular ring shape, not solid pie)"
    - "Leader lines connect each segment to percentage + name labels"
    - "Leader lines have a small dot at the start point on the donut edge"
    - "Converge entry animation still works (segments separated then merge)"
  artifacts:
    - path: "frontend/components/dashboard/AssessmentDonut.tsx"
      provides: "Smooth SVG donut chart matching prototype exactly"
      min_lines: 200
  key_links:
    - from: "frontend/components/dashboard/AssessmentDonut.tsx"
      to: "DashboardPage.tsx"
      via: "props: weights, highlightType, courseColor, courseCode"
      pattern: "AssessmentDonut"
---

<objective>
Full rewrite of AssessmentDonut.tsx to match the HTML prototype exactly (Gap 2). Replace Rough.js cross-hatch fill with smooth SVG path-based donut using the prototype's gradient color palette and fine leader lines with dot endpoints.

Purpose: The current donut uses Rough.js cross-hatch which looks hand-drawn but doesn't match the prototype design. The prototype uses smooth filled arcs with solid colors and fine leader lines.
Output: AssessmentDonut.tsx rewritten with smooth SVG rendering.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-page/05-CONTEXT.md
@.planning/phases/05-dashboard-page/05-UAT.md
@prototype/dashboard.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AssessmentDonut with smooth SVG rendering matching prototype</name>
  <files>frontend/components/dashboard/AssessmentDonut.tsx</files>
  <read_first>
    frontend/components/dashboard/AssessmentDonut.tsx
    prototype/dashboard.html
    frontend/components/dashboard/DashboardPage.tsx
  </read_first>
  <action>
**Full rewrite of AssessmentDonut.tsx.** Remove Rough.js dependency entirely. Build a smooth SVG donut chart that matches the prototype's `drawDonut()` function (prototype lines 1008-1117).

**Donut geometry (from prototype):**
- SVG viewBox: `0 0 360 300`
- Center: cx=180, cy=150
- Outer radius: 95
- Inner radius: 55
- This creates an annular ring (donut with hole), NOT a solid pie chart

**Segment rendering:**
- Each segment is drawn as an SVG `<path>` using arc commands:
  ```
  M x1outer y1outer
  A outerR outerR 0 largeArcFlag 1 x2outer y2outer
  L x1inner y1inner
  A innerR innerR 0 largeArcFlag 0 x2inner y2inner
  Z
  ```
- Start angle at -PI/2 (top of circle)
- Segment colors use the same 4-color palette from the prototype: `['#d97757', '#6a9bcc', '#788c5d', '#b08968']` as base, but adapt to the `courseColor` prop for dynamic coloring using the existing `generateSegmentPalette` function logic. Keep the palette generation but apply colors as **solid fill** (not cross-hatch).
- For `upcoming` status segments, use the existing `desaturateColor` function to mute the color.
- Highlighted segment (when `highlightType` matches): push outward by 4px along midpoint angle.

**Leader lines (from prototype):**
- Leader start radius: outerR + 14 = 109
- Elbow radius: outerR + 42 = 137
- Tail length: 30px horizontal
- Each leader line has 3 parts:
  1. Small filled circle (diameter 4px) at start point on donut edge, colored same as segment
  2. Line from start to elbow point (radial direction), stroke = segment color, strokeWidth = 1
  3. Horizontal tail line from elbow to end, same color
- Labels positioned at tail end + 5px offset:
  - Percentage: `font-family: 'Source Serif 4', Georgia, serif`, font-size: 15, font-weight: 700, fill: segment color
  - Assessment name: `font-family: 'Inter', sans-serif`, font-size: 12, fill: #6b6b65
- Text anchor: "start" if midAngle cosine >= 0 (right side), "end" if left side

**Converge animation:**
- Keep the existing rAF-based animation (800ms duration, cubic-bezier easing)
- Each segment gets a separation offset along its midpoint angle direction: `separationOffset = 20 * (1 - animationProgress)`
- The offset is applied to the segment center (cx + offsetX, cy + offsetY) — shift each segment's arc path by the offset amount
- Leader lines should only appear after animation completes (when `animationProgress >= 0.95`)

**Implementation approach:**
- Remove `import rough from "roughjs"` entirely
- Keep `RoughCard` wrapper for the card border (that's the card container, not the donut itself)
- Compute all paths as string SVG path data in React, render with JSX `<path>`, `<line>`, `<circle>`, `<text>` elements
- Use `useMemo` for path computation, `useState` + `useEffect` for animation state (reuse existing animation code)
- Keep the existing props interface (`AssessmentDonutProps`) and exported `AssessmentWeight` type unchanged
- Keep the existing empty state rendering unchanged

**Key difference from current code:**
- Current: `rough.svg(svg).arc(...)` with `fillStyle: "cross-hatch"` and imperative DOM manipulation
- New: React JSX `<path d={...} fill={color} stroke={color} />` with declarative rendering
- Current: SVG ref + replaceChildren() in useEffect
- New: Return SVG elements directly in JSX render (no imperative DOM)
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AssessmentDonut.tsx does NOT import "roughjs"
    - AssessmentDonut.tsx contains SVG path commands with arc syntax (A outerR outerR)
    - AssessmentDonut.tsx has innerR = 55 and outerR = 95 constants
    - AssessmentDonut.tsx renders `<path` elements (not rough.svg().arc)
    - AssessmentDonut.tsx renders `<circle` elements for leader line start dots
    - AssessmentDonut.tsx renders `<text` elements for labels
    - AssessmentDonut.tsx still exports AssessmentWeight interface and default function
    - AssessmentDonut.tsx still has animationProgress state and rAF animation
    - TypeScript compilation succeeds with no errors
  </acceptance_criteria>
  <done>Assessment donut renders as smooth filled annular ring with fine leader lines and dot endpoints, matching the prototype. Converge animation still works. No Rough.js dependency.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` passes
2. Navigate to dashboard — donut shows smooth blue-tone gradient segments with hole in center
3. Leader lines extend from donut edge with small dots, connecting to percentage + name labels
4. Clicking different deadlines switches donut course and segments animate (converge from separated to joined)
5. Empty state still shows "No upcoming deadlines" message
</verification>

<success_criteria>
- Donut matches prototype design: smooth fills, annular shape, fine leader lines with dot endpoints
- No Rough.js dependency in AssessmentDonut
- Converge entry animation preserved
- Props interface unchanged (no breaking changes to DashboardPage)
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-page/05-07-SUMMARY.md`
</output>
