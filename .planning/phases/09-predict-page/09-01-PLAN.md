---
phase: 09-predict-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/predict/faculty-weights.ts
  - frontend/lib/predict/wam-engine.ts
  - frontend/lib/predict/wam-to-gpa.ts
  - frontend/messages/en.json
  - frontend/messages/zh.json
  - frontend/__tests__/predict/wam-engine.test.ts
  - frontend/__tests__/predict/faculty-weights.test.ts
  - frontend/__tests__/predict/PredictCard.test.tsx
  - frontend/__tests__/predict/PredictPage.test.tsx
autonomous: true
requirements:
  - UI-04

must_haves:
  truths:
    - "computeCurrent returns correct weighted average for graded assessments only"
    - "computeProjected returns projected final including predictions, null when not all filled"
    - "computeWAM calculates cross-course WAM using faculty-specific level weights"
    - "computeRequired reverse-calculates minimum scores per course to hit target WAM"
    - "Engineering scheme excludes 1000-level courses (weight 0)"
    - "Standard scheme weights all levels equally at 1"
    - "i18n predict namespace has all required keys in both en.json and zh.json"
  artifacts:
    - path: "frontend/lib/predict/faculty-weights.ts"
      provides: "FacultyScheme type and FACULTY_WEIGHTS map"
      exports: ["FacultyScheme", "FACULTY_WEIGHTS", "getLevelFromCode"]
    - path: "frontend/lib/predict/wam-engine.ts"
      provides: "Pure WAM computation functions"
      exports: ["computeCurrent", "computeProjected", "computeWAM", "computeRequired"]
    - path: "frontend/lib/predict/wam-to-gpa.ts"
      provides: "WAM-to-GPA 4.0 conversion"
      exports: ["wamToGpa", "getFeasibility", "Feasibility"]
    - path: "frontend/__tests__/predict/wam-engine.test.ts"
      provides: "WAM engine unit tests"
    - path: "frontend/__tests__/predict/faculty-weights.test.ts"
      provides: "Faculty weights unit tests"
  key_links:
    - from: "frontend/lib/predict/wam-engine.ts"
      to: "frontend/lib/predict/faculty-weights.ts"
      via: "import FACULTY_WEIGHTS"
      pattern: "FACULTY_WEIGHTS\\[scheme\\]"
---

<objective>
Build the WAM calculation engine (pure functions), faculty weight system, WAM-to-GPA conversion, i18n messages for the predict namespace, and Wave 0 test stubs for all predict components.

Purpose: Establish the computational foundation and test infrastructure that all predict UI components depend on. The WAM engine is the core differentiator of this page and must be correct for all 3 faculty schemes.

Output: `frontend/lib/predict/` with 3 modules (faculty-weights, wam-engine, wam-to-gpa), i18n predict namespace in both locales, and Wave 0 test files for all predict components.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-predict-page/09-CONTEXT.md
@.planning/phases/09-predict-page/09-RESEARCH.md
@.planning/phases/09-predict-page/09-VALIDATION.md
@prototype/predict.html

<interfaces>
<!-- Key types the executor needs from the existing codebase -->

From frontend/lib/api/types.gen.d.ts:
```typescript
AssessmentWeight: {
    name: string;
    weight: number;        // 0-1 scale (e.g., 0.15 = 15%)
    score: number | null;
    max_score: number;
    status: "graded" | "upcoming" | "submitted";
    group_name: string;
    due_date?: string;
};

GpaCourseSummary: {
    course_id: string;
    code: string;
    name: string;
    credit_points: number;
    level_weight: number;
    current_mark: number | null;
    grade_letter: string | null;
    completed_weight: number;  // 0-1 scale
};

GpaReport: {
    scale: "wam" | "gpa_4" | "gpa_7";
    current_wam: number;
    current_gpa_4: number;
    target_wam: number | null;
    gap: number | null;
    courses: GpaCourseSummary[];
    last_sync_at: string;
};
```

From frontend/lib/utils/grade-band.ts:
```typescript
export function getGradeBand(mark: number | null | undefined): string;
// Returns "HD" | "D" | "CR" | "P" | "F" | "—"
```

From frontend/lib/dashboard/course-colors.ts:
```typescript
export function getCourseColor(courseCode: string): { base: string; soft: string };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WAM calculation engine + faculty weights + WAM-to-GPA conversion</name>
  <files>
    frontend/lib/predict/faculty-weights.ts,
    frontend/lib/predict/wam-engine.ts,
    frontend/lib/predict/wam-to-gpa.ts,
    frontend/__tests__/predict/wam-engine.test.ts,
    frontend/__tests__/predict/faculty-weights.test.ts
  </files>
  <read_first>
    prototype/predict.html (lines 300-534 for JS computation functions),
    frontend/lib/api/types.gen.d.ts (AssessmentWeight and GpaCourseSummary schemas),
    frontend/lib/fixtures/gpa.ts (fixture data for test expectations),
    frontend/components/course-detail/AssessmentSection.tsx (existing grade calculation pattern using 0-1 weight scale),
    .planning/phases/09-predict-page/09-RESEARCH.md (code examples section with verified implementations)
  </read_first>
  <behavior>
    faculty-weights.test.ts:
    - standard scheme: getLevelFromCode("COMP2017") returns 2; FACULTY_WEIGHTS.standard(2) returns 1; FACULTY_WEIGHTS.standard(1) returns 1
    - engineering scheme: FACULTY_WEIGHTS.engineering(1) returns 0 (excluded); engineering(2) returns 2; engineering(3) returns 3; engineering(4) returns 4
    - science_honours scheme: science_honours(1) returns 0 (excluded); science_honours(2) returns 2; science_honours(3) returns 3
    - getLevelFromCode: "COMP2017" -> 2, "COMP3221" -> 3, "EDGU1003" -> 1, "MATH2021" -> 2, "COMP4XXX" -> 4

    wam-engine.test.ts:
    - computeCurrent: COMP2017 (85*15+80*25)/(15+25) = 81.875; MATH1005 no grades -> 0
    - computeProjected: COMP2017 with predictions {2:75,3:80} -> (85*15+80*25+75*10+80*50)/100 = 80.75; partial predictions -> null
    - computeWAM standard: all courses with mock projections, verify WAM = sum(mark*cp*lw)/sum(cp*lw) with all weights=1
    - computeWAM engineering: 1000-level (EDGU1003) excluded from numerator AND denominator
    - computeRequired: with target=85 and standard scheme, verify each course gets a required score; locked courses (100% graded) return locked:true; 0-weight-level courses return excluded:true
  </behavior>
  <action>
    Create `frontend/lib/predict/faculty-weights.ts`:
    - Export type `FacultyScheme = "standard" | "engineering" | "science_honours"`
    - Export const `FACULTY_WEIGHTS: Record<FacultyScheme, (level: number) => number>` with:
      - standard: always returns 1
      - engineering: 0/2/3/4 for levels 1/2/3/4+
      - science_honours: 0/2/3 for levels 1/2/3+
    - Export function `getLevelFromCode(code: string): number` — parses the first digit of the numeric portion of a USYD course code (e.g., COMP**2**017 -> 2). Use regex `/\d/` on the code to find first digit. Return `parseInt(firstDigit)`.

    Create `frontend/lib/predict/wam-engine.ts`:
    - Define internal interface `CourseComputeData { courseId: string; code: string; creditPoints: number; level: number; assessments: { weight: number; score: number | null; maxScore: number }[]; predictions: Record<number, number | null> }`
    - CRITICAL: Assessments use **0-1 weight scale** (from fixture data), NOT 0-100 like prototype. The prototype JS uses weight=15 meaning 15%, but our fixtures use weight=0.15 meaning 15%. All functions must handle 0-1 weights correctly. The key insight: `sum(weight)` should equal ~1.0 per course, not 100.
    - Export `computeCurrent(assessments)`: iterate graded assessments, compute `sum((score/maxScore) * weight) / sum(weight) * 100`. Returns percentage (0-100 scale).
    - Export `computeProjected(assessments, predictions)`: same as computeCurrent but includes predictions for ungraded. Returns null if any ungraded assessment has no prediction. Returns percentage.
    - Export `computeWAM(courses: CourseComputeData[], scheme: FacultyScheme)`: returns `{ wam: number; allFilled: boolean }`. For each course, compute projected (or current if not all filled), then WAM = sum(mark * cp * lw) / sum(cp * lw). Courses with lw=0 are excluded from both numerator and denominator.
    - Export `computeRequired(courses: CourseComputeData[], targetWAM: number, scheme: FacultyScheme)`: returns `RequiredScoreResult[]`. For each course: if lw=0 return excluded:true; if remainWeight<=0 return locked:true with current mark; otherwise solve the WAM equation for this course's needed final mark, assuming other courses maintain their current average on remaining work. Algorithm matches prototype computeRequired() (lines 479-518 of predict.html) but adapted for 0-1 weight scale.
    - Export type `RequiredScoreResult = { code: string; required: number; locked?: boolean; excluded?: boolean }`

    Create `frontend/lib/predict/wam-to-gpa.ts`:
    - Export `wamToGpa(wam: number): number` — step-function: 85+ -> 4.0, 75+ -> 3.5, 65+ -> 2.5, 50+ -> 1.5, else 0.0
    - Export type `Feasibility = "feasible" | "warning" | "impossible"`
    - Export `getFeasibility(required: number): Feasibility` — <=85 feasible, <=100 warning, >100 impossible

    Write tests FIRST (RED), then implement (GREEN).
    Test file locations: `frontend/__tests__/predict/wam-engine.test.ts` and `frontend/__tests__/predict/faculty-weights.test.ts`.

    Use the prototype COURSES data (lines 308-364 of predict.html) as test fixture data, converting weights from 0-100 to 0-1 scale (e.g., weight:15 -> weight:0.15).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx vitest run --testPathPattern "predict/(wam-engine|faculty-weights)" -x</automated>
  </verify>
  <acceptance_criteria>
    - frontend/lib/predict/faculty-weights.ts exports FacultyScheme, FACULTY_WEIGHTS, getLevelFromCode
    - frontend/lib/predict/wam-engine.ts exports computeCurrent, computeProjected, computeWAM, computeRequired, RequiredScoreResult
    - frontend/lib/predict/wam-to-gpa.ts exports wamToGpa, getFeasibility, Feasibility
    - frontend/__tests__/predict/faculty-weights.test.ts has tests for all 3 schemes and getLevelFromCode
    - frontend/__tests__/predict/wam-engine.test.ts has tests for computeCurrent, computeProjected, computeWAM (standard + engineering), computeRequired
    - FACULTY_WEIGHTS.engineering(1) === 0 (1000-level excluded)
    - FACULTY_WEIGHTS.standard(1) === 1 (all equal)
    - All tests pass: `npx vitest run --testPathPattern "predict/(wam-engine|faculty-weights)" -x` exits 0
  </acceptance_criteria>
  <done>WAM engine pure functions correctly implement USYD WAM formula for all 3 faculty schemes, reverse calculation, and GPA conversion. All unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: i18n predict namespace + Wave 0 component test stubs</name>
  <files>
    frontend/messages/en.json,
    frontend/messages/zh.json,
    frontend/__tests__/predict/PredictCard.test.tsx,
    frontend/__tests__/predict/PredictPage.test.tsx
  </files>
  <read_first>
    frontend/messages/en.json (current message structure to add predict namespace),
    frontend/messages/zh.json (current message structure to add predict namespace),
    prototype/predict.html (all visible text strings for i18n keys),
    frontend/__tests__/i18n/message-keys.test.ts (existing i18n test pattern),
    .planning/phases/09-predict-page/09-CONTEXT.md (i18n requirements)
  </read_first>
  <action>
    Add `predict` namespace to `frontend/messages/en.json` with these keys:
    ```json
    "predict": {
      "title": "Grade Predictor",
      "semesterBadge": "2026 S1",
      "cpBadge": "{cp} cp",
      "facultyLabel": "Weighting",
      "facultyStandard": "Standard",
      "facultyEngineering": "Engineering",
      "facultyScienceHonours": "Science Honours",
      "assessed": "{pct}% assessed",
      "current": "Current",
      "projected": "Projected",
      "columns": {
        "assessment": "Assessment",
        "weight": "Weight",
        "score": "Score"
      },
      "scorePlaceholder": "?",
      "gradedBadge": "graded",
      "gradeSummary": {
        "current": "Current",
        "projected": "Projected",
        "assessedSub": "{pct}% assessed",
        "gradeBand": "{band} grade band",
        "enterScores": "Enter scores above",
        "note": "Projected grade updates live as you type predicted scores."
      },
      "wamOverview": {
        "fillPredictions": "Fill predictions",
        "gpa": "GPA: {gpa} / 4.0",
        "basisPartial": "Based on current marks (partial)",
        "basisAll": "Based on all predicted scores"
      },
      "target": {
        "label": "Target WAM",
        "toGo": "+{gap} to go",
        "onTrack": "On track!"
      },
      "required": {
        "title": "Min. Scores Needed",
        "note": "Average on remaining assessments",
        "excluded": "Excluded",
        "locked": "Locked"
      },
      "progress": {
        "title": "Semester Progress",
        "overall": "{pct}% of semester assessed"
      },
      "empty": {
        "noCourses": "No courses available",
        "noAssessments": "No assessments for this course"
      },
      "error": {
        "loadFailed": "Failed to load prediction data",
        "retry": "Retry"
      }
    }
    ```

    Add matching Chinese translations to `frontend/messages/zh.json`:
    ```json
    "predict": {
      "title": "成绩预测",
      "semesterBadge": "2026 S1",
      "cpBadge": "{cp} 学分",
      "facultyLabel": "权重方案",
      "facultyStandard": "标准",
      "facultyEngineering": "工程学院",
      "facultyScienceHonours": "理学院荣誉",
      "assessed": "已评估 {pct}%",
      "current": "当前",
      "projected": "预计",
      "columns": {
        "assessment": "评估项",
        "weight": "权重",
        "score": "分数"
      },
      "scorePlaceholder": "?",
      "gradedBadge": "已出分",
      "gradeSummary": {
        "current": "当前",
        "projected": "预计",
        "assessedSub": "已评估 {pct}%",
        "gradeBand": "{band} 等级",
        "enterScores": "请在上方输入分数",
        "note": "预计成绩会在您输入分数时实时更新。"
      },
      "wamOverview": {
        "fillPredictions": "填写预测分数",
        "gpa": "GPA: {gpa} / 4.0",
        "basisPartial": "基于当前已出分成绩（部分）",
        "basisAll": "基于所有预测分数"
      },
      "target": {
        "label": "目标 WAM",
        "toGo": "还差 +{gap}",
        "onTrack": "已达标！"
      },
      "required": {
        "title": "各科目最低分数",
        "note": "剩余评估的平均分",
        "excluded": "已排除",
        "locked": "已锁定"
      },
      "progress": {
        "title": "学期进度",
        "overall": "学期已评估 {pct}%"
      },
      "empty": {
        "noCourses": "暂无课程数据",
        "noAssessments": "该课程暂无评估项"
      },
      "error": {
        "loadFailed": "预测数据加载失败",
        "retry": "重试"
      }
    }
    ```

    Create Wave 0 test stubs:

    `frontend/__tests__/predict/PredictCard.test.tsx`:
    - Use `it.todo()` for: "renders collapsed card with course info and current mark", "expands on click showing assessment table", "shows score input for ungraded assessments", "clamps input to 0-100 range", "shows graded badge for graded assessments", "displays projected mark when all predictions filled"

    `frontend/__tests__/predict/PredictPage.test.tsx`:
    - Use `it.todo()` for: "renders title row with Grade Predictor heading", "renders course cards for all courses", "updates WAM overview on prediction input", "target slider changes required scores", "deep-link ?course=COMP2017 auto-expands matching card", "faculty selector changes WAM calculation", "shows skeleton during loading"

    Ensure `it.todo()` pattern (not `it.skip`) matching Phase 05-00 convention.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx vitest run --testPathPattern "i18n/message-keys" -x</automated>
  </verify>
  <acceptance_criteria>
    - en.json contains "predict" key with nested keys: title, semesterBadge, cpBadge, facultyLabel, facultyStandard, facultyEngineering, facultyScienceHonours, assessed, current, projected, columns.assessment, columns.weight, columns.score, scorePlaceholder, gradedBadge, gradeSummary.current, gradeSummary.projected, wamOverview.fillPredictions, target.label, target.toGo, target.onTrack, required.title, required.note, progress.title, progress.overall, empty.noCourses, error.loadFailed, error.retry
    - zh.json contains matching "predict" key with same nested structure
    - frontend/__tests__/predict/PredictCard.test.tsx exists with 6+ it.todo() stubs
    - frontend/__tests__/predict/PredictPage.test.tsx exists with 7+ it.todo() stubs
    - i18n message-keys test passes: `npx vitest run --testPathPattern "i18n/message-keys" -x` exits 0
  </acceptance_criteria>
  <done>Predict i18n namespace complete in both locales. Wave 0 test stubs created for PredictCard and PredictPage components.</done>
</task>

</tasks>

<verification>
cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx vitest run --testPathPattern "predict|i18n/message-keys" -x
</verification>

<success_criteria>
- WAM engine produces correct results for all 3 faculty schemes
- Reverse calculation matches prototype algorithm behavior
- i18n predict namespace has parity between en.json and zh.json
- All existing tests still pass (no regressions)
- Wave 0 test stubs exist for Plans 02 and 03 components
</success_criteria>

<output>
After completion, create `.planning/phases/09-predict-page/09-01-SUMMARY.md`
</output>
