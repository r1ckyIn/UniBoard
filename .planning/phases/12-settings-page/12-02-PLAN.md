---
phase: 12-settings-page
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/components/settings/TokensSection.tsx
  - frontend/components/settings/GpaTargetSection.tsx
  - frontend/components/settings/NotificationsSection.tsx
  - frontend/components/settings/SettingsPage.tsx
  - frontend/__tests__/settings/TokensSection.test.tsx
  - frontend/__tests__/settings/GpaTargetSection.test.tsx
  - frontend/__tests__/settings/NotificationsSection.test.tsx
autonomous: true
requirements:
  - UI-06

must_haves:
  truths:
    - "Token management shows connection status (Active/Invalid/Not Configured) for Canvas and Ed"
    - "Token visibility can be toggled with eye icon"
    - "Update button validates and saves new token via useConfigureToken"
    - "Sync Now button triggers sync with spinning animation"
    - "GPA slider (0-100, step 0.5) and numeric input stay synced bidirectionally"
    - "GPA display shows current target number and grade band badge (HD/D/CR/P/F)"
    - "Save Target button calls useUpdateProfile with new gpa_target"
    - "Notification toggles persist to localStorage under 'uniboard-notification-prefs' key"
    - "Deadline reminder toggles (72h/24h/3h) are independent on/off switches"
  artifacts:
    - path: "frontend/components/settings/TokensSection.tsx"
      provides: "Token management UI with status badges, visibility toggle, update and sync"
      exports: ["default"]
    - path: "frontend/components/settings/GpaTargetSection.tsx"
      provides: "GPA target slider + numeric input + grade band badge + save"
      exports: ["default"]
    - path: "frontend/components/settings/NotificationsSection.tsx"
      provides: "Notification preferences with toggle switches + localStorage persistence"
      exports: ["default"]
  key_links:
    - from: "frontend/components/settings/TokensSection.tsx"
      to: "frontend/hooks/use-user.ts"
      via: "useConfigureToken mutation"
      pattern: "useConfigureToken"
    - from: "frontend/components/settings/GpaTargetSection.tsx"
      to: "frontend/hooks/use-user.ts"
      via: "useUpdateProfile mutation"
      pattern: "useUpdateProfile"
    - from: "frontend/components/settings/TokensSection.tsx"
      to: "frontend/hooks/use-sync.ts"
      via: "useSyncTrigger mutation"
      pattern: "useSyncTrigger"
---

<objective>
Build the three interactive form sections: API Tokens (token CRUD + sync), GPA Target (slider + numeric input), and Notifications (toggle switches with localStorage).

Purpose: These are the core interactive sections that fulfill UI-06's token management, GPA target, and notification preferences requirements.
Output: Three fully functional section components wired into SettingsPage.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-settings-page/12-CONTEXT.md
@.planning/phases/12-settings-page/12-RESEARCH.md
@.planning/phases/12-settings-page/12-01-SUMMARY.md
@prototype/settings.html

<interfaces>
<!-- Existing hooks and components the executor will use -->

From frontend/hooks/use-user.ts:
```typescript
export function useCurrentUser(): UseQueryResult<{ data: User }>;
// User.tokens.canvas.status: "active" | "invalid" | "not_configured"
// User.tokens.canvas.last_verified_at: string | null
// User.tokens.ed.status / last_verified_at same shape
// User.gpa_target: number | null (e.g. 85.0)
// User.display_name: string
// User.email: string
// User.created_at: string

export function useUpdateProfile(): UseMutationResult;
// mutate({ gpa_target: 75.0 }) or mutate({ display_name: "..." })

export function useConfigureToken(): UseMutationResult;
// mutate({ platform: "canvas" | "ed", body: { token: "..." } })

export function useDeleteToken(): UseMutationResult;
// mutate({ platform: "canvas" | "ed" })
```

From frontend/hooks/use-sync.ts:
```typescript
export function useSyncTrigger(): UseMutationResult;
// mutate({ scope: "all" })
```

From frontend/lib/validations/token.ts:
```typescript
export function validateCanvasToken(value: string): boolean; // /^\d+~[A-Za-z0-9]{20,}$/
export function validateEdToken(value: string): boolean; // /^[a-zA-Z0-9_-]{10,50}$/
```

From frontend/lib/utils/grade-band.ts:
```typescript
export function getGradeBand(mark: number | null | undefined): string; // 85+ HD, 75+ D, 65+ CR, 50+ P, else F
```

From frontend/components/setup/platform-config.ts:
```typescript
export const PLATFORM_CONFIG: {
  canvas: { icon: LayoutDashboard, iconBg: "rgba(217,60,50,.08)", iconColor: "#d93c32" };
  ed: { icon: MessageCircle, iconBg: "rgba(106,155,204,.11)", iconColor: "#6a9bcc" };
};
```

From frontend/components/setup/TokenInput.tsx:
```typescript
// Self-contained token input with platform icon, monospace font, status icons
// Props: { platform, value, onChange, status, error?, onClear? }
// Uses next-intl translations from "setup.tokens" namespace
```

Section card pattern (from SettingsPage Plan 01):
```typescript
// Each section: <div id="sec-xxx"><RoughCard>content</RoughCard></div>
// Section title: icon (18px) + title text (1.1rem 600) via i18n
// Section desc: 0.82rem text-3 below title, margin-bottom 18px
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TokensSection with status badges, visibility toggle, update and sync</name>
  <files>
    frontend/components/settings/TokensSection.tsx,
    frontend/__tests__/settings/TokensSection.test.tsx
  </files>
  <read_first>
    frontend/components/settings/SettingsPage.tsx,
    frontend/components/setup/TokenInput.tsx,
    frontend/components/setup/platform-config.ts,
    frontend/hooks/use-user.ts,
    frontend/hooks/use-sync.ts,
    frontend/lib/validations/token.ts,
    frontend/lib/fixtures/users.ts,
    prototype/settings.html,
    frontend/__tests__/settings/TokensSection.test.tsx
  </read_first>
  <behavior>
    - Test 1: renders Canvas and Ed platform rows with correct names and icons
    - Test 2: shows "Active" badge when token status is "active"
    - Test 3: shows "Invalid" badge when token status is "invalid"
    - Test 4: shows "Not Configured" badge when token status is "not_configured"
    - Test 5: token input toggles between password and text type on eye icon click
    - Test 6: Update button is disabled when token input is empty
    - Test 7: Sync Now button renders and shows section-level sync trigger
  </behavior>
  <action>
1. Replace the it.todo() stubs in TokensSection.test.tsx with real tests:
   - Create test helper that renders TokensSection with QueryClient wrapper and NextIntlProvider (settings namespace)
   - Mock useCurrentUser to return mockUser (from fixtures/users.ts)
   - Mock useSyncTrigger to return { mutate: vi.fn(), isPending: false }
   - Test renders: check for "Canvas LMS" and "Ed Discussion" text
   - Test status badges: render with different token statuses, check for "Active"/"Invalid"/"Not Configured" badge text
   - Test visibility toggle: find the eye icon button (data-testid="token-toggle-canvas"), click it, verify input type changes from password to text
   - Test Update disabled: verify Update button has disabled attribute when input is empty
   - Test Sync Now: verify button with sync text renders

2. Create frontend/components/settings/TokensSection.tsx:
   - Props: `{ user: User }` (user data passed from SettingsPage)
   - Imports: useTranslations("settings"), useConfigureToken, useSyncTrigger, validateCanvasToken, validateEdToken, PLATFORM_CONFIG, Key and Eye/EyeOff and RefreshCw icons from lucide-react
   - For each platform (canvas, ed), render a "platform-row" block:
     - Platform header: icon (28px rounded, from PLATFORM_CONFIG) + name + status badge
     - Status badge: green bg/text for "active" (CheckCircle icon + "Active"), orange for "invalid" (AlertCircle + "Invalid"), grey for "not_configured" ("Not Configured")
     - "Last synced: {time}" text using formatDistanceToNow from date-fns with user.tokens[platform].last_verified_at
     - Token input row: input with type password/text toggle, relative container with eye/eye-off button (data-testid="token-toggle-{platform}"), Update button (orange, disabled when input empty)
     - Token validation on Update click: validateCanvasToken or validateEdToken, show error text below if invalid, call configureToken.mutate if valid
   - At bottom: full-width "Sync Now" button with RefreshCw icon, animate-spin when syncing, calls syncTrigger.mutate({ scope: "all" })
   - State: canvasToken (string), edToken (string), canvasVisible (boolean), edVisible (boolean)
   - Styling: match prototype CSS — platform-row has 16px padding, 1px border divider, 8px border-radius, 12px bottom margin

3. Update SettingsPage.tsx: replace the sec-tokens placeholder with `<TokensSection user={userData.data} />` (only if userData is loaded). Import TokensSection.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx vitest run __tests__/settings/TokensSection.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/settings/TokensSection.tsx contains "useConfigureToken" import
    - frontend/components/settings/TokensSection.tsx contains "useSyncTrigger" import
    - frontend/components/settings/TokensSection.tsx contains "validateCanvasToken" and "validateEdToken" imports
    - frontend/components/settings/TokensSection.tsx contains "PLATFORM_CONFIG" import
    - frontend/components/settings/TokensSection.tsx contains data-testid="token-toggle-canvas" and data-testid="token-toggle-ed"
    - frontend/components/settings/TokensSection.tsx contains "animate-spin" for sync spinning animation
    - frontend/__tests__/settings/TokensSection.test.tsx contains no it.todo (all replaced with real tests)
    - All tests in TokensSection.test.tsx pass (npx vitest run __tests__/settings/TokensSection.test.tsx exits 0)
  </acceptance_criteria>
  <done>TokensSection shows Canvas/Ed platform rows with status badges, eye toggle for token visibility, Update button with validation, Sync Now with spinning animation. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GpaTargetSection with bidirectional slider + NotificationsSection with toggle switches</name>
  <files>
    frontend/components/settings/GpaTargetSection.tsx,
    frontend/components/settings/NotificationsSection.tsx,
    frontend/components/settings/SettingsPage.tsx,
    frontend/__tests__/settings/GpaTargetSection.test.tsx,
    frontend/__tests__/settings/NotificationsSection.test.tsx
  </files>
  <read_first>
    frontend/components/settings/SettingsPage.tsx,
    frontend/hooks/use-user.ts,
    frontend/lib/utils/grade-band.ts,
    frontend/lib/fixtures/users.ts,
    prototype/settings.html,
    frontend/__tests__/settings/GpaTargetSection.test.tsx,
    frontend/__tests__/settings/NotificationsSection.test.tsx
  </read_first>
  <behavior>
    GpaTargetSection:
    - Test 1: renders current target value from user.gpa_target (e.g. "85.0")
    - Test 2: renders grade band badge matching target (e.g. "HD" for 85)
    - Test 3: changing slider updates displayed value
    - Test 4: renders USYD scale reference text
    - Test 5: Save Target button calls useUpdateProfile.mutate

    NotificationsSection:
    - Test 1: renders 3 deadline reminder toggles (72h, 24h, 3h)
    - Test 2: renders GPA risk alert toggle
    - Test 3: renders digest frequency selector (daily/weekly)
    - Test 4: renders email notifications toggle
    - Test 5: toggling a switch updates its visual state
  </behavior>
  <action>
1. Replace it.todo() stubs in GpaTargetSection.test.tsx:
   - Mock useUpdateProfile to return { mutate: vi.fn(), isPending: false }
   - Render with props: `{ user: mockUser }` (mockUser.gpa_target = 85.0)
   - Test displays: "85.0" text present, "HD" badge present
   - Test slider: find range input, fire change to 75, check "75.0" and "D" displayed
   - Test scale text: check for "USYD 7-point" text
   - Test save: find "Save Target" button, click, verify mutate called with { gpa_target: 85.0 }

2. Create GpaTargetSection.tsx:
   - Props: `{ user: User }`
   - State: `gpaValue` initialized from user.gpa_target ?? 75
   - Display row: "Current Target" label + large number (Source Serif 4, 2.2rem, 700, orange color #d97757) + grade band badge (getGradeBand(gpaValue), green bg/text for HD/D, amber for CR, grey for P, red for F)
   - Input row: range input (min=0, max=100, step=0.5, value=gpaValue, className for orange thumb styling) + numeric input (width 60px, Source Serif 4 font, centered, same min/max/step) + Save Target button (btn-sm btn-orange)
   - Both inputs onChange: setGpaValue(parseFloat(e.target.value)) — single state source prevents circular updates
   - Save Target onClick: updateProfile.mutate({ gpa_target: gpaValue }), show "Saved!" checkmark briefly (2s timeout)
   - Scale reference: "Scale: USYD 7-point (HD >= 85, D >= 75, CR >= 65, P >= 50)" from i18n, styled as 0.72rem text-3 with top border
   - Slider styling via Tailwind: appearance-none, h-[6px], bg-divider, rounded-full, custom thumb via [&::-webkit-slider-thumb] classes (18px, orange bg, white border, rounded-full, shadow)

3. Replace it.todo() stubs in NotificationsSection.test.tsx:
   - Render NotificationsSection
   - Test 3 deadline toggles: find toggle switches, verify 3 for deadline reminders
   - Test GPA risk toggle: find toggle with associated label text
   - Test digest frequency: find daily/weekly selector
   - Test email toggle: find email notifications toggle
   - Test toggle click: click one toggle, verify its checked state changes

4. Create NotificationsSection.tsx:
   - No props needed (uses localStorage for persistence)
   - Use localStorage key "uniboard-notification-prefs" to load/save preferences
   - Default state: `{ reminder72h: true, reminder24h: true, reminder3h: false, gpaRiskAlert: true, digestFrequency: "daily" as "daily" | "weekly", emailNotifications: true }`
   - Initialize from localStorage on mount (useEffect + JSON.parse with try/catch fallback to defaults)
   - Save to localStorage on every state change (useEffect watching prefs state)
   - Render 4 subsections:
     a. **Deadline Reminders**: heading + 3 independent toggle switches (72h ON by default, 24h ON, 3h OFF), each row has label text + toggle
     b. **GPA Risk Alert**: toggle + description text below
     c. **Digest Frequency**: two radio-style buttons (Daily / Weekly), active one has orange bg
     d. **Email Notifications**: toggle + description text
   - Toggle switch component (inline, not separate file): styled `<input type="checkbox">` with Tailwind — appearance-none, w-[44px] h-[24px], rounded-full, bg-card-border (off) / bg-orange (on), transition, peer pattern for thumb circle via ::after or sibling div
   - Use `data-testid="toggle-{key}"` on each toggle for testability

5. Update SettingsPage.tsx: replace sec-gpa and sec-notifications placeholders with `<GpaTargetSection user={userData.data} />` and `<NotificationsSection />`. Import both.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx vitest run __tests__/settings/GpaTargetSection.test.tsx __tests__/settings/NotificationsSection.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/settings/GpaTargetSection.tsx contains "getGradeBand" import
    - frontend/components/settings/GpaTargetSection.tsx contains "useUpdateProfile" import
    - frontend/components/settings/GpaTargetSection.tsx contains 'type="range"' for slider input
    - frontend/components/settings/GpaTargetSection.tsx contains 'step={0.5}' or 'step="0.5"'
    - frontend/components/settings/NotificationsSection.tsx contains "uniboard-notification-prefs" localStorage key
    - frontend/components/settings/NotificationsSection.tsx contains "reminder72h" and "reminder24h" and "reminder3h"
    - frontend/components/settings/NotificationsSection.tsx contains data-testid="toggle-reminder72h"
    - frontend/__tests__/settings/GpaTargetSection.test.tsx contains no it.todo
    - frontend/__tests__/settings/NotificationsSection.test.tsx contains no it.todo
    - All tests pass (npx vitest run __tests__/settings/GpaTargetSection.test.tsx __tests__/settings/NotificationsSection.test.tsx exits 0)
  </acceptance_criteria>
  <done>GpaTargetSection shows large target number + grade band badge, slider and numeric input stay synced, Save Target saves via useUpdateProfile. NotificationsSection has 6 toggle controls (3 deadline + GPA risk + digest freq + email) with localStorage persistence. All tests pass.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run __tests__/settings/TokensSection.test.tsx __tests__/settings/GpaTargetSection.test.tsx __tests__/settings/NotificationsSection.test.tsx --reporter=verbose` -- all tests pass
2. `cd frontend && npx tsc --noEmit` -- TypeScript compiles cleanly
3. Navigate to http://localhost:3001/en/settings -- Tokens section shows Canvas/Ed with status badges, GPA section shows slider, Notifications section shows toggles
</verification>

<success_criteria>
- TokensSection shows Canvas and Ed platform rows with Active/Invalid/Not Configured badges
- Token visibility toggles between password and text input types
- GPA slider and numeric input sync bidirectionally without infinite loop
- Grade band badge updates in real-time as slider moves
- Notification toggles persist across page reload via localStorage
- All 3 test files pass with real assertions (no it.todo remaining)
</success_criteria>

<output>
After completion, create `.planning/phases/12-settings-page/12-02-SUMMARY.md`
</output>
