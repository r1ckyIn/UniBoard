---
phase: 12-settings-page
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/components/settings/CourseLinkingSection.tsx
  - frontend/components/settings/ProfileSection.tsx
  - frontend/components/settings/DangerZoneSection.tsx
  - frontend/components/settings/SettingsAccountCard.tsx
  - frontend/components/settings/SettingsSyncCard.tsx
  - frontend/components/settings/SettingsQuickActions.tsx
  - frontend/components/settings/SettingsAboutCard.tsx
  - frontend/components/settings/SettingsPage.tsx
  - frontend/__tests__/settings/ProfileSection.test.tsx
  - frontend/__tests__/settings/DangerZoneSection.test.tsx
autonomous: true
requirements:
  - UI-06

must_haves:
  truths:
    - "Course linking table shows 5 real courses with code, name, semester, sources, and Auto-linked status"
    - "Profile section shows editable display name and readonly email"
    - "Password fields are disabled with 'coming soon' label"
    - "Disconnect tokens button shows confirmation dialog before calling useDeleteToken"
    - "Delete account button requires typing 'DELETE' before action is enabled"
    - "Right panel shows Account Card with avatar, name, email, stats (Courses/WAM/Band)"
    - "Right panel shows Sync Status Card with Canvas, Ed, Unit Outlines status items"
    - "Right panel shows Quick Actions Card with Force Sync, Export, Help, Feedback links"
    - "Right panel shows About Card with version, member since, data stored, footer links"
  artifacts:
    - path: "frontend/components/settings/CourseLinkingSection.tsx"
      provides: "Read-only course table with 5 courses"
      exports: ["default"]
    - path: "frontend/components/settings/ProfileSection.tsx"
      provides: "Profile form with display name edit, email readonly, password disabled"
      exports: ["default"]
    - path: "frontend/components/settings/DangerZoneSection.tsx"
      provides: "Danger zone with disconnect tokens and delete account dialogs"
      exports: ["default"]
    - path: "frontend/components/settings/SettingsAccountCard.tsx"
      provides: "Right panel account card with avatar, stats row"
      exports: ["default"]
    - path: "frontend/components/settings/SettingsSyncCard.tsx"
      provides: "Right panel sync status for Canvas, Ed, Unit Outlines"
      exports: ["default"]
    - path: "frontend/components/settings/SettingsQuickActions.tsx"
      provides: "Right panel quick action buttons"
      exports: ["default"]
    - path: "frontend/components/settings/SettingsAboutCard.tsx"
      provides: "Right panel about card with version info"
      exports: ["default"]
  key_links:
    - from: "frontend/components/settings/DangerZoneSection.tsx"
      to: "frontend/hooks/use-user.ts"
      via: "useDeleteToken and useDeleteAccount"
      pattern: "useDeleteToken|useDeleteAccount"
    - from: "frontend/components/settings/ProfileSection.tsx"
      to: "frontend/hooks/use-user.ts"
      via: "useUpdateProfile mutation"
      pattern: "useUpdateProfile"
    - from: "frontend/components/settings/SettingsQuickActions.tsx"
      to: "frontend/hooks/use-sync.ts"
      via: "useSyncTrigger for Force Full Sync"
      pattern: "useSyncTrigger"
    - from: "frontend/components/settings/SettingsQuickActions.tsx"
      to: "frontend/hooks/use-user.ts"
      via: "useExportData for Export My Data"
      pattern: "useExportData"
---

<objective>
Build the remaining 3 main content sections (Course Linking, Profile, Danger Zone) and all 4 right panel cards (Account, Sync Status, Quick Actions, About), then wire everything into the SettingsPage orchestrator.

Purpose: Complete the settings page by adding the readonly/form sections and the full right panel, making it the final M1 frontend page.
Output: All settings components implemented, wired into SettingsPage, right panel fully populated via portal.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-settings-page/12-CONTEXT.md
@.planning/phases/12-settings-page/12-RESEARCH.md
@.planning/phases/12-settings-page/12-01-SUMMARY.md
@prototype/settings.html

<interfaces>
<!-- Existing hooks and data shapes -->

From frontend/hooks/use-user.ts:
```typescript
export function useCurrentUser(): UseQueryResult<{ data: User }>;
// User.display_name: string ("Alex Chen")
// User.email: string ("student@sydney.edu.au")
// User.gpa_target: number | null (85.0)
// User.created_at: string ("2026-02-01T00:00:00Z")
// User.tokens.canvas.status: "active" | "invalid" | "not_configured"
// User.tokens.ed.status: "active" | "invalid" | "not_configured"

export function useUpdateProfile(): UseMutationResult;
// mutate({ display_name: "new name" })

export function useDeleteToken(): UseMutationResult;
// mutate({ platform: "canvas" }) or mutate({ platform: "ed" })

export function useDeleteAccount(): UseMutationResult;
// mutate(undefined) -- clears auth on success

export function useExportData(): UseQueryResult;
// { refetch: triggerExport } -- enabled: false, on-demand
```

From frontend/hooks/use-sync.ts:
```typescript
export function useSyncStatus(): UseQueryResult<{ data: SyncStatusResponse }>;
// platforms.canvas.status: "healthy" | "degraded" | "error"
// platforms.canvas.last_success: string ISO date
// platforms.ed same shape

export function useSyncTrigger(): UseMutationResult;
// mutate({ scope: "all" })
```

From frontend/lib/fixtures/courses.ts:
```typescript
// 5 courses: COMP2017, COMP3221, STAT2011, EDGU1003, MATH2021
// Each has: code, name, semester ("2026-S1"), ed_course_id (undefined for EDGU1003)
```

From frontend/lib/utils/grade-band.ts:
```typescript
export function getGradeBand(mark: number | null | undefined): string;
```

From frontend/components/setup/platform-config.ts:
```typescript
export const PLATFORM_CONFIG: {
  canvas: { icon: LayoutDashboard, iconBg: "rgba(217,60,50,.08)", iconColor: "#d93c32" };
  ed: { icon: MessageCircle, iconBg: "rgba(106,155,204,.11)", iconColor: "#6a9bcc" };
};
```

Course linking data (hardcoded from prototype, not from API):
```
COMP2017 - Systems Programming - 2026 S1 - Canvas + Ed - Auto-linked
COMP3221 - Distributed Systems - 2026 S1 - Canvas + Ed - Auto-linked
STAT2011 - Probability & Estimation - 2026 S1 - Canvas + Ed - Auto-linked
EDGU1003 - Diet & Nutrition - 2026 S1 - Canvas only - Auto-linked
MATH2021 - Vector Calculus - 2026 S1 - Canvas + Ed - Auto-linked
```

Right panel portal pattern (established):
```typescript
const [portalTarget, setPortalTarget] = useState<HTMLElement | null>(null);
useEffect(() => { setPortalTarget(document.getElementById("right-panel-slot")); }, []);
// then: {portalTarget && createPortal(<>...cards...</>, portalTarget)}
```

Native dialog pattern (from Phase 05):
```typescript
const dialogRef = useRef<HTMLDialogElement>(null);
// Open: dialogRef.current?.showModal()
// Close: dialogRef.current?.close()
// In jsdom: mock showModal/close
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CourseLinkingSection, ProfileSection, DangerZoneSection with tests</name>
  <files>
    frontend/components/settings/CourseLinkingSection.tsx,
    frontend/components/settings/ProfileSection.tsx,
    frontend/components/settings/DangerZoneSection.tsx,
    frontend/__tests__/settings/ProfileSection.test.tsx,
    frontend/__tests__/settings/DangerZoneSection.test.tsx
  </files>
  <read_first>
    frontend/components/settings/SettingsPage.tsx,
    frontend/hooks/use-user.ts,
    frontend/lib/fixtures/courses.ts,
    frontend/lib/fixtures/users.ts,
    prototype/settings.html,
    frontend/__tests__/settings/ProfileSection.test.tsx,
    frontend/__tests__/settings/DangerZoneSection.test.tsx
  </read_first>
  <behavior>
    ProfileSection:
    - Test 1: renders display name input with user's current name
    - Test 2: renders email input as readonly with user's email
    - Test 3: renders password section with disabled inputs and "coming soon" hint
    - Test 4: Save Changes button calls useUpdateProfile.mutate with new display name
    - Test 5: renders account creation date

    DangerZoneSection:
    - Test 1: renders section title with red color and alert-triangle icon
    - Test 2: Disconnect button opens confirmation dialog
    - Test 3: Delete Account button opens dialog with text input
    - Test 4: Delete Account confirm button is disabled until user types "DELETE"
    - Test 5: Typing "DELETE" enables the confirm button
  </behavior>
  <action>
1. Create CourseLinkingSection.tsx (no tests needed -- read-only static table):
   - No props needed (data is hardcoded matching prototype)
   - Section title: Link2 icon (18px, blue #6a9bcc) + "Course Linking" from i18n
   - Section desc: "Your synced courses and data sources" from i18n
   - HTML table with 4 columns: Course, Semester, Sources, Status
   - Table header: 0.68rem, font-weight 600, uppercase, letter-spacing 0.04em, text-3 color, bottom border 1.5px
   - 5 rows hardcoded:
     ```
     COMP2017 | Systems Programming    | 2026 S1 | Canvas + Ed  | Auto-linked
     COMP3221 | Distributed Systems    | 2026 S1 | Canvas + Ed  | Auto-linked
     STAT2011 | Probability & Estimation | 2026 S1 | Canvas + Ed  | Auto-linked
     EDGU1003 | Diet & Nutrition       | 2026 S1 | Canvas only  | Auto-linked
     MATH2021 | Vector Calculus        | 2026 S1 | Canvas + Ed  | Auto-linked
     ```
   - Course cell: code in font-weight 600 text-1 + name in 0.72rem text-3 with margin-left 6px
   - Status cell: "Auto-linked" badge in green-soft bg + green text, 0.64rem, font-weight 600, 2px 8px padding, 4px border-radius
   - Footer italic note: "Manual linking for unmatched courses coming soon." (0.72rem text-3, italic)

2. Replace it.todo() stubs in ProfileSection.test.tsx:
   - Mock useUpdateProfile returning { mutate: vi.fn(), isPending: false }
   - Render with props: `{ user: mockUser }`
   - Test display name: find input with value "Alex Chen", type new text
   - Test email readonly: find email input, verify it has readOnly attribute and value "student@sydney.edu.au"
   - Test password disabled: find password inputs, verify disabled attribute and "coming soon" hint text
   - Test save: click "Save Changes", verify mutate called with { display_name: expectedNewName }
   - Test creation date: find text containing "Account created" or the formatted date

3. Create ProfileSection.tsx:
   - Props: `{ user: User }`
   - State: displayName (initialized from user.display_name)
   - Section title: User icon (18px, text-2) + "Profile" from i18n
   - Section desc from i18n
   - Profile form (flex-col gap-4):
     a. Display Name: label (0.72rem 600 text-3) + input (form-input-sm styling: 9px 12px padding, 1.5px border card-border, radius-sm, cream bg, focus:border-orange focus:shadow-orange-soft)
     b. Email: label + readonly input (bg card-bg-hover, text-3 color, cursor-default) + hint "Email cannot be changed"
     c. Password (disabled group, opacity 0.45, pointer-events-none): label + 2 disabled inputs (current + new password) + hint "Password change coming soon"
     d. Footer: flex between, "Save Changes" button (btn-sm btn-orange) + account creation date text (0.72rem text-3) formatted using `format(new Date(user.created_at), "d MMM yyyy")`
   - Save onClick: updateProfile.mutate({ display_name: displayName })

4. Replace it.todo() stubs in DangerZoneSection.test.tsx:
   - Mock useDeleteToken and useDeleteAccount
   - Mock HTMLDialogElement.prototype.showModal = vi.fn() and .close = vi.fn()
   - Test red title: check section title element has red color styling and contains "Danger Zone"
   - Test disconnect dialog: click Disconnect button, verify showModal called (or dialog becomes visible), find confirm button
   - Test delete dialog: click "Delete Account" button, verify dialog opens, find text input and confirm button
   - Test delete disabled: verify confirm button is disabled when input is empty
   - Test delete enabled: type "DELETE" in input, verify confirm button becomes enabled

5. Create DangerZoneSection.tsx:
   - Props: `{ user: User }` (or no props, uses hooks directly)
   - Import useDeleteToken, useDeleteAccount from hooks/use-user
   - Section title: AlertTriangle icon (18px, red #cc4455) + "Danger Zone" text in red color
   - Two danger rows (flex between, 14px vertical padding, bottom border):
     a. **Disconnect all tokens**: info (title + desc) + "Disconnect" button (1.5px red border, red text, transparent bg, hover: red-soft bg)
        - On click: open disconnect dialog (native `<dialog>`)
        - Dialog content: title, description, Cancel + "Yes, Disconnect" (red) buttons
        - Confirm: call deleteToken.mutate({ platform: "canvas" }) then deleteToken.mutate({ platform: "ed" }), close dialog
     b. **Delete account**: info (title + desc) + "Delete Account" button (same red outlined style)
        - On click: open delete dialog (native `<dialog>`)
        - Dialog content: title, description, text input with placeholder "Type DELETE to confirm", Cancel + "Permanently Delete" (red, disabled until input === "DELETE") buttons
        - State: deleteConfirmText (string)
        - Confirm button disabled unless deleteConfirmText === "DELETE"
        - Confirm: call deleteAccount.mutate(undefined), close dialog
   - Dialog styling: padding 24px, max-width 400px, border-radius 14px, bg cream, ::backdrop bg rgba(0,0,0,0.3)
   - data-testid="disconnect-dialog" and data-testid="delete-dialog" on dialogs
   - data-testid="disconnect-confirm" and data-testid="delete-confirm" on confirm buttons
   - data-testid="delete-input" on the DELETE text input

6. Update SettingsPage.tsx: replace sec-courses, sec-profile, sec-danger placeholders with real components. Import all three. Pass user prop where needed.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx vitest run __tests__/settings/ProfileSection.test.tsx __tests__/settings/DangerZoneSection.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/settings/CourseLinkingSection.tsx contains "COMP2017" and "COMP3221" and "STAT2011" and "EDGU1003" and "MATH2021"
    - frontend/components/settings/CourseLinkingSection.tsx contains "Auto-linked" text
    - frontend/components/settings/CourseLinkingSection.tsx contains "Canvas only" for EDGU1003 row
    - frontend/components/settings/ProfileSection.tsx contains "useUpdateProfile" import
    - frontend/components/settings/ProfileSection.tsx contains "readOnly" attribute on email input
    - frontend/components/settings/ProfileSection.tsx contains "pointer-events-none" or "disabled" for password section
    - frontend/components/settings/DangerZoneSection.tsx contains "useDeleteToken" and "useDeleteAccount" imports
    - frontend/components/settings/DangerZoneSection.tsx contains '<dialog' element
    - frontend/components/settings/DangerZoneSection.tsx contains data-testid="delete-input"
    - frontend/components/settings/DangerZoneSection.tsx contains "DELETE" string comparison for confirm gate
    - frontend/__tests__/settings/ProfileSection.test.tsx contains no it.todo
    - frontend/__tests__/settings/DangerZoneSection.test.tsx contains no it.todo
    - All tests pass
  </acceptance_criteria>
  <done>CourseLinkingSection shows 5-course table with Auto-linked badges. ProfileSection has editable name, readonly email, disabled password, save button. DangerZoneSection has disconnect dialog and delete account with DELETE confirmation gate. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Right panel cards (Account, SyncStatus, QuickActions, About) and final SettingsPage wiring</name>
  <files>
    frontend/components/settings/SettingsAccountCard.tsx,
    frontend/components/settings/SettingsSyncCard.tsx,
    frontend/components/settings/SettingsQuickActions.tsx,
    frontend/components/settings/SettingsAboutCard.tsx,
    frontend/components/settings/SettingsPage.tsx
  </files>
  <read_first>
    frontend/components/settings/SettingsPage.tsx,
    frontend/hooks/use-user.ts,
    frontend/hooks/use-sync.ts,
    frontend/lib/utils/grade-band.ts,
    frontend/lib/fixtures/users.ts,
    frontend/lib/fixtures/sync.ts,
    frontend/lib/fixtures/courses.ts,
    frontend/components/ui/RoughCard.tsx,
    prototype/settings.html
  </read_first>
  <action>
1. Create SettingsAccountCard.tsx:
   - Props: `{ user: User; courseCount: number; wam: number | null }`
   - RoughCard wrapper with centered content (text-align center, padding 24px 20px)
   - Avatar: 56px rounded-[14px] div, gradient bg (linear-gradient 135deg, #d97757, #e8956e), white text, font-weight 700, 20px font, Source Serif 4 font, initials from display_name (first char of first name + first char of last name, uppercased)
   - Name: Source Serif 4, 1.1rem, font-weight 600
   - Email: 0.74rem, text-3 color
   - Stats row (flex center, gap 16px, border-top 1px divider, padding-top 12px):
     - Courses: count value in orange (#d97757), "COURSES" label
     - WAM: value in green (#788c5d), "WAM" label
     - Band: getGradeBand(wam) in blue (#6a9bcc), "BAND" label
   - Stat values: Source Serif 4, 1.1rem, 700
   - Stat labels: 0.64rem, text-3, uppercase, letter-spacing 0.04em

2. Create SettingsSyncCard.tsx:
   - Props: `{ syncData?: SyncStatusResponse }`
   - RoughCard wrapper
   - Title: Activity icon (16px, orange) + "Sync Status" text (0.82rem, 600)
   - 3 sync items (each: flex items-center gap-[10px], padding 10px 0, border-bottom divider except last):
     a. Canvas LMS: icon (28px rounded, canvas red bg + LayoutDashboard icon), name, time text "12 min ago . 5 courses", OK badge (green-soft bg, green text)
     b. Ed Discussion: icon (blue bg + MessageCircle), name, time "12 min ago . 4 courses", OK badge
     c. Unit Outlines: icon (amber-soft bg #b08968, FileText), name, time "3 days ago . 5 parsed", OK badge
   - Badge styling: 0.6rem, 600, 2px 7px padding, 4px border-radius
   - Use hardcoded mock data for now (sync status fixture provides structure but times are mock)

3. Create SettingsQuickActions.tsx:
   - Props: none (uses hooks internally)
   - Import useSyncTrigger, useExportData
   - RoughCard wrapper
   - Title: Zap icon (16px, orange) + "Quick Actions" text
   - 4 action buttons (each: flex items-center gap-[10px], 10px 14px padding, radius-sm, full-width, text-left, border-bottom divider except last, hover: card-bg-hover):
     a. **Force Full Sync**: orange-soft bg icon (RefreshCw 14px), text, onClick: syncTrigger.mutate({ scope: "all" })
     b. **Export My Data**: blue-soft bg icon (Download 14px), text, onClick: triggerExport()
     c. **Help & Support**: green-soft bg icon (HelpCircle 14px), text, onClick: window.open("#", "_blank") (placeholder URL)
     d. **Send Feedback**: purple-soft bg icon (MessageCircle 14px, purple #9b7bb8), text, onClick: window.open("https://github.com/r1ckyIn/UniBoard/issues", "_blank")
   - Action icon wrapper: 28px, rounded-[7px], grid place-items-center
   - Text: 0.82rem, 500, text-1

4. Create SettingsAboutCard.tsx:
   - Props: `{ user: User }`
   - RoughCard wrapper
   - Title: Info icon (16px, orange) + "About" text
   - Info rows (flex justify-between, 0.76rem, text-3, gap-[6px] vertical):
     - Version: "1.0.0-beta" (text-2, 600)
     - Member since: formatted user.created_at with `format(new Date(user.created_at), "d MMM yyyy")` (text-2, 600)
     - Data stored: "2.4 MB" (hardcoded mock, text-2, 600)
   - Footer (margin-top 6px, padding-top 8px, border-top divider, text-center):
     - Three links: Terms (href="#"), Privacy (href="#"), GitHub (href="https://github.com/r1ckyIn/UniBoard")
     - Links: orange color, 600, 0.72rem, no underline
     - Separated by " . " (divider colored dot)

5. Final SettingsPage.tsx wiring:
   - Replace ALL remaining placeholder content with real components
   - Import all section components: TokensSection, GpaTargetSection, NotificationsSection, CourseLinkingSection, ProfileSection, DangerZoneSection
   - Import all right panel cards: SettingsAccountCard, SettingsSyncCard, SettingsQuickActions, SettingsAboutCard
   - Import useCourses (or use hardcoded count 5) for course count in AccountCard
   - Import useGpaReport or compute WAM from user data for AccountCard stats
   - Pass correct props to each:
     - TokensSection: user={userData.data}
     - GpaTargetSection: user={userData.data}
     - NotificationsSection: (no props)
     - CourseLinkingSection: (no props)
     - ProfileSection: user={userData.data}
     - DangerZoneSection: (no props, or user if needed)
   - Portal right panel: SettingsAccountCard (user, courseCount=5, wam=user.gpa_target or 77.5 mock) + SettingsSyncCard() + SettingsQuickActions() + SettingsAboutCard(user)
   - Remove all placeholder text from the skeleton
   - Ensure loading state: show skeleton or loading indicator while userData is loading
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx tsc --noEmit 2>&1 | tail -20 && npx vitest run __tests__/settings/ --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/settings/SettingsAccountCard.tsx contains "getGradeBand" import
    - frontend/components/settings/SettingsAccountCard.tsx contains "linear-gradient" for avatar
    - frontend/components/settings/SettingsSyncCard.tsx contains "Canvas LMS" and "Ed Discussion" and "Unit Outlines"
    - frontend/components/settings/SettingsQuickActions.tsx contains "useSyncTrigger" import
    - frontend/components/settings/SettingsQuickActions.tsx contains "useExportData" import
    - frontend/components/settings/SettingsQuickActions.tsx contains "https://github.com/r1ckyIn/UniBoard/issues" URL
    - frontend/components/settings/SettingsAboutCard.tsx contains "1.0.0-beta" version string
    - frontend/components/settings/SettingsAboutCard.tsx contains "https://github.com/r1ckyIn/UniBoard" URL
    - frontend/components/settings/SettingsPage.tsx contains imports for all 6 section components (TokensSection, GpaTargetSection, NotificationsSection, CourseLinkingSection, ProfileSection, DangerZoneSection)
    - frontend/components/settings/SettingsPage.tsx contains imports for all 4 right panel cards (SettingsAccountCard, SettingsSyncCard, SettingsQuickActions, SettingsAboutCard)
    - frontend/components/settings/SettingsPage.tsx does NOT contain "placeholder" or "coming in Plan" text
    - TypeScript compilation passes (npx tsc --noEmit exits 0)
    - All settings tests pass (npx vitest run __tests__/settings/ exits 0)
  </acceptance_criteria>
  <done>All 4 right panel cards implemented matching prototype. SettingsPage fully wired with all 6 section components + 4 portal cards. No placeholders remain. TypeScript compiles. All tests pass.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- TypeScript compiles cleanly
2. `cd frontend && npx vitest run __tests__/settings/ --reporter=verbose` -- all settings tests pass
3. `cd frontend && npx vitest run` -- full test suite passes
4. Navigate to http://localhost:3001/en/settings -- complete settings page renders with all sections and right panel
</verification>

<success_criteria>
- Course linking table displays 5 real courses with correct sources (Canvas + Ed or Canvas only)
- Profile form has editable name, readonly email, disabled password with coming soon
- Danger zone has two red-outlined buttons with proper confirmation dialogs
- Delete account requires typing DELETE to enable confirm button
- Right panel shows all 4 cards: Account (avatar, stats), Sync Status (3 platforms), Quick Actions (4 buttons), About (version, links)
- Send Feedback links to https://github.com/r1ckyIn/UniBoard/issues
- All tests pass, TypeScript compiles, no placeholders remain
</success_criteria>

<output>
After completion, create `.planning/phases/12-settings-page/12-03-SUMMARY.md`
</output>
