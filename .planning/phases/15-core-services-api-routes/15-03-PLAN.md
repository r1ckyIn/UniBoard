---
phase: 15-core-services-api-routes
plan: 03
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - tests/integration/test_contract_alignment.py
  - tests/integration/test_courses_routes.py
  - tests/integration/test_deadline_routes.py
  - tests/integration/conftest.py
autonomous: true
requirements: [GPA-01, GPA-02, GPA-03, GPA-04, GPA-05, DL-01, INTEL-01, INTEL-05, FILE-01, FILE-02]

must_haves:
  truths:
    - "Integration tests verify every endpoint response shape matches types.gen.d.ts field names"
    - "GPA endpoints (GET /gpa, POST /gpa/predict, POST /gpa/path) return correct field names"
    - "Course endpoints (GET /courses, GET /courses/{id}, GET /courses/{id}/grades) return correct shapes"
    - "Deadline endpoints (GET /deadlines, GET /deadlines/upcoming) return status and days_remaining"
    - "Materials endpoint returns flat Material[] not nested object"
    - "Search endpoint returns flat SearchResult[] not wrapped object"
    - "Discussions endpoint returns Discussion[] with pagination"
  artifacts:
    - path: "tests/integration/test_contract_alignment.py"
      provides: "Contract shape validation tests for all Phase 15 endpoints"
      min_lines: 150
    - path: "tests/integration/test_courses_routes.py"
      provides: "Integration tests for GET /courses, /courses/{id}, /courses/{id}/grades, /courses/{id}/deadlines, /courses/{id}/outline"
      min_lines: 100
    - path: "tests/integration/test_deadline_routes.py"
      provides: "Integration tests for GET /deadlines, GET /deadlines/upcoming"
      min_lines: 50
  key_links:
    - from: "tests/integration/test_contract_alignment.py"
      to: "src/web/routes/gpa.py"
      via: "HTTP GET /api/v1/gpa"
      pattern: "client.get.*gpa"
    - from: "tests/integration/test_courses_routes.py"
      to: "src/web/routes/courses.py"
      via: "HTTP GET /api/v1/courses"
      pattern: "client.get.*courses"
---

<objective>
Integration tests validating all Phase 15 endpoints match OpenAPI contract shapes.

Purpose: Contract alignment is the highest-risk work in this phase. These tests serve as the automated verification that every endpoint returns the exact field names and types that the frontend expects (types.gen.d.ts). They catch field renames, missing fields, wrong nesting, and type mismatches before the frontend is connected.

Output: Integration test files that validate response shapes for all Phase 15 endpoints with seed data.
</objective>

<execution_context>
@/Users/qinyuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qinyuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-core-services-api-routes/15-CONTEXT.md
@.planning/phases/15-core-services-api-routes/15-RESEARCH.md

# Prior plan SUMMARYs needed for test patterns
@.planning/phases/15-core-services-api-routes/15-01-SUMMARY.md
@.planning/phases/15-core-services-api-routes/15-02-SUMMARY.md

<interfaces>
<!-- Test infrastructure already established -->

From tests/conftest.py:
  - Uses httpx.AsyncClient with ASGITransport for integration tests
  - pytest-asyncio for async test functions
  - Database session fixtures for test isolation

From tests/integration/test_gpa_routes.py (existing pattern):
  - Tests call endpoints via `client.get("/api/v1/gpa/...")`
  - Assertions check response.status_code and response.json() shape
  - Auth header: `{"Authorization": f"Bearer {test_token}"}`

Expected contract field names per endpoint (from types.gen.d.ts):

GET /gpa -> { data: { scale, current_wam, current_gpa_4, target_wam, gap, courses: [{course_id, code, name, credit_points, level_weight, current_mark, grade_letter, completed_weight}], last_sync_at }, meta: {...} }

POST /gpa/predict -> { data: { current_wam, predicted_wam, delta, per_course: [{course_id, code, current_mark, predicted_mark, applied_assumptions}] }, meta: {...} }

POST /gpa/path -> { data: { target_wam, current_wam, is_achievable, per_course: [{course_id, code, current_mark, minimum_remaining_avg, remaining_assessments, difficulty}] }, meta: {...} }

GET /courses -> { data: [{id, name, code, semester, credit_points, ...}], meta: {...} }

GET /courses/{id} -> { data: {id, name, code, ..., assessment_weights: [{name, weight, score, max_score, status, group_name, due_date?}], weight_source}, meta: {...} }

GET /courses/{id}/grades -> { data: [{id, assessment_name, score, max_score, weight, group_name, graded_at, submitted_at}], meta: {...} }

GET /deadlines -> { data: [{id, title, due_date, source, weight?, status, days_remaining, course_code, course_name, is_confirmed}], meta: {...} }

GET /deadlines/upcoming -> same shape as GET /deadlines

GET /courses/{id}/materials -> { data: [{id, title, source, source_type, items?, slide_count?, url?}], meta: {...} }

GET /courses/{id}/discussions -> { data: [{id, ed_thread_id, title, author, category, is_endorsed, is_staff_post, gpa_relevance_score, relevance_category, summary, created_at}], meta: {...}, pagination?: {...} }

GET /search?q=... -> { data: [{type, title, source, course_code, snippet, url, relevance}], meta: {...} }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test seed data fixtures and conftest additions</name>
  <files>tests/integration/conftest.py, tests/fixtures/seed_phase15.py</files>
  <read_first>
    tests/integration/conftest.py
    tests/conftest.py
    tests/fixtures/
    src/models/course.py
    src/models/grade.py
    src/models/deadline.py
    src/models/discussion.py
    src/models/module.py
    src/models/unit_outline.py
    src/models/user.py
  </read_first>
  <action>
    **Create tests/fixtures/seed_phase15.py** — factory functions to seed test data:

    ```python
    """Seed data factories for Phase 15 contract alignment tests."""
    import uuid
    from datetime import UTC, datetime, timedelta
    from sqlalchemy.ext.asyncio import AsyncSession
    from src.models.course import Course
    from src.models.grade import Grade
    from src.models.deadline import UnifiedDeadline
    from src.models.discussion import DiscussionThread
    from src.models.module import Module, ModuleItem
    from src.models.unit_outline import UnitOutline
    ```

    Implement these factory functions:

    a. `async def seed_test_course(session, user_id, code="COMP2017", name="Systems Programming", credit_points=6, semester="2026-S1") -> Course`
       - Creates and returns a Course with given params

    b. `async def seed_test_grades(session, course_id, count=3) -> list[Grade]`
       - Creates count grades: e.g., "Assignment 1" (score=85, max=100, weight=0.3, group="Assignments"), "Quiz 1" (score=70, max=100, weight=0.2, group="Quizzes"), "Midterm" (score=None, max=100, weight=0.5, group="Exams")
       - Includes both graded and ungraded items

    c. `async def seed_test_deadlines(session, course, count=3) -> list[UnifiedDeadline]`
       - Creates deadlines: one upcoming (due in 3 days), one due in 10 days, one past (due 2 days ago)
       - source="canvas", is_confirmed=True, weight=0.2

    d. `async def seed_test_discussions(session, course_id, count=5) -> list[DiscussionThread]`
       - Mix: 2 endorsed, 1 staff, 2 community posts
       - Includes author, category, content_summary fields
       - Sets gpa_relevance for endorsed ones

    e. `async def seed_test_modules(session, course_id, count=2) -> list[Module]`
       - Creates modules with items (ModuleItem children)

    f. `async def seed_test_outline(session, course_id) -> UnitOutline`
       - Creates a UnitOutline with assessment_weights JSON, learning_outcomes list

    g. `async def seed_full_phase15_data(session, user_id) -> dict`
       - Seeds one complete course with grades, deadlines, discussions, modules, outline
       - Returns `{"course": course, "grades": grades, "deadlines": deadlines, ...}`

    **In tests/integration/conftest.py:**

    Add a `phase15_seed_data` fixture that calls `seed_full_phase15_data` and yields the result dict. Make sure it's compatible with the existing async session fixture.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && python -c "from tests.fixtures.seed_phase15 import seed_full_phase15_data, seed_test_course, seed_test_grades; print('Seed factories import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/seed_phase15.py exists with `seed_full_phase15_data` function
    - tests/fixtures/seed_phase15.py contains `seed_test_course`, `seed_test_grades`, `seed_test_deadlines`, `seed_test_discussions`, `seed_test_modules`, `seed_test_outline` functions
    - All factory functions accept `session` (AsyncSession) and return ORM objects
    - `seed_test_grades` creates mix of graded (score not None) and ungraded (score=None) items
    - `seed_test_deadlines` creates mix of upcoming, future, and past deadlines
    - `seed_test_discussions` creates mix of endorsed, staff, and community posts with author field populated
    - tests/integration/conftest.py references seed_phase15 fixtures
  </acceptance_criteria>
  <done>Test seed data factories exist and provide complete coverage data for all Phase 15 endpoint scenarios. Integration test conftest wired to seed fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contract alignment integration tests for all Phase 15 endpoints</name>
  <files>tests/integration/test_contract_alignment.py, tests/integration/test_courses_routes.py, tests/integration/test_deadline_routes.py</files>
  <read_first>
    tests/integration/test_gpa_routes.py
    tests/integration/conftest.py
    tests/conftest.py
    src/web/routes/gpa.py
    src/web/routes/courses.py
    src/web/routes/deadlines.py
    src/web/routes/materials.py
    src/web/routes/intelligence.py
    src/schemas/gpa.py
    src/schemas/course.py
    src/schemas/deadline.py
    src/schemas/materials.py
    src/schemas/intelligence.py
    frontend/lib/api/types.gen.d.ts
  </read_first>
  <behavior>
    - test_gpa_report_shape: GET /gpa returns 200, data has scale/current_wam/current_gpa_4/target_wam/gap/courses/last_sync_at, each course has course_id/code/name/credit_points/level_weight/current_mark/grade_letter/completed_weight
    - test_gpa_predict_shape: POST /gpa/predict returns 200, data has current_wam/predicted_wam/delta/per_course, each per_course has course_id/code/current_mark/predicted_mark/applied_assumptions
    - test_gpa_path_shape: POST /gpa/path returns 200, data has target_wam/current_wam/is_achievable/per_course, each per_course has difficulty/minimum_remaining_avg/remaining_assessments
    - test_list_courses_shape: GET /courses returns 200, data is array, each item has id/name/code/semester/credit_points
    - test_course_detail_shape: GET /courses/{id} returns 200, data has assessment_weights array and weight_source
    - test_course_grades_shape: GET /courses/{id}/grades returns 200, data is array, each has id/assessment_name/score/max_score/weight/group_name/graded_at/submitted_at
    - test_deadlines_shape: GET /deadlines returns 200, data is array, each has id/title/due_date/source/status/days_remaining/course_code/course_name/is_confirmed
    - test_upcoming_deadlines_shape: GET /deadlines/upcoming returns 200, data is array with same shape
    - test_materials_shape: GET /courses/{id}/materials returns 200, data is array (NOT object), each has id/title/source/source_type
    - test_discussions_shape: GET /courses/{id}/discussions returns 200, data is array, each has id/ed_thread_id/title/author/category/is_endorsed/is_staff_post/gpa_relevance_score/relevance_category/summary/created_at
    - test_search_shape: GET /search?q=test returns 200, data is array (NOT wrapped), each has type/title/source/course_code/snippet/url/relevance
    - test_no_field_name_regression: verify specific field names are NOT present (course_code should not appear in GPA course summary, content_summary should not appear in discussions, rank should not appear in search)
  </behavior>
  <action>
    **Create tests/integration/test_contract_alignment.py:**

    This is the PRIMARY verification test for Phase 15. Each test:
    1. Seeds data using phase15_seed_data fixture
    2. Calls the endpoint via httpx.AsyncClient
    3. Asserts status code 200
    4. Parses response JSON
    5. Checks `data` has exactly the expected top-level keys (using set comparison)
    6. For array responses, checks first item has expected keys
    7. Checks specific renamed fields do NOT appear (regression guard)

    Key test structure:
    ```python
    @pytest.mark.asyncio
    async def test_gpa_report_contract(client, phase15_seed_data):
        resp = await client.get("/api/v1/gpa")
        assert resp.status_code == 200
        body = resp.json()
        data = body["data"]
        # Top-level fields
        expected_keys = {"scale", "current_wam", "current_gpa_4", "target_wam", "gap", "courses", "last_sync_at"}
        assert expected_keys <= set(data.keys()), f"Missing keys: {expected_keys - set(data.keys())}"
        # Course field names
        if data["courses"]:
            course = data["courses"][0]
            course_keys = {"course_id", "code", "name", "credit_points", "level_weight", "current_mark", "grade_letter", "completed_weight"}
            assert course_keys <= set(course.keys()), f"Missing course keys: {course_keys - set(course.keys())}"
            # Regression: old field names must NOT be present
            assert "course_code" not in course, "Legacy field course_code still present"
            assert "course_name" not in course, "Legacy field course_name still present"
            assert "wam" not in course, "Legacy field wam still present"
            assert "grade_band" not in course, "Legacy field grade_band still present"
            assert "pct_assessed" not in course, "Legacy field pct_assessed still present"
    ```

    Repeat for all endpoints listed in behavior section.

    **Create tests/integration/test_courses_routes.py:**

    Tests for the new /courses router:
    - `test_list_courses_returns_array`
    - `test_course_detail_has_assessment_weights`
    - `test_course_grades_has_graded_at`
    - `test_course_deadlines_has_status`
    - `test_course_outline_has_learning_outcomes`
    - `test_course_not_found_returns_404`

    **Create tests/integration/test_deadline_routes.py:**

    Tests for deadline contract:
    - `test_deadlines_have_status_and_days_remaining`
    - `test_upcoming_deadlines_within_7_days`
    - `test_upcoming_excludes_past_deadlines`
    - `test_deadline_status_values` (only "upcoming", "submitted", "overdue", "completed")

    Each test function follows existing pattern from test_gpa_routes.py using async client fixture.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && python -m pytest tests/integration/test_contract_alignment.py tests/integration/test_courses_routes.py tests/integration/test_deadline_routes.py -x -q --timeout=120 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/test_contract_alignment.py exists with at least 10 test functions
    - tests/integration/test_contract_alignment.py contains `test_gpa_report_contract` checking for `current_wam` and `code` (not `course_code`)
    - tests/integration/test_contract_alignment.py contains `test_materials_contract` verifying data is a list (not object with `folders` key)
    - tests/integration/test_contract_alignment.py contains `test_search_contract` verifying data is a list (not object with `query` key)
    - tests/integration/test_contract_alignment.py contains `test_discussions_contract` checking for `author` and `summary` (not `content_summary`)
    - tests/integration/test_courses_routes.py exists with tests for all 5 /courses endpoints
    - tests/integration/test_deadline_routes.py exists with `test_upcoming_deadlines_within_7_days`
    - All integration tests pass (or skip cleanly if DB not available)
    - No test uses legacy field names (course_code in GPA context, content_summary, rank, etc.)
  </acceptance_criteria>
  <done>Integration tests validate all Phase 15 endpoints against OpenAPI contract shapes. Tests serve as automated regression guard — any future schema drift will be caught immediately. All 10 requirement IDs covered by at least one test.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/integration/test_contract_alignment.py -x -q --timeout=120` -- all contract tests pass
2. `python -m pytest tests/integration/test_courses_routes.py -x -q --timeout=120` -- courses routes tested
3. `python -m pytest tests/integration/test_deadline_routes.py -x -q --timeout=120` -- deadline routes tested
4. `python -m pytest tests/unit/ -x -q --timeout=60` -- existing unit tests still green
</verification>

<success_criteria>
- Contract alignment tests exist for ALL Phase 15 endpoints (GPA, Predict, Path, Courses, CourseDetail, Grades, Deadlines, Upcoming, Materials, Discussions, Search)
- Tests verify exact field names (not just status codes)
- Tests include regression guards against legacy field names
- All tests pass with seeded test data
- Existing unit tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/15-core-services-api-routes/15-03-SUMMARY.md`
</output>
