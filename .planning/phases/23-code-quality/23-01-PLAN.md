---
phase: 23-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sync/__init__.py
  - src/sync/_shared.py
  - src/sync/grades.py
  - src/sync/deadlines.py
  - src/sync/modules.py
  - src/sync/outlines.py
  - src/sync/discussions.py
  - src/sync/scheduled.py
  - src/sync/tasks.py
  - src/sync/engine.py
  - src/web/routes/sync.py
  - tests/unit/test_sync_tasks.py
  - tests/unit/test_sync_ed_discussions.py
  - tests/unit/test_deadline_reminders.py
  - tests/unit/test_token_health.py
  - tests/integration/test_sync_engine.py
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "sync/tasks.py no longer exists (deleted after split)"
    - "No single file in src/sync/ exceeds 300 lines"
    - "All existing sync tests pass with zero failures"
    - "engine.py imports all public functions from sync/__init__.py without changes to scheduler job registration"
  artifacts:
    - path: "src/sync/_shared.py"
      provides: "_get_sync_session_factory, _record_sync_history, _MAX_RETRIES, _sync_engine"
      contains: "_get_sync_session_factory"
    - path: "src/sync/grades.py"
      provides: "sync_all_grades, _sync_user_grades"
      contains: "async def sync_all_grades"
    - path: "src/sync/deadlines.py"
      provides: "sync_all_deadlines"
      contains: "async def sync_all_deadlines"
    - path: "src/sync/modules.py"
      provides: "sync_all_modules, _sync_canvas_modules, _sync_ed_lessons, _translate_user_courses"
      contains: "async def sync_all_modules"
    - path: "src/sync/outlines.py"
      provides: "sync_all_outlines"
      contains: "async def sync_all_outlines"
    - path: "src/sync/discussions.py"
      provides: "sync_ed_discussions, _evaluate_synced_threads"
      contains: "async def sync_ed_discussions"
    - path: "src/sync/scheduled.py"
      provides: "check_deadline_reminders, generate_daily_digests, check_token_health"
      contains: "async def check_deadline_reminders"
    - path: "src/sync/__init__.py"
      provides: "Re-exports all public functions for backward compat"
      contains: "from src.sync.grades import sync_all_grades"
  key_links:
    - from: "src/sync/engine.py"
      to: "src/sync/__init__.py"
      via: "import from package"
      pattern: "from src\\.sync\\.(tasks|__init__|grades|scheduled|deadlines|modules|outlines|discussions) import"
    - from: "tests/unit/test_sync_tasks.py"
      to: "src/sync/grades.py"
      via: "updated patch targets"
      pattern: "@patch\\(\"src\\.sync\\.grades\\."
---

<objective>
Split the 1146-line god module `src/sync/tasks.py` into 7 domain-specific modules, update all consumers (engine.py, route sync.py, 5 test files), and delete the original file.

Purpose: QUAL-01 requires no single sync file exceeds 300 lines. The split follows clear domain boundaries already visible at function level.
Output: 7 new domain modules under `src/sync/`, updated `__init__.py` re-exports, all 421+ tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-code-quality/23-RESEARCH.md

<interfaces>
<!-- Function-to-module mapping (source: AST analysis of tasks.py) -->

_shared.py (shared infrastructure, ~50 lines):
  - _MAX_RETRIES = 3 (constant)
  - _sync_engine: AsyncEngine | None (module-level singleton)
  - _get_sync_session_factory() -> async_sessionmaker[AsyncSession]  (L41-50)
  - _record_sync_history(session_factory, user_id, domain, status) (L836-860)

grades.py (~180 lines):
  - _sync_user_grades(user, canvas_token, session) -> int  (L53-139)
  - sync_all_grades() -> None  (L142-231)

deadlines.py (~170 lines):
  - sync_all_deadlines() -> None  (L234-401)

modules.py (~270 lines):
  - sync_all_modules() -> None  (L404-490)
  - _translate_user_courses(user, courses, session) (L493-522)
  - _sync_canvas_modules(user, canvas_token, courses, session) (L525-592)
  - _sync_ed_lessons(user, ed_token, courses, session) (L756-833)

outlines.py (~105 lines):
  - sync_all_outlines() -> None  (L1042-1146)

discussions.py (~175 lines):
  - sync_ed_discussions() -> None  (L863-1009)
  - _evaluate_synced_threads(thread_ids, session, course) (L1012-1039)

scheduled.py (~155 lines):
  - check_deadline_reminders() -> None  (L595-661)
  - generate_daily_digests() -> None  (L664-699)
  - check_token_health() -> None  (L702-753)

<!-- Consumer import patterns that MUST be updated -->

engine.py currently imports from src.sync.tasks:
  sync_all_grades, sync_all_deadlines, sync_all_modules,
  sync_all_outlines, sync_ed_discussions,
  check_deadline_reminders, generate_daily_digests, check_token_health

web/routes/sync.py currently imports from src.sync.tasks:
  sync_all_grades, sync_all_deadlines, sync_all_modules, sync_all_outlines

Test files @patch targets (src.sync.tasks.X -> src.sync.{module}.X):
  test_sync_tasks.py: patches _record_sync_history, get_encryption, _get_sync_session_factory, CanvasAdapter
  test_sync_ed_discussions.py: patches _record_sync_history, get_encryption, _get_sync_session_factory
  test_deadline_reminders.py: patches _get_sync_session_factory
  test_token_health.py: patches _get_sync_session_factory
  test_sync_engine.py: imports _sync_user_grades from src.sync.tasks
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract domain modules from tasks.py and wire __init__.py re-exports</name>
  <files>
    src/sync/_shared.py,
    src/sync/grades.py,
    src/sync/deadlines.py,
    src/sync/modules.py,
    src/sync/outlines.py,
    src/sync/discussions.py,
    src/sync/scheduled.py,
    src/sync/__init__.py,
    src/sync/tasks.py
  </files>
  <read_first>
    src/sync/tasks.py,
    src/sync/__init__.py,
    src/sync/engine.py,
    src/web/routes/sync.py
  </read_first>
  <action>
Split src/sync/tasks.py into 7 domain modules following these exact boundaries:

1. **src/sync/_shared.py** — Extract lines 1-50 (imports + _MAX_RETRIES + _sync_engine + _get_sync_session_factory) AND lines 836-860 (_record_sync_history). Include ALL imports used by _get_sync_session_factory and _record_sync_history: `from __future__ import annotations`, `uuid`, `datetime` (UTC, datetime), `structlog`, `sqlalchemy` (select, AsyncEngine, AsyncSession, async_sessionmaker, create_async_engine), `src.config.get_settings`, `src.models.sync_history.SyncHistory` (if used by _record_sync_history). Preserve the `global _sync_engine` pattern exactly.

2. **src/sync/grades.py** — Extract _sync_user_grades (L53-139) and sync_all_grades (L142-231). Add `from src.sync._shared import _MAX_RETRIES, _get_sync_session_factory, _record_sync_history` at top. Keep the lazy `from src.adapters.canvas import CanvasAdapter` import inside functions (do NOT move to top level). Include all model/schema imports used by these functions.

3. **src/sync/deadlines.py** — Extract sync_all_deadlines (L234-401). Same shared import pattern. Keep lazy adapter imports (CanvasAdapter, EdLessonsAdapter, EdDiscussionAdapter) inside the function body.

4. **src/sync/modules.py** — Extract sync_all_modules (L404-490), _translate_user_courses (L493-522), _sync_canvas_modules (L525-592), _sync_ed_lessons (L756-833). Same shared import pattern. Note: _sync_ed_lessons is NOT adjacent in the original file but belongs with modules logically.

5. **src/sync/outlines.py** — Extract sync_all_outlines (L1042-1146). Same shared import pattern.

6. **src/sync/discussions.py** — Extract sync_ed_discussions (L863-1009) and _evaluate_synced_threads (L1012-1039). Same shared import pattern.

7. **src/sync/scheduled.py** — Extract check_deadline_reminders (L595-661), generate_daily_digests (L664-699), check_token_health (L702-753). Same shared import pattern.

For each module:
- Copy function bodies EXACTLY (no logic changes)
- Include ONLY the imports each module actually uses (inspect each function's references)
- Preserve lazy imports inside function bodies (e.g., `from src.adapters.canvas import CanvasAdapter`)
- Add `logger = structlog.get_logger()` if the module uses logger

After creating all 7 modules, update **src/sync/__init__.py** to re-export all public functions:
```python
"""Background sync engine package."""

from src.sync.deadlines import sync_all_deadlines
from src.sync.discussions import sync_ed_discussions
from src.sync.grades import sync_all_grades
from src.sync.modules import sync_all_modules
from src.sync.outlines import sync_all_outlines
from src.sync.scheduled import (
    check_deadline_reminders,
    check_token_health,
    generate_daily_digests,
)

__all__ = [
    "check_deadline_reminders",
    "check_token_health",
    "generate_daily_digests",
    "sync_all_deadlines",
    "sync_all_grades",
    "sync_all_modules",
    "sync_all_outlines",
    "sync_ed_discussions",
]
```

Then DELETE src/sync/tasks.py entirely.

CRITICAL: _shared.py must have ZERO imports from sibling domain modules. All dependency flows one direction: _shared -> domain modules.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && python -c "from src.sync import sync_all_grades, sync_all_deadlines, sync_all_modules, sync_all_outlines, sync_ed_discussions, check_deadline_reminders, generate_daily_digests, check_token_health; print('All imports OK')" && test ! -f src/sync/tasks.py && echo "tasks.py deleted" && find src/sync -name '*.py' ! -name '__pycache__' -exec wc -l {} + | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - src/sync/tasks.py does not exist (deleted)
    - src/sync/_shared.py contains `def _get_sync_session_factory`
    - src/sync/_shared.py contains `async def _record_sync_history`
    - src/sync/grades.py contains `async def sync_all_grades`
    - src/sync/deadlines.py contains `async def sync_all_deadlines`
    - src/sync/modules.py contains `async def sync_all_modules`
    - src/sync/modules.py contains `async def _sync_ed_lessons`
    - src/sync/outlines.py contains `async def sync_all_outlines`
    - src/sync/discussions.py contains `async def sync_ed_discussions`
    - src/sync/scheduled.py contains `async def check_deadline_reminders`
    - src/sync/scheduled.py contains `async def check_token_health`
    - src/sync/__init__.py contains `from src.sync.grades import sync_all_grades`
    - No file in src/sync/ exceeds 300 lines (verified by wc -l)
    - `python -c "from src.sync import sync_all_grades"` exits 0
  </acceptance_criteria>
  <done>All 7 domain modules created, __init__.py re-exports all public functions, tasks.py deleted, no file exceeds 300 lines</done>
</task>

<task type="auto">
  <name>Task 2: Update all consumer imports and test patch targets</name>
  <files>
    src/sync/engine.py,
    src/web/routes/sync.py,
    tests/unit/test_sync_tasks.py,
    tests/unit/test_sync_ed_discussions.py,
    tests/unit/test_deadline_reminders.py,
    tests/unit/test_token_health.py,
    tests/integration/test_sync_engine.py
  </files>
  <read_first>
    src/sync/engine.py,
    src/web/routes/sync.py,
    tests/unit/test_sync_tasks.py,
    tests/unit/test_sync_ed_discussions.py,
    tests/unit/test_deadline_reminders.py,
    tests/unit/test_token_health.py,
    tests/integration/test_sync_engine.py
  </read_first>
  <action>
Update all files that import from `src.sync.tasks` to use the new module paths. The exact mapping is:

**src/sync/engine.py** (L39-48): Change the import block from:
```python
from src.sync.tasks import (
    check_deadline_reminders, check_token_health, generate_daily_digests,
    sync_all_deadlines, sync_all_grades, sync_all_modules,
    sync_all_outlines, sync_ed_discussions,
)
```
to:
```python
from src.sync import (
    check_deadline_reminders, check_token_health, generate_daily_digests,
    sync_all_deadlines, sync_all_grades, sync_all_modules,
    sync_all_outlines, sync_ed_discussions,
)
```

**src/web/routes/sync.py** (L75-80): Change from:
```python
from src.sync.tasks import (
    sync_all_deadlines, sync_all_grades, sync_all_modules, sync_all_outlines,
)
```
to:
```python
from src.sync import (
    sync_all_deadlines, sync_all_grades, sync_all_modules, sync_all_outlines,
)
```

**tests/unit/test_sync_tasks.py**: Apply these patch target replacements throughout the entire file:
- `src.sync.tasks._record_sync_history` -> `src.sync.grades._record_sync_history` (for grade tests) OR `src.sync.deadlines._record_sync_history` etc. depending on which function the test exercises. Since _record_sync_history is imported from _shared into each module, patch it at the MODULE where it's called. For grade tests: patch `src.sync.grades._record_sync_history`. For module sync tests: patch `src.sync.modules._record_sync_history`. For deadline tests: patch `src.sync.deadlines._record_sync_history`. For outline tests: patch `src.sync.outlines._record_sync_history`.
- `src.sync.tasks.get_encryption` -> `src.sync.{domain}.get_encryption` (same module-level targeting)
- `src.sync.tasks._get_sync_session_factory` -> `src.sync.{domain}._get_sync_session_factory` (same pattern)
- Update the top-level import: `from src.sync.tasks import (...)` -> import from specific modules
- IMPORTANT: Read each test carefully to determine which domain module it exercises, then update patch targets accordingly.

**tests/unit/test_sync_ed_discussions.py**: All patches change from `src.sync.tasks.*` to `src.sync.discussions.*`. Update the function import from `from src.sync.tasks import sync_ed_discussions` to `from src.sync.discussions import sync_ed_discussions`, and same for `_evaluate_synced_threads`.

**tests/unit/test_deadline_reminders.py**: All patches change from `src.sync.tasks.*` to `src.sync.scheduled.*`. Update function import to `from src.sync.scheduled import check_deadline_reminders`.

**tests/unit/test_token_health.py**: All patches change from `src.sync.tasks.*` to `src.sync.scheduled.*`. Update function import to `from src.sync.scheduled import check_token_health`.

**tests/integration/test_sync_engine.py** (L142): Change `from src.sync.tasks import _sync_user_grades` to `from src.sync.grades import _sync_user_grades`.

After all changes, run the full test suite to confirm zero failures.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && python -m pytest tests/unit/test_sync_tasks.py tests/unit/test_sync_ed_discussions.py tests/unit/test_deadline_reminders.py tests/unit/test_token_health.py tests/integration/test_sync_engine.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - engine.py contains `from src.sync import` (not `from src.sync.tasks import`)
    - web/routes/sync.py contains `from src.sync import` (not `from src.sync.tasks import`)
    - test_sync_tasks.py contains no string `src.sync.tasks` in any @patch decorator
    - test_sync_ed_discussions.py contains `src.sync.discussions` in @patch decorators
    - test_deadline_reminders.py contains `src.sync.scheduled` in @patch decorators
    - test_token_health.py contains `src.sync.scheduled` in @patch decorators
    - test_sync_engine.py contains `from src.sync.grades import _sync_user_grades`
    - `python -m pytest tests/unit/test_sync_tasks.py -x -q` exits 0
    - `python -m pytest tests/unit/test_sync_ed_discussions.py -x -q` exits 0
    - `python -m pytest tests/unit/test_deadline_reminders.py -x -q` exits 0
    - `python -m pytest tests/unit/test_token_health.py -x -q` exits 0
  </acceptance_criteria>
  <done>All consumers and test files updated to new module paths, full test suite passes with zero failures</done>
</task>

</tasks>

<verification>
1. `test ! -f src/sync/tasks.py` — original god module deleted
2. `find src/sync -name '*.py' ! -name '__init__*' -exec wc -l {} +` — no file exceeds 300 lines
3. `python -m pytest tests/ -x -q` — full test suite passes
4. `grep -r 'src\.sync\.tasks' src/ tests/` — zero references remain (except possibly in __pycache__ which is fine)
</verification>

<success_criteria>
- sync/tasks.py (1146 lines) split into 7 modules, none exceeding 300 lines
- All 421+ existing tests pass
- engine.py and sync route work unchanged (import from sync package)
- No references to src.sync.tasks remain in source code
</success_criteria>

<output>
After completion, create `.planning/phases/23-code-quality/23-01-SUMMARY.md`
</output>
