---
phase: 24-build-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/deadline.py
  - src/schemas/intelligence.py
  - src/schemas/materials.py
  - src/schemas/skill.py
  - src/services/gpa.py
  - src/services/skill.py
  - src/services/tool_executor.py
  - src/services/ai_engine.py
  - src/services/qa.py
  - src/web/routes/gpa.py
  - tests/unit/test_contract_schemas.py
  - tests/unit/test_resilience.py
  - tests/unit/test_skill_service.py
  - tests/unit/test_sync_ed_discussions.py
  - tests/fixtures/seed_phase15.py
autonomous: true
requirements: [CRIT-02]

must_haves:
  truths:
    - "ruff check src/ tests/ reports zero violations"
    - "mypy --strict src/ reports zero errors"
  artifacts:
    - path: "src/schemas/skill.py"
      provides: "StrEnum migration for SkillStatus and SkillCategory"
      contains: "enum.StrEnum"
    - path: "src/services/skill.py"
      provides: "Clean type annotations, no stale type-ignores"
    - path: "src/services/tool_executor.py"
      provides: "Type-safe argument handling with None guards"
    - path: "src/services/qa.py"
      provides: "Correct SQLAlchemy result type annotations"
  key_links:
    - from: "src/schemas/skill.py"
      to: "src/services/skill.py"
      via: "SkillStatus/SkillCategory enum usage"
      pattern: "StrEnum"
---

<objective>
Fix all Python lint (ruff) and type-checking (mypy) errors to achieve zero violations.

Purpose: The codebase has 54 ruff errors and 20 mypy errors accumulated during rapid M2/M3/M4 development. Fixing them ensures code quality tools pass cleanly before CI/CD setup in Phase 26.
Output: Zero ruff violations, zero mypy errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-build-health/24-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all ruff violations (54 errors to zero)</name>
  <files>
    src/schemas/deadline.py, src/schemas/intelligence.py, src/schemas/materials.py,
    src/schemas/skill.py, src/services/gpa.py, src/web/routes/gpa.py,
    tests/unit/test_contract_schemas.py, tests/unit/test_resilience.py,
    tests/unit/test_skill_service.py, tests/unit/test_sync_ed_discussions.py,
    tests/fixtures/seed_phase15.py
  </files>
  <read_first>
    pyproject.toml (ruff config: line-length=100, select=E/F/I/UP/B/SIM, per-file-ignores for src/web/**)
    src/schemas/skill.py (UP042: SkillStatus and SkillCategory inherit from str+Enum)
  </read_first>
  <action>
    Step 1: Run `uv run python -m ruff check src/ tests/ --fix` to auto-resolve 25 safe-fixable errors (I001 import sorting, F401 unused imports, UP017 datetime.UTC, SIM117 combined with).

    Step 2: Fix the remaining ~29 manual errors:

    **F841 — Unused variables (10 errors):** For each occurrence, either:
    - Prefix with underscore `_var` if the assignment has a meaningful side effect
    - Remove the assignment entirely if the value is unused
    - Files: tests/unit/test_resilience.py, tests/unit/test_skill_service.py, tests/unit/test_sync_ed_discussions.py, tests/fixtures/seed_phase15.py, src/services/skill.py

    **E501 — Line too long (14 errors):** Wrap lines exceeding 100 chars using:
    - Multi-line function calls with trailing comma
    - String concatenation or parenthesized expressions
    - Multi-line imports with parentheses

    **UP042 — Replace str+Enum with StrEnum (2 errors in src/schemas/skill.py):**
    - Change `class SkillStatus(str, enum.Enum):` to `class SkillStatus(enum.StrEnum):`
    - Change `class SkillCategory(str, enum.Enum):` to `class SkillCategory(enum.StrEnum):`
    - Note: Python 3.12+ supports StrEnum natively

    **B017 — Broad assertRaises (1 error):** Narrow `assertRaises(Exception)` to the specific exception type being tested (e.g., `ValueError`, `TypeError`, `RuntimeError`). Read the test to determine which exception the code actually raises.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && uv run python -m ruff check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m ruff check src/ tests/` outputs "All checks passed!" or exits with code 0 and no error lines
    - `grep -r "class SkillStatus" src/schemas/skill.py` shows `StrEnum` not `str, enum.Enum`
    - `grep -r "class SkillCategory" src/schemas/skill.py` shows `StrEnum` not `str, enum.Enum`
    - No F401, F841, I001, E501, UP017, UP042, SIM117, or B017 violations remain
  </acceptance_criteria>
  <done>ruff check reports zero violations across all Python source and test files</done>
</task>

<task type="auto">
  <name>Task 2: Fix all mypy strict errors (20 errors to zero)</name>
  <files>
    src/services/skill.py, src/services/tool_executor.py, src/services/ai_engine.py,
    src/services/qa.py, src/web/routes/gpa.py
  </files>
  <read_first>
    src/services/skill.py (lines 210-420 for unused-ignore and var-annotated errors)
    src/services/tool_executor.py (lines 40-160 for call-overload, call-arg, arg-type, assignment errors)
    src/services/ai_engine.py (lines 310-335 for unused-ignore errors)
    src/services/qa.py (lines 180-190 for assignment and attr-defined errors)
    src/web/routes/gpa.py (line 196 for type-arg error)
    src/adapters/canvas.py (get_modules signature — to understand the call-arg error)
  </read_first>
  <action>
    Fix 20 mypy errors across 5 files:

    **src/services/skill.py (9 errors):**
    - Remove 7 stale `# type: ignore` comments at lines 216, 296, 297, 316, 317, 414, 415. These were added before prior fixes made them unnecessary.
    - Add type annotations for `trace_a` and `trace_b` at lines 265-266. These are the return values of a trace comparison — annotate as `list[Any]` or the specific trace type from the function signature.

    **src/services/tool_executor.py (5 errors):**
    - Line 44 (call-overload): `int(params.get("count", 20))` — `params.get()` returns `object`. Fix: `int(str(params.get("count", 20)))` or cast via intermediate variable with type annotation.
    - Line 86 (call-arg): `get_modules(include_items=True, ...)` — CanvasAdapter.get_modules does not accept `include_items` keyword. Read the actual signature in src/adapters/canvas.py and remove or rename the argument to match.
    - Line 87 (arg-type): `get_modules(course_id)` where course_id is `str | None`. Add a None guard: `if course_id is None: raise ValueError("course_id is required for get_modules")`
    - Line 121 (arg-type): `search_threads(query)` where query is `str | None`. Add None guard: `if query is None: raise ValueError("query is required for search_threads")`
    - Line 155 (assignment): Variable typed as `list[dict[str, Any]]` assigned a value of type `object`. Add explicit cast or re-annotate the variable.

    **src/services/ai_engine.py (3 errors):**
    - Remove 3 stale `# type: ignore` comments at lines 317, 324, 331.

    **src/services/qa.py (2 errors):**
    - Line 184 (assignment): Variable typed as `EmbeddingsObject` but assigned a SQLAlchemy `Result[tuple[ContentEmbedding]]`. These are different operations — read the code to understand whether the variable should be retyped or the assignment corrected.
    - Line 185 (attr-defined): `.scalars()` called on `EmbeddingsObject` which doesn't have it. This is likely the SQLAlchemy result from line 184 — fix the type annotation to match the actual SQLAlchemy Result type.

    **src/web/routes/gpa.py (1 error):**
    - Line 196 (type-arg): Bare `list` used without type parameter. Change to `list[SomeType]` — read the context to determine the element type (likely `list[WhatIfScenarioResponse]` or similar schema type).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard && uv run python -m mypy --strict src/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m mypy --strict src/` outputs "Success: no issues found" or exits with code 0
    - `grep -n "type: ignore" src/services/skill.py` returns zero lines (all stale ignores removed)
    - `grep -n "type: ignore" src/services/ai_engine.py` returns zero lines (all stale ignores removed)
    - No `[unused-ignore]`, `[arg-type]`, `[call-overload]`, `[call-arg]`, `[assignment]`, `[attr-defined]`, `[var-annotated]`, or `[type-arg]` errors remain
  </acceptance_criteria>
  <done>mypy --strict reports zero errors across all 103 Python source files</done>
</task>

</tasks>

<verification>
Both tools must pass with zero errors in sequence:
```bash
uv run python -m ruff check src/ tests/ && uv run python -m mypy --strict src/
```
</verification>

<success_criteria>
- ruff check reports 0 errors (down from 54)
- mypy --strict reports 0 errors (down from 20)
- No regressions: existing passing tests still pass (`uv run python -m pytest tests/unit/ -q --tb=short -x --ignore=tests/unit/test_gpa_service.py --ignore=tests/unit/test_notification_service.py --ignore=tests/unit/test_risk_alert_service.py --ignore=tests/unit/test_intelligence_service.py --ignore=tests/unit/test_digest_service.py`)
</success_criteria>

<output>
After completion, create `.planning/phases/24-build-health/24-01-SUMMARY.md`
</output>
