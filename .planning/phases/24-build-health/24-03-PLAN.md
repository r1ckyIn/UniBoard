---
phase: 24-build-health
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/__tests__/deadlines/DeadlineCard.test.tsx
  - frontend/__tests__/courses/CourseCard.test.tsx
  - frontend/components/course-detail/AiCourseChat.tsx
  - frontend/components/course-detail/QuickLinksPanel.tsx
  - frontend/components/course-detail/UnitReviewSection.tsx
  - frontend/components/deadlines/DeadlinesPage.tsx
  - frontend/components/digest/HighlightItem.tsx
  - frontend/components/predict/PredictPage.tsx
  - frontend/components/setup/GuideCard.tsx
  - frontend/components/timetable/TimetableGrid.tsx
  - frontend/components/timetable/TimetablePage.tsx
  - frontend/src/test/setup.ts
autonomous: true
requirements: [CRIT-02]

must_haves:
  truths:
    - "tsc --noEmit reports zero TypeScript errors"
    - "ESLint --max-warnings 0 reports zero warnings"
  artifacts:
    - path: "frontend/__tests__/deadlines/DeadlineCard.test.tsx"
      provides: "Test fixtures with required course_id field"
      contains: "course_id"
    - path: "frontend/components/predict/PredictPage.tsx"
      provides: "Stable useMemo for courses derived value"
      contains: "useMemo"
    - path: "frontend/components/timetable/TimetablePage.tsx"
      provides: "Stable useMemo for semesterWeeks, allSessions, allDeadlines"
      contains: "useMemo"
  key_links:
    - from: "frontend/__tests__/deadlines/DeadlineCard.test.tsx"
      to: "frontend/src/types/types.gen.d.ts"
      via: "DeadlineWithCourse type compliance"
      pattern: "course_id"
---

<objective>
Fix all frontend TypeScript and ESLint errors to achieve zero violations.

Purpose: 8 tsc errors in test files (missing type fields) and 23 ESLint warnings (10 unused vars + 13 exhaustive-deps) must be resolved for clean builds.
Output: Zero tsc errors, zero ESLint warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-build-health/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 8 TypeScript errors in test files</name>
  <files>
    frontend/__tests__/deadlines/DeadlineCard.test.tsx,
    frontend/__tests__/courses/CourseCard.test.tsx
  </files>
  <read_first>
    frontend/__tests__/deadlines/DeadlineCard.test.tsx (lines 90-210 — all 7 test fixture objects missing course_id)
    frontend/__tests__/courses/CourseCard.test.tsx (lines 65-80 — beforeEach usage without type import)
    frontend/src/types/types.gen.d.ts (search for DeadlineWithCourse or course_id to find the required type shape)
  </read_first>
  <action>
    **DeadlineCard.test.tsx (7 TS2322 errors — all same root cause):**
    The `DeadlineWithCourse` type requires a `course_id: string` field, but all 7 test fixture objects are missing it. Add `course_id: "test-course-uuid-1"` to each test fixture object. The fixtures are at approximately lines 104, 122, 139, 154, 166, 180, 197. Each fixture looks like:
    ```typescript
    const deadline = {
      id: "1", title: "...", due_date: "...",
      // ADD THIS LINE:
      course_id: "test-course-uuid-1",
      course_code: "INFO1110", course_name: "...",
      ...
    };
    ```

    **CourseCard.test.tsx (1 TS2304 error — `beforeEach` not found):**
    The test file uses `beforeEach` but the TypeScript compiler cannot find the type. This is a vitest global type issue. Fix by adding a triple-slash reference at the top of the file:
    ```typescript
    /// <reference types="vitest/globals" />
    ```
    OR check if `frontend/tsconfig.json` has `"types": ["vitest/globals"]` in compilerOptions. If not, add the reference directive to the test file.
    Alternatively, explicitly import from vitest: `import { beforeEach, describe, it, expect, vi } from 'vitest';`
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits with code 0 (zero errors)
    - `grep "course_id" frontend/__tests__/deadlines/DeadlineCard.test.tsx | wc -l` returns at least 7
    - `head -5 frontend/__tests__/courses/CourseCard.test.tsx` contains either `/// <reference types="vitest/globals" />` or `import { beforeEach` from vitest
  </acceptance_criteria>
  <done>tsc --noEmit reports zero TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Fix 23 ESLint warnings (unused vars + exhaustive-deps)</name>
  <files>
    frontend/components/course-detail/AiCourseChat.tsx,
    frontend/components/course-detail/QuickLinksPanel.tsx,
    frontend/components/course-detail/UnitReviewSection.tsx,
    frontend/components/deadlines/DeadlinesPage.tsx,
    frontend/components/digest/HighlightItem.tsx,
    frontend/components/predict/PredictPage.tsx,
    frontend/components/setup/GuideCard.tsx,
    frontend/components/timetable/TimetableGrid.tsx,
    frontend/components/timetable/TimetablePage.tsx,
    frontend/src/test/setup.ts
  </files>
  <read_first>
    frontend/components/predict/PredictPage.tsx (lines 55-70 for unused 't' and 'courses' logical expression at line 64; lines 160-230 for all useMemo/useEffect hooks referencing courses)
    frontend/components/timetable/TimetablePage.tsx (lines 15-25 for unused getCourseColor import; lines 85-130 for semesterWeeks, allSessions, allDeadlines logical expressions; lines 155-200 for dependent hooks)
    frontend/components/deadlines/DeadlinesPage.tsx (lines 15-25 for deadlineList logical expression; lines 45-70 for dependent useMemo hooks)
    frontend/components/digest/HighlightItem.tsx (lines 40-50 for unused sourceThreadId and courseCode destructured vars)
    frontend/components/course-detail/AiCourseChat.tsx (line 25 for unused clearMessages)
    frontend/components/course-detail/QuickLinksPanel.tsx (line 28 for unused courseCode)
    frontend/components/course-detail/UnitReviewSection.tsx (line 22 for unused courseName)
    frontend/components/setup/GuideCard.tsx (line 6 for unused cn import)
    frontend/components/timetable/TimetableGrid.tsx (line 58 for unused weekPosition)
    frontend/src/test/setup.ts (line 23 for unused _options)
  </read_first>
  <action>
    **Fix 10 @typescript-eslint/no-unused-vars warnings:**

    1. `AiCourseChat.tsx:25` — `clearMessages` assigned but never used. Remove it from the destructuring (e.g., change `const { messages, clearMessages, ... } = useChat()` to `const { messages, ... } = useChat()`)
    2. `QuickLinksPanel.tsx:28` — `courseCode` defined but never used. Remove from destructuring or prefix with `_courseCode`
    3. `UnitReviewSection.tsx:22` — `courseName` defined but never used. Remove from destructuring or prefix with `_courseName`
    4. `HighlightItem.tsx:43` — `sourceThreadId` defined but never used. Remove from destructuring or prefix with `_`
    5. `HighlightItem.tsx:46` — `courseCode` defined but never used. Remove from destructuring or prefix with `_`
    6. `PredictPage.tsx:59` — `t` assigned but never used. Remove the `const t = useTranslations()` line or rename to `_t` if the import is needed for side effects
    7. `GuideCard.tsx:6` — `cn` imported but never used. Remove the import `import { cn } from ...`
    8. `TimetableGrid.tsx:58` — `weekPosition` defined but never used. Remove from destructuring or prefix with `_`
    9. `TimetablePage.tsx:19` — `getCourseColor` imported but never used. Remove from the import statement
    10. `setup.ts:23` — `_options` defined but never used. Prefix with `__options` (double underscore) or change the parameter pattern to omit it. Check ESLint config for underscore prefix rules — some configs allow single `_` prefix.

    **Fix 13 react-hooks/exhaustive-deps warnings:**

    The pattern is identical for all 13: a logical expression like `const x = data?.items || []` creates a new array reference on every render, making downstream useMemo/useEffect hooks unstable.

    Fix pattern: Wrap each logical expression in its own `useMemo`:
    ```typescript
    // Before (causes exhaustive-deps warning)
    const courses = data?.courses || [];
    const filtered = useMemo(() => courses.filter(...), [courses]);

    // After (stable reference)
    const courses = useMemo(() => data?.courses || [], [data]);
    const filtered = useMemo(() => courses.filter(...), [courses]);
    ```

    Apply to:
    1. `DeadlinesPage.tsx:19` — `deadlineList` (affects 2 warnings, lines 51 and 65): `const deadlineList = useMemo(() => data?.deadlines || [], [data]);`
    2. `PredictPage.tsx:64` — `courses` (affects 5 warnings, lines 166/185/194/199/227): `const courses = useMemo(() => data?.courses || [], [data]);`
    3. `TimetablePage.tsx:87` — `semesterWeeks` (affects 2 warnings, lines 98/106): `const semesterWeeks = useMemo(() => semesterData?.weeks || [], [semesterData]);`
    4. `TimetablePage.tsx:113` — `allSessions` (affects 1 warning, line 121): `const allSessions = useMemo(() => sessionsData?.sessions || [], [sessionsData]);`
    5. `TimetablePage.tsx:124` — `allDeadlines` (affects 3 warnings, lines 162/177/192): `const allDeadlines = useMemo(() => deadlinesData?.deadlines || [], [deadlinesData]);`

    Import `useMemo` from React if not already imported in the affected files. TanStack Query `data` is referentially stable between renders when unchanged, so using `[data]` as dependency is safe.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && npx next lint --max-warnings 0 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx next lint --max-warnings 0` exits with code 0 (zero warnings)
    - `grep "clearMessages" frontend/components/course-detail/AiCourseChat.tsx` returns no matches (removed unused var)
    - `grep "getCourseColor" frontend/components/timetable/TimetablePage.tsx` returns no matches (removed unused import)
    - `grep "useMemo.*data\?" frontend/components/predict/PredictPage.tsx` returns at least one match (courses wrapped in useMemo)
    - `grep "useMemo.*data\?" frontend/components/timetable/TimetablePage.tsx` returns at least one match (semesterWeeks/allSessions/allDeadlines wrapped)
  </acceptance_criteria>
  <done>ESLint reports zero warnings with --max-warnings 0</done>
</task>

</tasks>

<verification>
Both frontend tools must pass with zero errors in sequence:
```bash
cd frontend && npx tsc --noEmit && npx next lint --max-warnings 0
```
</verification>

<success_criteria>
- tsc --noEmit reports 0 errors (down from 8)
- ESLint --max-warnings 0 reports 0 warnings (down from 23)
- No regressions: `cd frontend && pnpm build` still succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/24-build-health/24-03-SUMMARY.md`
</output>
