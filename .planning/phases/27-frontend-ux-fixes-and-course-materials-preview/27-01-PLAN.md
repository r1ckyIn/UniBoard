---
phase: 27-frontend-ux-fixes-and-course-materials-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/notifications/map-to-activity.ts
  - frontend/components/dashboard/RecentActivity.tsx
  - frontend/__tests__/dashboard/RecentActivity.test.tsx
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "Clicking a grade activity item navigates in-app to /courses/{courseId}"
    - "Clicking a deadline activity item navigates in-app to /deadlines?date={due_date}"
    - "Clicking a discussion or endorsed activity item opens ExternalLinkDialog with the external URL"
    - "Activity items with internal paths do NOT open ExternalLinkDialog"
    - "Predict button on course cards already navigates to /predict?course={courseId} (no change needed)"
  artifacts:
    - path: "frontend/lib/notifications/map-to-activity.ts"
      provides: "ActivityItem type with internalPath field; URL classification logic"
      contains: "internalPath"
    - path: "frontend/components/dashboard/RecentActivity.tsx"
      provides: "Per-type click handler routing grade/deadline to router.push and discussion/endorsed to ExternalLinkDialog"
      contains: "router.push"
    - path: "frontend/__tests__/dashboard/RecentActivity.test.tsx"
      provides: "Tests for per-type navigation routing"
      contains: "router.push"
  key_links:
    - from: "frontend/components/dashboard/RecentActivity.tsx"
      to: "router.push()"
      via: "handleItemClick checks internalPath vs externalUrl"
      pattern: "item\\.internalPath.*router\\.push"
    - from: "frontend/lib/notifications/map-to-activity.ts"
      to: "RecentActivity.tsx"
      via: "ActivityItem interface with internalPath and externalUrl fields"
      pattern: "internalPath\\?.*string"
---

<objective>
Make dashboard reminder cards functional with per-type navigation routing. Grade and deadline activities navigate in-app; discussion and endorsed activities open ExternalLinkDialog for external Ed links.

Purpose: UX-01 requires reminder cards to be interactive, not just display-only. Currently all clicks go through ExternalLinkDialog indiscriminately. D-01 decision specifies type-based routing.
Output: Modified RecentActivity.tsx with router.push for internal navigation, updated map-to-activity.ts with URL classification, and implemented tests.
Note: UX-02 (predict button) is confirmed already working -- no changes needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-frontend-ux-fixes-and-course-materials-preview/27-CONTEXT.md
@.planning/phases/27-frontend-ux-fixes-and-course-materials-preview/27-RESEARCH.md

@frontend/lib/notifications/map-to-activity.ts
@frontend/components/dashboard/RecentActivity.tsx
@frontend/components/dashboard/ExternalLinkDialog.tsx
@frontend/lib/fixtures/notifications.ts
@frontend/__tests__/dashboard/RecentActivity.test.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/lib/notifications/map-to-activity.ts:
```typescript
export interface ActivityItem {
  id: string;
  type: "grade" | "discussion" | "deadline" | "endorsed";
  text: string;
  strongText: string;
  time: string;
  externalUrl?: string;
}
```

From frontend/lib/api/types.gen.d.ts (Notification schema):
```typescript
Notification: {
  id: string;
  type: string;                              // "grade_published" | "discussion_reply" | "endorsed_answer" | "deadline_reminder" | "token_expired" | "sync_complete"
  severity: "info" | "warning" | "critical";
  title: string;
  body: string;
  is_read: boolean;
  action_url?: string;                       // Could be internal ("/courses/crs_comp2017") or external ("https://edstem.org/...")
  created_at: string;
};
```

From frontend/components/dashboard/ExternalLinkDialog.tsx:
```typescript
interface ExternalLinkDialogProps {
  open: boolean;
  url: string;
  onConfirm: () => void;
  onCancel: () => void;
}
```

From frontend/lib/fixtures/notifications.ts:
- ntf_004 (grade_published): action_url="/courses/crs_comp2017"
- ntf_001 (deadline_reminder): action_url="/courses/crs_comp3221" (currently points to course, D-01 says deadline should go to /deadlines?date=)
- All current fixtures use internal paths; real discussion_reply/endorsed_answer will have external Ed URLs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update ActivityItem type and map-to-activity classification + implement RecentActivity tests</name>
  <files>frontend/lib/notifications/map-to-activity.ts, frontend/__tests__/dashboard/RecentActivity.test.tsx</files>
  <read_first>
    frontend/lib/notifications/map-to-activity.ts
    frontend/components/dashboard/RecentActivity.tsx
    frontend/components/dashboard/ExternalLinkDialog.tsx
    frontend/lib/fixtures/notifications.ts
    frontend/__tests__/dashboard/RecentActivity.test.tsx
  </read_first>
  <behavior>
    - Test 1: mapNotificationToActivity for grade_published notification returns ActivityItem with internalPath="/courses/crs_comp2017" and externalUrl=undefined
    - Test 2: mapNotificationToActivity for deadline_reminder notification returns ActivityItem with internalPath="/deadlines" (deadline type always routes to /deadlines) and externalUrl=undefined
    - Test 3: mapNotificationToActivity for discussion_reply notification with external URL returns externalUrl set and internalPath=undefined
    - Test 4: mapNotificationToActivity for endorsed_answer notification with external URL returns externalUrl set and internalPath=undefined
    - Test 5: mapNotificationToActivity for notification with internal path (starts with "/") sets internalPath, no externalUrl
    - Test 6: mapNotificationToActivity for notification with external URL (starts with "http") sets externalUrl, no internalPath
    - Test 7: RecentActivity renders all activity items
    - Test 8: RecentActivity - clicking grade item calls router.push with internal path
    - Test 9: RecentActivity - clicking discussion item with external URL opens ExternalLinkDialog (dialog becomes visible)
    - Test 10: RecentActivity - items with internalPath have cursor-pointer and role="button"
  </behavior>
  <action>
    **Step 1: Update `frontend/lib/notifications/map-to-activity.ts`:**

    Add `internalPath?: string` field to `ActivityItem` interface:
    ```typescript
    export interface ActivityItem {
      id: string;
      type: "grade" | "discussion" | "deadline" | "endorsed";
      text: string;
      strongText: string;
      time: string;
      externalUrl?: string;
      internalPath?: string;  // NEW: for in-app navigation
    }
    ```

    In `mapNotificationToActivity`, replace the blanket `externalUrl: n.action_url` assignment with URL classification logic:

    For `grade` type: always set `internalPath = n.action_url` (grade notifications always point to internal course pages).

    For `deadline` type: set `internalPath = "/deadlines"` (per D-01, deadline activities navigate to /deadlines page; the action_url currently points to /courses/ but D-01 says deadline -> /deadlines).

    For `discussion` and `endorsed` types: classify based on URL format:
    - If `action_url` starts with "/" -> set `internalPath = action_url` (internal route)
    - If `action_url` starts with "http" -> set `externalUrl = action_url` (external Ed link)
    - If no `action_url` -> neither field set

    For all other types (token_expired, sync_complete, etc.): if `action_url` starts with "/" set `internalPath`, otherwise `externalUrl`.

    **Step 2: Implement tests in `frontend/__tests__/dashboard/RecentActivity.test.tsx`:**

    Replace the .todo() stubs with real tests. Use the standard test setup pattern from other dashboard tests in this project:
    - Mock `next-intl` useTranslations
    - Mock `@/lib/i18n/navigation` useRouter
    - Use `@testing-library/react` render + screen + fireEvent

    Test `mapNotificationToActivity` with mock notifications for each type.
    Test `RecentActivity` component rendering and click behavior:
    - Grade item click -> verify router.push called with internalPath
    - Discussion item with externalUrl -> verify ExternalLinkDialog opens (dialog element has open attribute)
    - Items with internalPath get role="button" and cursor-pointer

    Mock HTMLDialogElement.prototype.showModal as vi.fn() (jsdom doesn't implement native dialog).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm vitest run __tests__/dashboard/RecentActivity.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - frontend/lib/notifications/map-to-activity.ts contains `internalPath?: string` in ActivityItem interface
    - mapNotificationToActivity function contains logic checking `action_url` with `startsWith("/")` or `startsWith("http")`
    - For grade type: internalPath is set from action_url
    - For deadline type: internalPath is "/deadlines"
    - For discussion/endorsed: URL classified as internal or external based on prefix
    - frontend/__tests__/dashboard/RecentActivity.test.tsx has NO .todo() stubs remaining
    - frontend/__tests__/dashboard/RecentActivity.test.tsx contains at least 7 test cases
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>mapNotificationToActivity classifies URLs by type and format; ActivityItem has internalPath field; RecentActivity test file has all stubs replaced with passing tests</done>
</task>

<task type="auto">
  <name>Task 2: Wire per-type click routing in RecentActivity component</name>
  <files>frontend/components/dashboard/RecentActivity.tsx</files>
  <read_first>
    frontend/components/dashboard/RecentActivity.tsx
    frontend/lib/notifications/map-to-activity.ts
    frontend/components/dashboard/ExternalLinkDialog.tsx
    frontend/__tests__/dashboard/RecentActivity.test.tsx
  </read_first>
  <action>
    Modify `RecentActivity.tsx` to implement per-type click routing per D-01:

    **Step 1: Import useRouter:**
    Add `import { useRouter } from "@/lib/i18n/navigation";` at the top.
    Inside the component, add `const router = useRouter();`.

    **Step 2: Update handleItemClick to accept ActivityItem:**
    Change the callback from `handleItemClick(url?: string)` to `handleItemClick(item: ActivityItem)`:

    ```typescript
    const handleItemClick = useCallback((item: ActivityItem) => {
      if (item.internalPath) {
        router.push(item.internalPath);
      } else if (item.externalUrl) {
        setDialogUrl(item.externalUrl);
      }
    }, [router]);
    ```

    **Step 3: Update the onClick in the activity item div:**
    Change from `onClick={() => handleItemClick(item.externalUrl)}` to `onClick={() => handleItemClick(item)}`.

    **Step 4: Update role and tabIndex conditions:**
    Change `item.externalUrl` conditions to `item.internalPath || item.externalUrl` since both internal and external items are now clickable:
    - `role={item.internalPath || item.externalUrl ? "button" : undefined}`
    - `tabIndex={item.internalPath || item.externalUrl ? 0 : undefined}`
    - `className` cursor-pointer condition: `(item.internalPath || item.externalUrl) && "cursor-pointer"`

    **Step 5: Update onKeyDown handler similarly:**
    The existing onKeyDown guard should check `item.internalPath || item.externalUrl` instead of just `item.externalUrl`, and call `handleItemClick(item)`.

    **Step 6: Import ActivityItem type** (already imported via `import type { ActivityItem } from "@/lib/notifications/map-to-activity"`).
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm vitest run __tests__/dashboard/RecentActivity.test.tsx -x && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - RecentActivity.tsx contains `import { useRouter } from "@/lib/i18n/navigation"`
    - RecentActivity.tsx contains `const router = useRouter()`
    - handleItemClick callback accepts `item: ActivityItem` parameter (not `url?: string`)
    - handleItemClick body contains `if (item.internalPath)` followed by `router.push(item.internalPath)`
    - handleItemClick body contains `else if (item.externalUrl)` followed by `setDialogUrl(item.externalUrl)`
    - role attribute uses condition `item.internalPath || item.externalUrl`
    - onClick calls `handleItemClick(item)` not `handleItemClick(item.externalUrl)`
    - All RecentActivity tests pass (exit code 0)
    - tsc --noEmit shows no new errors related to RecentActivity or map-to-activity
  </acceptance_criteria>
  <done>Grade/deadline activities navigate in-app via router.push; discussion/endorsed activities open ExternalLinkDialog; all tests green; TypeScript clean</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm vitest run __tests__/dashboard/RecentActivity.test.tsx -x` -- all tests pass
2. `cd frontend && pnpm tsc --noEmit 2>&1 | grep -c "error"` -- zero TypeScript errors
3. Grade activity fixture (ntf_004) has internalPath set, no externalUrl
4. Deadline activity fixture has internalPath="/deadlines"
5. Discussion/endorsed types with http URLs use externalUrl; with / paths use internalPath
</verification>

<success_criteria>
- Clicking a grade reminder card navigates to /courses/{courseId} in-app (no ExternalLinkDialog)
- Clicking a deadline reminder card navigates to /deadlines in-app
- Clicking a discussion/endorsed card with external URL opens ExternalLinkDialog
- Items without any URL are not clickable (no role="button")
- All existing tests still pass
- UX-02 confirmed already working (no changes)
</success_criteria>

<output>
After completion, create `.planning/phases/27-frontend-ux-fixes-and-course-materials-preview/27-01-SUMMARY.md`
</output>
