---
phase: 30-bff-proxy-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/api/proxy.ts
  - frontend/__tests__/api/proxy.test.ts
  - frontend/__tests__/api/mock-routes.test.ts
autonomous: true
requirements: [BFF-01, BFF-02, BFF-03]
must_haves:
  truths:
    - "proxyRequest() utility builds correct backend URL from incoming request path"
    - "proxyRequest() forwards Authorization header from incoming request"
    - "proxyRequest() forwards query parameters intact"
    - "proxyRequest() transforms backend error responses into user-friendly messages"
    - "proxyRequest() handles 204 No Content without trying to parse JSON"
    - "proxyRequest() pipes SSE streams as raw Response with text/event-stream content type"
  artifacts:
    - path: "frontend/lib/api/proxy.ts"
      provides: "Shared BFF proxy utility function"
      exports: ["proxyRequest"]
      min_lines: 60
    - path: "frontend/__tests__/api/proxy.test.ts"
      provides: "Unit tests for proxyRequest utility"
      min_lines: 80
  key_links:
    - from: "frontend/lib/api/proxy.ts"
      to: "process.env.NEXT_PUBLIC_API_URL"
      via: "environment variable with fallback"
      pattern: "NEXT_PUBLIC_API_URL.*localhost:8000"
    - from: "frontend/lib/api/proxy.ts"
      to: "Python FastAPI backend"
      via: "server-side fetch with Authorization forwarding"
      pattern: "Authorization.*request\\.headers\\.get"
---

<objective>
Create the shared `proxyRequest()` utility that all 25 Route Handler conversions will use, plus comprehensive tests covering JWT forwarding (BFF-02) and error transformation (BFF-03).

Purpose: This is the foundation for the entire BFF proxy conversion. A single well-tested utility eliminates duplication across 25 routes and centralizes error handling.
Output: `frontend/lib/api/proxy.ts` (utility), `frontend/__tests__/api/proxy.test.ts` (tests), updated `frontend/__tests__/api/mock-routes.test.ts` (health test kept, mock helper tests preserved)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-bff-proxy-conversion/30-RESEARCH.md

<interfaces>
<!-- Existing proxy reference pattern from the codebase (the ONE already-proxied route) -->
From frontend/app/api/v1/threads/[threadId]/feedback/route.ts:
```typescript
export async function POST(
  request: NextRequest,
  { params }: { params: Promise<{ threadId: string }> },
) {
  const { threadId } = await params;
  const body = await request.json();
  const backendUrl = `${
    process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000"
  }/api/v1/threads/${threadId}/feedback`;
  const resp = await fetch(backendUrl, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: request.headers.get("Authorization") || "",
    },
    body: JSON.stringify(body),
  });
  const data = await resp.json();
  return NextResponse.json(data, { status: resp.status });
}
```

From frontend/lib/api/client.ts (frontend ky client - shows JWT is set as Bearer token):
```typescript
export const api = ky.create({
  prefixUrl: "/api/v1",
  hooks: {
    beforeRequest: [
      (request) => {
        const token = useAuthStore.getState().accessToken;
        if (token) {
          request.headers.set("Authorization", `Bearer ${token}`);
        }
      },
    ],
  },
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create proxyRequest utility and tests</name>
  <files>frontend/lib/api/proxy.ts, frontend/__tests__/api/proxy.test.ts</files>
  <read_first>
    - frontend/app/api/v1/threads/[threadId]/feedback/route.ts (existing proxy pattern to replicate)
    - frontend/lib/api/client.ts (understand how JWT is attached)
    - frontend/__tests__/api/mock-routes.test.ts (understand existing test patterns)
    - frontend/vitest.config.ts (test configuration)
    - .planning/phases/30-bff-proxy-conversion/30-RESEARCH.md (proxy utility design, error messages, pitfalls)
  </read_first>
  <behavior>
    - Test 1: proxyRequest forwards GET request to BACKEND_URL + same path + query params
    - Test 2: proxyRequest forwards Authorization header from incoming request
    - Test 3: proxyRequest forwards POST body to backend
    - Test 4: proxyRequest with backendPath option overrides the URL path
    - Test 5: proxyRequest returns NextResponse.json(data) for 200 responses
    - Test 6: proxyRequest returns friendly error message for 401 ("Session expired. Please sign in again.")
    - Test 7: proxyRequest returns friendly error message for 404 ("The requested resource was not found.")
    - Test 8: proxyRequest returns friendly error message for 500 ("Something went wrong. Please try again later.")
    - Test 9: proxyRequest preserves backend error code in response (e.g. "TOKEN_INVALID")
    - Test 10: proxyRequest handles 204 No Content (returns empty body, no JSON parse)
    - Test 11: proxyRequest with stream=true returns raw Response with text/event-stream header
    - Test 12: proxyRequest handles backend returning non-JSON error body gracefully
    - Test 13: proxyRequest includes Content-Type: application/json only when body is provided
    - Test 14: proxyRequest falls back to http://localhost:8000 when NEXT_PUBLIC_API_URL is not set
  </behavior>
  <action>
    **RED phase:** Create `frontend/__tests__/api/proxy.test.ts` with all 14 test cases. Mock `global.fetch` using vi.fn() to simulate backend responses. Test the proxyRequest function imported from `@/lib/api/proxy`. Run tests -- all must fail (module not found).

    **GREEN phase:** Create `frontend/lib/api/proxy.ts` with:

    1. Constants:
       - `const BACKEND_URL = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000"`
       - `ERROR_MESSAGES` map: `{ 400: "Invalid request. Please check your input.", 401: "Session expired. Please sign in again.", 403: "You don't have permission to access this resource.", 404: "The requested resource was not found.", 422: "The provided data could not be processed.", 429: "Too many requests. Please try again later.", 500: "Something went wrong. Please try again later.", 502: "External service temporarily unavailable.", 503: "Service temporarily unavailable." }`

    2. Interface `ProxyOptions`:
       - `backendPath?: string` -- override backend path (for dynamic param routes like `/courses/${id}`)
       - `method?: string` -- HTTP method override
       - `body?: string | null` -- request body to forward (read with `request.text()` in caller)
       - `extraHeaders?: Record<string, string>` -- additional headers
       - `stream?: boolean` -- whether to return SSE streaming response

    3. `export async function proxyRequest(request: NextRequest, options: ProxyOptions = {}): Promise<NextResponse | Response>`:
       - Build URL: `${BACKEND_URL}${backendPath ?? new URL(request.url).pathname}${new URL(request.url).search}`
       - Build headers: `{ Authorization: request.headers.get("Authorization") || "" }` + extraHeaders. Add `"Content-Type": "application/json"` ONLY if body is not null/undefined.
       - Call `fetch(url, { method: method ?? request.method, headers, body: body ?? undefined })`
       - If `stream && resp.ok`: return `new Response(resp.body, { status: resp.status, headers: { "Content-Type": "text/event-stream", "Cache-Control": "no-cache", Connection: "keep-alive" } })`
       - If `!resp.ok`: try parse JSON for `error.code`, use `ERROR_MESSAGES[status]` for friendly message, fallback to "An unexpected error occurred.", return `NextResponse.json({ error: { code, message: friendlyMessage } }, { status })`
       - If `resp.status === 204`: return `new NextResponse(null, { status: 204 })`
       - Otherwise: `const data = await resp.json(); return NextResponse.json(data, { status: resp.status })`

    Run tests -- all must pass.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm test -- --run __tests__/api/proxy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/lib/api/proxy.ts` contains `export async function proxyRequest(`
    - `frontend/lib/api/proxy.ts` contains `NEXT_PUBLIC_API_URL`
    - `frontend/lib/api/proxy.ts` contains `"Session expired. Please sign in again."`
    - `frontend/lib/api/proxy.ts` contains `text/event-stream`
    - `frontend/lib/api/proxy.ts` contains `status === 204`
    - `frontend/__tests__/api/proxy.test.ts` contains at least 10 `it(` or `test(` calls
    - `cd frontend && pnpm test -- --run __tests__/api/proxy.test.ts` exits 0
  </acceptance_criteria>
  <done>proxyRequest utility handles GET/POST/SSE/204/errors, all 14+ tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update existing mock-routes test to remain valid</name>
  <files>frontend/__tests__/api/mock-routes.test.ts</files>
  <read_first>
    - frontend/__tests__/api/mock-routes.test.ts (current tests that test mock helpers and health endpoint)
    - frontend/app/api/v1/health/route.ts (health endpoint stays static -- not being converted)
    - frontend/lib/fixtures/helpers.ts (mock helpers that tests exercise)
  </read_first>
  <action>
    The `mock-routes.test.ts` file tests mock helpers (`mockResponse`, `mockError`, `requireAuth`, `mockPaginatedResponse`) and the health endpoint. These tests are STILL VALID because:
    - Mock helpers are still used by the 5 routes that stay mock (health, timetable x2, export, verify)
    - Health endpoint stays static

    No changes needed to this file IF all existing tests still pass after Plan 01. Verify by running the test suite. If tests pass, this task is a no-op validation.

    If any tests fail (e.g., due to import resolution changes), fix the specific failing assertions without rewriting the entire file.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm test -- --run __tests__/api/mock-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm test -- --run __tests__/api/mock-routes.test.ts` exits 0
    - `cd frontend && pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>All existing mock-routes tests still pass, no regressions from proxy utility addition</done>
</task>

</tasks>

<verification>
```bash
cd /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/frontend && pnpm test -- --run && pnpm typecheck
```
All tests pass, type checking passes, proxy utility is ready for route conversions.
</verification>

<success_criteria>
- proxyRequest() utility exists with JWT forwarding, error transformation, SSE streaming, 204 handling
- All 14+ proxy tests pass
- Existing mock-routes tests unaffected
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/30-bff-proxy-conversion/30-01-SUMMARY.md`
</output>
