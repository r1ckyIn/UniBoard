---
phase: 31-e2e-verification-ai-config
task: UAT testing
total_tasks: 5
status: in_progress
last_updated: 2026-04-06T12:55:56Z
---

<current_state>
Phase 31 code execution complete (2 plans). Currently in UAT testing phase.
E2E flow partially verified: register → setup → token validation → success page works.
Sync trigger fires but sync may be failing silently (need to investigate sync logs).
UAT test 1 (Token validation) passed after multiple bug fixes. Tests 2-5 not yet run.
</current_state>

<completed_work>

## Phase 31 Plans
- Plan 31-01: Wire TokenStep to backend API + SuccessStep to real sync polling — Done
- Plan 31-02: Fix SSE dual-path to BFF proxy + AI API key guard — Done

## Bug Fixes During UAT (10 fixes total)
1. `fix(31): add locale prefix to all route redirects and dashboard auth guard` — /setup was 404, new users bypassed setup
2. `fix(31): add pool_recycle, pool_pre_ping to sync engine` — Sentry DBAPIError from stale connections
3. `fix(31): add locale to useEffect dependency arrays` — ESLint CI failure
4. `fix(31): Ed token regex allow dots, defer tokenConfigured until SuccessStep` — Ed token format mismatch, SetupGuard race condition
5. `fix(31): remove unused useAuthStore import` — ESLint CI failure
6. `fix(31): only clearAuth on 401 for GET requests` — Token validation 401 was clearing auth mid-setup
7. `fix(31): support ES256 (JWKS) + HS256 (legacy) JWT verification` — Supabase rotated to ECC P-256 keys
8. `fix(31): use /api/user for Ed token validation instead of /api/courses` — Ed API /courses returns 403 with Bearer
9. `fix(31): add 30s timeout fallback for sync` — Sync failure left user stuck with no Dashboard button
10. `fix(31): add syncSlow i18n key` — Raw key shown instead of translated text
</completed_work>

<remaining_work>

## UAT Tests Remaining
- Test 2: Sync status polling + real courses displayed — NOT TESTED (sync may be failing)
- Test 3: AI SSE streaming through BFF proxy — NOT TESTED
- Test 4: AI 503 graceful degradation — NOT TESTED
- Test 5: Railway API key production verification — NOT TESTED

## Known Issues to Investigate
- Sync appears to hang — need Railway logs to determine if sync tasks are crashing
- Sentry DuplicatePreparedStatement errors from sync (may be resolved by pool fix)
- Database intermittently disconnects (health endpoint showed "degraded" once)
</remaining_work>

<decisions_made>

- Supabase "Confirm email" turned off for testing (Phase 32 will add proper email flow)
- JWT verification supports both ES256 (JWKS) and HS256 (legacy) for Supabase key rotation
- ky afterResponse 401 handler only fires on GET, not mutations (prevents auth clear during setup)
- SuccessStep shows Dashboard button after 30s regardless of sync status
- DashboardGuard added to protect dashboard from unauthenticated/unconfigured users
</decisions_made>

<blockers>
- Sync may be failing silently — need to check Railway logs or Sentry for sync-related errors
- Ed API token auth: Bearer token works on /api/user but not /api/courses (fixed in backend)
- Supabase JWT expires in 1 hour — long testing sessions hit 401 (user needs to re-login)
</blockers>

<context>
Phase 31 originally had 2 clean plans but UAT revealed 10 production bugs that all needed
fixing before E2E could work. The biggest issues were: (1) locale routing — Next.js [locale]
route group meant /setup was 404, (2) Supabase ECC key rotation broke JWT verification,
(3) Ed API endpoint inconsistency. All fixed and pushed.

The sync system needs investigation — token validation works end-to-end but data sync
after setup may be failing. This is the next priority.

UX improvements noted in memory:
- TokenStep should skip re-validating already-validated tokens
- SuccessStep needs step-by-step sync progress (not just spinner)
</context>

<next_action>
1. Run `/gsd:resume-work` to restore context
2. Investigate sync failure: check Railway logs or add logging to sync trigger endpoint
3. Continue UAT tests 2-5
4. If sync works: verify Dashboard shows real course data
5. If AI key configured: test AI SSE streaming
</next_action>
