---
phase: 32-production-email
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/hooks/use-auth.ts
  - frontend/components/auth/ForgotPasswordForm.tsx
  - frontend/components/auth/UpdatePasswordForm.tsx
  - frontend/components/auth/RegisterForm.tsx
  - frontend/components/auth/LoginForm.tsx
  - frontend/components/auth/AuthFormCard.tsx
  - frontend/components/auth/AuthPage.tsx
  - frontend/messages/en.json
  - frontend/messages/zh.json
  - frontend/__tests__/auth/ForgotPasswordForm.test.tsx
  - frontend/__tests__/auth/UpdatePasswordForm.test.tsx
  - frontend/__tests__/auth/RegisterForm.test.tsx
  - frontend/__tests__/auth/LoginForm.test.tsx
autonomous: true
requirements: [EMAIL-02]

must_haves:
  truths:
    - "After registration, user sees 'check your email' message instead of auto-redirect to /setup"
    - "User can request password reset from login page via ForgotPasswordForm"
    - "User can set new password after clicking reset link via UpdatePasswordForm"
    - "Auth page supports 4 modes: login, register, forgot-password, reset-password"
  artifacts:
    - path: "frontend/components/auth/ForgotPasswordForm.tsx"
      provides: "Email input form for requesting password reset"
      contains: "resetPasswordForEmail"
    - path: "frontend/components/auth/UpdatePasswordForm.tsx"
      provides: "New password form after reset link clicked"
      contains: "updateUser"
    - path: "frontend/hooks/use-auth.ts"
      provides: "Auth mutation hooks including useResetPassword and useUpdatePassword"
      exports: ["useLogin", "useRegister", "useLogout", "useResetPassword", "useUpdatePassword"]
    - path: "frontend/components/auth/RegisterForm.tsx"
      provides: "Updated register form showing check-email message after signup"
      contains: "checkEmail"
    - path: "frontend/components/auth/AuthPage.tsx"
      provides: "Auth page supporting 4 modes via URL search params"
      contains: "forgot-password"
  key_links:
    - from: "frontend/components/auth/ForgotPasswordForm.tsx"
      to: "frontend/hooks/use-auth.ts"
      via: "useResetPassword hook"
      pattern: "useResetPassword"
    - from: "frontend/components/auth/UpdatePasswordForm.tsx"
      to: "frontend/hooks/use-auth.ts"
      via: "useUpdatePassword hook"
      pattern: "useUpdatePassword"
    - from: "frontend/components/auth/AuthPage.tsx"
      to: "frontend/components/auth/AuthFormCard.tsx"
      via: "mode prop controls which form is rendered"
      pattern: "forgot-password|reset-password"
    - from: "frontend/components/auth/LoginForm.tsx"
      to: "frontend/components/auth/AuthPage.tsx"
      via: "Forgot password link switches mode to forgot-password"
      pattern: "onSwitchToForgotPassword"
---

<objective>
Implement the complete frontend auth flow changes for email confirmation and password reset: new ForgotPasswordForm and UpdatePasswordForm components, updated RegisterForm (check-email UI), updated LoginForm (forgot-password link), updated AuthFormCard/AuthPage (4-mode routing), new auth hooks, and i18n strings.

Purpose: With email confirmations enabled, the existing auto-confirm flow breaks. Users need a "check your email" state after registration and a working forgot-password flow replacing the demo toast.
Output: 2 new components, 5 updated files, 2 new hooks, i18n strings in both locales, tests for all new components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-production-email/32-RESEARCH.md

@frontend/hooks/use-auth.ts
@frontend/components/auth/RegisterForm.tsx
@frontend/components/auth/LoginForm.tsx
@frontend/components/auth/AuthFormCard.tsx
@frontend/components/auth/AuthPage.tsx
@frontend/components/auth/SuccessOverlay.tsx
@frontend/lib/supabase/client.ts
@frontend/messages/en.json
@frontend/messages/zh.json
@frontend/__tests__/auth/RegisterForm.test.tsx
@frontend/__tests__/auth/LoginForm.test.tsx

<interfaces>
<!-- Current use-auth.ts exports (to be extended): -->
From frontend/hooks/use-auth.ts:
```typescript
export function useLogin(): UseMutationResult
export function useRegister(): UseMutationResult
export function useLogout(): UseMutationResult
// NEW: useResetPassword and useUpdatePassword to be added
```

From frontend/components/auth/AuthFormCard.tsx:
```typescript
interface AuthFormCardProps {
  mode: "login" | "register";  // WILL CHANGE to include "forgot-password" | "reset-password"
  onSwitchMode: (mode: string) => void;
  onRegisterSuccess: () => void;
}
```

From frontend/components/auth/RegisterForm.tsx:
```typescript
interface RegisterFormProps {
  onSwitchToLogin: () => void;
  onRegisterSuccess: () => void;  // WILL CHANGE behavior: show check-email instead of SuccessOverlay
}
```

From frontend/components/auth/LoginForm.tsx:
```typescript
interface LoginFormProps {
  onSwitchToRegister: () => void;
  // WILL ADD: onSwitchToForgotPassword: () => void;
}
```

Supabase client methods (existing @supabase/supabase-js):
```typescript
supabase.auth.resetPasswordForEmail(email: string, options?: { redirectTo?: string }): Promise<{ data, error }>
supabase.auth.updateUser({ password: string }): Promise<{ data, error }>
```

Existing design tokens (from LoginForm/RegisterForm):
```
inputBaseClass = "w-full px-3.5 py-2.5 text-[0.84rem] border-[1.5px] border-card-border rounded-lg bg-cream text-text-1 outline-none transition-[border-color,box-shadow] duration-150 placeholder:text-text-3 focus:border-[#d97757] focus:shadow-[0_0_0_3px_rgba(217,119,87,0.11)]"
inputErrorClass = "border-[#cc4455]"
buttonClass = "w-full h-[44px] font-semibold text-[0.86rem] text-white bg-[#d97757] rounded-lg hover:bg-[#c5674a] hover:-translate-y-px active:translate-y-0 disabled:opacity-60 disabled:cursor-default disabled:transform-none transition-[background,transform] duration-150"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add auth hooks and create ForgotPasswordForm + UpdatePasswordForm</name>
  <files>frontend/hooks/use-auth.ts, frontend/components/auth/ForgotPasswordForm.tsx, frontend/components/auth/UpdatePasswordForm.tsx, frontend/__tests__/auth/ForgotPasswordForm.test.tsx, frontend/__tests__/auth/UpdatePasswordForm.test.tsx</files>
  <read_first>
    - frontend/hooks/use-auth.ts
    - frontend/components/auth/LoginForm.tsx
    - frontend/components/auth/RegisterForm.tsx
    - frontend/__tests__/auth/LoginForm.test.tsx
    - frontend/messages/en.json
    - frontend/messages/zh.json
  </read_first>
  <behavior>
    ForgotPasswordForm tests:
    - Test 1: Renders email input and submit button with correct text
    - Test 2: Shows validation error for invalid email
    - Test 3: Calls resetPasswordForEmail on valid submit and shows success message
    - Test 4: Calls onSwitchToLogin when "Back to sign in" is clicked
    - Test 5: Shows API error message when mutation fails

    UpdatePasswordForm tests:
    - Test 1: Renders new password and confirm password inputs
    - Test 2: Shows validation error when passwords don't match
    - Test 3: Calls updateUser on valid submit
    - Test 4: Shows success message after password update
    - Test 5: Enforces minimum password length
  </behavior>
  <action>
1. **Update `frontend/hooks/use-auth.ts`** — Add two new hooks after the existing `useLogout`:

   ```typescript
   export function useResetPassword() {
     return useMutation({
       mutationFn: async (email: string) => {
         const supabase = createClient();
         const { error } = await supabase.auth.resetPasswordForEmail(email, {
           redirectTo: `${window.location.origin}/auth/confirm`,
         });
         if (error) throw error;
       },
     });
   }

   export function useUpdatePassword() {
     return useMutation({
       mutationFn: async (newPassword: string) => {
         const supabase = createClient();
         const { error } = await supabase.auth.updateUser({
           password: newPassword,
         });
         if (error) throw error;
       },
     });
   }
   ```

2. **Create `frontend/__tests__/auth/ForgotPasswordForm.test.tsx`** — Follow the exact mock pattern from LoginForm.test.tsx. Mock `next-intl`, `motion/react`, `@/hooks/use-auth` (exposing `useResetPassword` mock), `next/navigation`. Test all 5 behaviors listed above. The i18n mock map should include keys:
   - `auth.forgotPassword.title` -> "Reset your password"
   - `auth.forgotPassword.subtitle` -> "Enter your email and we'll send you a reset link"
   - `auth.forgotPassword.emailLabel` -> "Email"
   - `auth.forgotPassword.emailPlaceholder` -> "you@uni.sydney.edu.au"
   - `auth.forgotPassword.submitButton` -> "Send Reset Link"
   - `auth.forgotPassword.backToLogin` -> "Back to sign in"
   - `auth.forgotPassword.successTitle` -> "Check your email"
   - `auth.forgotPassword.successMessage` -> "We've sent a password reset link to your email address."
   - `auth.errors.resetFailed` -> "Failed to send reset email. Please try again."

3. **Create `frontend/components/auth/ForgotPasswordForm.tsx`** — A "use client" component:
   - Props: `{ onSwitchToLogin: () => void }`
   - Uses `useForm` with `zodResolver` for email-only form (reuse email validation from `@/lib/validations/auth` — import just the email part or create a simple zod schema inline: `z.object({ email: z.string().email().endsWith("@uni.sydney.edu.au") })`)
   - On submit: calls `useResetPassword().mutate(email)` with onSuccess that sets `sent` state to true
   - When `sent` is true: shows success UI with Mail icon (from lucide-react), "Check your email" heading, description, and "Back to sign in" button
   - When `sent` is false: shows email input, "Send Reset Link" button, "Back to sign in" link
   - Same inputBaseClass/inputErrorClass/buttonClass as LoginForm
   - Uses `useTranslations()` for all text with `auth.forgotPassword.*` keys

4. **Create `frontend/__tests__/auth/UpdatePasswordForm.test.tsx`** — Same mock pattern. Mock `useUpdatePassword`. The i18n mock map:
   - `auth.updatePassword.title` -> "Set new password"
   - `auth.updatePassword.subtitle` -> "Enter your new password below"
   - `auth.updatePassword.passwordLabel` -> "New Password"
   - `auth.updatePassword.passwordPlaceholder` -> "At least 8 characters"
   - `auth.updatePassword.confirmLabel` -> "Confirm New Password"
   - `auth.updatePassword.confirmPlaceholder` -> "Re-enter your new password"
   - `auth.updatePassword.submitButton` -> "Update Password"
   - `auth.updatePassword.successTitle` -> "Password updated!"
   - `auth.updatePassword.successMessage` -> "Your password has been updated. You can now sign in."
   - `auth.updatePassword.backToLogin` -> "Back to sign in"
   - `auth.errors.updatePasswordFailed` -> "Failed to update password. Please try again."

5. **Create `frontend/components/auth/UpdatePasswordForm.tsx`** — A "use client" component:
   - Props: `{ onSwitchToLogin: () => void }`
   - Uses `useForm` with zodResolver for password + confirmPassword (reuse `z.object({ password: z.string().min(8), confirmPassword: z.string() }).refine(data => data.password === data.confirmPassword)`)
   - On submit: calls `useUpdatePassword().mutate(password)` with onSuccess that sets `updated` state to true
   - When `updated` is true: shows success UI with Check icon, "Password updated!" heading, "Back to sign in" button
   - When `updated` is false: shows password fields with eye toggle (same pattern as RegisterForm), "Update Password" button
   - Includes PasswordStrengthMeter for the new password field
   - Same styling as other auth forms
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run __tests__/auth/ForgotPasswordForm.test.tsx __tests__/auth/UpdatePasswordForm.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/hooks/use-auth.ts exports `useResetPassword` and `useUpdatePassword`
    - frontend/hooks/use-auth.ts `useResetPassword` calls `supabase.auth.resetPasswordForEmail`
    - frontend/hooks/use-auth.ts `useUpdatePassword` calls `supabase.auth.updateUser`
    - frontend/components/auth/ForgotPasswordForm.tsx exists with `"use client"` directive
    - ForgotPasswordForm.tsx contains `useResetPassword`
    - ForgotPasswordForm.tsx contains `onSwitchToLogin` prop
    - frontend/components/auth/UpdatePasswordForm.tsx exists with `"use client"` directive
    - UpdatePasswordForm.tsx contains `useUpdatePassword`
    - UpdatePasswordForm.tsx contains `PasswordStrengthMeter`
    - frontend/__tests__/auth/ForgotPasswordForm.test.tsx has at least 4 test cases
    - frontend/__tests__/auth/UpdatePasswordForm.test.tsx has at least 4 test cases
    - All tests pass: `cd frontend && pnpm test -- --run __tests__/auth/ForgotPasswordForm.test.tsx __tests__/auth/UpdatePasswordForm.test.tsx` exits 0
  </acceptance_criteria>
  <done>Two new auth hooks (useResetPassword, useUpdatePassword) added to use-auth.ts. ForgotPasswordForm and UpdatePasswordForm components created with proper form validation, success states, and consistent styling. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update RegisterForm, LoginForm, AuthFormCard, AuthPage for email confirmation flow + i18n</name>
  <files>frontend/components/auth/RegisterForm.tsx, frontend/components/auth/LoginForm.tsx, frontend/components/auth/AuthFormCard.tsx, frontend/components/auth/AuthPage.tsx, frontend/messages/en.json, frontend/messages/zh.json, frontend/__tests__/auth/RegisterForm.test.tsx, frontend/__tests__/auth/LoginForm.test.tsx</files>
  <read_first>
    - frontend/components/auth/RegisterForm.tsx
    - frontend/components/auth/LoginForm.tsx
    - frontend/components/auth/AuthFormCard.tsx
    - frontend/components/auth/AuthPage.tsx
    - frontend/components/auth/SuccessOverlay.tsx
    - frontend/__tests__/auth/RegisterForm.test.tsx
    - frontend/__tests__/auth/LoginForm.test.tsx
    - frontend/messages/en.json
    - frontend/messages/zh.json
  </read_first>
  <action>
1. **Update `frontend/components/auth/RegisterForm.tsx`**:
   - Add `useState<boolean>` for `emailSent` state (default false)
   - Change `onSubmit` success handler: instead of calling `onRegisterSuccess()`, set `emailSent = true`
   - When `emailSent` is true: render a "Check your email" UI (similar to ForgotPasswordForm success state) — Mail icon from lucide-react, heading "Check your email", description "We've sent a confirmation link to your email. Click it to activate your account.", "Back to sign in" button that calls `onSwitchToLogin()`
   - Keep `onRegisterSuccess` prop in interface for backward compatibility but it is now unused (can remove or keep as optional `onRegisterSuccess?: () => void`)
   - Comment in the onSubmit handler: `// Email confirmation enabled -- show check-email UI instead of auto-redirect`

2. **Update `frontend/components/auth/LoginForm.tsx`**:
   - Add `onSwitchToForgotPassword: () => void` to `LoginFormProps`
   - Replace the "Forgot password?" button's onClick handler: change from `toast(...)` to `onSwitchToForgotPassword()`
   - Remove the sonner `toast` import if no longer used in this component
   - Check auth page error param: if `searchParams` has `error=confirmation_failed`, show a message. Actually, since LoginForm doesn't have access to searchParams directly, handle this in AuthPage instead (see step 5).

3. **Update `frontend/components/auth/AuthFormCard.tsx`**:
   - Extend mode type: `"login" | "register" | "forgot-password" | "reset-password"`
   - Update `AuthFormCardProps` accordingly
   - Add ForgotPasswordForm and UpdatePasswordForm imports
   - Add two more AnimatePresence cases for the new modes:
     ```tsx
     {mode === "forgot-password" && (
       <motion.div key="forgot-password" ...>
         <ForgotPasswordForm onSwitchToLogin={() => onSwitchMode("login")} />
       </motion.div>
     )}
     {mode === "reset-password" && (
       <motion.div key="reset-password" ...>
         <UpdatePasswordForm onSwitchToLogin={() => onSwitchMode("login")} />
       </motion.div>
     )}
     ```
   - Pass `onSwitchToForgotPassword={() => onSwitchMode("forgot-password")}` to LoginForm

4. **Update `frontend/components/auth/AuthPage.tsx`**:
   - Extend mode type to: `"login" | "register" | "forgot-password" | "reset-password"`
   - Update initial mode detection from searchParams: `mode=register` -> register, `mode=reset-password` -> reset-password, `mode=forgot-password` -> forgot-password, default -> login
   - The SuccessOverlay is now only shown for the `register` mode when emailSent (handled inside RegisterForm), so `showSuccess` state and SuccessOverlay can be removed from AuthPage (RegisterForm now handles its own success UI internally)
   - Remove `setShowSuccess(true)` callback and `<SuccessOverlay>` JSX
   - Update `handleSwitchMode` to handle all 4 modes in URL params
   - If searchParams has `error=confirmation_failed`, show a toast notification on mount (useEffect): `toast.error(t("auth.errors.confirmationFailed"))` using sonner

5. **Update `frontend/messages/en.json`** — Add these keys under `auth`:
   ```json
   "forgotPassword": {
     "title": "Reset your password",
     "subtitle": "Enter your email and we'll send you a reset link",
     "emailLabel": "Email",
     "emailPlaceholder": "you@uni.sydney.edu.au",
     "submitButton": "Send Reset Link",
     "backToLogin": "Back to sign in",
     "successTitle": "Check your email",
     "successMessage": "We've sent a password reset link to your email address."
   },
   "updatePassword": {
     "title": "Set new password",
     "subtitle": "Enter your new password below",
     "passwordLabel": "New Password",
     "passwordPlaceholder": "At least 8 characters",
     "confirmLabel": "Confirm New Password",
     "confirmPlaceholder": "Re-enter your new password",
     "submitButton": "Update Password",
     "successTitle": "Password updated!",
     "successMessage": "Your password has been updated. You can now sign in.",
     "backToLogin": "Back to sign in"
   },
   "checkEmail": {
     "title": "Check your email",
     "description": "We've sent a confirmation link to your email. Click it to activate your account.",
     "backToLogin": "Back to sign in"
   }
   ```
   Also add to `auth.errors`:
   ```json
   "resetFailed": "Failed to send reset email. Please try again.",
   "updatePasswordFailed": "Failed to update password. Please try again.",
   "confirmationFailed": "Email confirmation failed. The link may have expired. Please try again."
   ```
   Remove from `auth.errors`:
   ```json
   "forgotPasswordDemo": ...,
   "forgotPasswordDemoDesc": ...
   ```
   (These demo-mode keys are no longer needed since forgot-password is now real)

6. **Update `frontend/messages/zh.json`** — Add equivalent Chinese translations under `auth`:
   ```json
   "forgotPassword": {
     "title": "重置密码",
     "subtitle": "输入你的邮箱，我们将发送重置链接",
     "emailLabel": "邮箱",
     "emailPlaceholder": "you@uni.sydney.edu.au",
     "submitButton": "发送重置链接",
     "backToLogin": "返回登录",
     "successTitle": "检查你的邮箱",
     "successMessage": "我们已发送密码重置链接到你的邮箱。"
   },
   "updatePassword": {
     "title": "设置新密码",
     "subtitle": "在下方输入你的新密码",
     "passwordLabel": "新密码",
     "passwordPlaceholder": "至少 8 个字符",
     "confirmLabel": "确认新密码",
     "confirmPlaceholder": "再次输入你的新密码",
     "submitButton": "更新密码",
     "successTitle": "密码已更新！",
     "successMessage": "你的密码已更新，现在可以登录了。",
     "backToLogin": "返回登录"
   },
   "checkEmail": {
     "title": "检查你的邮箱",
     "description": "我们已发送确认链接到你的邮箱，点击链接激活你的账户。",
     "backToLogin": "返回登录"
   }
   ```
   Also add to `auth.errors`:
   ```json
   "resetFailed": "发送重置邮件失败，请重试。",
   "updatePasswordFailed": "更新密码失败，请重试。",
   "confirmationFailed": "邮箱确认失败，链接可能已过期，请重试。"
   ```
   Remove `forgotPasswordDemo` and `forgotPasswordDemoDesc` from zh.json too.

7. **Update `frontend/__tests__/auth/RegisterForm.test.tsx`**:
   - Update the test "calls register mutation and onRegisterSuccess on valid submit": after mutation success, it should now show "Check your email" text (from `auth.checkEmail.title` key) instead of calling `onRegisterSuccess`. Update the mock i18n map to include `auth.checkEmail.title` -> "Check your email" and `auth.checkEmail.description` and `auth.checkEmail.backToLogin`.
   - Add a new test: "shows check-email UI after successful registration" — submit valid form, verify "Check your email" text appears
   - The `onRegisterSuccess` callback should NOT be called anymore (or make it optional in the test)

8. **Update `frontend/__tests__/auth/LoginForm.test.tsx`**:
   - Add `onSwitchToForgotPassword` mock to the LoginForm render calls
   - Update the "shows forgot password toast on click" test: instead of expecting toast, it should call `onSwitchToForgotPassword`
   - Remove mock for sonner toast (if no longer needed by LoginForm)
   - Add i18n mock key if needed
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run __tests__/auth/RegisterForm.test.tsx __tests__/auth/LoginForm.test.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RegisterForm.tsx contains `emailSent` state variable
    - RegisterForm.tsx contains "Check your email" or references `auth.checkEmail.title` i18n key
    - RegisterForm.tsx does NOT call `onRegisterSuccess()` in onSubmit success handler
    - LoginForm.tsx has `onSwitchToForgotPassword` in its props interface
    - LoginForm.tsx does NOT import from `sonner`
    - AuthFormCard.tsx mode type includes `"forgot-password" | "reset-password"`
    - AuthFormCard.tsx imports `ForgotPasswordForm` and `UpdatePasswordForm`
    - AuthPage.tsx mode type includes `"forgot-password" | "reset-password"`
    - AuthPage.tsx does NOT render `<SuccessOverlay>` (removed)
    - en.json contains `auth.forgotPassword.title`
    - en.json contains `auth.updatePassword.title`
    - en.json contains `auth.checkEmail.title`
    - en.json contains `auth.errors.confirmationFailed`
    - en.json does NOT contain `auth.errors.forgotPasswordDemo`
    - zh.json contains `auth.forgotPassword.title`
    - zh.json contains `auth.updatePassword.title`
    - zh.json contains `auth.checkEmail.title`
    - All tests pass: `cd frontend && pnpm test -- --run __tests__/auth/RegisterForm.test.tsx __tests__/auth/LoginForm.test.tsx` exits 0
    - TypeScript compiles clean: `cd frontend && pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Auth page supports 4 modes (login, register, forgot-password, reset-password). RegisterForm shows "check your email" after signup instead of auto-redirect. LoginForm links to forgot-password. SuccessOverlay removed from AuthPage. All i18n keys added for both locales. All tests updated and passing.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm test -- --run` — All frontend tests pass (including updated RegisterForm and LoginForm tests)
2. `cd frontend && pnpm typecheck` — Zero TypeScript errors
3. `cd frontend && pnpm lint` — Zero ESLint warnings
4. `cd frontend && pnpm build` — Build succeeds
5. `grep -c 'forgotPasswordDemo' frontend/messages/en.json` — Returns 0 (demo keys removed)
6. `grep -c 'forgot-password' frontend/components/auth/AuthFormCard.tsx` — Returns > 0 (new mode supported)
</verification>

<success_criteria>
- ForgotPasswordForm and UpdatePasswordForm exist with proper form validation and success states
- RegisterForm shows "check your email" instead of auto-redirecting after signup
- LoginForm's "Forgot password?" navigates to forgot-password mode instead of showing demo toast
- AuthPage/AuthFormCard support 4 modes via URL search params
- SuccessOverlay removed from auth page (no longer needed)
- All i18n keys added for en and zh
- All tests pass, TypeScript and ESLint clean, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/32-production-email/32-02-SUMMARY.md`
</output>
