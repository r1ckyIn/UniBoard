---
phase: 32.1-sync-integration-fixes
plan: 01
subsystem: sync
tags: [canvas, course-linking, regex-filter, sync-fix-05, wave-1, tdd]

requires:
  - phase: 32.1
    plan: 00
    provides: test stubs + courses_with_shell.json fixture
provides:
  - Shell-course filter (_SHELL_COURSE_PATTERNS + _is_shell_course) blocking Final Exam/Concession/Supplementary Canvas courses at link_courses entry
  - Structured log event course_filtered_shell for observability
  - 3 strict-green unit tests + 1 new integration test covering the filter
  - VALIDATION.md frontmatter flipped to nyquist_compliant: true + wave_0_complete: true
affects: [32.1-02, 32.1-03, 32.1-04, 32.1-05]

tech-stack:
  added: []
  patterns:
    - "Entry-point filter pattern: shell courses dropped at link_courses() top so they never enter the (code, semester) index or appear in downstream sync results"
    - "structlog.get_logger() module-level logger matches existing src/services/*.py convention"
    - "Wave 1 xfail->strict flip: remove pytest.mark.xfail decorator + correct signature mismatches from Wave 0 stub"

key-files:
  created: []
  modified:
    - src/services/course_linking.py
    - tests/unit/test_sync_courses.py
    - tests/integration/test_course_linking.py
    - .planning/phases/32.1-sync-integration-fixes/32.1-VALIDATION.md

key-decisions:
  - "Corrected Wave 0 stub signature: link_courses takes (canvas_courses, ed_courses) only -- no user_id param. Plan's interface block was slightly wrong; real signature in source file is authoritative."
  - "Used canvas_name field on LinkedCourse (not 'name'); dataclass has no 'name' attribute, only course_code + canvas_name + ed_name."
  - "Used structlog.get_logger() without __name__ argument to match existing project convention across qa.py/ai_engine.py/roi.py/skill.py/intelligence.py."
  - "Appended test_shell_courses_filtered_integration to pre-existing tests/integration/test_course_linking.py (11 pre-existing assertions untouched) per plan instruction 'If the file is pre-existing and has real tests, keep them untouched and APPEND'."

patterns-established:
  - "Shell-course filter pattern: regex list + single any() helper + entry-point filter at link_courses top. Reusable if future SYNC-FIXes discover more shell patterns."

requirements-completed: [SYNC-FIX-05]

duration: 3min
completed: 2026-04-14
---

# Phase 32.1 Plan 01: SYNC-FIX-05 Shell-Course Filter Summary

**Canvas concession/final-exam shell courses now filtered at link_courses entry with 3 regex patterns; xfail stub flipped to strict-green with 3 unit tests + 1 integration test.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-14T04:32:47Z
- **Completed:** 2026-04-14T04:35:34Z
- **Tasks:** 2/2
- **Files modified:** 4 (1 source + 2 test + 1 VALIDATION)

## Accomplishments

- Wave 0 xfail stub on `test_filters_final_exam_shells` flipped to strict-green
- Added `_SHELL_COURSE_PATTERNS` + `_is_shell_course` helper to `src/services/course_linking.py`
- Shell filter applied at `link_courses` entry, before the Ed index is built -- zero chance of a shell course polluting downstream grades/deadlines/outlines sync
- Structured log event `course_filtered_shell` emits `canvas_course_id` + `name` for production observability (so we can spot-check post-deploy via Sentry/Railway logs)
- 3 unit tests in `tests/unit/test_sync_courses.py` all green
- 1 new integration test `test_shell_courses_filtered_integration` in `tests/integration/test_course_linking.py` green; 11 pre-existing integration tests untouched
- VALIDATION.md frontmatter now accurately reflects Wave 0 completion

## The 3 Regex Patterns

```python
_SHELL_COURSE_PATTERNS: list[re.Pattern[str]] = [
    re.compile(r"^final\s+exam\s+for:", re.IGNORECASE),  # "Final Exam for: COMP2017"
    re.compile(r"concession", re.IGNORECASE),            # "Concession Assessment for COMP3221"
    re.compile(r"^supplementary", re.IGNORECASE),        # "Supplementary Exam COMP3221"
]
```

## Task Commits

Atomic commits on `feature/gsd-32.1-sync-integration-fixes`:

1. **Task 1 RED** -- `cdc2c55` (test): flip xfail + add 2 additional tests (test_real_courses_pass_through, test_concession_case_insensitive)
2. **Task 1 GREEN** -- `04b6799` (fix): implement _SHELL_COURSE_PATTERNS/_is_shell_course + entry-point filter + integration test
3. **Task 2** -- `df17194` (docs): flip VALIDATION.md frontmatter flags

No REFACTOR commit needed -- GREEN implementation was already clean.

## Files Created/Modified

### Modified (Task 1 -- cdc2c55, 04b6799)

- `src/services/course_linking.py`
  - Added `import structlog` + `logger = structlog.get_logger()`
  - Added `_SHELL_COURSE_PATTERNS` (3 patterns)
  - Added `_is_shell_course(name: str) -> bool` helper
  - Added filter loop at top of `link_courses()` body with `course_filtered_shell` log event
- `tests/unit/test_sync_courses.py`
  - Removed `@pytest.mark.xfail(...)` decorator from `test_filters_final_exam_shells`
  - Fixed signature: `link_courses(canvas, ed_courses=[])` (no user_id)
  - Fixed field: `r.canvas_name` (LinkedCourse has no `name` attribute)
  - Added `test_real_courses_pass_through` (3 real courses all pass through)
  - Added `test_concession_case_insensitive` (8 names -- 7 shells across case variants, 1 real)
- `tests/integration/test_course_linking.py`
  - Added `import json` + `from pathlib import Path` + `FIXTURES` constant
  - Appended `test_shell_courses_filtered_integration` using `courses_with_shell.json` fixture
  - 11 pre-existing tests untouched

### Modified (Task 2 -- df17194)

- `.planning/phases/32.1-sync-integration-fixes/32.1-VALIDATION.md`
  - `nyquist_compliant: false` -> `nyquist_compliant: true`
  - `wave_0_complete: false` -> `wave_0_complete: true`

## Deviations from Plan

None in spirit -- all behaviors + artifacts + key-links specified in the plan frontmatter landed exactly as described.

Two minor adjustments where the plan's `<interfaces>` block did not match the actual source file:

1. **[Rule 1 - Spec drift] link_courses signature:** Plan described `link_courses(canvas_courses, ed_courses, user_id)` returning `LinkedCourse(code, name, semester, canvas_course_id, ed_course_id, ed_name, is_linked)`. Actual signature is `link_courses(canvas_courses, ed_courses)` and dataclass fields are `course_code`, `semester`, `canvas_course_id`, `ed_course_id`, `canvas_name`, `ed_name`, `is_linked`. Followed the plan's own fallback instruction ("read the file to confirm exact signature"). No production change needed -- only test-code updated to match real types.

2. **[Rule 1 - Spec drift] logger initialization:** Plan suggested `structlog.get_logger(__name__)`. Project convention (5 existing services: qa.py, ai_engine.py, roi.py, skill.py, intelligence.py) uses `structlog.get_logger()` without argument. Followed project convention.

No Rule 2/3/4 items. No auth gates.

## Acceptance Criteria Checklist

### Task 1

- [x] `grep -n "_SHELL_COURSE_PATTERNS" src/services/course_linking.py` returns 2 matches (line 18 def, line 32 usage)
- [x] `grep -n "_is_shell_course" src/services/course_linking.py` returns 2 matches (line 25 def, line 102 usage)
- [x] `grep -n "course_filtered_shell" src/services/course_linking.py` returns exactly 1 match (line 104)
- [x] `grep -c "xfail" tests/unit/test_sync_courses.py` returns 0
- [x] `uv run pytest tests/unit/test_sync_courses.py::test_filters_final_exam_shells -q` passes (not xfailed)
- [x] `uv run pytest tests/unit/test_sync_courses.py -q` shows 3 passing tests
- [x] `uv run pytest tests/integration/test_course_linking.py -q` passes (12 tests incl. new one)
- [x] `grep -n "test_shell_courses_filtered_integration" tests/integration/test_course_linking.py` returns 1 match
- [x] `uv run ruff check src/services/course_linking.py` exits 0
- [x] `uv run mypy --strict src/services/course_linking.py` exits 0

### Task 2

- [x] `nyquist_compliant: true` present in VALIDATION.md frontmatter
- [x] `wave_0_complete: true` present in VALIDATION.md frontmatter
- [x] `nyquist_compliant: false` absent from VALIDATION.md frontmatter
- [x] `wave_0_complete: false` absent from VALIDATION.md frontmatter

## Verification Evidence

### Final targeted run (Plan verification step 1)
```
$ uv run pytest tests/unit/test_sync_courses.py tests/integration/test_course_linking.py -q
15 passed in 0.21s
```

### Ruff (verification step 2)
```
$ uv run ruff check src/services/course_linking.py tests/unit/test_sync_courses.py tests/integration/test_course_linking.py
All checks passed!
```

### Mypy strict (verification step 3)
```
$ uv run mypy --strict src/services/course_linking.py
Success: no issues found in 1 source file
```

### Full unit suite sanity (verification step 4)
```
$ uv run pytest tests/unit/ -q --timeout=30
316 passed, 7 xfailed, 23 warnings, 26 errors in 24.19s
```
7 xfailed = remaining Wave 0 stubs awaiting Waves 2/3 (test_sync_grades, test_sync_deadlines, test_sync_ed_link, test_unit_outline_url_resolver x2, test_unit_outline_parser_fallbacks x2). Expected -- these are flipped by downstream plans.

## Deferred Issues

26 errors in full unit suite (`tests/unit/test_notification_service.py::*` + `tests/unit/test_risk_alert_service.py::*`) are **pre-existing asyncpg connection errors** unrelated to this plan's changes. These tests require a running Postgres integration harness (see Phase 24 decision on dual auto-skip for db-marker tests). Not introduced by Plan 32.1-01; not in scope for this plan. Tracked here for awareness only.

`tests/unit/test_sync_courses.py` (this plan's target) has zero regressions.

## SQL Probe (Post-Deploy Verification)

After the next production sync runs, operator can verify the filter took effect via:

```sql
SELECT id, name FROM courses
WHERE user_id = '<test-user-uuid>'
  AND (
    name ILIKE 'Final Exam for:%'
    OR name ILIKE '%concession%'
    OR name ILIKE 'Supplementary%'
  );
-- Expected: 0 rows
```

If rows return, either the filter isn't live yet (pre-deploy DB snapshot) or there's a code path that skips `link_courses`. Check Railway logs for the `course_filtered_shell` event to confirm the filter is firing.

## Next Step

Execute Wave 2 (Plan 32.1-02 Unit Outline URL resolver + parser fallbacks, and Plan 32.1-03 grades submission.score) in parallel -- neither depends on the other and both were gated on Wave 0 fixtures + Wave 1 clean-course stream which are now both landed.

## Self-Check: PASSED

- `src/services/course_linking.py` FOUND (modified)
- `tests/unit/test_sync_courses.py` FOUND (modified)
- `tests/integration/test_course_linking.py` FOUND (modified)
- `.planning/phases/32.1-sync-integration-fixes/32.1-VALIDATION.md` FOUND (modified)
- Commit `cdc2c55` FOUND
- Commit `04b6799` FOUND
- Commit `df17194` FOUND
