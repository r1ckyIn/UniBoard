---
phase: 33-token-lifecycle-onboarding
task: discuss
total_tasks: 4
status: paused
last_updated: 2026-04-14T11:04:47.170Z
---

<current_state>
Mid `/gsd:discuss-phase 33`. Branch `feature/gsd-33-token-lifecycle-onboarding`
is already created off main (2a38e62). Loaded prior context, scouted codebase,
identified 6 gray areas. Presented A-F to user and asked which to deep-dive.
User paused before answering — wants to handle Phase 32-03 email blocker first.
</current_state>

<completed_work>

- Read PROJECT.md, REQUIREMENTS.md, STATE.md
- Scanned prior CONTEXT.md files (21, 24, 27, 28, 29)
- Confirmed Phase 33 has no existing CONTEXT.md, no plans
- Scouted codebase: found `check_token_health()` in src/sync/scheduled.py (Phase 17 — in-app only, no email yet), 8 setup components in frontend/components/setup/, profiles table token status fields, Resend SMTP from Phase 32-03
- Mapped 3 sub-domains (EMAIL-03, ONBD-01, ONBD-02) to 6 gray areas A-F
- Carried forward memory items: project_sync_progress_ux.md (per-domain SuccessStep), project_ux_improvements_backlog.md (TokenStep skip re-validate), project_ai_summary_direction.md (precise concise content)
</completed_work>

<remaining_work>

- User picks which of A-F to discuss (A=trigger timing/freq, B=email content, C=onboarding flow, D=tone, E=token invalid UX, F=API failure UX)
- Deep-dive each selected area — capture decisions
- Write `.planning/phases/33-token-lifecycle-onboarding/33-CONTEXT.md`
- Commit and route to `/gsd:plan-phase 33`
</remaining_work>

<decisions_made>

- Branch first, then discuss (followed gsd-branch-setup before discuss-phase)
- Phase 32 marked partial (2/3) instead of complete — see PR #72; Phase 33 does NOT depend on Phase 32-03 unblock, so can proceed in parallel
- Excluded scope creep: token auto-refresh (Canvas/Ed don't support refresh tokens), email preferences UI (belongs in Phase 36)
</decisions_made>

<blockers>

- Not blocking Phase 33 itself, but user wants to address Phase 32-03 email confirmation (Mimecast quarantine of uniboard.uk emails for USYD recipients) before continuing this discuss. Retest trigger 2026-04-16+, recorded in memory project_phase32_email_retest.md.
</blockers>

<context>
Discuss workflow runs interactively. The 6 gray areas were tailored to the
existing codebase (not generic):

- A: Trigger timing/frequency for email reminders — pre-expiry vs post-expiry, cadence, vs in-app
- B: Email content — HTML vs text, deep-link to /setup, video/screenshot
- C: Onboarding flow structure — 5 steps preserved? add skip? per-domain progress?
- D: Tone — serious vs friendly, brand decoration, demo mode
- E: Token invalid UX — last validated time, troubleshoot checklist
- F: API/Sync failure UX — retry strategy, partial success display, TokenStep re-validate skip

Existing assets to extend (not replace):

- src/sync/scheduled.py::check_token_health — already runs every 30 min, currently creates only in-app notifications. EMAIL-03 needs to also send via Resend.
- frontend/components/setup/{Welcome,Token,Tutorial,Sync,Success}Step.tsx — onboarding skeleton exists, polish via ONBD-01.
- Resend SMTP works for direct transactional sends even though Supabase Auth confirmation is OFF.
</context>

<next_action>
Re-present the 6 gray areas (A-F) to the user as a numbered/lettered list and
wait for their selection. Do NOT regenerate the analysis — it's already done.
The full text of the 6 areas is preserved in the conversation history before
the pause. If conversation history is lost, re-run gray area identification
from this file's `<context>` section as the seed.
</next_action>
