---
phase: 33-token-lifecycle-onboarding
task: discuss
total_tasks: 4
status: paused
last_updated: 2026-04-14T11:30:00.000Z
---

<current_state>
Mid `/gsd:discuss-phase 33`. Branch `feature/gsd-33-token-lifecycle-onboarding`
is already created off main (2a38e62). Loaded prior context, scouted codebase,
identified 6 gray areas (A-F). Presented to user; user then paused to handle
Phase 32-03 email investigation. That investigation produced a strategy shift
that REWROTE Phase 33 scope — see updated_scope below.

Phase 32 is now closed (3/3) strategically; recipient placement handed to
Phase 33 AUTH-HARDEN.
</current_state>

<completed_work>

- Read PROJECT.md, REQUIREMENTS.md, STATE.md
- Scanned prior CONTEXT.md files (21, 24, 27, 28, 29)
- Confirmed Phase 33 has no existing CONTEXT.md, no plans
- Scouted codebase: found `check_token_health()` in src/sync/scheduled.py (Phase 17 — in-app only, no email yet), 8 setup components in frontend/components/setup/, profiles table token status fields, Resend SMTP from Phase 32-03
- Mapped 3 sub-domains (EMAIL-03, ONBD-01, ONBD-02) to 6 gray areas A-F
- Carried forward memory items: project_sync_progress_ux.md (per-domain SuccessStep), project_ux_improvements_backlog.md (TokenStep skip re-validate), project_ai_summary_direction.md (precise concise content)
</completed_work>

<remaining_work>

- User picks which of A-F to discuss (A=trigger timing/freq, B=email content, C=onboarding flow, D=tone, E=token invalid UX, F=API failure UX)
- Deep-dive each selected area — capture decisions
- Write `.planning/phases/33-token-lifecycle-onboarding/33-CONTEXT.md`
- Commit and route to `/gsd:plan-phase 33`
</remaining_work>

<decisions_made>

- Branch first, then discuss (followed gsd-branch-setup before discuss-phase)
- Phase 32 marked partial (2/3) instead of complete — see PR #72; Phase 33 does NOT depend on Phase 32-03 unblock, so can proceed in parallel
- Excluded scope creep: token auto-refresh (Canvas/Ed don't support refresh tokens), email preferences UI (belongs in Phase 36)
</decisions_made>

<blockers>

- Not blocking Phase 33 itself, but user wants to address Phase 32-03 email confirmation (Mimecast quarantine of uniboard.uk emails for USYD recipients) before continuing this discuss. Retest trigger 2026-04-16+, recorded in memory project_phase32_email_retest.md.
</blockers>

<context>
Discuss workflow runs interactively. The 6 gray areas were tailored to the
existing codebase (not generic):

- A: Trigger timing/frequency for email reminders — pre-expiry vs post-expiry, cadence, vs in-app
- B: Email content — HTML vs text, deep-link to /setup, video/screenshot
- C: Onboarding flow structure — 5 steps preserved? add skip? per-domain progress?
- D: Tone — serious vs friendly, brand decoration, demo mode
- E: Token invalid UX — last validated time, troubleshoot checklist
- F: API/Sync failure UX — retry strategy, partial success display, TokenStep re-validate skip

Existing assets to extend (not replace):

- src/sync/scheduled.py::check_token_health — already runs every 30 min, currently creates only in-app notifications. EMAIL-03 needs to also send via Resend.
- frontend/components/setup/{Welcome,Token,Tutorial,Sync,Success}Step.tsx — onboarding skeleton exists, polish via ONBD-01.
- Resend SMTP works for direct transactional sends even though Supabase Auth confirmation is OFF.
</context>

<next_action>
Re-enter `/gsd:discuss-phase 33` with the EXPANDED scope below (not the
original 6 gray areas). Present the new scope summary, then ask which areas
to deep-dive. Several areas are now low-ambiguity decisions made by the
user — confirm them quickly and move on.
</next_action>

<updated_scope>

Phase 33 absorbed AUTH-HARDEN work after Phase 32 closeout (2026-04-14).
ROADMAP.md and REQUIREMENTS.md reflect the expanded requirement list:

- EMAIL-03 (redesigned): Token expiry — in-app first; 14-day absence-triggered
  recall email as backup
- ONBD-01: Onboarding polish + per-domain sync progress
- ONBD-02: Setup edge cases + TokenStep skip re-validate
- AUTH-HARDEN-01: Sign in with Google OAuth (USYD Google Workspace)
- AUTH-HARDEN-02: Registration page USYD-specific banner
- AUTH-HARDEN-03: RegisterForm Resend email button + 60s cooldown
- AUTH-HARDEN-04: Document permanent confirmation OFF (largely
  documentation; coordinates with PROJECT.md and onboarding copy)

Locked decisions (do not re-debate; surface as `<decisions>` in CONTEXT.md):

- Email is FALLBACK channel, not primary. In-app + Push (Phase 35) are primary.
- Google OAuth is the recommended primary auth path for USYD users.
- Email confirmation is permanently OFF in Supabase.
- Recall email (14d absence) content must be high-value (e.g. "You have N
  deadlines waiting") not bare "your token expired" — engagement-positive.

Open gray areas to discuss (revised from original 6):

- A. Recall-email trigger + content shape (EMAIL-03)
- B. Onboarding flow structure (ONBD-01) — keep 5 steps? add Google OAuth
  shortcut button on first step? per-domain progress visualization
- C. Setup edge case copy + retry strategy (ONBD-02)
- D. Google OAuth UX (AUTH-HARDEN-01) — placement (login + register +
  setup?), Connect existing account flow, account-merge policy when a USYD
  user has both password and OAuth credentials
- E. USYD banner copy + placement (AUTH-HARDEN-02)
- F. Resend email button copy + cooldown UX (AUTH-HARDEN-03) — show
  countdown? success toast?

</updated_scope>

<resume_prompt>
When resuming with `/gsd:resume-work` or by re-invoking discuss-phase 33,
read this file first, then say:

"Phase 33 scope expanded after Phase 32 closeout. ROADMAP and REQUIREMENTS
already updated. Locked decisions are in `<updated_scope>`. We have 6 areas
to deep-dive (A-F, revised). Which would you like to discuss?"

Do NOT re-spawn research agents or rebuild the gray-area list — they're
already in this file.
</resume_prompt>
