---
phase: 38.1-prefetch-consumer-parity
verified: 2026-04-21T00:58:00Z
status: human_needed
score: 4/6 automatable criteria verified (2 HUMAN_NEEDED by design — naked-eye production UAT)
overrides_applied: 0
re_verification:
  previous_status: null
  previous_score: null
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Hard-refresh (Cmd+Shift+R) each of Dashboard, Courses, Deadlines, Predict, Digest, Timetable on https://uniboard.r1cky.dev (zh-CN locale) after the 38.1 merge deploys to Vercel production. Observe first paint with naked eye."
    expected: "Zero SkeletonCard visible above the fold on any of the 6 pages. All cards (ProfileCard, RecentActivity, DigestHistoryCard, TimetableGrid, etc.) show real data on first paint. No cards flashing from skeleton to content."
    why_human: "Success Criterion #1 is literally a naked-eye production observation. Automated coverage exists at the static-invariant level (prefetch-consumer-parity.test.ts GREEN 8/8) + unit test level (dashboard-prefetch.test.ts GREEN 3/3), but the naked-eye pixel observation on warm production Railway requires a human. Playwright pixel-diff baselines (Phase 38-04 Task 5) are still gated on separate Supabase credential checkpoint."
  - test: "Phase 38 HUMAN-UAT #1 (cached-auth revisit skeleton flash, 6 pages) flip from `pending` to `passed` in .planning/phases/38-first-load-performance/38-HUMAN-UAT.md after the above observation succeeds"
    expected: "File .planning/phases/38-first-load-performance/38-HUMAN-UAT.md Test #1 `result: [pending]` updates to `result: pass` with timestamp + observing commit SHA. Parent Phase 38 VERIFICATION.md Truth #2 status transitions from HUMAN_NEEDED to VERIFIED."
    why_human: "Success Criterion #6 is a bookkeeping flip that can only be made after Success Criterion #1 is confirmed by a human. Cannot be automated — requires the human UAT outcome as input."
---

# Phase 38.1: Server-Prefetch ↔ Client-Consumer Parity — Verification Report

**Phase Goal:** Close the Phase 38 HUMAN-UAT Truth #2 gap — eliminate SkeletonCard flash on hard-refresh of any `force-dynamic` dashboard page by aligning each page's `run()` prefetch list with the full set of client `useQuery*` consumers it renders.

**Verified:** 2026-04-21T00:58:00Z
**Status:** human_needed
**Re-verification:** No — initial verification
**Base commit (planning):** 214db28
**Head commit:** f852656

## Goal Achievement

### Observable Truths (6 Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Hard-refresh each of Dashboard / Courses / Deadlines / Predict / Digest / Timetable — zero SkeletonCard visible above the fold (naked eye, production) | HUMAN_NEEDED | All 6 `force-dynamic` page.tsx files confirmed via grep to prefetch 100% of the client consumer's useQuery hooks (Truth #2 VERIFIED below). The static invariant test proves parity at source level. The naked-eye pixel observation on production Railway is the remaining step — see human_verification item #1. |
| 2 | All `force-dynamic` page.tsx prefetch lists cover 100% of client-side `useQuery*` consumers | VERIFIED | `prefetch-consumer-parity.test.ts` runs 6 per-page parity assertions + 1 no-unknown-bucket guard + 1 waterfall invariant (8 total). Full run output: `Test Files 1 passed (1) / Tests 8 passed (8) / Duration 1.76s`. Each page's factory references confirmed via grep: Dashboard (userOptions.me, notificationOptions.list, alertOptions.list, courseOptions.list, courseOptions.detail, deadlineOptions.upcoming, gpaOptions.report, studyRecOptions.latest), Predict (userOptions.me, studyRecOptions.latest, gpaOptions.report, courseOptions.detail, roiOptions.course), Timetable (courseOptions.list, courseOptions.detail, timetableOptions.sessions, timetableOptions.weeks, deadlineOptions.list), Digest (digestOptions.latest, digestOptions.history), Courses (courseOptions.list), Deadlines (courseOptions.list, deadlineOptions.list). |
| 3 | Static invariant test added — every client `useQuery` consumed by a `force-dynamic` page has a matching `prefetchQuery`/`fetchQuery` in the page's `run` body — fails CI if a future regression drifts | VERIFIED | File created: `frontend/__tests__/rsc/prefetch-consumer-parity.test.ts` (266 lines). Harness covers all 6 pages with PAGES array + HOOK_TO_FACTORY (13 entries) + MUTATION_HOOKS (10 entries) + IGNORED_HOOKS (32 entries). Includes no-unknown-bucket audit + the preserved waterfall invariant. Two documented executor deviations vs PATTERNS.md §7 (stripComments pre-pass + FACTORY_TO_HOOK reverse scan for useQueries inlined factory detection) are correctness fixes — both documented in test file header and 38.1-01-SUMMARY.md. Picked up automatically by vitest config `include: ["__tests__/**/*.test.{ts,tsx}"]` → runs in CI. |
| 4 | No regression in automated tests (vitest + Playwright smoke) — sidebar navigation warm-path performance preserved | VERIFIED | `pnpm vitest run __tests__/rsc/` → 3 test files, 16 tests, all passing (parity 8/8 + dashboard-prefetch 3/3 + create-prefetched-page 5/5). `pnpm typecheck` → exit 0. `pnpm lint --max-warnings 0` → exit 0. Pre-existing `__tests__/**` next-intl test-harness failures (CourseDetailPage, DeadlineCard, DeadlinesPage, AppShell, SetupGuard) are documented in `deferred-items.md` as baseline — not regressions from 38.1. Playwright smoke run is out of scope for 38.1 (Phase 38-04 credential-gated checkpoint). |
| 5 | Sentry tag convention preserved (`phase: "38"`, `operation: "rsc_prefetch"`, `query: <label>`) for all new prefetches | VERIFIED | All new prefetches use `wrapSentry` helper imported from `@/lib/rsc/create-prefetched-page` (frozen since Phase 38). Grep confirms `phase: "38"` is the only phase tag in the codebase — zero `phase: "38.1"` strings anywhere. New labels follow Phase 38 kebab-case convention: Dashboard adds `"users"`, `"notifications"`, `"alerts"`; Digest adds `"digest-latest"`, `"digest-history"` (disambiguated per Phase 38 precedent `deadlines-upcoming` / `deadlines-list`); Predict adds `"users"`, `"study-rec"`; Timetable adds `"course-detail"` (with `{ courseId: id }` extra for per-id Sentry aggregation). No new tag taxonomy introduced. |
| 6 | Phase 38 HUMAN-UAT #1 (skeleton-flash on cached-auth revisit) flips to `passed` post-deploy | HUMAN_NEEDED | Blocked on Truth #1 completion. `.planning/phases/38-first-load-performance/38-HUMAN-UAT.md` Test #1 currently `result: [pending]`. Automated parity invariant is GREEN — the remaining gap is the naked-eye production observation. See human_verification item #2. |

**Score:** 4 automatable criteria VERIFIED / 2 HUMAN_NEEDED by design (naked-eye production UAT + its bookkeeping follow-up) = **4/6 verified**, with zero FAILED.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/__tests__/rsc/prefetch-consumer-parity.test.ts` | Static-invariant test covering 6 pages + no-unknown-bucket + waterfall | VERIFIED | 266 lines. PAGES (6 entries) + HOOK_TO_FACTORY (13) + FACTORY_TO_HOOK (auto-derived reverse map) + MUTATION_HOOKS (10) + IGNORED_HOOKS (32) + stripComments helper + 8 assertions. All 8 pass. |
| `frontend/app/[locale]/(dashboard)/page.tsx` | Dashboard RSC with 7 prefetchQuery + 1 awaited fetchQuery covering all 8 DashboardPage.tsx data consumers | VERIFIED | 172 lines. 7 prefetchQuery calls (courses, gpa, study-rec, users/me, notifications, alerts, conditional course-detail). 1 awaited fetchQuery (deadlines/upcoming for waterfall collapse). All 8 factories referenced via grep (alertOptions.list, courseOptions.detail, courseOptions.list, deadlineOptions.upcoming, gpaOptions.report, notificationOptions.list, studyRecOptions.latest, userOptions.me). Waterfall invariant preserved (≤ 2). |
| `frontend/app/[locale]/(dashboard)/digest/page.tsx` | Digest RSC with 2 prefetches (latest + history) via Promise.allSettled | VERIFIED | 67 lines. Converted from single-await to `Promise.allSettled([...])` over 2 entries. digestOptions.latest() + digestOptions.history() both referenced (no-arg calls matching consumer). Inner `<Suspense>` preserved. Labels `digest-latest`, `digest-history`. |
| `frontend/app/[locale]/(dashboard)/predict/page.tsx` | Predict RSC with 2 hoisted top-level prefetches + N-fanout preserved | VERIFIED | 133 lines. `topLevelPrefetches` array (userOptions.me + studyRecOptions.latest) hoisted outside `if (courseIds.length > 0)` guard. Both branches (if + else) await `Promise.allSettled` that includes topLevelPrefetches — gpa-failure does not skip user-me/study-rec hydration. Existing N-fanout (courseOptions.detail × N + roiOptions.course × N) preserved verbatim. Labels: `users`, `study-rec`, `course-detail` (with courseId extra), `roi` (with courseId extra). Awaited fetchQuery = 1 (gpa). |
| `frontend/app/[locale]/(dashboard)/timetable/page.tsx` | Timetable RSC restructured to await courses.list → N × courseOptions.detail fanout + 3 top-level parallel | VERIFIED | 121 lines. Step 1: `await queryClient.fetchQuery(courseOptions.list())` → `courseIds = coursesResp.data.map((c) => c.id)` with try/catch+wrapSentry("courses"). Step 2: `Promise.allSettled` over 3 top-level (timetable-sessions, timetable-weeks, deadlines-list) + `...courseIds.map((id) => prefetchQuery(courseOptions.detail(id), { courseId }))`. No `roiOptions` present (Predict-specific). `<Suspense>` preserved. Awaited fetchQuery = 1 (courses.list). |
| `frontend/app/[locale]/(dashboard)/courses/page.tsx` | Audit-only — no change required | VERIFIED (unchanged) | 46 lines. Single-query baseline preserved from Phase 38. CoursesPage.tsx consumes only useCourses → courseOptions.list is already prefetched. No diff vs Phase 38 baseline. |
| `frontend/app/[locale]/(dashboard)/deadlines/page.tsx` | Audit-only — no change required | VERIFIED (unchanged) | 62 lines. 2-query parallel (courseOptions.list + deadlineOptions.list) preserved from Phase 38. DeadlinesPage.tsx consumes useDeadlines → deadlineOptions.list; courseOptions.list is cross-page cache warm (documented in header comment). No diff vs Phase 38 baseline. |
| `frontend/lib/rsc/create-prefetched-page.tsx` | Frozen HOF — unchanged | VERIFIED | `git diff 214db28..HEAD -- frontend/lib/rsc/create-prefetched-page.tsx` returns empty. Phase 38.1 edits the contents of `run` callbacks only; helper is untouched. |
| `frontend/lib/rsc/server-query-fn.ts` | Frozen helper — unchanged | VERIFIED | No diff vs planning commit. |
| `frontend/lib/query/server.ts` | Frozen factory — unchanged | VERIFIED | No diff vs planning commit. |
| `frontend/components/Sidebar.tsx` | Phase 38 Sidebar — unchanged | VERIFIED | Not in `git diff 214db28..HEAD --name-only`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `frontend/app/[locale]/(dashboard)/page.tsx` | `userOptions.me()` / `notificationOptions.list()` / `alertOptions.list()` | prefetchQuery spread with queryFn override + wrapSentry.catch | WIRED | Lines 102-122. All 3 use `{ ...factory(), queryFn: () => api.get("<endpoint>").json<T>() }` + `.catch(wrapSentry("<label>", userId))`. Endpoints: `users/me`, `notifications` (no args matches consumer's no-arg call), `alerts`. |
| `frontend/app/[locale]/(dashboard)/digest/page.tsx` | `digestOptions.history()` | Promise.allSettled entry parallel with digestOptions.latest() | WIRED | Line 54-62. No-arg call matches `useDigestHistory()` consumer at DigestPage.tsx:67. Label `digest-history`. Endpoint `digest/history`. |
| `frontend/app/[locale]/(dashboard)/predict/page.tsx` | `userOptions.me()` + `studyRecOptions.latest()` | topLevelPrefetches array hoisted outside courseIds guard | WIRED | Lines 84-98. Both branches of `if (courseIds.length > 0) { ... } else { ... }` await `Promise.allSettled([...topLevelPrefetches, ...])`. Confirms hoist per PATTERNS.md §5 fact #7. |
| `frontend/app/[locale]/(dashboard)/timetable/page.tsx` | `N × courseOptions.detail(id)` | courseIds.map in Step 2 Promise.allSettled + awaited courses.list in Step 1 | WIRED | Lines 72-80 (Step 1 awaited fetchQuery + data.map → courseIds). Lines 107-115 (N-fanout map with wrapSentry("course-detail", userId, { courseId: id })). `.data.map((c) => c.id)` confirms correct source shape from CoursesResponse envelope. |
| Every new prefetchQuery | `wrapSentry(<label>, userId[, { courseId }])` | shared frozen helper from @/lib/rsc/create-prefetched-page | WIRED | wrapSentry call counts confirmed: Dashboard 12, Predict 7, Timetable 7, Digest 3, Courses 3, Deadlines 3 — all import from the single frozen source. No hand-rolled Sentry.captureException call sites added. |

### Data-Flow Trace (Level 4)

| Page | Data Source | Produces Real Data | Status |
|------|-------------|--------------------|--------|
| Dashboard | `getServerApiClient(accessToken)` → Railway backend via `users/me`, `notifications`, `alerts`, etc. | YES (Phase 38 infrastructure verified; JWT injection via Authorization Bearer header at server-query-fn.ts) | FLOWING |
| Digest | Same server API path for `digest/latest` + `digest/history` | YES | FLOWING |
| Predict | Same server API path for `gpa`, `users/me`, `ai/study-recommendations`, `courses/{id}`, `courses/{id}/roi` | YES | FLOWING |
| Timetable | Same server API path for `courses`, `timetable/sessions`, `timetable/weeks`, `deadlines`, `courses/{id}` | YES | FLOWING |

No HOLLOW_PROP or DISCONNECTED artifacts. All consumer `useQuery` hooks read from the same queryKey the server populates (confirmed by spreading `{ ...xxxOptions.yyy() }` verbatim — never hand-rolled keys).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Parity invariant passes 8/8 | `cd frontend && pnpm vitest run __tests__/rsc/prefetch-consumer-parity.test.ts` | Test Files 1 passed (1) / Tests 8 passed (8) / Duration 1.76s | PASS |
| Dashboard waterfall regression preserved | `cd frontend && pnpm vitest run __tests__/rsc/dashboard-prefetch.test.ts` | Test Files 1 passed (1) / Tests 3 passed (3) | PASS |
| Full RSC suite regression-free | `cd frontend && pnpm vitest run __tests__/rsc/` | Test Files 3 passed (3) / Tests 16 passed (16) | PASS |
| TypeScript compiles | `cd frontend && pnpm typecheck` | exit 0 (no output) | PASS |
| ESLint clean | `cd frontend && pnpm lint --max-warnings 0` | exit 0 (no output) | PASS |
| Dashboard awaited fetchQuery count | Grep `await[^\n]*?\.fetchQuery\b` in page.tsx | 1 occurrence (deadlines/upcoming — Wave B) — preserved ≤ 2 per PERF-02 | PASS |
| Predict awaited fetchQuery count | Grep `await\s+queryClient\s*\n?\s*\.\s*fetchQuery` in predict/page.tsx | 1 occurrence (gpa — Step 1) | PASS |
| Timetable awaited fetchQuery count | Grep `await\s+queryClient\s*\n?\s*\.\s*fetchQuery` in timetable/page.tsx | 1 occurrence (courses.list — Step 1) | PASS |
| No `phase: "38.1"` tag regression | Grep `phase:\s*["']38` in frontend/ | Only `phase: "38"` found (4 occurrences, all in frozen HOF or HOF unit tests) | PASS |
| Frozen files unchanged | `git diff 214db28..HEAD -- frontend/lib/rsc/create-prefetched-page.tsx frontend/lib/rsc/server-query-fn.ts frontend/lib/query/server.ts frontend/components/Sidebar.tsx` | empty diff | PASS |
| Scope confined to planned files | `git diff 214db28..HEAD --name-only` | 12 files: 4 page.tsx modifications + 2 test files (1 new + 1 mock extension) + 5 planning docs + 1 STATE.md update | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PERF-01 | 38.1-01-PLAN, 38.1-02-PLAN, 38.1-03-PLAN | First paint shows real data on Dashboard/Courses/Deadlines/Predict/Digest/Timetable for cached-auth revisits (no SkeletonCard flash) | SATISFIED (pending naked-eye confirmation) | Static-invariant test proves 100% consumer parity at source level. Data flows confirmed through frozen Phase 38 infrastructure (getServerApiClient + HydrationBoundary + HOF). Naked-eye production observation is the final step (human_verification item #1). Completion requirement only — the original implementation landed in Phase 38. |

### Anti-Patterns Found

None. Grep scan across 4 modified page.tsx files returns no `TODO`, `FIXME`, `XXX`, `HACK`, `PLACEHOLDER`, hardcoded-empty-prop, or stubbed implementations. All new prefetches wire real `getServerApiClient(accessToken)` → Railway endpoints. No empty handlers, no `return null` patterns, no console.log-only implementations.

Deferred items in `deferred-items.md` (5 pre-existing test files failing with `No intl context found`) are documented as baseline unrelated to 38.1 — verified by Plan 03 executor stashing edits and running baseline on commit 9be33b2 before Task 2.

### Human Verification Required

#### 1. Naked-eye SkeletonCard flash absence on 6 pages (production, zh-CN, hard-refresh)

**Test:** Hard-refresh (Cmd+Shift+R) each of Dashboard / Courses / Deadlines / Predict / Digest / Timetable on the Vercel production URL after this phase ships (post-merge deploy). Use the zh-CN locale variant.

**Expected:** First paint shows real data on every card above the fold — no SkeletonCard shimmer visible to the naked eye on any page. In particular:
- Dashboard: ProfileCard + RecentActivity + stats row + deadlines timeline all show real values instantly
- Digest: DigestHistoryCard shows history list (not skeleton)
- Predict: UsernameChip + StudyRecCard + course rows all show real values
- Timetable: TimetableGrid course-detail tooltips + legend all show real data
- Courses + Deadlines: already fine (audit-only pages)

**Why human:** Success Criterion #1 is literally a naked-eye pixel observation on warm production Railway. Automated coverage (Playwright pixel-diff baselines at Phase 38-04 Task 5) is gated on a separate Supabase credential checkpoint and cannot run without live DB state. The static-invariant test GREEN is a strong proxy at source level, but the binding contract is "no SkeletonCard visible to naked eye on production."

#### 2. Flip Phase 38 HUMAN-UAT Truth #2 to passed

**Test:** After item #1 confirms zero skeleton flash, update `.planning/phases/38-first-load-performance/38-HUMAN-UAT.md` Test #1 `result: [pending]` → `result: pass` with observing commit SHA + timestamp. Update Phase 38 VERIFICATION.md Truth #2 status from HUMAN_NEEDED to VERIFIED.

**Expected:** Parent Phase 38 VERIFICATION score transitions from 7/10 → 8/10 once Truth #2 flips. Phase 38.1 phase officially closed.

**Why human:** Bookkeeping flip dependent on the outcome of item #1. Only a human can confirm the UAT outcome and update the audit trail.

### Gaps Summary

**No actionable gaps.** All automatable criteria pass. The two HUMAN_NEEDED items are explicitly scheduled naked-eye observations on production — by design for this gap-closure phase.

The static-invariant test is the forcing function: it GREEN-blocks regression at CI time. If any future PR adds a new consumer hook without prefetching, the test fails with a literal message pointing to the missing factory. This guards PERF-01 long-term and is the primary deliverable beyond the 4 page.tsx edits.

---

## Summary

**Goal achieved at source level.** All 6 force-dynamic page.tsx prefetch lists cover 100% of their client-side useQuery consumers (Truth #2 VERIFIED). The static-invariant test (Truth #3 VERIFIED) prevents future regression in CI. Sentry tag convention preserved (Truth #5 VERIFIED). No test regressions (Truth #4 VERIFIED). Frozen Phase 38 infrastructure (HOF, wrapSentry, getServerApiClient, getServerQueryClient, Sidebar) genuinely unchanged — verified via git diff against the planning baseline commit 214db28.

The remaining 2 criteria (Truths #1 and #6) are naked-eye production observations that can only be confirmed by the developer on the next deployed Vercel build. This is the expected shape for a skeleton-flash gap-closure phase — automation can prove parity exists, but the binding "no skeleton visible" contract requires human eyes.

**Commits covered in scope:**
- `7dc8eaf` — test(38.1-01): add failing prefetch-consumer-parity invariant (RED)
- `a0e684f` — feat(38.1-03): hoist userOptions.me + studyRecOptions.latest in Predict RSC
- `4405e93` — feat(38.1-02): add userOptions.me + notifications + alerts prefetches to Dashboard Wave A
- `9367b85` — feat(38.1-02): add digestOptions.history to Digest Promise.allSettled
- `aea80df` — feat(38.1-03): restructure Timetable RSC to Step 1 + N-fanout on course detail
- Plus 5 planning + tracking doc commits (non-code)

---

_Verified: 2026-04-21T00:58:00Z_
_Verifier: Claude (gsd-verifier, Opus 4.7 1M context)_
