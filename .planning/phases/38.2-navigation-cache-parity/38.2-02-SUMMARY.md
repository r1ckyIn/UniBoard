---
phase: 38.2
plan: 02
subsystem: infrastructure
tags:
  - github-actions
  - railway
  - warmup-cron
  - documentation
  - audit-trail
dependency_graph:
  requires:
    - "Phase 38-03 railway-warmup.yml infrastructure (shipped dormant)"
    - "Phase 38-03 railway-coldstart-measure.yml workflow"
    - "Phase 38-03 measure-coldstart.ts runner script"
    - "Phase 38-03 coldstart.spec.ts Playwright placeholder"
    - "Phase 38-03 coldstart-report.md stub"
    - "Phase 38 38-VERIFICATION.md Truth #1 annotation target"
  provides:
    - "Active Railway warmup cron (every 10 min) pinging /health"
    - "Correct /health endpoint across all 4 warmup/measurement files"
    - "Phase 38.2 activation decision record (coldstart-report.md)"
    - "SUPERSEDED footer preserving Phase 38 Truth #1 audit trail"
  affects:
    - "Phase 38 HUMAN-UAT item #2 (measurement still pending, now post-activation)"
    - "Phase 38.2 Plan 01 (coupled — force-dynamic reversal + warmup activation land together)"
tech_stack:
  added: []
  patterns:
    - "GH Actions schedule cron with 5-min safety margin under Railway idle threshold"
    - "env-var indirection for secret interpolation in GH Actions (T-38.2-07 mitigation)"
    - "Append-only markdown annotation preserving verbatim audit trail (D-12)"
key_files:
  created:
    - ".planning/phases/38.2-navigation-cache-parity/38.2-02-SUMMARY.md"
  modified:
    - ".github/workflows/railway-warmup.yml"
    - ".github/workflows/railway-coldstart-measure.yml"
    - "frontend/tests/e2e/perf/measure-coldstart.ts"
    - "frontend/tests/e2e/perf/coldstart.spec.ts"
    - ".planning/phases/38-first-load-performance/coldstart-report.md"
    - ".planning/phases/38-first-load-performance/38-VERIFICATION.md"
decisions:
  - "Activate warmup cron with `*/10 * * * *` (10 min) — preserves 5-min safety margin under Railway's 15-min idle timeout (CONTEXT D-06)"
  - "Endpoint fix applied to all 4 /healthz references (2 workflows + measure runner + Playwright spec) — RESEARCH R5 comprehensive fix (Option B)"
  - "Append-only annotation strategy for audit trail files: coldstart-report.md gets new Decision Record section below existing TBD rubric; 38-VERIFICATION.md gets footer paragraph below Observable Truths table, Truth #1 row preserved verbatim (D-07 + D-12)"
metrics:
  duration: "~20 minutes executor time"
  completed: "2026-04-22"
  commits: 3
  files_modified: 6
  tasks_completed: 3
  deviations: 0
---

# Phase 38.2 Plan 02: Infrastructure Activation + Documentation — Summary

**One-liner:** Activated dormant Railway warmup cron (every 10 min) + fixed pre-existing `/healthz → /health` bug across 4 files + annotated Phase 38 Truth #1 as SUPERSEDED with append-only audit-trail-preserving footer.

## Scope

Plan 02 is the **infrastructure + documentation** half of Phase 38.2. Runs in parallel with Plan 01 (frontend `force-dynamic` reversal + `loading.tsx` deletion + diagnostic cleanup) — zero `files_modified` overlap. The two plans are coupled by rationale: Plan 01 makes Railway cold-start latency user-visible again (by removing the `loading.tsx` Suspense fallback that previously masked it), Plan 02 activates the warmup that keeps that latency negligible.

## Tasks Executed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Activate warmup cron + /healthz → /health in railway-warmup.yml | `1a9bfbb` | `.github/workflows/railway-warmup.yml` |
| 2 | Fix /healthz → /health in 3 remaining files | `2c940b4` | `.github/workflows/railway-coldstart-measure.yml`, `frontend/tests/e2e/perf/measure-coldstart.ts`, `frontend/tests/e2e/perf/coldstart.spec.ts` |
| 3 | Append Phase 38.2 activation record + SUPERSEDED footer | `f948656` | `.planning/phases/38-first-load-performance/coldstart-report.md`, `.planning/phases/38-first-load-performance/38-VERIFICATION.md` |

All 3 commits use conventional-commit scope `38.2`, avoid `--amend` semantics, and preserve the per-task atomic granularity required by hunk-validation.

## Files Modified (6 files, +53 / -22 lines net)

### `.github/workflows/railway-warmup.yml` (-17 / +12 lines)
- Rewrote header comment block: `DISABLED BY DEFAULT` → `ACTIVE` framing
- Uncommented `schedule:` block (both lines lost leading `# `)
- Cron expression: `*/10 * * * *` — every 10 min
- Step name: `Ping /healthz` → `Ping /health`
- Curl target: `${RAILWAY_URL%/}/healthz` → `${RAILWAY_URL%/}/health`
- Comment body: replaced explicit `/healthz (404 on backend)` historical reference with endpoint-neutral phrasing so the file contains zero `/healthz` strings (acceptance criterion `! grep -q '/healthz'`)

### `.github/workflows/railway-coldstart-measure.yml` (-1 / +1 lines)
- Step name line: `Run N spaced samples against /healthz` → `Run N spaced samples against /health`

### `frontend/tests/e2e/perf/measure-coldstart.ts` (-1 / +1 lines)
- Line 35 endpoint constant: `…/healthz` → `…/health`

### `frontend/tests/e2e/perf/coldstart.spec.ts` (-3 / +3 lines)
- Line 15 comment: `…\`/healthz\`…` → `…\`/health\`…`
- Line 29 test name: `single sample to /healthz` → `single sample to /health`
- Line 32 endpoint constant: `…/healthz` → `…/health`
- `@ts-nocheck` + `test.skip(true, ...)` guards preserved (Phase 38-04 concern)

### `.planning/phases/38-first-load-performance/coldstart-report.md` (+21 lines)
Appended new "Decision Record — Phase 38.2 Activation (2026-04-22)" section between the existing `Follow-up (if warmup is enabled)` block and the `## Phase 38 Gate` header. Records:
- Decision: warmup cron ENABLED with cron `*/10 * * * *`
- Rationale: coupling to force-dynamic reversal
- Measurement status: formal p50/p95 run still pending (Phase 38 HUMAN-UAT item #2)
- Next checkpoint: 48 h re-measurement
Original TBD rubric + decision checkboxes preserved verbatim — additive record, not in-place edit.

### `.planning/phases/38-first-load-performance/38-VERIFICATION.md` (+12 lines)
Appended SUPERSEDED footer paragraph between the Observable Truths table and the Score summary line. Content:
- Documents that Truth #1's `force-dynamic` clause was reversed by Phase 38.2
- Forward-references `.planning/phases/38.2-navigation-cache-parity/38.2-VERIFICATION.md`
- Preserves the rest of Truth #1 (async Server Component, HOF usage, HydrationBoundary) as still-VERIFIED
Truth #1 row itself (including the `export const dynamic = "force-dynamic"` string) preserved verbatim per D-12 audit-trail requirement.

## Validation Results

| Gate | Result |
|------|--------|
| YAML validates — `railway-warmup.yml` | PASS (python3 yaml.safe_load) |
| YAML validates — `railway-coldstart-measure.yml` | PASS |
| Zero `/healthz` references in `.github/workflows/` + `frontend/tests/` | PASS (`! grep -rq '/healthz'`) |
| `schedule:` block uncommented | PASS (`grep -qE '^  schedule:$'`) |
| Cron `*/10 * * * *` present on active line | PASS |
| `pnpm typecheck` (frontend) | PASS (exit 0) |
| Playwright spec guards preserved (`@ts-nocheck` + `test.skip(true, ...)`) | PASS |
| Phase 38.2 Activation header added | PASS |
| Original coldstart-report TBD rubric preserved | PASS |
| `## Phase 38 Gate` header preserved | PASS |
| SUPERSEDED footer added to 38-VERIFICATION.md | PASS |
| Truth #1 `force-dynamic` text preserved verbatim | PASS |
| `**Score:**` line preserved | PASS |
| Forward reference to 38.2-VERIFICATION.md present | PASS |
| Scope confinement — no frontend src (app/lib/components/hooks) changes | PASS (zero matches) |

All 15 checks pass.

## Deviations from Plan

**None.** Plan execution was 1:1 with Plan 02's Option B scope and atomic-commit scheme.

One micro-adjustment was applied in Task 1: the plan's target YAML included a historical comment mentioning `/healthz (404 on backend)`. The plan acceptance criterion `! grep -q '/healthz' .github/workflows/railway-warmup.yml` requires zero `/healthz` substrings anywhere in the file (including comments). Rewrote that sentence using endpoint-neutral phrasing ("Phase 38-03 originally targeted the wrong endpoint") to satisfy the invariant while preserving the historical note. No semantic loss — the `/health` corrected-target is still explicitly named.

## Authentication Gates

None encountered. Plan 02 executes entirely in repo filespace with no external service auth required.

## Threat Model Verification

| Threat ID | Status | Mitigation Evidence |
|-----------|--------|---------------------|
| T-38.2-07 (warmup info disclosure) | MITIGATED | `curl ... > /dev/null` preserved; `env:` block indirects `NEXT_PUBLIC_API_URL`; step name no longer exposes endpoint variant via `/healthz` vs `/health` distinction (both now `/health`) |
| T-38.2-08 (DoS — rate limit / budget) | ACCEPTED | 10-min cron × 24 h = 144 runs/day × ~5s each ≈ 12 min/day, well under public-repo free-tier 2000 min/month |
| T-38.2-09 (regression — /healthz fix breaks measurement) | MITIGATED | YAML validates; pnpm typecheck passes; Playwright spec guards preserved |
| T-38.2-10 (Truth #1 tampering) | MITIGATED | Append-only edit verified: `grep -q 'export const dynamic = "force-dynamic"'` succeeds (verbatim preservation) |
| T-38.2-11 (unreviewed main commit) | ACCEPTED | Phase 38.2 ships as one PR per project convention; guard-branch hook enforces no direct main commits |
| T-38.2-12 (no activation audit log) | MITIGATED | Task 3 created explicit "Decision Record — Phase 38.2 Activation (2026-04-22)" section with date, rationale, next checkpoint |

## Post-Merge Human Validation (Deferred)

After this plan's PR squash-merges into `main`:

1. **GitHub → Actions → "Railway Warmup"** — first scheduled run should appear within ~15 min of merge (GH Actions cron jitter).
2. **Run logs:** should show `HTTP/2 200` response from `/health` (previously would have shown `404` from `/healthz`).
3. **Railway dashboard:** container CPU/network activity every ~10 min confirms warm state.
4. **Phase 38 HUMAN-UAT item #2** (cold-start measurement + p95 characterisation): still `pending` — can run post-activation to quantify improvement; 48 h wall-clock before meaningful re-measurement.

This validation is HUMAN-only, scheduled as item #2 of the Phase 38 verification.md `human_verification` list. Not part of Plan 02's automated verification scope.

## Parallel Execution Note

Plan 01 (frontend `force-dynamic` reversal) and Plan 02 (this plan) ran in parallel agents in the same wave. Zero file overlap:
- Plan 01 scope: `frontend/app/[locale]/(dashboard)/**`, `frontend/lib/rsc/create-prefetched-page.tsx`, potentially `frontend/app/[locale]/(dashboard)/loading.tsx` (delete)
- Plan 02 scope: `.github/workflows/**`, `frontend/tests/e2e/perf/**`, `.planning/phases/38-first-load-performance/**`

The two plans are architecturally coupled (Plan 01 exposes the cold-start latency that Plan 02's warmup activation hides) but ship as a single atomic PR per one-phase-one-PR convention.

## Known Stubs

None. No hardcoded empty values introduced; no placeholder UI text added; no TODO/FIXME markers left unresolved. The existing `TBD` placeholders in `coldstart-report.md` (10 sample rows + p50/p95 values + decision checkboxes) are **unchanged** — they belong to Phase 38-03's original measurement contract and intentionally remain pending (human_verification item #2). Plan 02 adds an adjacent activation record, not a replacement.

## Threat Flags

No new threat surface introduced. Scope additions:
- Warmup cron invokes an *existing* `/health` endpoint with an existing secret (`NEXT_PUBLIC_API_URL`) through existing env-var indirection — no new attack surface.
- Markdown doc annotations are repo-local audit artifacts with no runtime impact.

## Self-Check: PASSED

- `.planning/phases/38.2-navigation-cache-parity/38.2-02-SUMMARY.md` — **FOUND** (this file)
- Commit `1a9bfbb` (Task 1) — **FOUND**
- Commit `2c940b4` (Task 2) — **FOUND**
- Commit `f948656` (Task 3) — **FOUND**
- All 15 regression gates pass

---

*Plan 02 complete. STATE.md / ROADMAP.md updates deferred to orchestrator (per executor prompt).*
