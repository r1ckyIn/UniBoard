---
phase: 38.2-navigation-cache-parity
reviewed: 2026-04-22T17:50:00Z
depth: standard
files_reviewed: 16
files_reviewed_list:
  - frontend/app/[locale]/(dashboard)/page.tsx
  - frontend/app/[locale]/(dashboard)/timetable/page.tsx
  - frontend/app/[locale]/(dashboard)/courses/page.tsx
  - frontend/app/[locale]/(dashboard)/deadlines/page.tsx
  - frontend/app/[locale]/(dashboard)/predict/page.tsx
  - frontend/app/[locale]/(dashboard)/digest/page.tsx
  - frontend/app/[locale]/(dashboard)/loading.tsx
  - frontend/next.config.ts
  - frontend/lib/rsc/create-prefetched-page.tsx
  - frontend/__tests__/rsc/prefetch-consumer-parity.test.ts
  - .github/workflows/railway-warmup.yml
  - .github/workflows/railway-coldstart-measure.yml
  - frontend/tests/e2e/perf/measure-coldstart.ts
  - frontend/tests/e2e/perf/coldstart.spec.ts
  - .planning/phases/38-first-load-performance/coldstart-report.md
  - .planning/phases/38-first-load-performance/38-VERIFICATION.md
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
status: clean
---

# Phase 38.2: Code Review Report

**Reviewed:** 2026-04-22T17:50:00Z
**Depth:** standard
**Files Reviewed:** 16
**Status:** clean

## REVIEW PASSED

All 12 focus areas verified. The phase executes exactly the scope locked in `38.2-CONTEXT.md` + `38.2-PATTERNS.md`, nothing more and nothing less.

## Summary

Phase 38.2 is a high-discipline architectural correction: ~188 deletions, ~51 additions across 10 source files + 2 planning-doc annotations. Every critical invariant from the frozen Phase 38 contract is preserved.

### Invariants verified (K1-K14 from PATTERNS.md)

Counted directly in `frontend/lib/rsc/create-prefetched-page.tsx` (155 lines, matches SUMMARY target):

| Invariant | Expected | Actual | Status |
|-----------|----------|--------|--------|
| `queryCache.config.onError` subscriber | 1 | 1 (line 92) | OK |
| `export function wrapSentry` | 1 | 1 (line 140) | OK |
| `const PHASE_TAG = "38"` (not `"38.2"`) | 1 | 1 (line 38) | OK |
| `Sentry.captureException` call sites | 3 | 3 (lines 96, 121, 146) | OK |
| `Sentry.captureMessage` (diagnostic residue) | 0 | 0 | OK |
| `diagLog` / `rsc_hof_entered` / `rsc_hof_completed` / `rsc_prefetch_null_session` / `[RSC_DIAG]` | 0 | 0 | OK |
| `import { headers } from "next/headers"` | 0 | 0 | OK |
| `pathHint` variable | 0 | 0 | OK |
| `export type PrefetchContext` unchanged | 1 | 1 (line 40) | OK |
| `function deriveQueryLabel` unchanged | 1 | 1 (line 54) | OK |
| `createPrefetchedPage` signature unchanged | 1 | 1 (line 59, returns `Promise<ReactNode>`) | OK |
| `phase: "38.1"` / `phase: "38.2"` anywhere in frontend | 0 | 0 | OK |

### force-dynamic removal (6 pages)

All 6 `page.tsx` diffs are byte-clean single-hunk deletions of `export const dynamic = "force-dynamic";` + one trailing blank line. No `"auto"` replacement (D-02 respected), no `fetchCache` override (D-03 respected). All other code (imports, Props type, default Page function, `setRequestLocale`, `createPrefetchedPage` call, `wrapSentry` calls, inner `<Suspense>` wrappers on timetable/predict/digest) preserved verbatim.

```bash
grep -rE "export\s+const\s+dynamic\s*=\s*['\"]force-dynamic['\"]" frontend/app/
# Returns: 0 matches
```

### loading.tsx deletion

`frontend/app/[locale]/(dashboard)/loading.tsx` fully deleted. No phantom import references elsewhere. `find frontend/app -name loading.tsx` returns zero. No parent-level `loading.tsx` at `app/` or `app/[locale]/` (confirmed by both grep and the new static-invariant test at prefetch-consumer-parity.test.ts:282).

### next.config.ts staleTimes

Shape correct: `experimental: { staleTimes: { dynamic: 30 } }`. Integer `30` (seconds). Nested under `experimental`, not top-level. `dynamic` key (not `static`). Inserted as the last key of `nextConfig` object before the `withSentryConfig(withNextIntl(nextConfig), {...})` wrapper chain — Sentry outermost wrapper preserved (line 85).

### Warmup activation + /healthz → /health (4 files)

- `.github/workflows/railway-warmup.yml:14-17` — `schedule:` block uncommented (not stringified/anchored), cron exactly `"*/10 * * * *"`, `workflow_dispatch:` trigger preserved, endpoint `/health` (line 36).
- `.github/workflows/railway-coldstart-measure.yml:53` — step name corrected; step body is shell loop that delegates HTTP to `measure-coldstart.ts`.
- `frontend/tests/e2e/perf/measure-coldstart.ts:35` — `const endpoint = ${RAILWAY_URL.replace(/\/$/, "")}/health`.
- `frontend/tests/e2e/perf/coldstart.spec.ts` — 3 references migrated (comment, test name, endpoint constant). `@ts-nocheck` + `test.skip(true, ...)` placeholder guards preserved.

Zero `/healthz` references remain under `.github/workflows/` or `frontend/` (confirmed via grep). Remaining `/healthz` mentions live only in `.planning/` (ROADMAP, research, plan artifacts) — historical/non-executable context, correctly left alone.

Both YAML files parse cleanly via `python3 yaml.safe_load`.

### VERIFICATION.md SUPERSEDED annotation

`.planning/phases/38-first-load-performance/38-VERIFICATION.md`:
- Truth #1 table row is byte-identical to pre-38.2 (including the exact string `All 6 pages declare \`export const dynamic = "force-dynamic"\``).
- SUPERSEDED footer inserted at line 50 — AFTER the Observable Truths table, BEFORE the `**Score:**` summary. Append-only, no inline edit.
- Forward-reference to `.planning/phases/38.2-navigation-cache-parity/38.2-VERIFICATION.md` present.
- Rationale clearly distinguishes what is superseded (force-dynamic clause) from what remains VERIFIED (async RSC, HOF usage, HydrationBoundary, no anti-patterns).

### coldstart-report.md activation record

New "Decision Record — Phase 38.2 Activation (2026-04-22)" section appended at line 110, positioned after the `Follow-up (if warmup is enabled)` block and before the `## Phase 38 Gate` header — preserves the original Phase 38-03 TBD rubric (lines 82-95) without in-place edit. The activation record correctly distinguishes "warmup activated" from "p50/p95 measurement run" (the latter still HUMAN_NEEDED per Phase 38 HUMAN-UAT item #2).

### Test extension (prefetch-consumer-parity.test.ts)

Existing 3 `it()` declarations (1 looped over 6 PAGES + 2 standalone) preserved verbatim. 2 new standalone `it()` blocks appended inside the same `describe(...)`:
1. Line 261 — force-dynamic absence invariant. Correctly uses `stripComments` so a future code comment referencing the string cannot false-positive.
2. Line 282 — no parent-level `loading.tsx` invariant (guards against RESEARCH Pitfall 3 re-inheritance).

Reuses existing `PAGES` array (single source of truth) and `stripComments` helper per PATTERNS.md §14 Option A recommendation. Added only `existsSync` to the `node:fs` import. File now declares 5 `it()` blocks (one looped × 6 = 6 tests + 4 standalone = 10 tests total), consistent with the SUMMARY's "10/10 passing (was 8/8)".

### Scope discipline

`git diff --name-only 5466819..HEAD` scoped to:
- `frontend/**` (10 files including the test extension)
- `.github/workflows/**` (2 files)
- `.planning/**` (4 files: 38.2 summaries, ROADMAP/STATE bookkeeping, 2 Phase 38 annotations)

Zero touches outside scope:
- `src/**` (backend) — untouched
- `frontend/lib/query/client.tsx` (QueryProvider) — untouched
- `DashboardGuard` (`frontend/app/[locale]/(dashboard)/layout.tsx`) — untouched
- Phase 38 / 38.1 core source files — untouched (only the VERIFICATION.md annotation is documentation, not code)

### Security (T-01 through T-05 mitigations)

No new log statements leak tokens or userIds. No new public endpoints. Warmup cron uses existing `env:` indirection for `NEXT_PUBLIC_API_URL`; `curl -fsS ... > /dev/null` suppresses response body; step name no longer distinguishes `/healthz` vs `/health` in logs. `Authorization` header and Supabase JWT paths unchanged. VERIFICATION.md annotation contains no user data.

### Minor cleanup

No dead imports surfaced — `import * as Sentry from "@sentry/nextjs"` is still used by the retained `queryCache.config.onError` subscriber + outer catch + `wrapSentry` factory. No stale "[Phase 38.1 diagnostic]" / "TEMPORARY DIAGNOSTIC BREADCRUMB" comment blocks remain. Zero `TODO 38.2` / `FIXME 38.2` markers found.

---

_Reviewed: 2026-04-22T17:50:00Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_
