---
phase: 39-design-token-foundation
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - frontend/__tests__/styles/typography-tokens.test.ts
  - frontend/app/globals.css
  - .planning/phases/39-design-token-foundation/TYPO-USAGE.md
autonomous: true
requirements: [TYPO-01, TYPO-02]
requirements_addressed: [TYPO-01, TYPO-02]
tags: [design-tokens, typography, source-serif-4, inter, tailwind-v4]

must_haves:
  truths:
    - "4-tier typography scale (--text-hero/section/body/caption) is registered under Tailwind v4 namespaces and generates text-{tier} utility classes"
    - "Each tier has a matching --leading-{tier} line-height token"
    - "Hero and section have --tracking-{tier}: -0.02em; caption has --tracking-caption: 0.06em"
    - "Hero size is 2.8rem (42px), section 1.5rem (22.5px), body 0.95rem (14.25px), caption 0.74rem (11.1px) per D-06"
    - "TYPO-USAGE.md exists with at least 9 Source Serif 4 elements + 9 Inter elements per D-07 mapping"
    - "An English-only CSS comment in globals.css notes the Tailwind v4 namespace correction (per RESEARCH §Q1) and explains why Source Serif 4 + Inter are kept (per D-05 — brand-guidelines fonts not applied)"
    - "Existing color tokens, spacing tokens, shadow tokens, font-family tokens (--font-sans, --font-serif), and keyframes are preserved unchanged"
  artifacts:
    - path: "frontend/__tests__/styles/typography-tokens.test.ts"
      provides: "File-as-text vitest unit asserting --text-/--leading-/--tracking- tokens (per PATTERNS.md §typography-tokens.test.ts)"
      exports: ["describe('Phase 39 typography tokens (TYPO-01)')"]
    - path: "frontend/app/globals.css"
      provides: "Extended @theme block with 4-tier typography scale under Tailwind v4 namespaces"
      contains: "--text-hero"
    - path: ".planning/phases/39-design-token-foundation/TYPO-USAGE.md"
      provides: "Project-internal reference doc mapping page elements → font choice (D-07 verbatim)"
      contains: "Source Serif 4"
  key_links:
    - from: "frontend/__tests__/styles/typography-tokens.test.ts"
      to: "frontend/app/globals.css"
      via: "readFileSync"
      pattern: "readFileSync.*globals\\.css"
    - from: "frontend/app/globals.css @theme"
      to: "Tailwind v4 utility generation (text-hero, leading-section, tracking-caption)"
      via: "@tailwindcss/postcss reads --text-* / --leading-* / --tracking-* namespaces"
      pattern: "--text-hero:"
    - from: ".planning/phases/39-design-token-foundation/TYPO-USAGE.md"
      to: "Phase 40-43 component implementations"
      via: "downstream agent reference doc (font-serif vs font-sans decisions)"
      pattern: "Source Serif 4|Inter"
---

<objective>
Add the 4-tier serif type scale (hero / section / body / caption) to the existing Tailwind v4 `@theme` block in `frontend/app/globals.css` under the **v4-correct namespaces** (`--text-*`, `--leading-*`, `--tracking-*`) so utilities `text-hero`, `leading-section`, `tracking-caption` etc. generate. Distill from D-06's spec: hero 2.8rem/700/1.15/-0.02em, section 1.5rem/700/1.3/-0.02em, body 0.95rem/600/1.5/—, caption 0.74rem/600/1.4/0.06em. Document the serif-vs-Inter usage rule per D-07 in `.planning/phases/39-design-token-foundation/TYPO-USAGE.md` so Phase 40-43 implementers have a single reference.

**RESEARCH-driven correction (per RESEARCH §Q1 + Pitfall 4):** CONTEXT.md D-06 names the tokens `--font-size-*` / `--line-height-*` / `--letter-spacing-*`, but Tailwind v4's namespace registry generates utilities **only** from `--text-*` / `--leading-*` / `--tracking-*`. The CONTEXT.md names compile but produce zero utilities. This plan adopts the v4-correct namespace and embeds an inline CSS comment explaining the correction so future readers understand the divergence.

**Per D-05** (brand-guidelines font conflict reconciliation): Fonts stay Source Serif 4 + Inter (v2.0 Phase 1 lock); brand-guidelines is SSOT for **colors only**. An English-only CSS comment in globals.css makes the rationale explicit.

Purpose: Phase 40 SHARED-01 (Card/Button/Input/Modal/Tooltip) and Phase 41 STATES (Loading/Empty/Error states) need `text-hero` / `text-body` / `text-caption` utilities to compose component typography per the new design vocabulary. This plan publishes those utilities.

Output:
- `frontend/__tests__/styles/typography-tokens.test.ts` (NEW) — file-as-text invariant assertions enforcing v4 namespaces
- `frontend/app/globals.css` (MODIFIED) — additive `@theme` extension: 4-tier `--text-*` + `--leading-*` + `--tracking-*` block with English-only annotation
- `.planning/phases/39-design-token-foundation/TYPO-USAGE.md` (NEW) — project-internal reference mapping page elements → font choice
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-design-token-foundation/39-CONTEXT.md
@.planning/phases/39-design-token-foundation/39-RESEARCH.md
@.planning/phases/39-design-token-foundation/39-PATTERNS.md
@.planning/phases/39-design-token-foundation/39-VALIDATION.md
@frontend/app/globals.css
@./CLAUDE.md

<interfaces>
<!-- Authoritative typography token block (RESEARCH §Code Example 8 + Q1 namespace correction) -->

```css
@theme {
  /* === TYPO-01 4-tier typography scale (Phase 39 plan-2; D-06 distilled from v2.0 prototype) === */
  /* Source: 01-UI-SPEC.md §Typography (25-row prototype scale collapsed to 4 tiers) */
  /* html { font-size: 15px } so 1rem = 15px → all values resolve to v2.0 px exactly */
  /*
   * Tailwind v4 namespace correction per RESEARCH §Q1 + §Pitfall 4:
   * CONTEXT.md D-06 names tokens --font-size-*/--line-height-*/--letter-spacing-*
   * but Tailwind v4 generates utilities ONLY from these namespaces:
   *   --text-*    → text-{name} font-size utility
   *   --leading-* → leading-{name} line-height utility
   *   --tracking-*→ tracking-{name} letter-spacing utility
   * (CITED: tailwindcss.com/docs/theme)
   */
  /*
   * Per D-05 — fonts unchanged (Source Serif 4 + Inter, v2.0 Phase 1 lock).
   * anthropics/skills/brand-guidelines specifies Poppins (headings) + Lora (body)
   * for PPT/Doc artifacts. UniBoard is a web app; font decisions are locked at
   * v2.0 Phase 1 after 103 prototype iterations validated Source Serif 4 + Inter
   * as the visual identity. brand-guidelines colors apply unmodified.
   */

  /* Hero (Source Serif 4) — apply font-serif when needed */
  --text-hero:     2.8rem;   /* 42px @ 15px base */
  --leading-hero:  1.15;
  --tracking-hero: -0.02em;

  /* Section (Source Serif 4) */
  --text-section:     1.5rem;   /* 22.5px */
  --leading-section:  1.3;
  --tracking-section: -0.02em;

  /* Body (Inter) — default body font; explicit only when overriding inherited serif */
  --text-body:    0.95rem;   /* 14.25px */
  --leading-body: 1.5;
  /* tracking-body intentionally omitted — browser default 'normal' per RESEARCH §Q6 */

  /* Caption (Inter, uppercase rhythm) */
  --text-caption:     0.74rem;   /* 11.1px */
  --leading-caption:  1.4;
  --tracking-caption: 0.06em;
}
```

From PATTERNS.md §typography-tokens.test.ts (verbatim):
```typescript
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { resolve } from "node:path";
const source = readFileSync(resolve(__dirname, "../../app/globals.css"), "utf8");
describe("Phase 39 typography tokens (TYPO-01)", () => {
  const TIERS = ["hero", "section", "body", "caption"] as const;
  it.each(TIERS)("declares --text-%s font-size token", (tier) => {
    expect(source).toMatch(new RegExp(`--text-${tier}:\\s*[\\d.]+rem`));
  });
  it.each(TIERS)("declares --leading-%s line-height token", (tier) => {
    expect(source).toMatch(new RegExp(`--leading-${tier}:\\s*[\\d.]+`));
  });
  it("hero + section have negative tracking (-0.02em per D-06)", () => {
    expect(source).toMatch(/--tracking-hero:\s*-0\.02em/);
    expect(source).toMatch(/--tracking-section:\s*-0\.02em/);
  });
  it("caption has +0.06em tracking", () => {
    expect(source).toMatch(/--tracking-caption:\s*0\.06em/);
  });
  it("hero size = 2.8rem, section = 1.5rem, body = 0.95rem, caption = 0.74rem", () => {
    expect(source).toMatch(/--text-hero:\s*2\.8rem/);
    expect(source).toMatch(/--text-section:\s*1\.5rem/);
    expect(source).toMatch(/--text-body:\s*0\.95rem/);
    expect(source).toMatch(/--text-caption:\s*0\.74rem/);
  });
});
```

From RESEARCH §Pattern 7 + PATTERNS.md §TYPO-USAGE.md (D-07 mapping):
**Source Serif 4 (Narrative)** — apply via `font-serif`:
- Hero greeting (dashboard top text)
- Page titles (h1, h2)
- Card titles (`.card-title`)
- WAM number (dashboard stat)
- GPA target value
- Stat values (any --text-hero size)
- Profile name (right panel)
- Scroll hint italic ("your dashboard")
- (one more bullet — e.g., "Section headings inside narrative content")

**Inter (UI Chrome / Data Labels)** — apply via `font-sans` (default):
- Button labels
- Sidebar nav labels (.nav-label)
- Search input + placeholder
- Dropdown items + timestamps
- Form input labels
- Calendar day numbers
- Stat labels (uppercase, --text-caption size)
- Grade badges (.grade-badge)
- (one more bullet — e.g., "Toast messages and inline error text")

PATTERNS.md §globals.css "Plan-2 typography insertion point": After existing `--radius-card`/`--radius-sm` block (line 39), before `--spacing-sidebar-w` (line 42). NOTE: plan-1 also inserts the 8-point spacing scale near here — plan-2 lands just BEFORE plan-1's spacing additions OR just AFTER, whichever maintains @theme block readability. Both plans add to the same @theme block; ordering inside it does not affect Tailwind compilation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Write failing typography-tokens test (RED)</name>
  <files>
    frontend/__tests__/styles/typography-tokens.test.ts
  </files>
  <read_first>
    - frontend/__tests__/styles/tokens-css.test.ts (sibling created in plan-1 Task 1 — exact template per PATTERNS.md §typography-tokens.test.ts; if not yet created, fall back to PATTERNS.md §tokens-css.test.ts which is verbatim)
    - frontend/__tests__/rsc/dashboard-prefetch.test.ts (lines 217-230 — file-as-text idiom anchor)
    - frontend/__tests__/i18n/message-keys.test.ts (key-set parity assertion idiom — analog per PATTERNS.md §typography-tokens.test.ts blend)
    - .planning/phases/39-design-token-foundation/39-PATTERNS.md (§typography-tokens.test.ts — full verbatim test body with TIERS const + 5 it() blocks)
    - .planning/phases/39-design-token-foundation/39-RESEARCH.md (§Q1 — namespace correction rationale; §Pitfall 4 — what goes wrong with D-06 names; §Code Example 8 — authoritative @theme typography block; §Q6 — line-height/tracking justifications)
    - .planning/phases/39-design-token-foundation/39-CONTEXT.md (D-06 — original 4-tier scale spec; D-05 — font lock decision; D-07 — TYPO-USAGE mapping)
  </read_first>
  <behavior>
    TDD RED — test exists and fails because `frontend/app/globals.css` does not yet declare `--text-*` / `--leading-*` / `--tracking-*` tokens.
    - For each tier in `["hero", "section", "body", "caption"]`: assert `--text-{tier}: <num>rem`, `--leading-{tier}: <num>`
    - Assert `--tracking-hero: -0.02em` and `--tracking-section: -0.02em`
    - Assert `--tracking-caption: 0.06em`
    - Assert exact sizes: hero 2.8rem, section 1.5rem, body 0.95rem, caption 0.74rem (D-06)
    - Note in test comment: "Per RESEARCH Q1 correction — Tailwind v4 namespaces are --text-*/--leading-*/--tracking-*, NOT D-06's --font-size-*/--line-height-*/--letter-spacing-*"

    Currently fails because plan-1 only added color + spacing tokens; typography is plan-2's job.
  </behavior>
  <action>
    Create `frontend/__tests__/styles/typography-tokens.test.ts` per PATTERNS.md §typography-tokens.test.ts — copy verbatim. Hoist `source = readFileSync(resolve(__dirname, "../../app/globals.css"), "utf8")` at module top per PATTERNS.md §File-as-text Vitest Idiom. Use `it.each(TIERS)` for the per-tier size + leading assertions. Comments English only.

    Add a top-of-file comment block (English only, per CLAUDE.md `code-comments.md`):
    ```typescript
    /**
     * Phase 39 plan-2 — TYPO-01 typography token assertions.
     * Per RESEARCH §Q1 correction: Tailwind v4 namespaces are
     *   --text-*    → text-{name} font-size utility
     *   --leading-* → leading-{name} line-height utility
     *   --tracking-*→ tracking-{name} letter-spacing utility
     * NOT CONTEXT.md D-06's --font-size-*/--line-height-*/--letter-spacing-*
     * (which compile but generate zero utilities — see RESEARCH §Pitfall 4).
     */
    ```

    Confirm RED: `cd frontend && pnpm test -- --run __tests__/styles/typography-tokens.test.ts` must fail (current globals.css has no --text-* tokens after plan-1).

    Commit: `test(39-02): add failing typography-tokens invariant tests`
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run __tests__/styles/typography-tokens.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/__tests__/styles/typography-tokens.test.ts` exists
    - File contains `readFileSync.*globals\\.css` (file-as-text pattern)
    - File contains `["hero", "section", "body", "caption"]` (TIERS const)
    - File contains a comment string referencing "RESEARCH" and "Q1" or "namespace" (Q1 correction documented in test)
    - `cd frontend && pnpm test -- --run __tests__/styles/typography-tokens.test.ts` exits non-zero (RED)
    - Commit message starts with `test(39-02):`
  </acceptance_criteria>
  <done>
    Failing typography test committed. RED phase complete; Task 2 implementation will turn it GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2 (Wave 1): Add 4-tier typography tokens to globals.css with v4-namespace correction annotation + write TYPO-USAGE.md reference doc</name>
  <files>
    frontend/app/globals.css
    .planning/phases/39-design-token-foundation/TYPO-USAGE.md
  </files>
  <read_first>
    - frontend/app/globals.css (current state after plan-1 if running concurrently — typography goes in the same @theme block; per PATTERNS.md §globals.css insertion point: after --radius-sm, before --spacing-sidebar-w; if plan-1 has already added --spacing-1..16 in that area, insert typography either just before or just after the spacing block)
    - .planning/phases/39-design-token-foundation/39-RESEARCH.md (§Code Example 8 — full @theme typography block authoritative; §Pattern 6 — namespace rationale; §Pitfall 2 — DO NOT touch @theme inline; §Pitfall 4 — namespace correction rationale; §Q1 — recommendation to use v4 names)
    - .planning/phases/39-design-token-foundation/39-PATTERNS.md (§globals.css plan-2 insertion point; §TYPO-USAGE.md — full doc structure with Source Serif 4 + Inter sections + Disambiguation + Anti-pattern)
    - .planning/phases/39-design-token-foundation/39-CONTEXT.md (D-05, D-06, D-07, D-08 — typography decisions; D-12 — plan ordering confirms plan-2 owns typography)
    - .planning/phases/01-design-system-foundation/01-UI-SPEC.md (§Typography — 25-row prototype scale; lets the executor cross-check D-06's distillation if line-height/tracking ambiguity arises)
    - frontend/__tests__/styles/typography-tokens.test.ts (the failing test from Task 1 — implementation must satisfy all assertions verbatim)
  </read_first>
  <behavior>
    GREEN — implement the @theme typography block + TYPO-USAGE.md doc to make Task 1 test pass and provide the downstream reference. No new behavior beyond what the test asserts and what D-07 specifies.
  </behavior>
  <action>
    **Step 1 — Edit `frontend/app/globals.css`**: Insert the typography token block per RESEARCH §Code Example 8 verbatim (with the English-only comment block annotating the v4 namespace correction per RESEARCH §Q1 and the brand-guidelines font reconciliation per D-05).

    Insertion point per PATTERNS.md §globals.css "Plan-2 typography insertion point": after the existing `--radius-sm: 8px;` line. If plan-1 has concurrently added the `--spacing-1..16` 8-point block in that area, insert the typography block IMMEDIATELY AFTER `--radius-sm: 8px;` and BEFORE plan-1's `/* DESIGN-02 8-point spacing scale */` comment. Both plan-1 and plan-2 land in the same @theme block; the relative order inside it does not affect Tailwind compilation, but the typography-spacing-layout grouping reads cleanest as: radius → typography → 8-point spacing → layout spacing.

    The exact block to insert (copy verbatim from RESEARCH §Code Example 8 + the inline-correction comment):
    ```css
    /* === TYPO-01 4-tier typography scale (Phase 39 plan-2; D-06 distilled from v2.0 prototype) === */
    /* Source: 01-UI-SPEC.md §Typography (25-row prototype scale collapsed to 4 tiers) */
    /* html { font-size: 15px } so 1rem = 15px → all values resolve to v2.0 px exactly */
    /*
     * Tailwind v4 namespace correction per RESEARCH §Q1 + §Pitfall 4:
     * CONTEXT.md D-06 names tokens --font-size-*/--line-height-*/--letter-spacing-*
     * but Tailwind v4 generates utilities ONLY from these namespaces:
     *   --text-*    → text-{name} font-size utility
     *   --leading-* → leading-{name} line-height utility
     *   --tracking-*→ tracking-{name} letter-spacing utility
     * (CITED: tailwindcss.com/docs/theme)
     */
    /*
     * Per D-05 — fonts unchanged (Source Serif 4 + Inter, v2.0 Phase 1 lock).
     * anthropics/skills/brand-guidelines specifies Poppins (headings) + Lora (body)
     * for PPT/Doc artifacts. UniBoard is a web app; font decisions are locked at
     * v2.0 Phase 1 after 103 prototype iterations validated Source Serif 4 + Inter
     * as the visual identity. brand-guidelines colors apply unmodified.
     */

    /* Hero (Source Serif 4) — hero greeting, page titles, card titles, WAM, GPA target */
    --text-hero:     2.8rem;   /* 42px */
    --leading-hero:  1.15;
    --tracking-hero: -0.02em;

    /* Section (Source Serif 4) */
    --text-section:     1.5rem;   /* 22.5px */
    --leading-section:  1.3;
    --tracking-section: -0.02em;

    /* Body (Inter — default body font; explicit only when overriding inherited serif) */
    --text-body:    0.95rem;   /* 14.25px */
    --leading-body: 1.5;
    /* tracking-body intentionally omitted — browser default 'normal' per RESEARCH §Q6 */

    /* Caption (Inter, uppercase rhythm) */
    --text-caption:     0.74rem;   /* 11.1px */
    --leading-caption:  1.4;
    --tracking-caption: 0.06em;
    ```

    DO NOT modify any existing color tokens, spacing tokens, shadow tokens, font-loading tokens (`@theme inline { --font-sans/--font-serif }` — per Pitfall 2), keyframes, or radius tokens. Plan-3 will add motion + SSE keyframes.

    **Step 2 — Run test**: `cd frontend && pnpm test -- --run __tests__/styles/typography-tokens.test.ts` must pass (GREEN).

    **Step 3 — Run build**: `cd frontend && pnpm build` to confirm Tailwind compiles. Confirm `.next/static/css/*.css` contains `.text-hero { font-size: var(--text-hero) }` style output.

    **Step 4 — Create `.planning/phases/39-design-token-foundation/TYPO-USAGE.md`** per D-07 + RESEARCH §Pattern 7 + PATTERNS.md §TYPO-USAGE.md. Markdown structure:

    ```markdown
    # Typography Usage Rules

    > Phase 39 plan-2 deliverable. Reference doc for Phase 40-43 component implementations.
    > Per D-07 + RESEARCH §Pattern 7. English-only technical guidance.

    ## Source Serif 4 — Narrative Voice

    Use the Tailwind class `font-serif` (or token `var(--font-serif)`).

    Apply to:
    - Hero greeting (dashboard top text)
    - Page titles (h1, h2)
    - Card titles (`.card-title`)
    - WAM number (dashboard stat)
    - GPA target value
    - Stat values (any `--text-hero` size)
    - Profile name (right panel)
    - Scroll hint italic ("your dashboard")
    - Section headings inside narrative content (h3 within an article body)

    ## Inter — UI Chrome / Data Labels

    Use the Tailwind class `font-sans` (or token `var(--font-sans)`). This is also the body default — apply `font-sans` explicitly only when overriding a serif inheritance.

    Apply to:
    - Button labels
    - Sidebar nav labels (`.nav-label`)
    - Search input + placeholder
    - Dropdown items + timestamps
    - Form input labels
    - Calendar day numbers
    - Stat labels (uppercase, `--text-caption` size)
    - Grade badges (`.grade-badge`)
    - Toast messages and inline error text

    ## Disambiguation

    - If the text reads as **content the user wrote or that is meaningful in their grade narrative** → Source Serif 4
    - If the text is a **system label, control affordance, or chrome** → Inter

    ## Anti-pattern

    Per RESEARCH §Pattern 5 — DO NOT put `.text-hero` content inside a `<button>` element without an explicit `font-family` adjustment. Buttons inherit Inter (D-07 chrome rule), but the `text-hero` utility sets only font-size; if the surrounding rule applies Source Serif 4 the button text will render in the wrong family. Always pair `text-hero` with explicit `font-serif` (or `font-sans` if the button context truly wants Inter at hero size).

    ## Tailwind v4 namespace note

    Per RESEARCH §Q1 (corrects CONTEXT.md D-06): the tokens are registered under Tailwind v4's `--text-*` / `--leading-*` / `--tracking-*` namespaces (NOT `--font-size-*` / `--line-height-*` / `--letter-spacing-*` as D-06 implied). Component code should use the generated utilities `text-hero` / `text-section` / `text-body` / `text-caption` plus `leading-hero` / `tracking-caption` etc. Direct `var(--text-hero)` consumption also works.

    ## CJK fallback

    Both Source Serif 4 and Inter fall back to system fonts for CJK glyphs (per `frontend/app/globals.css` `--font-sans` / `--font-serif` definitions in the `@theme inline` block). zh-CN content renders correctly without font-loading changes; per D-08, no new font weights are loaded.
    ```

    Comments English only (per CLAUDE.md `02-standards/code-comments.md`); per PATTERNS.md §Comment Style, CN sample text is permitted only if marked `<!-- zh-CN sample -->`. The doc above is pure technical guidance — no zh-CN samples needed.

    **Step 5 — Verify file presence + content**: `test -f .planning/phases/39-design-token-foundation/TYPO-USAGE.md && grep -c '^- ' .planning/phases/39-design-token-foundation/TYPO-USAGE.md` should return >= 18 (9 serif bullets + 9 inter bullets per RESEARCH §Validation row TYPO-02).

    **Step 6 — Commit**: `feat(39-02): add 4-tier typography scale (text-hero/section/body/caption) per D-06 + TYPO-USAGE.md per D-07`
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run __tests__/styles/typography-tokens.test.ts && pnpm build && test -f /Users/qinyuan/claude/r1ckyIn_GitHub/UniBoard/.planning/phases/39-design-token-foundation/TYPO-USAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/globals.css` contains `--text-hero:[[:space:]]+2\.8rem` (D-06 hero size; whitespace-tolerant regex per ISSUE-39-03; `grep -E '--text-hero:[[:space:]]+2\.8rem' returns one match)
    - `frontend/app/globals.css` contains `--text-section:[[:space:]]+1\.5rem`, `--text-body:[[:space:]]+0\.95rem`, `--text-caption:[[:space:]]+0\.74rem` (whitespace-tolerant `grep -E` checks per ISSUE-39-03)
    - `frontend/app/globals.css` contains `--leading-hero`, `--leading-section`, `--leading-body`, `--leading-caption` token names (all 4 tiers have line-height; `grep -cE '--leading-(hero|section|body|caption):' returns 4)
    - `frontend/app/globals.css` contains `--tracking-hero:[[:space:]]+-0\.02em`, `--tracking-section:[[:space:]]+-0\.02em`, `--tracking-caption:[[:space:]]+0\.06em` (whitespace-tolerant regex per ISSUE-39-03)
    - `frontend/app/globals.css` does NOT contain `--font-size-hero` or `--line-height-hero` or `--letter-spacing-hero` (the wrong D-06 namespace must NOT appear; v4 correction enforced)
    - `frontend/app/globals.css` contains a CSS comment substring "Tailwind v4 namespace correction" or "RESEARCH §Q1" (the namespace correction rationale is annotated inline)
    - `frontend/app/globals.css` contains a CSS comment substring "brand-guidelines" and "Source Serif 4" (D-05 font reconciliation annotated inline)
    - `frontend/app/globals.css` STILL contains `@theme inline {` and `--font-sans:` and `--font-serif:` (font-family loading preserved per Pitfall 2)
    - `cd frontend && pnpm test -- --run __tests__/styles/typography-tokens.test.ts` exits 0 (GREEN)
    - `cd frontend && pnpm build` exits 0
    - `cd frontend && grep -E '\\.text-hero\\s*\\{' .next/static/css/*.css` returns at least one match (Tailwind generated `.text-hero` utility)
    - `.planning/phases/39-design-token-foundation/TYPO-USAGE.md` exists
    - `grep -c '^- ' .planning/phases/39-design-token-foundation/TYPO-USAGE.md` returns >= 18 (9 serif bullets + 9 inter bullets per D-07 + RESEARCH §Validation TYPO-02)
    - TYPO-USAGE.md contains substring "Source Serif 4" and "Inter" and "Anti-pattern" and "Disambiguation"
    - Commit message starts with `feat(39-02):`
  </acceptance_criteria>
  <done>
    Typography test passes GREEN. globals.css has 4-tier scale under v4-correct namespaces with inline correction annotation. TYPO-USAGE.md exists with 18+ bullets covering D-07 mapping. Phase 40 SHARED-01 + Phase 41 STATES can now consume `text-hero/section/body/caption` utilities.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS parser (browser) | `globals.css` ships in compiled bundle; browser parses at page load; no untrusted CSS injection surface |
| ESLint config (PR-reviewed) | All token additions pass through PR review |
| Markdown reference doc | `TYPO-USAGE.md` is project-internal under `.planning/`; not user-facing; no XSS surface |

## STRIDE Threat Register

Phase 39 plan-2 is pure CSS + Markdown documentation (per RESEARCH §Security Domain). Most ASVS categories non-applicable.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09 | Tampering (V14 Configuration) | globals.css typography token values | mitigate | All token additions reviewed in PR; `__tests__/styles/typography-tokens.test.ts` asserts exact sizes and tracking values; deviation fails CI |
| T-39-10 | Information Disclosure (V7) | TYPO-USAGE.md content | accept | Doc is project-internal under `.planning/`; values are public design specifications; zero secrets |
| T-39-11 | Tampering (V5 partial) | typography token names | accept | Token names are hardcoded in CSS; no dynamic compilation or user input |
| T-39-12 | Spoofing | n/a | n/a | No authentication surface |
| T-39-13 | Repudiation | n/a | n/a | No audit-logged operations |
| T-39-14 | Denial of Service | n/a | n/a | Build-time only; no runtime endpoint |
| T-39-15 | Elevation of Privilege | n/a | n/a | No permission boundary touched |

**Non-applicable ASVS categories**: V2 Authentication, V3 Session Management, V4 Access Control, V6 Cryptography. No auth/session/crypto surface in plan-2.
</threat_model>

<verification>
**Wave-level checks** (after Task 2 GREEN, runs in parallel with plan-1):

1. `cd frontend && pnpm test -- --run __tests__/styles/typography-tokens.test.ts` — passes
2. `cd frontend && pnpm test -- --run` — full vitest suite stays green
3. `cd frontend && pnpm build` — Tailwind v4 compiles new --text-/--leading-/--tracking- tokens
4. `grep -E '\.text-hero\s*\{' frontend/.next/static/css/*.css` — Tailwind generated the utility
5. `grep -E '\.leading-section\s*\{' frontend/.next/static/css/*.css` — generated
6. `grep -E '\.tracking-caption\s*\{' frontend/.next/static/css/*.css` — generated
7. TYPO-USAGE.md exists with >= 18 bullets

**Coordination with plan-1**: Both plans modify `frontend/app/globals.css` but in different sections:
- Plan-1 modifies the color tokens (lines 4-27) and inserts spacing scale around the `--radius-sm` area
- Plan-2 inserts typography near the `--radius-sm` area too

If both run truly in parallel and write the file simultaneously, the second writer wins. **Recommended execution order**: Run plan-1 Wave 1 GREEN first (publishes color + spacing changes), then plan-2 Wave 1 GREEN (additive typography insertion preserves plan-1's changes). Wave 0 RED tasks for both plans can run truly parallel because they only create test files in `__tests__/`. Plan-3 Wave 0 (also creates test files) can also run parallel with both plans' Wave 0.

**Manual UAT**: Open `http://localhost:3001/zh-CN/dashboard` after dev server restart; inspect a known hero element (`.greeting`); verify `font-size` resolves to `42px` (rem * 15px base).

**No UI-SPEC required** — additive token layer only; no user-facing visual change in this phase. Phase 40-43 components consume the new utilities; visual contract preserved by AC #2 grep gate + Playwright visual regression spec in plan-3.
</verification>

<success_criteria>
- ROADMAP.md Phase 39 success criterion #4 (Source Serif 4 4-tier scale renders with consistent line-height and letter-spacing): satisfied by the 4-tier --text-/--leading-/--tracking- block under v4-correct namespaces
- TYPO-01 (4-tier serif type scale with consistent line-height + letter-spacing tokens): satisfied by the 4-tier block matching D-06 specifications
- TYPO-02 (Serif vs Inter usage clarified in design system doc): satisfied by TYPO-USAGE.md with 9+ Source Serif 4 elements + 9+ Inter elements
- D-05 brand-guidelines font reconciliation persisted as English-only CSS comment in globals.css and as "Tailwind v4 namespace note" + "CJK fallback" sections in TYPO-USAGE.md
- Tailwind v4 namespace correction (RESEARCH §Q1) embedded inline in both the @theme block CSS comment AND in TYPO-USAGE.md so future readers understand why D-06's names were not used
</success_criteria>

<output>
After completion, create `.planning/phases/39-design-token-foundation/39-02-SUMMARY.md` capturing:
- Files modified (3 files)
- Commits (2 atomic: test(39-02) RED, feat(39-02) GREEN)
- Tailwind v4 namespace correction documented inline in both globals.css + TYPO-USAGE.md
- TYPO-USAGE.md bullet count (must be >= 18)
- Brand-guidelines font reconciliation (D-05) annotated in CSS comment block
- pnpm build pass + utility generation evidence (`.text-hero { ... }` in compiled CSS)
- Notable: no UI-SPEC required — additive layer; visual contract preserved by AC #2 grep gate + Playwright visual regression spec in plan-3
</output>
</content>
</invoke>