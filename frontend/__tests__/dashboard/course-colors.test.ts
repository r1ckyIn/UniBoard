import { describe, it, expect } from "vitest";

describe("course-colors", () => {
  it.todo("returns correct color for known course code");
  it.todo("returns default color for unknown course code");
  it.todo("COURSE_COLORS has 4 entries");
});
