/**
 * RED-state tests for useAiStream sources event parsing (AIFEAT-02 frontend).
 * Phase 34 Wave 0: it.todo() stubs. Wave 2 (Plan 34-05) writes real bodies.
 */
import { describe, it } from "vitest";

describe("useAiStream sources event", () => {
  it.todo(
    "parses sources event into state.sources (Phase 34: implementation pending — 34-05)",
  );
  it.todo(
    "clearMessages resets sources to empty array (Phase 34: implementation pending — 34-05)",
  );
});
