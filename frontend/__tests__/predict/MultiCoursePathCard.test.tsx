/**
 * RED-state tests for MultiCoursePathCard verdict + advisory (AIFEAT-03).
 * Phase 34 Wave 0: it.todo() stubs. Wave 2 (Plan 34-05) writes real bodies.
 */
import { describe, it } from "vitest";

describe("MultiCoursePathCard", () => {
  it.todo(
    "renders green Reachable badge when is_achievable=true (Phase 34: implementation pending — 34-05)",
  );
  it.todo(
    "hides advisory paragraph when advisory_text is null — D-D1 silent fallback (Phase 34: implementation pending — 34-05)",
  );
  it.todo(
    "renders red Unreachable badge with suggested_target when is_achievable=false (Phase 34: implementation pending — 34-05)",
  );
});
