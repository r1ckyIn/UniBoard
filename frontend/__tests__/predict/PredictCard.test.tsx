import { describe, it } from "vitest";

describe("PredictCard", () => {
  it.todo("renders collapsed card with course info and current mark");

  it.todo("expands on click showing assessment table");

  it.todo("shows score input for ungraded assessments");

  it.todo("clamps input to 0-100 range");

  it.todo("shows graded badge for graded assessments");

  it.todo("displays projected mark when all predictions filled");
});
