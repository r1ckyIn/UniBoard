import { describe, it } from "vitest";

describe("PredictPage", () => {
  it.todo("renders title row with Grade Predictor heading");

  it.todo("renders course cards for all courses");

  it.todo("updates WAM overview on prediction input");

  it.todo("target slider changes required scores");

  it.todo("deep-link ?course=COMP2017 auto-expands matching card");

  it.todo("faculty selector changes WAM calculation");

  it.todo("shows skeleton during loading");
});
