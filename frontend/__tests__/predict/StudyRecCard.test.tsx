/**
 * RED-state tests for StudyRecCard Top-3 list component (AIFEAT-01).
 * Phase 34 Wave 0: it.todo() stubs. Wave 2 (Plan 34-05) writes real bodies.
 */
import { describe, it } from "vitest";

describe("StudyRecCard", () => {
  it.todo(
    "renders Top-3 items with course color dots (Phase 34: implementation pending — 34-05)",
  );
  it.todo(
    "hides AI badge when ai_generated=false (Phase 34: implementation pending — 34-05)",
  );
  it.todo(
    "shows skeleton state on isLoading=true (Phase 34: implementation pending — 34-05)",
  );
});
