import { describe, it } from "vitest";

describe("GpaTargetSection", () => {
  it.todo("renders slider and input synced to current GPA target");
  it.todo("displays grade band label for current value");
  it.todo("saves target when save button is clicked");
  it.todo("shows saved confirmation after successful save");
  it.todo("clamps input value between 0 and 100");
});
