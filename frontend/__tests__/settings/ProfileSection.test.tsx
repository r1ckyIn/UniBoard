import { describe, it } from "vitest";

describe("ProfileSection", () => {
  it.todo("renders display name input with current value");
  it.todo("renders email field as readonly");
  it.todo("shows email hint that email cannot be changed");
  it.todo("saves display name changes when save button is clicked");
  it.todo("shows saved confirmation after successful save");
  it.todo("displays account created date");
});
