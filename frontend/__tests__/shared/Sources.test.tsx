/**
 * RED-state tests for Sources citation panel component (AIFEAT-02).
 * Phase 34 Wave 0: it.todo() stubs. Wave 2 (Plan 34-05) writes real bodies.
 */
import { describe, it } from "vitest";

describe("Sources component", () => {
  it.todo(
    "renders [N] inline markers (Phase 34: implementation pending — 34-05)",
  );
  it.todo(
    "collapsible details opens on summary click (Phase 34: implementation pending — 34-05)",
  );
  it.todo(
    "renders title + anchor + score per source (Phase 34: implementation pending — 34-05)",
  );
});
